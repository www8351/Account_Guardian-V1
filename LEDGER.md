# LEDGER

## ISSUES

Issue:  NEXT BEST ACTION, THE D2/D4 DEFECT-FIX PLAN SESSION. R10 acceptance is COMPLETE as of 2026-09-08, nine of nine live rows and four of four static rows, so the item that stood ahead of this one is discharged and this is now the top of the section. Standing context that has not changed: VERSION 1 OF THE REALIZED PEAK TRAILING FLOOR IS ACCEPTED ON ALL FOUR ROWS, V1-A, V1-B, V1-C AND V1-D, each closed in the 2026-09-01 ACTIONS entry below on the eight files banked at commit `7aef069` and the build identity of owner ruling TWO of 2026-08-31, `AccountGuardian.ex5` md5 `124BA610201A6D67C050572C6A84DE37`, 86866 bytes. Input hardening RULED 2026-09-03, DECISIONS `:2205` (D1f, mandatory list inputs), `:2209` (D2/D3/D3b, void), `:2213` (D4a), `:2217` (D5a), `:2221` (D6a), `:2225` (D7b, SweepPeriodSeconds and HistoryStablePolls constants), `:2229` (D8b, a9 vectors), `:2233` (D9a), `:2237` (D10a, build scope), `:2241` (D11a, CrashLoop constants, dated 2026-09-02).
Action: NEXT SESSION IS THE D2/D4 DEFECT-FIX PLAN SESSION, then the enforcement phase kickoff, Sweep.mqh flatten engine. Two things are named so the plan session does not have to rediscover them, and neither is settled here: the R10 build is still UNMERGED on `worktree-r10-build-20260903` at `f1d2a6b` with no push performed, and merging it is a separate decision under the merge gate ruling; and the D2 and D4 fix shapes are RESERVED to the owner by the ISSUES entries below, which name the fix order ruled 2026-08-19 and explicitly decline to write the fixes.
CORRECTION 2026-09-08, same session, before session close: the clause above is FALSE and is left unedited so the correction is visible rather than silent. The owner merged the R10 build into main before this session began; branch `worktree-r10-build-20260903` no longer exists, locally or on origin. Verified: `git merge-base --is-ancestor f1d2a6b HEAD` on this worktree returns true, `f1d2a6b` is an ancestor of `d88a71f`, and `git branch -a` after a fetch lists no `worktree-r10-build-20260903` anywhere. The build IS merged into main.
UPDATED 2026-09-08 BY THE PLAN SESSION, ACTIONS entry of that date. THE D2/D4 PLAN IS WRITTEN AND NOTHING IS BUILT. `docs/FIXPLAN_PHASE3_DEFECTS_2_4_2026-09-08.md` at commit `eb72f5329666474b98a15f5702fa0992e71355f2` carries both defects as recorded, a source map of every read, write and journal site at `bd61a0f`, two closed options per defect worked against the 2026-08-31 lock and the 2026-08-30 battery outage boot, predictions P61 to P75, and a ruling sheet of FIFTEEN CLOSED QUESTIONS, D2D4-1 to D2D4-15, which are the owner's and are not answered anywhere in that document. NO SHAPE IS RECOMMENDED and no code is authorised. The plan tags forty one FINAL entries INHERITED and finds ZERO clauses any option supersedes.
UPDATED 2026-09-09. THE RULING SHEET IS ANSWERED, FINAL in DECISIONS (end of section), owner ruling 2026-09-09. THE NEXT BEST ACTION IS THE D2/D4 BUILD SESSION, to implement D2D4-1 to D2D4-15 per the fix order FINAL of 2026-08-19 and per the plan's section 2.7 style task list, which that build session writes from these answers. This entry is a ruling record only: no code, no build, no vector edit, no write under the Terminal data folder, no merge, no push was performed or is authorised by it. The enforcement phase kickoff, `Sweep.mqh`, stays behind the D2/D4 build session as this entry already says.
Status: OPEN, next best action

Issue:  R10 BUILD ACCEPTANCE, COMPLETE 2026-09-08, kept in this section as the acceptance record rather than moved, on the same footing as the two CLOSED entries below it. The build ruled 2026-09-03 (D1f, D7b, D11a, D10a, DECISIONS `:2205` to `:2246`, defaults FINAL appended after `:2246`) is implemented on branch `worktree-r10-build-20260903` at commit `f1d2a6b` per `docs/PLAN_R10_INPUT_HARDENING_2026-09-03.md`. Static rows R10-S1 to R10-S4 are CLOSED on measured values, all quoted in the ACTIONS entry of 2026-09-03 below: three compiles at `Result: 0 errors, 0 warnings`, the four retired input names surviving on two comment lines only, the `AgEvaluateActive` breach tail byte identical at md5 `A3176C1C0E9F2D03586EAD6CBB175AA3`, and the five untouched includes identical to `00e2b43` by md5. One measured divergence from the plan's own expectation is recorded there and is not a defect: R10-S3 predicted one changed line above the breach tail and three changed, all three required by the plan's own 2.4 edit table. UPDATED 2026-09-07 BY THE ACCEPTANCE SESSION, ACTIONS entry of that date. SEVEN OF NINE LIVE ROWS ARE CLOSED ON QUOTED ARTIFACT LINES: R10-D deploy (ten of ten landed md5 equal to the worktree, first init refused on FIRST INIT HAZARD outcome (a) at `journal-20260907-r10-deploy-restart.txt:3205` to `:3210`), R10-0 (`AGVEC|SUMMARY|95/95`, eleven `r10_*` PASS, 84 plus 11), and R10-2 to R10-7, each refused with the raw value its vector carries, 0, 510, 575, 0, 12 and 205. R10-1 and R10-8 were DEFERRED to a second session after the 01:00 rollover, and they are NOW CLOSED, per the update at the end of this entry. One R10-D clause is UNREACHABLE and no substitute was taken: the properties reading on the running chart after the restart, because a refused init unloads the EA and the restart came up with no guardian. THE PLATFORM QUESTION THIS ENTRY CARRIED IS ANSWERED AND CLOSES: the terminal delivers an off-list `.set` integer to an enum input UNMODIFIED and does not coerce it, proven on six raw values reaching `OnInit` exactly as the six vector files carry them, so the rows refuse and PASS-BY-CONSTRUCTION applies to none of them. The `docs/SPEC_v0.1.md:114` executor reading recorded in the 2026-09-03 ACTIONS entry above is now RULED, owner ruling 2026-09-03, R10 build review, DECISIONS (end of section): ratified as an owner ruling, and the SPEC amendment series is closed at A6, no A7 for R10. CLOSED 2026-09-08 BY ACCEPTANCE SESSION TWO, ACTIONS entry of that date, which carries every measured value. THE LAST TWO LIVE ROWS ARE CLOSED ON QUOTED LINES IN `docs/evidence/journal-20260908-r10-acceptance.txt`, both run after the 01:00 rollover reseeded the floor from 114.74 to 126.21 at `:362`: R10-8 `a9_optional_bad.set` at `:627` to `:638`, with `limits accepted|percent=5.50|currency=200.00|raw=550/200` at `:628` and `WARN|optional config invalid: LogVerbosity unrecognised, falling back to NORMAL` at `:629`, ahead of the mutex step at `:630` as the plan required, reaching `SYNCING->ACTIVE|history stable|polls=3/3` at `:637`; and R10-1 `a9_defaults.set` at `:653` to `:663`, with the identical `limits accepted` line at `:654`, `SYNCING->ACTIVE|history stable|polls=3/3` at `:662` and `LIFE|state=ACTIVE|base=2294.76|limit=126.21` at `:671`. Two negative controls counted over the whole 672 line file: ZERO `ALERT` lines, so neither row refused, and EXACTLY ONE `optional config` line, R10-8's, so R10-1 emitted none, which is what distinguishes two rows carrying identical limit keys. NO HOLDING WARN on either row, the outcome the deferral was taken to obtain. SO R10 ACCEPTANCE IS COMPLETE AT NINE OF NINE LIVE ROWS AND FOUR OF FOUR STATIC ROWS. One R10-D clause remains UNREACHABLE with its reason recorded and no substitute taken, unchanged from 2026-09-07. Also settled by that session and not by a row: the halt file move between the two sessions, which is an unrecorded owner terminal close and reopen on 2026-09-07 at 15:23:00 to 15:26:19, corroborated on two independent artifacts.
Action: NOTHING FURTHER IS REQUIRED OF THIS ENTRY. The end state is `a9_defaults.set` as last used, one healthy instance ACTIVE on `XAUUSD.ecn,M15` with `enforced_limit=126.21`, and the halt file at md5 `5DBED6319B1248C9871B2548D3B52432`. The build remains UNMERGED on `worktree-r10-build-20260903` at `f1d2a6b` and no push has been performed; merging it is a separate decision under the merge gate ruling and is not authorized here. NEXT: the D2/D4 defect-fix plan session, per the next best action entry below.
CORRECTION 2026-09-08, same session, before session close: the clause above is FALSE and is left unedited so the correction is visible rather than silent. The owner merged the R10 build into main before this session began; branch `worktree-r10-build-20260903` no longer exists, locally or on origin. Verified: `git merge-base --is-ancestor f1d2a6b HEAD` on this worktree returns true, `f1d2a6b` is an ancestor of `d88a71f`, and `git branch -a` after a fetch lists no `worktree-r10-build-20260903` anywhere. The build IS merged into main.
Status: CLOSED 2026-09-08, R10 acceptance complete, nine of nine live rows and four of four static rows

Issue:  A REFUSED INIT UNLOADS THE EA AND THE NEXT RESTART COMES UP WITH NO GUARDIAN AT ALL, so an upgrade that lands a bad input set leaves the account unprotected across the next restart with nothing on the chart to say so. MEASURED 2026-09-07, not inferred: `docs/evidence/terminal-20260907-r10-deploy-restart.txt:29` `13:19:28.765 Experts initializing of AccountGuardian (XAUUSD.ecn,M15) failed with code 32767 (incorrect parameters)` and `:30` `13:19:28.773 Experts expert AccountGuardian (XAUUSD.ecn,M15) removed`. The decisive comparison is between the two terminal starts rather than the word removed, which also appears on an ordinary shutdown at `:14`: the 13:19:26 start carries `:23` `expert AccountGuardian (XAUUSD.ecn,M15) loaded successfully`, and the owner's 13:26:25 restart at `:35` carries NO expert line at all, with the Experts journal ending at 13:19:28.765. THE GUARDIAN IS NOT AT FAULT: returning `INIT_PARAMETERS_INCORRECT` on a non member limit is exactly what D1f requires, and this is the platform's response to a correct refusal. The exposure is procedural. The REFUSED banner the owner read at 13:19:29 was also gone after the restart although `:3210` says deinit deliberately left it, so the one visible warning did not survive either; no mechanism for that is established and none is asserted.
Action: RULED 2026-09-07, R10 acceptance review, DECISIONS (end of section): deploy procedure amended, after any build upgrade the EA is attached and a healthy init confirmed on the chart, its `limits accepted` line read, BEFORE any terminal restart; a refused init is corrected while the EA is still attached. Related and NOT the same item: whether the REFUSED banner should survive an unload at all is DEFERRED, not ruled, in the same owner ruling.
Status: CLOSED, ruled 2026-09-07

Issue:  THE PROPERTIES DIALOG IS NOT A WITNESS OF THE VALUE THE EA RECEIVES, measured 2026-09-07 at row R10-4 and recorded because it bears on the project's evidence standard and not merely on one row. Three values, all measured in the same row: the landed `a9_percent_over_ceiling.set` carries `DailyLossPercent=575`, read in place with md5 verified equal to the worktree at deploy; the properties dialog rendered `5.50`, a valid list member; `OnInit` received 575, quoted at `docs/evidence/journal-20260907-r10-acceptance.txt:3332`. The same file carries `DailyLossCurrency=200`, the top currency member, which rendered as an EMPTY field here while rendering as `200` in R10-2 and R10-3 from the identical key. Across the six refusal rows the EA side never diverged, receiving 0, 510, 575, 0, 12 and 205 exactly as the files carry them, so the unreliable component is the DIALOG RENDERING and not the `.set` channel. CONSEQUENCE FOR THE EVIDENCE STANDARD: an owner dialog reading remains owner eyewitness fact for what the dialog DISPLAYED, and it is no longer evidence of what the EA WILL RECEIVE. `LEDGER.md:2177` treats a dialog reading as owner eyewitness fact and nothing here overturns that; what changes is the proposition such a reading can support.
Action: NO CODE CHANGE IS PROPOSED and none may be inferred: the divergence is in the terminal's rendering of an enum input, outside this project's source. Two things a future session must not do, written so they are not done by habit: do not close any row on a dialog reading of an input value alone, and do not read tonight's finding as evidence that the `.set` channel coerces, which the same rows disprove. RULED 2026-09-07, R10 acceptance review, DECISIONS (end of section): no deliberate vector is authorized for a dialog rendering one valid member while `OnInit` receives another; the `limits accepted raw=` line is the witness after every input change. The observation stays banked.
Status: CLOSED, ruled 2026-09-07, observation banked

Issue:  ZERO GUARDIAN ENFORCEMENT EXISTS WHILE THE TERMINAL IS NOT RUNNING, evidenced concretely by the 31 minute 44 second gap of 2026-08-30, `docs/evidence/terminal-20260830-zero-positions.txt:19` `System|terminal stopped due to system shutdown` at `15:14:10` to `:20` `Terminal|MetaTrader 5 x64 build 6140 started` at `15:45:54`, during which about 192 was lost to mobile trades the guardian could only detect after the fact, on restart, by replaying server history. Owner ruling FOUR accepts that catch as correct; it does not rule on the window itself.
Action: OWNER DECIDES whether any mitigation is wanted, for example VPS hosting to shrink outage windows, which the terminal's own Trades log already advertises unprompted on the 2026-08-31 file at `docs/evidence/terminal-20260831-v1a-lock.txt:32`, or whether the after-the-fact catch demonstrated on 2026-08-30 is accepted as sufficient protection on its own. Owner direction 2026-09-02, DECISIONS R8: VPS. Recurrence 2026-09-01, ACTIONS A2.
Status: OPEN

Issue:  THE `stale_anchor_reconstructed` D3.1 BRANCH HAS ONLY BEEN EXERCISED IN ITS DEGENERATE ALL ZERO CASE, at the 2026-08-31 01:00:05 boot, `docs/evidence/journal-20260831-flat-expiry-and-v1a-lock.txt:125`. No banked evidence across any of the four days exercises the same branch with a NONZERO stale figure, which is the case where reconstruction and the stale persisted value could actually disagree and the branch would be tested rather than merely fired.
Action: A FUTURE SESSION captures a run where a restart or boot derivation hits a stale anchor carrying a nonzero peak, so the branch is exercised on the case that can distinguish a correct reconstruction from an incorrect one.
Status: OPEN

Issue:  DEFECT 2 OF 4 IN THE FIX ORDER RULED 2026-08-19. THE GV LOCK MIRROR ERASES ITSELF ON EVERY COLD BOOT, BEFORE ITS OWN WITNESS CAN READ IT. The mechanism is three lines of source in one callback. `OnTimer` writes the mirror at `AccountGuardian.mq5:881`, `GlobalVariableSet(AgGvLock(), (double)(long)g_ag_locked_until)` followed by `GlobalVariablesFlush()`, guarded only by `if(g_owns_mutex)`, and the boot derivation that READS that mirror runs at `AccountGuardian.mq5:926`, in the SAME `OnTimer`, AFTER the write. On a cold boot `g_ag_locked_until` is 0, because the only thing that sets it is `AgEnterLockFromBoot`, which does not run until the SYNCING exit several ticks later, so the first ticks flush a zero over the persisted mirror and the witness then fails the `(datetime)(long)gv_raw > now` test. LIVE EVIDENCE, `docs/evidence/journal-20260818-stage7-forced-kill.txt`, 2026-08-18, build `74D666E9`, three boots in one file and the split is exact. Cold boot one, `AG|2026.08.18 13:46:41|TRANSITION|BOOT->SYNCING|boot|weekly=on|timer=1s`, fired FILE and DERIVED at 13:46:44 and NO GV line. Cold boot two, `AG|2026.08.18 16:01:24|TRANSITION|BOOT->SYNCING|boot|weekly=on|timer=1s`, fired FILE and DERIVED at 16:01:27 and NO GV line. The ONLY GV firing in the whole day is `AG|2026.08.18 15:36:27|INFO|boot witness GV fired|raw=2026.08.19 01:00:00|bounded=2026.08.19 01:00:00`, on the one boot whose transition reads `LOCKED->SYNCING` and which therefore preserved its memory.
Action: FIX SECOND, per the ruled order, and the fix itself is not written in this session. Fix shape named and NOT RULED, since the owner reserves the design: the write is unconditional on state and could be gated, or ordered after the dispatch, or the boot read could be taken once in `OnInit` before any timer tick. SEVERITY, stated so the fix is scoped against what is actually lost: the GV witness fires only when the EA RETAINED ITS MEMORY, which is exactly the case where a mirror is redundant, and it is guaranteed zeroed in the case it was built for, a genuine restart. So the independent third recovery path does not exist in practice, and with the state file deleted a restart recovery rests on the derived history witness ALONE, making the strictest wins OR over three witnesses an OR over two. This does NOT weaken any lock the file and derived witnesses carry, and it does weaken the deletion resistance argument that justified building three.
Status: OPEN

Issue:  DEFECT 4 OF 4 IN THE FIX ORDER RULED 2026-08-19. LOCKED LIFE LINES CARRY NO NUMBERS. A LOCKED LIFE line prints state, `seconds_in_state` and `waiting_on` and nothing else, while an ACTIVE LIFE line prints anchor, realized, floating, base, limit and `pnl_vs_limit`. LIVE EVIDENCE, `docs/evidence/journal-20260818-stage7-forced-kill.txt`, 2026-08-18, build `74D666E9`, and the cost was paid the same day rather than being hypothetical: at 13:38:07 the broker force closed thirteen positions on this account, and the line the guardian emitted across that event is `AG|2026.08.18 13:38:02|LIFE|state=LOCKED|seconds_in_state=2121|waiting_on=expiry: TimeCurrent >= locked_until|server=2026.08.18 13:38:02|local=2026.08.18 13:38:03`, carrying not one figure. Seventy six consecutive LOCKED LIFE lines from 13:03:03 to 13:40:33 are identical apart from the counter and the two clocks.
Action: FIX FOURTH, per the ruled order, and the fix itself is not written in this session. WHAT THIS ACTUALLY COST, recorded so the fix is scoped against evidence rather than against a feeling: when the owner asked for balance and equity at the moment of the liquidation the executor could supply NEITHER from any readable artifact, because no artifact carries them, and the same gap blocked a direct answer three further times in one session, on the input value the EA booted with, on the 2133 deposit landing while locked, and on the account state through the P2-C disconnect. Naming the obvious scope WITHOUT RULING IT, since the owner reserves the design: the numbers block already exists and is already computed for the ACTIVE path, so the question is which fields still mean anything under a lock rather than what to compute.
Status: OPEN

Issue:  P2-A IS OPEN AND IS NOT A CODE DEFECT. It is an acceptance row NEVER WITNESSED, not a mechanism that failed. The row asks for exactly one `TRANSITION ACTIVE->LOCKED` line carrying the full breach arithmetic. LIVE EVIDENCE, `docs/evidence/journal-20260818-stage7-forced-kill.txt`, 2026-08-18, build `74D666E9`: `ACTIVE->LOCKED` appears ZERO times in the whole day and `SYNCING->LOCKED` appears four times. THE RUNNING PATH WAS PRE EMPTED BY A CHART CHANGE INSIDE A TWO SECOND WINDOW, quoted in sequence: `AG|2026.08.18 13:02:37|WARN|breach deferred one pass: no new deal visible, count=21`, which is the Q9 one pass deferral behaving exactly as ruled, then `AG|2026.08.18 13:02:39|INFO|deinit|reason=3|session marked clean|timer_armed=1|timer_ticks=5586`, the owner changing the chart period from M1 to M15, so the second pass the deferral was waiting for NEVER RAN. The replacement session caught the same breach independently three seconds later through the boot derivation, `AG|2026.08.18 13:02:42|TRANSITION|SYNCING->LOCKED|boot derivation: DAILY_BREACH|locked_until=2026.08.19 01:00:00`. The lock is real, correct and fully evidenced, and it arrived by the wrong path for this row.
Action: OWNER PERFORMS THE OBSERVATION PROCEDURE, written this session at `docs/PROCEDURE_P2A_OBSERVATION_2026-08-19.md` and not performed by the executor. THE LIVE WINDOW IT NEEDS: one ACTIVE breach on the demo, taken with the chart, the timeframe, the properties dialog, the template and the terminal all left untouched from the first losing position until the LOCKED LIFE lines are running, so that no re init can pre empt the pass. NO ATTEMPT IS MADE TO PROVE THIS ROW BY CODE READING and none may be: the deferral, the transition and the arithmetic are all reachable in source, and reading them would prove the code contains the path rather than that the path ran, which is the whole of what the row exists to witness.
Status: OPEN

Issue:  P2-H IS OPEN AND IS NOT CLOSABLE BY CODE OR BY CODE READING, ruled 2026-08-19 and FINAL in DECISIONS of that date. The row's claim is that a limit raised while locked produces a loud WARN while the Q6 snapshot holds. LIVE EVIDENCE, `docs/evidence/journal-20260818-stage7-p2h-input-change.txt`, 2026-08-18, build `74D666E9`: the owner raised `DailyLossPercent` from 5 to 10 while the account was locked at 15:36 and there are ZERO `input changed while LOCKED` lines in the journal, so the row is UNTESTED rather than passed or failed. The cause was published before the row ran and is now ruled intended behaviour: MT5 reloads the EA on an input change, `INFO|deinit|reason=5`, the reload is visible as `AG|2026.08.18 15:36:26|TRANSITION|LOCKED->SYNCING|boot|weekly=on|timer=1s`, and `AgEnterLockFromBoot` reseeds the Q6 input witness from the LIVE inputs at `AccountGuardian.mq5:386` to `:388`, so the witness captured percent 10 and had nothing left to warn about.
Action: WAIT FOR THE LIVE WINDOW THE RULING NAMES, the coming weekend market close, and do nothing else with this row. A substitute test stays FORBIDDEN under the standing instruction of 2026-08-18, since a substitute would prove something else while carrying this row's name. Recorded so the two entries are not read as contradicting each other: the reseed ruling of 2026-08-19 settles that the reseed behaviour is correct and is not a defect, and it does NOT close this row, because what P2-H measures is whether the WARN fires on a LOCKED evaluation pass that observes a divergence, which no input change through the dialog can produce while the reload always reseeds first. The row may prove unreachable through the terminal UI altogether, and that outcome is the owner's to declare rather than the executor's to infer.
Updated 2026-08-23 by the weekend harvest. THE WINDOW THE RULING NAMED HAS PASSED AND THE ROW STILL DID NOT RUN, and it did not fail either, because its PRECONDITION NEVER AROSE. The row needs a LOCKED evaluation pass to observe, and THE ACCOUNT WAS NEVER LOCKED at any point in the window. Every state value appearing in the four banked journals is ACTIVE or SYNCING and nothing else. Stated precisely so no later session miscounts: a plain search for LOCKED returns exactly one hit in each of Saturday and Sunday and BOTH are the substring inside `locked_until` in the boot line `INFO|lock state file loaded|reason=-|locked_until=1970.01.01 00:00:00|weighed by the boot derivation at the SYNCING exit`, which is the epoch value a file with no lock carries. The string `input changed` occurs ZERO times in all four files. There was no exposure to lock on: every LIFE line reads `realized=0.00|floating=0.00|base=2133.00|limit=106.65` and the terminal journal confirms `0 positions, 0 orders` at each authorization. THE REASON THE WEEKEND DID NOT SUPPLY THE ROW HAS NOTHING TO DO WITH THE MARKET BEING SHUT: the row needs a lock, a lock needs a losing position, and there was none. So the market close was necessary for the row's setting and was never sufficient for the row itself, which is worth writing down because the 2026-08-19 ruling named the weekend as the window and a later session could otherwise read this harvest as the window having failed. NO SUBSTITUTE WAS ATTEMPTED and none may be, per the standing instruction of 2026-08-18. The ruling's other clause is untouched and is restated rather than acted on: the row may prove unreachable through the terminal UI altogether, since a reload always reseeds the witness first, and THAT OUTCOME IS THE OWNER'S TO DECLARE. Nothing here declares it. Evidence: the four journals banked at commit `c843be5` and listed in the ACTIONS entry of this date.
Status: OPEN, the named weekend window has passed without supplying the row, because no lock existed to observe

Issue:  THE MARKET CLOSED DISTINCTION HAS NEVER BEEN OBSERVED, and it is the last unmeasured item of the Stage 6 observability set. `AgObservabilityNote` returns `AgQuoteSessionOpen() ? ("quote_age=" + age + "s") : "market closed"`, so the two branches are mutually exclusive and only one of them has ever appeared in an artifact. LIVE EVIDENCE THAT THE OTHER BRANCH WAS NOT TAKEN, `docs/evidence/journal-20260819-stage7-lock-expiry.txt`, 2026-08-19, build `74D666E9`: across the entire 61 minute 32 second nightly break the note read the OPEN session branch and climbed to `AG|2026.08.18 23:57:59|LIFE|state=LOCKED|seconds_in_state=32283|waiting_on=expiry: TimeCurrent >= locked_until|quote_age=3691s|server=2026.08.18 23:57:59|local=2026.08.19 00:59:30`, so the broker's own session table reported the quote session OPEN throughout a completely frozen feed. Stated as one night's observation and not as a rule, it narrows how this item can ever close: if that behaviour holds generally then the `market closed` branch cannot be reached during a nightly break at all.
Action: WAIT FOR AN OUT OF SESSION WINDOW. THE LIVE WINDOW IT NEEDS: a weekend or a holiday, with the guardian left running and the machine untouched, which is the same harvest shape the P1-B weekend runs already used. NOT CLOSABLE BY CODE OR BY CODE READING for the same reason P2-A is not: the branch is reachable in source, and reading it proves the code contains it rather than that the broker ever reported the session closed. This entry REPLACES the Stage 4 boot derivation entry, which had already narrowed itself to this single item once all three boot witnesses and both degraded channels were measured on artifacts during Stage 7. That entry's full history is preserved in the ACTIONS entries of 2026-08-18 and 2026-08-19 and nothing of it is lost.
Updated 2026-08-23 by the weekend harvest, which supplies the out of session window this entry has been waiting for and DOES NOT CLOSE THE ROW. The `market closed` branch was NOT taken. The string occurs ZERO times across all four banked journals, counted over the whole files and not sampled, while the open session branch occurs 120 times Thursday, 23 times Friday, 2874 times Saturday and 2393 times Sunday. SATURDAY IS THE DECISIVE FILE, a complete market closed day of 2878 LIFE lines whose age climbs monotonically to `AG|2026.08.21 23:57:59|LIFE|state=ACTIVE|seconds_in_state=340175|waiting_on=quote_age=84698s|anchor=2026.08.21 01:00:00|realized=0.00|floating=0.00|base=2133.00|limit=106.65|pnl_vs_limit=0.00 vs -106.65|server=2026.08.21 23:57:59|local=2026.08.22 23:29:35`, which is 23 hours 31 minutes 38 seconds of frozen feed reported as an OPEN quote session. The four Saturday LIFE lines carrying no note are accounted for and none is a `market closed` line: one SYNCING line and three post boot ACTIVE lines under the ruled 120 second threshold. SO THE BROKER SESSION TABLE REPORTED THE QUOTE SESSION OPEN CONTINUOUSLY ACROSS THE ENTIRE WEEKEND CLOSE, on three independent accumulations totalling more than 44 hours, the third still climbing when the artifacts end. The narrowing this entry recorded from one night now extends to a weekend: on this broker and this Market Watch the `market closed` branch is not reached during a weekend either. STATED AS OBSERVATION AND NOT AS RULING. Whether the row is reclassified as unreachable by construction, the class amendment A5 and P1-K occupy, is the owner's to declare and nothing is reclassified here. MEASURED FOR FREE AND RECORDED BECAUSE IT BEARS ON THE SAME ROW: the note is SUPPRESSED WHOLE on a DEGRADED pass, all nine Sunday DEGRADED lines reading `waiting_on=DEGRADED: disconnected, no breach decisions|DEGRADED` with neither branch present, so a reader holding a DEGRADED line cannot tell whether the market was open or shut. TWO TERMINAL RESTARTS SIT INSIDE THE WINDOW, Saturday 23:29:57 to 23:30:18 and Sunday 20:45:30 to 20:46:17, and they truncated the CONTINUOUS MEASURE into three runs without touching the verdict, since every run reports the same branch. Evidence: `docs/evidence/journal-20260820-weekend-thursday.txt`, `journal-20260821-weekend-friday.txt`, `journal-20260822-weekend-saturday.txt`, `journal-20260823-weekend-sunday-vector-run.txt` and `journal-20260823-restart-window-extract.txt`.
Status: OPEN, the out of session window has now been observed and the `market closed` branch was still never taken

Issue:  UNRULED 2026-08-18. PHASE 3 DESIGN INPUT, MAXIMUM POSITION SIZE DERIVED FROM THE DAILY LIMIT. Owner input of this date, recorded during Stage 7 between Part 2 and Part 3, and it is an INPUT rather than a ruling: it is written here in the owner's terms and nothing in it is settled. The hazard: a position large enough to lose the entire daily limit in one move defeats the guardian, because the guardian locks only after the loss has landed. The proposal: the sweep in Phase 3 gains a size gate, and a position whose potential loss at a reasonable stop distance exceeds a fraction of the current enforced limit is closed on detection, through the same trade API family as sweep and flatten. Formula and fraction are to be ruled at Phase 3 kickoff. NOTHING IN PHASE 2 CHANGES and no code was touched when this was recorded.
Action: Four consequences the executor derived while recording this, offered as material for the kickoff and explicitly NOT as rulings or as a recommendation either way. ONE, this would be the first trade API call in the project. S1 and S2 have held at literally zero calls since Phase 0, most recently re run this session, and the only hit either row has ever returned is the header comment in `Sweep.mqh` asserting the rule. A size gate that closes positions changes the meaning of those rows from "no call exists" to "calls exist and are confined to `Sweep.mqh`", which is a different and weaker invariant, and the kickoff should decide the rows' new wording deliberately rather than let them quietly degrade. TWO, it sits in tension with ruling TWO, which keeps positions open when a lock is declared. Every enforcement the guardian owns today refuses, halts or locks and none of it touches a position; this one closes one. Reconciling the two is a kickoff question, not a detail: the natural reading is that ruling TWO governs the post breach window and the size gate governs the pre breach window, but that reading is not written anywhere and should not be assumed. THREE, "a fraction of the current enforced limit" has three candidate referents after question SEVEN and they diverge. The cascade is snapshot, then floor, then live, and the ratcheted floor can sit well below the live limit, so the same rule reads differently depending on which tier it binds to. FOUR, "potential loss at a reasonable stop distance" needs per symbol data the project does not yet hold: tick value, contract size and the broker's stops level. That is the Q7 symbol specification table, still unread since Phase 1 and still carrying the A8 corroboration debt, so this proposal inherits that debt rather than adding a new one.
UPDATED 2026-08-25 by the merge of `evidence/manual-trades-20260811`, on the fourth consequence's factual clause only; the consequence itself stands unchanged. The Q7 symbol specification table is NOT unread. It was read at the terminal on 2026-08-11 and Q7 closed WHOLE that day, discharging the A8 corroboration debt, evidence `docs/evidence/phase1-manual-trades-2026-08-11.md` with `docs/evidence/journal-20260811-manual-trades.txt` and `docs/evidence/terminal-journal-20260811-manual-trades.txt`, landed on main by commit `d4e2869`. So this proposal inherits NO corroboration debt. What it still needs is unchanged: the per symbol tick value, contract size and broker stops level carried into whatever formula the Phase 3 kickoff rules.
Status: UNRULED, Phase 3 input only, blocks nothing in Phase 2 and awaits the Phase 3 kickoff

Issue:  Next best action, and it is the owner's to run rather than the executor's. THE README TOKEN USAGE BADGE WRITER IS STILL LIVE AND IT LIVES OUTSIDE THIS REPOSITORY. The block itself is gone from README.md at commit `b8678f1`, eleven lines removed including the `<!--TOKENS-->` and `<!--/TOKENS-->` delimiters, per the owner's ruling of 2026-08-17 which is FINAL in DECISIONS of that date. That removal does NOT hold on its own and the reason is measured in the writer's own source rather than argued. Location, named exactly so the owner can act without hunting: `C:\Users\www83\.claude\token-tracker\`, which is inside the user profile and outside both this repository and the MetaTrader Terminal folder. Three parts. The script `Update-TokenReport.ps1`, 13284 bytes, last written 2026-08-02 16:25:26. The registration `projects.json`, which maps project id `lockmt5` to path `C:\Users\www83\Downloads\AI\AccountGuardian`, names this repository's README.md as its target, and carries `autoCommit` true. And the Windows Task Scheduler task `ClaudeTokenReport`, daily at 08:00 with StartWhenAvailable, read this session as State Ready, LastRunTime 2026-08-17 08:00:01, LastTaskResult 0, NextRunTime 2026-08-18 08:00:00. Its own log `run.log` records a run on every one of 2026-08-14, 08-15, 08-16 and 08-17, each ending `git=committed`, and the commit string it writes is `chore: token usage badge [skip ci]`, emitted at `Update-TokenReport.ps1:241`. THE MECHANISM THAT MATTERS, and it is the whole reason this entry exists rather than a note: `Update-Readme` at `:186` to `:211` does not merely replace an existing block. If the marker pair is present it replaces in place at `:199`. If the file is non empty and the markers are ABSENT it PREPENDS the entire badge to the top of the file at `:203`. So deleting the block is precisely the input that makes the job re author it whole, which is exactly the sequence that produced the committed conflict markers the 2026-08-17 merge caught: the owner deleted the header by hand, the job prepended a fresh block above what remained, and the two states were committed together.
Action: OWNER DISABLES IT. The executor did not, and will not, because the instruction of 2026-08-17 says to name the location and stop when the writer lives outside this repository. Two routes, neither recommended over the other because they differ in scope rather than in effect on this repository. Narrow, leaving the tracker running for any other project: delete the `lockmt5` object from the `projects` array in `C:\Users\www83\.claude\token-tracker\projects.json`, which stops both the README write and the auto commit for this repository while leaving the job itself alone. Whole, since the badge is ruled removed for good: `Disable-ScheduledTask -TaskName 'ClaudeTokenReport'`, or `Unregister-ScheduledTask -TaskName 'ClaudeTokenReport' -Confirm:$false` to remove it outright. Until one of those runs, expect the block back at the top of README.md at the next 08:00 firing, committed locally under the badge string above. The repository side was searched and is clean, recorded so nobody searches it twice: no `.github` directory exists at all, `.git/hooks` carries only the 212 byte `commit-msg` attribution stripper alongside the stock samples, a grep for TOKENS, token usage badge and token-usage across the whole tree returns hits only inside README.md itself and in prose inside LEDGER.md and docs/PLAN_PHASE1_PNL_CORE.md, and the owner's PowerShell profile at `C:\Users\www83\OneDrive\Documents\WindowsPowerShell\Microsoft.PowerShell_profile.ps1` carries `Initialize-GitWorkspace` and `Start-ClaudeWorkspace` but writes no badge and names no TOKENS marker anywhere. Nothing under the MetaTrader Terminal folder was read, written or approached, per Rule A.
Status: OPEN, owner action, block removed at `b8678f1` and it returns at the next scheduled run until the job is disabled

Issue:  P1-N IS GATED ON P1-L, established by source read 2026-08-11 and recorded so no future session wastes a pass trying to catch a deferral line during ordinary trading. The Q9 coherence-deferral emit sits strictly inside the breach branch at `AccountGuardian.mq5:217-223`: `bool breach_now = (pnl <= -limit + AG_PNL_EPSILON); if(breach_now) { if(current_count == g_ag_last_deal_count && !g_ag_breach_deferred_once) { AgWarn("breach deferred one pass: no new deal visible, count=" + (string)current_count); ... } }`. No breach means no deferral line can ever be written, whatever the deal-count coherence does. The plan's P1-N row reads "a demo sequence where a manual close's Balance update is observed to outrace HistoryDealsTotal by one pass, or a forced synthetic override if no such sequence occurs naturally", which is accurate about the coherence condition but silent on the breach precondition, and that silence is what this entry closes.
Action: None until P1-L runs. P1-N closes only inside a breach window, so the two rows are one scheduling unit and must be planned together: manufacture the loss, then watch for exactly one deferral line followed by a declaration on the next pass. The alternative route the plan already names, a forced synthetic override, remains available and does not need a live breach. Recorded 2026-08-11 during the manual-trades session, where eight deals produced zero deferral lines exactly as this gating predicts.
Status: OPEN, folded into P1-L scheduling

Issue:  MACHINE SLEEP FREEZES THE MUTEX HEARTBEAT FAR BEYOND THE STALENESS THRESHOLD, measured for free on 2026-08-05 when the machine slept mid-batch, and it is exactly the condition the dedicated-machine hardening exists to prevent. The window ran 16:17:35.941 to 17:21:27.077, 3832 seconds. Measured, not inferred: LIFE lines are WHOLLY ABSENT across it rather than partially flushed, one line at each edge and none of the roughly 127 that should sit between them; seconds_in_state advanced 6886 to 10718, a delta equal to the wall-clock hole to the second, so that counter is a timestamp difference and not a tick count and the state survived the sleep intact with no re-init and no transition at wake; and the halt file did not move at all, its mtime still sitting at the preceding re-init. The mutex consequence follows from a verified emission path rather than from silence: LIFE is emitted from OnTimer and is ungated, so its absence is admissible evidence that OnTimer did not fire, and AgMutexRefresh is the first unconditional statement of OnTimer, so the heartbeat sat frozen at its last pre-sleep value for about 383 times AG_MUTEX_STALE_SECONDS. Labelled an INFERENCE and not a measurement, deliberately: reading the live GV from outside is forbidden by standing observation rule 1 and it was not done. Consequence: a sleeping machine presents a healthy live guardian as an abandoned one, so any second instance starting inside that window takes the mutex from an instance that is perfectly fine. Nothing else was running here, so nothing happened.
Action: The hardening rationale now rests on measurement instead of reasoning, which is the whole value of the accident. Second finding recorded with it and NOT explained away: timer_ticks does not reset across an in-place re-init, which is further confirmation of the inherited-image property, and the session spanning the sleep delivered 3453 ticks across 11234 seconds of wall clock at one tick per two seconds, leaving about 496 seconds, some 248 ticks, unaccounted for even after subtracting the visible 3831 second gap. Suspend and resume transitions are the obvious suspect and nothing is claimed. The anomaly is bounded to the sleep: the very next session, 17:30:03 to 17:53:17 with no sleep in it, delivered 1393 ticks across 1393.4 seconds, an exact one per second. Operational rule for the rest of this batch and any future timed run: tick counts spanning a sleep are not a valid measure, and the halt file is.
Status: OPEN, Phase 1 and hardening input

Issue:  Standing measurement obligation, opened 2026-08-08 by the Phase 1 investigation: the broker server DST peg is documented but NOT measured. JustMarkets publishes server time as GMT+2 in winter and GMT+3 in summer, which means the server observes a DST calendar, and the calendar it follows determines the windows where server time and Israel civil time disagree by an hour. The plan enumerates the divergence windows for each candidate peg (US, EU, fixed GMT+3) rather than picking one, because picking one would be an assumption dressed as a fact. Own evidence is consistent with a US peg and proves nothing: the measured Monday reopen at server 01:00:20 equals Sunday 18:00 New York under GMT+3 with US DST in force, which is the metals-session convention, but a single reopen stamp cannot distinguish the pegs. Both server and Israel DST transitions land inside the weekend freeze on this Market Watch, so TimeCurrent never visibly steps from a server DST change; the Monday reopen simply stamps at the new offset, which is exactly why this needs a deliberate harvest rather than a passive observation.
Action: Harvest the dual-clock LIFE lines across the window 2026-10-25 to 2026-11-01, the first calendar stretch where Israel has left DST and the candidate server pegs disagree. Both clocks are already on every LIFE line since the 72ed960 fix, so the offset is readable per line and the transition instant is bounded by the lattice spacing. Record the measured offset before and after and name which peg it settles. Until measured, no session cites a server DST calendar as fact.
Corrected 2026-08-10 by the reopen measurement, figure only, conclusion unchanged. The "measured Monday reopen at server 01:00:20" this entry cites is a SAMPLING ARTIFACT and must not be re-cited as the resume instant: it was read off a LIFE line, and LIFE lines land on a 30 second grid. Measured today at the actual event lines, the resumed server clock came back at exactly `2026.08.10 01:00:00`, 21 seconds earlier, with the first Monday LIFE line stamping server 01:00:21 which is where the old figure came from. The peg reasoning is UNAFFECTED, because 01:00:00 server under GMT+3 is still Sunday 18:00 New York, so the entry's "consistent with a US peg and proves nothing" verdict stands exactly as written. Two things the reopen adds for the October harvest. The dual-clock fields are confirmed present and usable on every LIFE line, `server=` and `local=` both, which is the instrument this harvest depends on. And the local clock currently trails the server clock by about one second, measured across 33 post-resume lines, so the harvest must read the offset from the two fields on the same line and never infer it from the terminal's own line timestamp.
Status: OPEN, Phase 1 input, window opens 2026-10-25

Issue:  External reads of bases\gvariables.dat while the terminal is running are unreliable as evidence, in both directions: the file is a snapshot the process owns, absence there does not prove absence in the live store, and a stale length does not prove a stable variable set. One wrong conclusion has already been drawn from it.
Action: Never cite that file as evidence again. The live store is read through the terminal F3 dialog or from inside MQL5 via GlobalVariableCheck and GlobalVariableGet.
Status: OPEN

Issue:  NOTE, deferred phase. common.ini carries WebRequest=1 with the URL whitelist stored as an opaque 534-character encoded blob that cannot be decoded from outside the terminal. v0 has no network path, so it is inert now.
Action: Leave it. Re-examine when the deferred visibility phase opens, together with the fresh network-heartbeat check and its new UUID.
Status: OPEN

Issue:  A prior, unrelated AccountGuardian implementation exists inside the terminal data folder, outside this repo: MQL5\Experts\AccountGuardian_foreign.mq5.bak dated 2026-07-24, plus metaeditor.log references from 2026-07-25 to an AccountGuardian.mq5 and three AG_* scripts that are no longer on disk. The charter declares this project greenfield and any reference to prior implementations invalid by definition.
Action: Not read, not used, not deleted. Flagged for the owner. Nothing in this build derives from it.
Status: OPEN

---

## ACTIONS

2026-09-08. D2 AND D4 DEFECT FIX PLAN SESSION, PLAN ONLY. No source file was written, no compile was run, nothing was deployed, no vector file was edited, and nothing under the MetaTrader Terminal data folder was read, written or approached, per RULE A; no command named a path inside it, so RULE B's alias audit was not reached. Work in worktree `d2d4-plan-20260908` on branch `worktree-d2d4-plan-20260908` from `bd61a0f`, main's own head, verified after creation rather than assumed: `git rev-parse HEAD`, `main` and `origin/main` all return `bd61a0f3a44e1ff3f80ebb4dafdc7040846d376b` and `git status --porcelain` was empty, so the harness `origin/main` hazard of 2026-08-19 did not bite this time. LEDGER.md was read in full, all 2314 lines, before any of the work below, per execution rule 2. Two commits: `docs/FIXPLAN_PHASE3_DEFECTS_2_4_2026-09-08.md` (`eb72f5329666474b98a15f5702fa0992e71355f2`), 547 lines and 86098 bytes, scanned byte by byte and carrying ZERO non ASCII bytes and zero em dashes, confirmed present by `git ls-files` rather than by the commit's exit code per the standing lesson of 2026-08-19; and this entry, the second commit. THE PLAN quotes both ISSUES entries and the fix order FINAL whole, tags forty one FINAL entries INHERITED and finds ZERO clauses that any option supersedes, maps every read, write and journal site of the GV mirror at `bd61a0f` (the write at EA:1083-1087, the read at EA:259-271, the three setters of `g_ag_locked_until` at EA:386, :452 and :550, and the stability gate at Pnl.mqh:426-449 that puts the read on the third tick) across the cold boot, chart re init and 01:00 rollover paths, maps the single LIFE emitter Log.mqh:66-87 with its single call site EA:1109-1110 and what each argument carries in LOCKED, and lays out two closed options per defect: for defect 2 a state gate on the write or a boot capture of the mirror in OnInit, with reordering the write after the dispatch examined and CLOSED by tick arithmetic, since the derivation cannot run before the third tick and the write has already zeroed the mirror on ticks one and two. For defect 4 a snapshot and account group or a full recompute at the LIFE cadence. Each option is worked against the 2026-08-31 lock (`docs/evidence/journal-20260831-flat-expiry-and-v1a-lock.txt:3939` to `:3944`) and the 2026-08-30 battery outage boot (`docs/evidence/journal-20260830-rollover-and-boot-breach.txt:3663` to `:3670`), where BOTH defect 2 options change nothing on that boot itself, the account having been unlocked when power failed so the mirror already held 0, and would change the boot after it. Predictions P61 to P75 continue the defect 3 plan's numbering unbroken. THE RULING SHEET carries fifteen closed questions, D2D4-1 to D2D4-15: fix shape per defect, journal witness, equity source, `breach_time` provenance under the shape 1 FINAL, DEGRADED prefix, CORRUPT_STATE rendering, banner, vector reach, one build or two, the deploy hand, what the deploy row records of the 2026-09-07 procedure, the build label, P3-2's boot count, and the SPEC and README edits, each with the consequence of every option and NO RECOMMENDATION. THREE FACTS MEASURED THIS SESSION AND RECORDED BECAUSE THEY BEAR ON LATER SESSIONS. ONE, the source at `bd61a0f` is md5 identical to the R10 build identity on all six files that entry names, `AccountGuardian.mq5` `4F2939FD092711C9391A5F763F8ACA8A` at 68001 bytes among them, so this plan cites the source of the binary now guarding the account. TWO, `ACCOUNT_EQUITY` has ZERO occurrences in the whole `MQL5/` tree and the only `AccountInfoDouble` read anywhere is `Pnl.mqh:254`, which is what makes D2D4-4 a real question rather than a formality. THREE, the vectors script's include graph reaches `Persist.mqh` directly and `Log.mqh`, `Clock.mqh`, `State.mqh` and `Pnl.mqh` transitively and nothing of the EA, so neither defect 2 option is vector reachable and the defect 4 formatter is reachable only if it is placed in an include. VERIFICATION OF THE PLAN'S OWN CITATIONS, recorded honestly rather than claimed whole: a fourteen agent read only verification pass was launched and THIRTEEN OF ITS AGENTS DIED ON A SESSION LIMIT before returning. The one that completed byte compared the seven quoted ISSUES and DECISIONS blocks against `LEDGER.md:31`, `:32`, `:35`, `:36`, `:1898`, `:1899` and `:1900` and found all seven equal at identical lengths, confirmed every 2026-08-18 evidence line the plan cites, and found ONE defect, a quote attributed to `LEDGER.md:1024` that wraps across `:1024-1025`, which is corrected in the committed document. The executor then re derived the three load bearing claims by grep rather than leaving them to the dead agents: `g_ag_locked_until` is assigned at exactly three sites, the six `g_ag_last_*` globals and `g_ag_have_pnl_numbers` at exactly one block, and the include graph as above. The refuter and completeness passes DID NOT RUN and no claim rests on them. The build's ISSUES and ACTIONS entries are drafted inside the plan and not written. No FINAL entry was written or edited, no defect was marked DONE, nothing is merged and the executor performed no push.

2026-09-08. R10 ACCEPTANCE SESSION TWO, THE TWO DEFERRED ROWS CLOSED, R10 ACCEPTANCE COMPLETE AT NINE OF NINE LIVE ROWS. Worktree `r10-acceptance-session2-20260908` on branch `worktree-r10-acceptance-session2-20260908`, taken from main's own head `d88a71f`. ROLES exactly as on 2026-09-07: the owner performed every removal, attach, Load, dialog reading and OK; the executor dictated one action at a time and stopped, ran only read only commands, and wrote nothing under the MetaTrader Terminal data folder. Rows were run in the order R10-8 then R10-1, so the end state is `a9_defaults.set`.
RULE B ALIAS AUDIT, run before any path inside the data folder was named. Hazard confirmed live on all seven audited names: `rd`, `del`, `ri` and `rm` resolve to `Remove-Item`, `mv` to `Move-Item`, `cp` to `Copy-Item`, `sc` to `Set-Content`. MEASURED DIFFERENCE FROM THE 2026-09-07 RECORD, written so it is not later read as drift: today all seven carry `AllScope`, and two of them, `ri` and `sc`, carry `ReadOnly` in addition, whereas the 2026-09-07 entry recorded "four of them AllScope". Today's reading is seven of seven AllScope. Nothing follows from the difference for any row; it is recorded because the audit is the safety gate and its output should be comparable across sessions. `Get-FileHash` resolves to the stock function of the shipped `Microsoft.PowerShell.Utility` module at `C:\WINDOWS\system32\WindowsPowerShell\v1.0\Modules\Microsoft.PowerShell.Utility\Microsoft.PowerShell.Utility.psd1`, and no user defined function shadows any of `Get-FileHash`, `Get-ChildItem`, `Get-Item`, `Get-Content`, `Copy-Item` or `Set-Content`. Every executor command naming a path inside the data folder was a plain inline `Get-FileHash`, `Get-Item` or `Get-Content`, with no user defined helper in any call chain.
BUILD IDENTITY UNDER DATA, all four equal to the values banked at the 2026-09-07 deploy, so the artifact under test is the same artifact: `AccountGuardian.ex5` `842C6E52483A9D3469E1AD8A12266ACD` at 96912 bytes, mtime 2026-09-03 15:31:33; `AgPhase2StateVectors.ex5` `C192FDD2C758D540373C16CC9D904AAF` at 52144 bytes; `a9_defaults.set` `05730F31D7543F4D017D720EB83A3C01` at 87 bytes; `a9_optional_bad.set` `2983A50A9E2431474273C70AE59B64F2` at 88 bytes, both vectors at mtime 2026-09-03 15:34:52.
PRECONDITION, THE 01:00 ROLLOVER, TAKEN FROM THE ARTIFACT AND NOT FROM A REPORT. In `docs/evidence/journal-20260908-r10-acceptance.txt`: `:359` is the last of 120 HOLDING WARN lines for the night, `AG|2026.09.08 00:59:55|WARN|ratchet HOLDING against a raised limit (question SEVEN): live=126.21 floor=114.74; the floor is enforced until the next day anchor`, the run beginning at `:2`; then `:361` `AG|2026.09.08 01:00:00|INFO|day rollover|old_anchor=2026.09.07 01:00:00|new_anchor=2026.09.08 01:00:00|jump=86400s`, `:362` `INFO|ratchet reseeded at rollover|old_anchor=2026.09.07 01:00:00|new_anchor=2026.09.08 01:00:00|old_floor=114.74|new_floor=126.21`, `:363` `INFO|realized peak reset at rollover|old_peak=0.00|new_peak=0.00`. ZERO HOLDING lines appear after `:363` anywhere in the file, counted over the whole file and not sampled, so both rows ran on a reseeded floor as the deferral intended.
R10-8 `a9_optional_bad.set` CLOSED, OUTCOME AS PREDICTED IN EVERY CLAUSE. Halt file md5 before the attach `0925940AF73F6EB00C3C1704223DECFD` at 555 bytes, mtime 2026-09-07 15:26:19. Owner dialog reading before OK, banked as what the dialog DISPLAYED per the 2026-09-07 ruling and not as evidence of what the EA receives: `DailyLossPercent` `5.50`, `DailyLossCurrency` `200`, `WeeklyReportEnabled` `TRUE`, `LogVerbosity` EMPTY. Journal lines, all in `journal-20260908-r10-acceptance.txt`: `:626` `02:05:25.710` `INFO|deinit|reason=1|session marked clean|timer_armed=1|timer_ticks=38346`, reason 1 being the EA removal and therefore the fresh load precondition of `LEDGER.md:1801`; `:627` `02:06:56.416` `INFO|init|build=R10|account=1200252169|server=JustMarkets-Demo3`; `:628` `INFO|limits accepted|percent=5.50|currency=200.00|raw=550/200|both list members (D1f), effective limit is the min of the two legs`; `:629` `WARN|optional config invalid: LogVerbosity unrecognised, falling back to NORMAL`; `:630` `INFO|mutex acquire|id_name=AG_ID_1200252169|id_set=1|id_exists=1|hb_name=AG_HB_1200252169|hb_set=1|hb_exists=1|hb_value=1788833216`, which places the WARN BEFORE the mutex step exactly as the plan's R10-8 row requires; `:631` `INFO|lock state file loaded|reason=-|locked_until=1970.01.01 00:00:00`; `:632` `INFO|ratchet floor loaded|anchor=2026.09.08 01:00:00|floor=126.21`; `:633` `INFO|realized peak loaded|anchor=2026.09.08 01:00:00|peak=0.00|source=persisted`; `:634` `TRANSITION|BOOT->SYNCING|boot|weekly=on|timer=1s`; `:635` `INFO|timer armed|period=1s`; `:637` `02:06:59.439` `TRANSITION|SYNCING->ACTIVE|history stable|polls=3/3`; `:638` `INFO|realized peak reconciled|reconstructed=0.00|persisted=0.00|taken=0.00|source=equal_anchor_max`. Init proceeded, no HOLDING WARN, owner banner reading NONE. Halt file md5 after `6F215A22DE35DB0216BFA3AFD6C87828` at 555 bytes.
R10-1 `a9_defaults.set` CLOSED, OUTCOME AS PREDICTED IN EVERY CLAUSE, AND IT IS THE END STATE. Halt file md5 before the attach `6F215A22DE35DB0216BFA3AFD6C87828` at 555 bytes. Owner dialog reading before OK, banked on the same footing: `DailyLossPercent` `5.50`, `DailyLossCurrency` `200`, `WeeklyReportEnabled` `TRUE`, `LogVerbosity` `Normal`. Journal lines: `:652` `02:10:14.426` `INFO|deinit|reason=1|session marked clean|timer_armed=1|timer_ticks=197`; `:653` `02:11:42.933` `INFO|init|build=R10|account=1200252169|server=JustMarkets-Demo3`; `:654` `INFO|limits accepted|percent=5.50|currency=200.00|raw=550/200|both list members (D1f), effective limit is the min of the two legs`; `:655` `INFO|mutex acquire|id_name=AG_ID_1200252169|id_set=1|id_exists=1|hb_name=AG_HB_1200252169|hb_set=1|hb_exists=1|hb_value=1788833502`; `:656` `INFO|lock state file loaded|reason=-|locked_until=1970.01.01 00:00:00`; `:657` `INFO|ratchet floor loaded|anchor=2026.09.08 01:00:00|floor=126.21`; `:658` `INFO|realized peak loaded|peak=0.00|source=persisted`; `:659` `TRANSITION|BOOT->SYNCING|boot|weekly=on|timer=1s`; `:660` `INFO|timer armed|period=1s`; `:662` `02:11:45.938` `TRANSITION|SYNCING->ACTIVE|history stable|polls=3/3`; `:663` `INFO|realized peak reconciled|reconstructed=0.00|persisted=0.00|taken=0.00|source=equal_anchor_max`; `:671` `LIFE|state=ACTIVE|seconds_in_state=118|anchor=2026.09.08 01:00:00|realized=0.00|floating=0.00|base=2294.76|limit=126.21|pnl_vs_limit=0.00 vs -126.21` at `02:13:45`, which is the LIFE limit the row asks for. Halt file md5 after `5DBED6319B1248C9871B2548D3B52432` at 554 bytes.
THE TWO NEGATIVE CONTROLS THE ROWS DEPEND ON, COUNTED OVER THE WHOLE 672 LINE FILE RATHER THAN SAMPLED. The string `ALERT` occurs ZERO times, so neither row refused. The string `optional config` occurs EXACTLY ONCE, at `:629`, which is R10-8's; R10-1 emitted none, which is the discriminating observation between the two rows since they carry identical limit keys and differ only in `LogVerbosity`, 99 against 0. Without that count the two rows would be indistinguishable on their journal output.
THE HALT FILE IS WRITTEN AT INIT AS WELL AS AT DEINIT, AND THE 554 BYTE READING IS FULLY ACCOUNTED FOR. Both after hashes carry an mtime equal to their row's INIT timestamp, 02:06:56 for R10-8 and 02:11:42 for R10-1, not the 02:05:25 and 02:10:14 deinits. The file was read read only and its structure explains it: a header `AGHALT|1|1200252169`, 32 session rows of the form `S|<epoch>|<clean flag>`, a halt row `H|0||0` and a checksum row `C|911802977`, CRLF terminated, which totals 21 plus 32 times 16 plus 8 plus 13, that is 554 bytes exactly. The two newest session rows are `S|1788833216|1` and `S|1788833502|0`, and those two epochs are precisely the `hb_value` figures quoted at `:630` and `:655`, so each attach appends its own row at init with the clean flag clear and the flag is set at the clean deinit. That is why R10-8's row already reads 1 while the still running R10-1 row reads 0. THE ONE BYTE DIFFERENCE FROM 555 TO 554 IS THE CHECKSUM'S DECIMAL WIDTH, ten digits to nine, since the row count is 32 on both sides and every other field is fixed width; that clause is arithmetic on the measured structure rather than a second measurement, and is labelled as such. NO DEFECT IS CLAIMED and this is out of R10 scope. It refines rather than contradicts the 2026-09-07 entry's clause that "a healthy init loads the halt model and `OnDeinit` writes it": the write happens at both ends, and the 2026-09-07 reading was taken at a moment when that was not distinguishable.
THE UNEXPLAINED HALT MOVE BETWEEN THE TWO SESSIONS IS RESOLVED, ON TWO INDEPENDENT ARTIFACTS AND AN OWNER ACCOUNT THAT AGREE. The halt file was found at session start carrying md5 `0925940AF73F6EB00C3C1704223DECFD` with mtime 2026-09-07 15:26:19, against the `C1DE985A1C532323328049BC0DED7DBD` at mtime 14:32:50 that the 2026-09-07 entry banked, so it had moved once more after that session's recorded work. The executor flagged it before starting any row. OWNER ACCOUNT, given in two parts and recorded with its correction because the correction is the accurate one: the owner first reported an accidental EA removal and re-attach about a minute later with the same inputs, then corrected it to an accidental TERMINAL close and reopen, stating that the first phrasing was a translation error and not the account. THE ARTIFACTS CONFIRM THE CORRECTED ACCOUNT. In `docs/evidence/journal-20260907-r10-acceptance-complete.txt`: `:3668` `15:23:00.105` `INFO|deinit|reason=9|session marked clean|timer_armed=1|timer_ticks=3009`, reason 9 being the terminal close already fixed by the 2026-09-07 entry on `:3204`, where an EA removal would read reason 1 as both of tonight's rows do; `:3669` `15:26:19.499` `INFO|init|build=R10|account=1200252169|server=JustMarkets-Demo3`; `:3670` `INFO|limits accepted|percent=5.50|currency=200.00|raw=550/200`, byte for byte the line the 14:32 restoring attach produced, which corroborates the same inputs clause exactly; `:3673` `INFO|ratchet floor loaded|anchor=2026.09.07 01:00:00|floor=114.74`, the floor surviving the restart from the file; `:3676` `INFO|timer armed|period=1s`; `:3678` `15:26:22.501` `TRANSITION|SYNCING->ACTIVE|history stable|polls=3/3`. In `docs/evidence/terminal-20260907-close-reopen.txt`, which is the only artifact class that can witness a terminal close at all: `:67` `15:23:00.117 Experts expert AccountGuardian (XAUUSD.ecn,M15) removed`, `:68` `15:23:01.511 Terminal exit with code 0`, `:70` `15:23:07.037 Terminal stopped with 0`, `:71` `15:23:07.040 Terminal shutdown with 0`, `:72` `15:26:17.718 Terminal MetaTrader 5 x64 build 6182 started for MetaQuotes Ltd.`. THE MEASURED INTERVAL IS 3 MINUTES 19 SECONDS ON THE EA SIDE, 15:23:00 to 15:26:19, and 3 minutes 10 seconds of terminal downtime, 15:23:07 to 15:26:17, against the owner's recollection of about a minute; the artifact figure is the record. The `removed` word at `:67` is the ordinary shutdown unload and carries no refusal, the same distinction the 2026-09-07 entry drew at its `:14`. NOTHING IN THIS EPISODE IS A DEFECT and nothing about it touches R10: it is an unrecorded operator action between two sessions, now recorded.
THE 2026-09-07 DAY IS NOW BANKED COMPLETE AND ITS EARLIER LINE NUMBERS ARE PROVEN STABLE. `journal-20260907-r10-acceptance-complete.txt` holds 6766 lines against the 3397 of the already banked `journal-20260907-r10-acceptance.txt`, and the first 3397 lines of the complete copy are BYTE IDENTICAL to the banked file, verified by hashing the prefix rather than by comparing sizes, both `39759771908A08B9F6C30E180F98FA42` at 837578 bytes. So every line number quoted in the 2026-09-07 ACTIONS entry holds unchanged in the complete file and the two files may be used interchangeably below `:3397`. MEASURED FOR THE RECORD FROM THE NEW TAIL: 1131 HOLDING WARN lines across 2026-09-07, from `14:32:55` to `23:59:53`, every one reading `live=126.21 floor=114.74`, which is D9a holding the raised leg for the whole of the rest of that day exactly as ruled; ZERO `ALERT` lines anywhere after `:3364`, so no refusal followed the restoring attach; and the day closes at `:6766` with `INFO|lock level|enforced_limit=114.74`, handing off to today's `:362` reseed to 126.21 with no gap between the two files.
DIALOG RENDERING, ONE NEW DATA POINT THAT NARROWS THE 2026-09-07 FINDING WITHOUT RESOLVING IT. R10-8's `LogVerbosity=99` rendered as an EMPTY field, the same blank behaviour seen at R10-4 on 2026-09-07, while R10-1's `LogVerbosity=0` rendered as `Normal`, a correct member name. So a blank field is what this terminal shows for a stored integer that is not an enum member. THAT DOES NOT EXPLAIN R10-4, where the blank fell on `DailyLossCurrency=200`, which IS the top list member and rendered as `200` from the identical key in R10-2 and R10-3. The 2026-09-07 ISSUES finding and its ruling stand entirely unchanged, and no vector is proposed, per that ruling. Both of tonight's rows were closed on their `limits accepted` line and not on any dialog reading.
THE OWNER'S PASTED CHART READINGS MATCHED THE HARVESTED ARTIFACT LINE FOR LINE ON BOTH ROWS, and the artifact is nonetheless the record, per `LEDGER.md:1962`. Every line number above was read from the executor's own transcoded copy and not from the paste.
EVIDENCE BANKED, each transcoded from UTF-16 to UTF-8 by a plain `Get-Content` piped to `Set-Content` writing into the repository, the DATA path named only in the read: `docs/evidence/journal-20260908-r10-acceptance.txt` md5 `8B53E3486CABD74127743177F99D6530` at 672 lines and 162198 bytes, the canonical Experts record for this session and the file every `:` line number in the R10-8, R10-1, rollover and negative control clauses refers to; `docs/evidence/journal-20260907-r10-acceptance-complete.txt` md5 `9DCA5D73079ECF86CDBA9CB903442ABA` at 6766 lines, the complete 2026-09-07 Experts day, superset of the file banked that date; `docs/evidence/terminal-20260907-close-reopen.txt` md5 `F88523950267842229D63178B8473704` at 99 lines, the 2026-09-07 terminal log carrying the close and reopen. Line numbers are stable across copies because the daily log is append only. Nothing was merged, nothing was pushed, and the executor wrote nothing under the Terminal data folder.

2026-09-07. R10 ACCEPTANCE, SEVEN OF NINE LIVE ROWS CLOSED, R10-1 AND R10-8 DEFERRED. Owner ruling of 2026-09-06, the deploy hand, recorded as one dated FINAL in DECISIONS before any other work and committed at `67177ae` together with `scripts/deploy-r10.ps1`: RULE A is not amended, the executor may write one repository script containing only `Copy-Item` and `Get-FileHash` lines, the owner runs it, the executor never does. RULE B alias audit run before any path inside the Terminal data folder was named, hazard confirmed live on all seven names, `rd`, `del`, `ri` and `rm` resolving to `Remove-Item`, `mv` to `Move-Item`, `cp` to `Copy-Item`, `sc` to `Set-Content`, four of them AllScope. Every executor command naming a path inside that folder was a plain inline `Get-FileHash`, `Get-ChildItem`, `Get-Item` or `Get-Content` with no user defined helper in any call chain. The executor ran nothing that writes under the folder; the owner ran the deploy script and performed every attach, dialog action, restart and deletion.
STEP 0 IDENTITY: all twelve files match the B3 measurements of the 2026-09-03 ACTIONS entry by md5 and byte count. Seven source files, `AccountGuardian.mq5` `4F2939FD092711C9391A5F763F8ACA8A` at 68001, `Pnl.mqh` `B961E6F70B3EC270FF776E21F307D69F` at 20877, `Persist.mqh` `0728E30210CAB55C7F83FD46E4F8D0A6`, `State.mqh` `34792807C6B6B548943215866193EB3F`, `Clock.mqh` `DC5A04004F0CB7BDB9D6C602E944CEC1`, `Log.mqh` `C4E803FB81EBE7F521CC020E8FA1CE82`, `Sweep.mqh` `CF7D355A7C52265A7AFF5C465DA33C46`; the two vector sources `AgPhase2StateVectors.mq5` `16CF643735B9966312860753154BD74E` and `AgPhase1ClockVectors.mq5` `26E7AD2E8A3AC6EDD283CFC2BD7C31CF`; the three ex5 `AccountGuardian.ex5` `842C6E52483A9D3469E1AD8A12266ACD` at 96912, `AgPhase2StateVectors.ex5` `C192FDD2C758D540373C16CC9D904AAF` at 52144, `AgPhase1ClockVectors.ex5` `2598B4DEECB5ADA1D309D5F882B6C4D0` at 13138. No mismatch, so the session proceeded.
R10-D DEPLOY, CLOSED ON THE LANDING CHECK AND THE FIRST INIT, ONE CLAUSE UNREACHABLE. The owner ran `scripts/deploy-r10.ps1`; the executor then verified every landed file with its own read only `Get-FileHash` and all ten equal the worktree value: `AccountGuardian.ex5` `842C6E52483A9D3469E1AD8A12266ACD`, `AgPhase2StateVectors.ex5` `C192FDD2C758D540373C16CC9D904AAF`, `a9_defaults.set` `05730F31D7543F4D017D720EB83A3C01`, `a9_optional_bad.set` `2983A50A9E2431474273C70AE59B64F2`, `a9_percent_zero.set` `32D8463A209F98DA50740EABD0BADE72`, `a9_percent_off_grid.set` `2BDC97C5FD33CD7815AC09ACFAFE2970`, `a9_percent_over_ceiling.set` `7544B38427ABF8A4513FB2ED3B391404`, `a9_currency_zero.set` `C3DE1EDE72B2D93D6281FEE9427C2554`, `a9_currency_gap.set` `6A7ABC09BEC53AF8133D3CC261381E31`, `a9_currency_over_ceiling.set` `17444002F83ACEC375ED1246B494642C`. The Load browser listed all eight, owner reading taken at the R10-2 attach.
THE FIRST INIT ON THE NEW BUILD WAS NOT TRIGGERED BY THE DEPLOY, AND THE SESSION REPORT SAYING SO WAS CORRECTED BY THE ARTIFACT. `docs/evidence/terminal-20260907-r10-deploy-restart.txt:13` `13:19:12.226 LiveUpdate start "...\liveupdate\terminal64.exe" /update /path:"C:\Program Files\MetaTrader 5"`, then `:14` the expert removed at 13:19:13.408, `:15` `Terminal exit with code 0`, `:19` `13:19:26.702 Terminal MetaTrader 5 x64 build 6182 started`, `:23` `13:19:27.566 Experts expert AccountGuardian (XAUUSD.ecn,M15) loaded successfully`. A MetaTrader LiveUpdate restarted the terminal on its own, upgrading build 6180 to 6182, the update having been staged at 19:58:58 on 2026-09-06 per yesterday's terminal journal. The coincidence with the deploy is a coincidence and there was no hot reload of a replaced ex5. The pre deploy instance was healthy to the last moment, `journal-20260907-r10-deploy-restart.txt:3204` `INFO|deinit|reason=9|session marked clean|timer_armed=1|timer_ticks=70539`, reason 9 being the terminal close.
FIRST INIT HAZARD OUTCOME (a), REFUSED. Six lines at 13:19:28.765, `:3205` `INFO|init|build=R10|account=1200252169|server=JustMarkets-Demo3`, `:3206` `ALERT|refusing to run, core config invalid: DailyLossPercent is not a list member (raw 5); the list is 0.25 to 5.50 in steps of 0.25, stored as hundredths 25 to 550`, `:3207` the Alert echo, `:3208` `INFO|deinit|halt file NOT written`, `:3209` `INFO|deinit|reason=8|session marked clean|timer_armed=0|timer_ticks=0`, `:3210` `INFO|deinit|banner NOT cleared`. The `raw 5` reading is corroborated independently by the last healthy LIFE line, `base=2294.76|limit=114.74`, and 2294.76 at 5.00 percent is 114.74, so the profile's last used percent was 5.0. The currency leg is UNMEASURED on this row, the percent check refusing first.
R10-D'S DIALOG CLAUSE IS UNREACHABLE AND NO SUBSTITUTE WAS TAKEN. The row asks the owner to open the EA properties on the running chart immediately after the restart and read both limit inputs. `terminal-20260907-r10-deploy-restart.txt:29` `13:19:28.765 Experts initializing of AccountGuardian (XAUUSD.ecn,M15) failed with code 32767 (incorrect parameters)`, `:30` `13:19:28.773 Experts expert AccountGuardian (XAUUSD.ecn,M15) removed`. The owner's own restart follows at `:31` 13:26:25.440 and `:35` 13:27:09.117, and that start carries no expert line at all, against `:23` which does for the previous start. The chart came up with no guardian, so no dialog existed to open. Recorded as unreachable with its reason, per the standing instruction of 2026-08-18 forbidding a substitute that carries a row's name. Separately banked and NOT this clause: at the R10-2 fresh attach the dialog read 5.50 and 200 before Load, the first live confirmation that the shipped defaults render as list members and need no correction.
R10-0 PASS. `docs/evidence/journal-20260907-r10-acceptance.txt:3318` `AGVEC|SUMMARY|95/95` at 13:36:37.203, with eleven `r10_*` lines at `:3307` to `:3317` all `PASS`: `r10_percent_member_count_is_22`, `r10_currency_member_count_is_48`, `r10_percent_floor_and_ceiling`, `r10_percent_off_grid_rejected`, `r10_currency_two_segments`, `r10_map_percent_550_is_5_50`, `r10_map_percent_25_is_0_25`, `r10_map_currency_200`, `r10_default_limit_is_min_of_legs`, `r10_validate_rejects_currency_zero`, `r10_validate_accepts_defaults`. The previous denominator is `AGVEC|SUMMARY|84/84`, banked five times in this ledger and twice under `docs/evidence`, and 84 plus 11 is 95.
R10-2 TO R10-7 ALL PASS ON OUTCOME ONE, THE REFUSAL. Halt file md5 taken before every attach and after every row, `D40A322B1CBB2544B6367CD60FFF75DB` at 555 bytes on all seven measurements, never moved. Six lines per row in `journal-20260907-r10-acceptance.txt`: R10-2 `a9_percent_zero.set` `:3319` to `:3324`, `DailyLossPercent is not a list member (raw 0)` at `:3320`, 13:45:27.320. R10-3 `a9_percent_off_grid.set` `:3325` to `:3330`, `(raw 510)` at `:3326`, 14:07:51.448. R10-4 `a9_percent_over_ceiling.set` `:3331` to `:3336`, `(raw 575)` at `:3332`, 14:18:57.606. R10-5 `a9_currency_zero.set` `:3337` to `:3342`, `DailyLossCurrency is not a list member (raw 0); the list is 1 to 10 in steps of 1, then 15 to 200 in steps of 5` at `:3338`, 14:22:46.382. R10-6 `a9_currency_gap.set` `:3343` to `:3348`, `(raw 12)` at `:3344`, 14:24:30.833. R10-7 `a9_currency_over_ceiling.set` `:3349` to `:3354`, `(raw 205)` at `:3350`, 14:27:06.854. Every row carried the three deinit lines, `reason=8|session marked clean|timer_armed=0|timer_ticks=0` with no `timer armed` line anywhere, and an owner REFUSED banner reading.
THE OPEN PLATFORM QUESTION IS ANSWERED AND CLOSES: THE `.set` CHANNEL DELIVERS AN OFF LIST INTEGER TO AN ENUM INPUT UNMODIFIED. Six raw values reached `OnInit` exactly as the six vector files carry them, 0, 510, 575, 0, 12 and 205, each quoted above, and the healthy path delivered `raw=550/200` intact as well. No coercion occurred on any row, so PASS BY CONSTRUCTION applies to none of them and the membership refusal is reachable through the `.set` channel. Both legs are validated independently: R10-2 to R10-4 refused on percent while currency carried a valid 200, R10-5 to R10-7 refused on currency while percent carried a valid 550. The three refusal shapes were each exercised on both legs, zero, off grid or in gap, and over ceiling.
DIALOG RENDERING DIVERGES FROM THE VALUE THE EA RECEIVES, MEASURED AT R10-4. The landed `a9_percent_over_ceiling.set` carries `DailyLossPercent=575` and `DailyLossCurrency=200`, read in place and md5 verified equal to the worktree at deploy. The properties dialog rendered `DailyLossPercent` as `5.50`, a valid list member, and `DailyLossCurrency` as an empty field although 200 is the top currency member and rendered as `200` in R10-2 and R10-3 from the identical key. `OnInit` received 575, quoted at `:3332`. The owner stopped before clicking OK and reported it as unpredicted; the executor named two hypotheses with one discriminating test, the raw value in the ALERT, since 575 appears in no other vector, and the test refuted the wrong file hypothesis. The blank currency rendering has no established mechanism and none is asserted. See the ISSUES entry opened this date.
RESTORING ATTACH, EXPLICITLY NOT R10-1. Per the owner's standing instruction the account was returned to guarded with a fresh attach of `a9_defaults.set`, and that attach does not close R10-1. Healthy init, `:3355` to `:3368`: `:3356` `INFO|limits accepted|percent=5.50|currency=200.00|raw=550/200|both list members (D1f), effective limit is the min of the two legs`, `:3359` `INFO|ratchet floor loaded|anchor=2026.09.07 01:00:00|floor=114.74`, `:3361` `TRANSITION|BOOT->SYNCING|boot|weekly=on|timer=1s`, `:3362` `INFO|timer armed|period=1s`, `:3364` `TRANSITION|SYNCING->ACTIVE|history stable|polls=3/3` at 14:32:53.702, `:3365` `WARN|ratchet HOLDING against a raised limit (question SEVEN): live=126.21 floor=114.74; the floor is enforced until the next day anchor` at 14:32:54.689, `:3367` `INFO|lock level|enforced_limit=114.74|peak=0.00|ratchet_level=-114.74|peak_level=-114.74|chosen=tie`, `:3368` `LIFE|state=ACTIVE|base=2294.76|limit=126.21`. The ratchet figures were predicted before the attach from the banked base and matched to the cent, D9a behaving as ruled. The halt file moved on this row as predicted, `D40A322B1CBB2544B6367CD60FFF75DB` to `C1DE985A1C532323328049BC0DED7DBD` at 555 bytes, mtime 14:32:50, because a healthy init loads the halt model and `OnDeinit` writes it while a refused init deliberately does not. OBSERVATION, not a defect claim and out of R10 scope: the LIFE line advertises `limit=126.21` while `:3367` shows `enforced_limit=114.74`, so a reader holding only a LIFE line tonight sees the higher figure.
PRESETS: the owner deleted the ten retired a9 copies by hand and the executor verified read only that exactly eight `a9_*.set` remain, all dated 2026-09-03 15:34:52, the eight deployed. The executor issued no delete command and wrote nothing under the folder. Content of the ten remains recoverable from `4f93013`.
DEFERRED, OPEN AND NOT FAILED: R10-1 `a9_defaults.set` and R10-8 `a9_optional_bad.set`, both to a second session with a fresh attach after the 01:00 server rollover, per the owner's order for the night. Today's floor was written at 00:59:58 on the old 5.00 percent and holds at 114.74, which is why the two rows wait for a fresh anchor.
TERMINAL BUILD CHANGED MID SESSION, BEFORE ANY ROW RAN, 6180 to 6182. Every row tonight was measured on 6182, one consistent build, because the LiveUpdate landed before R10-0.
EVIDENCE BANKED: `docs/evidence/journal-20260907-r10-acceptance.txt` md5 `39759771908A08B9F6C30E180F98FA42` at 3397 lines, the canonical Experts record for the night and the file every line number above refers to; `docs/evidence/journal-20260907-r10-deploy-restart.txt` md5 `895FA9D1B35413F4482D7EA44CE86B95` at 3210 lines, the R10-D snapshot; `docs/evidence/terminal-20260907-r10-deploy-restart.txt` md5 `4E637212D7BEA855D2C760920FF4E008` at 43 lines, carrying the LiveUpdate and both restarts. All three transcoded from UTF-16 to UTF-8 by a plain `Get-Content` piped to `Set-Content` writing into the repository, the DATA path named only in the read. Line numbers are stable across copies because the daily log is append only. Nothing was merged and the executor performed no push.

2026-09-03. R10 BUILD IMPLEMENTED, D1f + D7b + D11a PER D10a, on branch `worktree-r10-build-20260903` at `f1d2a6b`, 24 files, 318 insertions and 224 deletions, per `docs/PLAN_R10_INPUT_HARDENING_2026-09-03.md`. RULE A held: nothing under the MetaTrader Terminal data folder was read, written or approached, and no command named a path inside it, so RULE B's alias audit was not reached; the compiler binary invoked is `C:\Program Files\MetaTrader 5\metaeditor64.exe`, the program folder and not the data folder, with `/include` rooted at the worktree `MQL5` tree. Five commits, one per plan task: `08f4c31` Tasks B1 and B2, `e880fe0` Task B3, `72a0b51` Task B4, `f1d2a6b` Task B5, and this entry as Task B6. COMPILE, standing rule 6, the exit code was 1 on every run and was ignored, the Result line and the ex5 timestamp are the signals: `Result: 0 errors, 0 warnings, 1024 ms elapsed, cpu='X64 Regular'` for `AccountGuardian.mq5`, `Result: 0 errors, 0 warnings, 655 ms elapsed, cpu='X64 Regular'` for `AgPhase2StateVectors.mq5`, `Result: 0 errors, 0 warnings, 385 ms elapsed, cpu='X64 Regular'` for `AgPhase1ClockVectors.mq5`, which is static row R10-S1; every include line in all three logs resolved to a path under the worktree, checked by matching each `including` line against the worktree root. BUILD IDENTITY, standing rule 7: `AccountGuardian.mq5` `4F2939FD092711C9391A5F763F8ACA8A` at 68001 bytes, `Pnl.mqh` `B961E6F70B3EC270FF776E21F307D69F` at 20877 bytes, and the five unchanged includes `Persist.mqh` `0728E30210CAB55C7F83FD46E4F8D0A6`, `State.mqh` `34792807C6B6B548943215866193EB3F`, `Clock.mqh` `DC5A04004F0CB7BDB9D6C602E944CEC1`, `Log.mqh` `C4E803FB81EBE7F521CC020E8FA1CE82`, `Sweep.mqh` `CF7D355A7C52265A7AFF5C465DA33C46`; the two vector scripts `AgPhase2StateVectors.mq5` `16CF643735B9966312860753154BD74E` at 32796 bytes and `AgPhase1ClockVectors.mq5` `26E7AD2E8A3AC6EDD283CFC2BD7C31CF` at 7033 bytes. EX5 FIGURES RECORDED AND EXPLICITLY NOT IDENTITY, standing rule 7: `AccountGuardian.ex5` `842C6E52483A9D3469E1AD8A12266ACD` at 96912 bytes, mtime 2026-09-03 15:31:33; `AgPhase2StateVectors.ex5` `C192FDD2C758D540373C16CC9D904AAF` at 52144 bytes, mtime 2026-09-03 15:31:48; `AgPhase1ClockVectors.ex5` `2598B4DEECB5ADA1D309D5F882B6C4D0` at 13138 bytes, mtime 2026-09-03 15:31:49. STATIC ROW R10-S2: a grep of the four retired input names over `MQL5/` returns 2 lines, both inside the 2.4 constants comment at `AccountGuardian.mq5:42` and `:43`, and a grep of `^input ` over the EA returns exactly 4 lines, `:22` `DailyLossPercent`, `:23` `DailyLossCurrency`, `:25` `WeeklyReportEnabled`, `:26` `LogVerbosity`. STATIC ROW R10-S3: the `AgEvaluateActive` breach tail is BYTE IDENTICAL across the build, 118 lines and 7383 bytes, md5 `A3176C1C0E9F2D03586EAD6CBB175AA3` before at EA:728-845 and after at EA:745-862; above the tail THREE lines changed, not the one the plan predicted, and all three are required by the plan's own 2.4 edit table: `if(!AgHistoryStable(HistoryStablePolls))` to `if(!AgHistoryStable(AG_HISTORY_STABLE_POLLS))` at old EA:656, the RESYNC waiting_on string at old EA:659, and `double limit     = AgLimitCurrency(base, DailyLossPercent, DailyLossCurrency);` to `double limit     = AgLimitCurrency(base, AgInputPercent(), AgInputCurrency());` at old EA:716. THE PLAN CONTRADICTS ITSELF ON THIS POINT AND THE BUILD FOLLOWED THE 2.4 TABLE: plan 2.3 says "EA:716 is the ONE line above the ACTIVE breach tail that changes" while plan 2.4 requires EA:656 and EA:659, both of which sit inside `AgEvaluateActive` above the tail. Recorded as a measured divergence from a plan expectation, not as a design change and not as a defect. STATIC ROW R10-S4: the five untouched includes are identical to `00e2b43` byte for byte, and `AgLimitCurrency`'s signature and body are byte identical, md5 `1B8036A4A9D6B6F1FF7B587411DADD92`, moved from `Pnl.mqh:289-299` to `:406-416` by the enum block inserted above it; only its comment box changed. SECOND MEASURED DIVERGENCE FROM A PLAN EXPECTATION: plan Task B1 step 2 predicted that the mid-build compile would FAIL with errors naming `AgValidateLimits`. It did not fail. Measured: `Result: 0 errors, 2 warnings, 1111 ms elapsed, cpu='X64 Regular'`, with `AccountGuardian.mq5(890,25)` and `AccountGuardian.mq5(890,43)` both `warning 43: possible loss of data due to type conversion from 'double' to 'int'`. MQL5 converts a double argument to an int parameter implicitly and warns rather than refusing, so a signature change alone does not break the build. B1 step 3 was followed exactly as written, no commit was made at that point, and both warnings are gone at B3 where the two inputs became enums. VECTORS: `a9_defaults.set` and `a9_optional_bad.set` rewritten to `DailyLossPercent=550` and `DailyLossCurrency=200` on four keys, six refusal vectors added, `a9_percent_zero.set`, `a9_percent_off_grid.set`, `a9_percent_over_ceiling.set`, `a9_currency_zero.set`, `a9_currency_gap.set` and `a9_currency_over_ceiling.set`, ten removed by `git rm` per D8b and the plan review ruling item ONE, all eight files ASCII with CRLF terminators, and `docs/vectors/README.md` rewritten to an eight row table plus a retired section naming each of the ten with the ruling that struck it. SPEC 4.3 line 90, the section 5 input table and the core config class sentence amended. `README.md` updated in both the English and the Hebrew section: the zero as disable text is gone, the four retired input names appear nowhere in the file, and the file still never names this ledger. THREE EXECUTOR READINGS, RECORDED AS READINGS AND NOT AS RULINGS. ONE, the retired section of `docs/vectors/README.md` cites `4f93013` as the last commit carrying all ten files, because the hash of the commit that removes them cannot be written into the file that commit contains. TWO, the SPEC's core config class sentence at `docs/SPEC_v0.1.md:114` was amended although Task B5's three steps enumerate only line 90 and the table; plan 2.6 names `:113-126` as a surface in scope, and leaving it would have committed a SPEC still asserting the both limits zero clause that D1f supersedes. THREE, the SPEC's own amendment series in section 9 stays at A6: Task B5 does not instruct an A7 entry, so none was written. UNRULED EXECUTOR READINGS CARRIED FORWARD FROM THE PLAN AND STILL AWAITING OWNER REVIEW, unchanged in status by this build: the backing integer as the value, the enum and accessor names, the six vector names and values, and the eleven synthetic checks. Ratified at plan review 2026-09-03 and applied here: the `limits accepted` INFO line, the `build=R10` label, retirement by `git rm`, the `README.md` update and the SPEC amendment. Nothing was deployed, nothing was merged, and the executor performed no push.

2026-09-03. R10 BUILD PLAN SESSION, PLAN ONLY. No source file was written, no compile was run, nothing was deployed, no vector or README file was edited, and nothing under the MetaTrader Terminal data folder was read, written or approached, per RULE A; no command named a path inside it, so RULE B's alias audit was not reached. Work in worktree `dreamy-tinkering-aurora` on branch `worktree-r10-plan-20260903` from `00e2b43`, main's own head. Three commits: two DECISIONS entries, the D1f defaults FINAL and the R10 plan review FINAL, appended after D11a (`95883e7`); `docs/PLAN_R10_INPUT_HARDENING_2026-09-03.md` (`7ae7748`); and this entry, the session's third commit. The plan maps every read, validation and persistence site of the six inputs (none is persisted; the derived limit is, at EA:374, :456, :463 and Persist.mqh:830-831), designs the two enums with the backing integer as the value, the membership refusal text and the four constants, tags each executor choice UNRULED 2026-09-03 except the four the plan review ratified, rewrites the two surviving a9 vectors to the ruled defaults, adds six refusal vectors and eleven synthetic checks, and sets the acceptance rows with two admissible outcomes for the `.set` channel. The build's ISSUES and ACTIONS entries are drafted inside the plan and not written. STATE FOUND AND LEFT FOR THE OWNER: the branch `worktree-input-hardening-r10-20260903`, deleted locally by the owner-instructed cleanup at 12:36, was re-created at 12:39:57 by a checkout in this worktree from the surviving remote-tracking ref `origin/worktree-input-hardening-r10-20260903` ("branch: Created from origin/..."), 52 seconds after this session's prompt and before its first git command; the session ran no checkout and its harness record names `worktree-dreamy-tinkering-aurora`, so the hand was owner-side. This session switched back to the survivor with one plain checkout, both refs at `00e2b43`, and touched neither the re-created branch nor the remote ref. The first incarnation's commit `69b29f8` of 10:32:37, reset away by the same cleanup, is reachable from no branch and still in the reflog; nothing here reuses it. Nothing is merged and the executor performed no push.

2026-09-03. A4. CLOSURE OF BOTH 2026-08-27 INCIDENTS PER R11. Evidence banked this session at `docs/evidence/metaeditor-20260902.txt`, raw copy md5 `6666BE488FCAC2D77DAF8C1F2EC3F9E1`. Quoted from that file with its own line numbers. `:437` `0	2026.08.27 00:41:11.879	Compile	C:\Users\www83\AppData\Roaming\MetaQuotes\Terminal\D0E8209F77C8CF37AD8BF550E51FF075\MQL5\Experts\AccountGuardian.mq5 - 0 errors, 0 warnings, 894 ms elapsed, cpu='AVX2 + FMA3'`, the owner's first Terminal Experts root compile. `:459` `2	2026.08.27 12:30:10.891	Compile	C:\Users\www83\Downloads\AI\AccountGuardian\.claude\worktrees\worktree-v1-realized-peak\MQL5\Experts\AccountGuardian\AccountGuardian.mq5 - 11 errors, 2 warnings`, the worktree compile immediately before it. `:461` `2	2026.08.27 12:30:42.663	Compile	C:\Users\www83\AppData\Roaming\MetaQuotes\Terminal\D0E8209F77C8CF37AD8BF550E51FF075\MQL5\Experts\AccountGuardian.mq5 - 11 errors, 2 warnings`, the owner's failed Terminal Experts root compile, 32 seconds after `:459` and carrying the same error count. `:465` `0	2026.08.27 15:11:52.235	Compile	C:\Users\www83\AppData\Roaming\MetaQuotes\Terminal\D0E8209F77C8CF37AD8BF550E51FF075\MQL5\Experts\AccountGuardian.mq5 - 0 errors, 0 warnings, 992 ms elapsed, cpu='AVX2 + FMA3'`, the owner's final clean Terminal Experts root compile. Both incidents close per DECISIONS R11 below: incident one is an owner MetaEditor action, not an executor RULE A violation, and the former ISSUES attribution to the implementation session was wrong; incident two, the worktree file deleted twice, closes as a fact with no established cause, the log carrying no file operation records at all.

2026-09-03. A5. CORRECTION referencing the consolidation entry, head "THE CONSOLIDATION OF 2026-08-26 AND", now at ACTIONS line 128. That entry describes a compile "performed in MetaEditor against the post merge main working tree". The banked log shows no compile of the main checkout path, `C:\Users\www83\Downloads\AI\AccountGuardian\MQL5\` outside `.claude\worktrees\`, after 2026-07-29. The only `0 errors, 0 warnings` compile in the window following the phase 3 merge is log line 437, quoted in A4 above, the owner's compile of the Terminal Experts root copy. The original line 128 is not edited.

2026-09-03. A6. RESOLUTION of the 2026-09-02 contradiction entry, head "ACTIONS line 407 states", now at ACTIONS line 89 below this entry. Both statements it found in conflict are true. The implementation session's "Nothing under the MT5 Terminal folder, not even a read" stands, per R11: the three Terminal Experts root compiles at log lines 437, 461 and 465 were the owner's own MetaEditor actions, not the implementation session's. Line 89 is not edited.

2026-09-03. A7. RECORD DISCREPANCY. The 2026-08-27 V1 implementation entry (ACTIONS, the entry beginning "2026-08-27. VERSION 1 OF THE REALIZED PEAK TRAILING FLOOR IS IMPLEMENTED IN SOURCE") names the tip build at 12:12:45 with mtime `2026-08-27 12:12:45` for `2A3320AABF6C000CB151842E1FBEFA19`. The banked log shows a further clean compile of the same worktree path, `:455` `0	2026.08.27 12:15:12.131	Compile	C:\Users\www83\Downloads\AI\AccountGuardian\.claude\worktrees\worktree-v1-realized-peak\MQL5\Experts\AccountGuardian\AccountGuardian.mq5 - 0 errors, 0 warnings, 1001 ms elapsed, cpu='X64 Regular'`, at 12:15:12, after the named tip build and not recorded in that entry. UNEXPLAINED, and ZERO RISK since `2A3320AA` was never deployed per DECISIONS R11 of 2026-08-31, ruling TWO. Recorded as fact, not investigated further.

2026-09-03. A8. THIS SESSION'S OWN RECORD. G6 closure session, owner ruling of 2026-09-03. Worktree `.claude/worktrees/g6-closure-20260903`, branch `g6-closure-20260903`, created from main at `83c9d55`. `tmp/metaeditor-20260902.log` re-verified before use, md5 `6666BE488FCAC2D77DAF8C1F2EC3F9E1`, 129154 bytes, 493 lines, unchanged from the 2026-09-02 investigation. Transcoded to `docs/evidence/metaeditor-20260902.txt`, UTF-8 no BOM, CRLF preserved, 493 lines, committed. DECISIONS R11 recorded FINAL, closing R5. ISSUES entries deleted per R6: the 2026-08-27 Terminal Experts root entry, the worktree-deleted-twice entry, and the commit-msg hook UTF8 BOM entry, measured clean 2026-09-02. Next best action Action line rewritten accordingly. Not merged, not pushed, main untouched.

2026-09-02. CORRECTION. ACTIONS line 407 states "Nothing under the MT5 Terminal folder, not even a read" for the 2026-08-27 session, while ISSUES lines 9-11 record files written at the Terminal Experts root the same day. Both cannot be true. Open contradiction pending R5 investigation against metaeditor.log. Line 407 is not edited.
2026-09-02. Machine sleep recurred on 2026-09-01 for a few minutes, owner statement in chat, no journal cited yet. References ISSUES 21-23 and 169-171.
2026-09-02. LEDGER HYGIENE SESSION. Worktree `.claude/worktrees/ledger-hygiene-20260902`, branch `ledger-hygiene-20260902`, created from main at `daf8364`. ISSUES section held 82 entries before this session: 19 kept as-is or with an append/rewrite, 7 moved verbatim to DECISIONS on their own carried Status: FINAL, 56 deleted as resolved, ruled FINAL elsewhere, or superseded. Ten new owner rulings R1 through R10 recorded FINAL in DECISIONS, dictated 2026-09-02 in Hebrew in chat and approved in English text in this instruction. Commits made in three groups: DECISIONS R1-R10 plus ACTIONS A1-A3 plus ISSUES R5 appends; ISSUES rewrite per R6; .gitignore fix per R1. Not merged, not pushed, main untouched.

2026-09-01. VERSION 1 OF THE REALIZED PEAK TRAILING FLOOR IS ACCEPTED ON ALL FOUR ROWS, V1-A, V1-B, V1-C AND V1-D, CLOSED ON THE EIGHT FILES BANKED READ ONLY AT COMMIT `7aef069` AND ON THE FOUR OWNER RULINGS OF 2026-08-31 RECORDED VERBATIM IN DECISIONS BELOW. This entry closes what the 2026-08-31 banking entry below explicitly left untouched: no line was quoted there, every row was quoted here. THE BUILD IDENTITY EVERY ROW CLOSES AGAINST IS RULING TWO'S, NOT THE EXECUTOR'S: `AccountGuardian.ex5` md5 `124BA610201A6D67C050572C6A84DE37`, 86866 bytes, compiled by the owner on 2026-08-27 from source md5 `6CF5F6A78E8D9A333D84780C33DE475A`, identical to commit `428614c`'s `AccountGuardian.mq5`; the executor's own `2A3320AA` build from that same commit was never deployed and closes nothing.
V1-A CLOSES: PROFIT THEN GIVE BACK LOCKING ON FULL EQUITY WITH THE POSITION STILL OPEN. Evidence `docs/evidence/journal-20260831-flat-expiry-and-v1a-lock.txt`, the 16:52:46 breach sequence. `:3936` `AG|2026.08.31 16:52:46|INFO|lock level|enforced_limit=117.54|peak=78.00|ratchet_level=-117.54|peak_level=-39.54|chosen=peak|pnl=-29.70|realized=75.90|floating=-105.60`, deferred one pass at `:3937` `WARN|breach deferred one pass: no new deal visible, count=3`, then `:3938` the same line one pass later, `pnl=-39.60|realized=75.90|floating=-115.50`, `:3939` `INFO|breach arithmetic|realized=75.90|floating=-115.50|base=2350.86|limit=117.54|pnl=-39.60`, `:3940` `INFO|lock bounds|breach_time=2026.08.31 16:52:46|...|locked_until=2026.09.01 01:00:00`, `:3941` `TRANSITION|ACTIVE->LOCKED|DAILY_BREACH|pnl=-39.60|limit=117.54|locked_until=2026.09.01 01:00:00`, `:3942`-`:3943` the ALERT pair. THE ROW'S SHAPE IS IN THE NUMBERS: `realized=75.90` is the day's booked profit, `peak=78.00` the high water mark it set, and enough of it was given back that `floating=-115.50` pushed `pnl=-39.60` past `peak_level=-39.54`, well inside the plain `ratchet_level=-117.54`; `chosen=peak` on both quoted passes is the peak mechanism catching what the ratchet alone would have missed. THE POSITION WAS STILL OPEN AT THE LOCK: `docs/evidence/terminal-20260831-v1a-lock.txt:39` opens 0.3 lots XAUUSD.ecn at 4424.51, `16:50:54.669`, and it is not closed until `:41`-`:43`, `16:52:52.937` to `16:52:53.012`, SEVEN SECONDS AFTER the lock, so the position the breach was computed against was open at breach time. THE LOCK ITSELF IS STILL LIVE, `locked_until=2026.09.01 01:00:00`; its expiry is banked in a separate follow-up session per the ISSUES head below, and this closure needs nothing past the breach declaration quoted above.

2026-09-01 FOLLOW-UP. THE V1-A LOCK'S 2026-09-01 01:00:00 EXPIRY IS BANKED AND QUOTED, closing what the entry above explicitly left open. Per RULE A the owner copied today's Experts journal out of the Terminal folder into a session tmp directory as `experts-20260901.log`; the terminal log for today does not yet exist at the live Terminal `Logs\` path, the last file present there being `20260831.log`, so no terminal file is banked for today and none is claimed. INTEGRITY: BYTE PREFIX, the live original still being written by the running EA, the tmp copy's own length taken as N=100770 and the first 100770 bytes of the live original hashed and compared against the tmp copy's whole-file hash, both `9AA0A944C3144D80CA5C99AE35F7D966`, MATCH. Banked as `docs/evidence/journal-20260901-v1a-lock-expiry.txt`, 100770 bytes, 219 lines, md5 `9AA0A944C3144D80CA5C99AE35F7D966`, re hashed after the copy and equal to the source value above. THE 01:00 SEQUENCE, quoted in full and in order. `:121` `AG|2026.09.01 01:00:00|INFO|lock expired|locked_until=2026.09.01 01:00:00|server=2026.09.01 01:00:00`. `:122` `AG|2026.09.01 01:00:00|TRANSITION|LOCKED->SYNCING|lock expired|history stability required before the first post-expiry breach decision|polls=0/3`. `:123` `AG|2026.09.01 01:00:02|TRANSITION|SYNCING->ACTIVE|history stable|polls=3/3`, NO RE-LOCK: the pass resumed cleanly into ACTIVE rather than declaring a fresh breach. `:124` `AG|2026.09.01 01:00:04|INFO|day rollover|old_anchor=2026.08.31 01:00:00|new_anchor=2026.09.01 01:00:00|jump=86400s`. `:125` `AG|2026.09.01 01:00:04|INFO|ratchet reseeded at rollover|old_anchor=2026.08.31 01:00:00|new_anchor=2026.09.01 01:00:00|old_floor=117.54|new_floor=114.74`. `:126` `AG|2026.09.01 01:00:04|INFO|realized peak reset at rollover|old_anchor=2026.08.31 01:00:00|new_anchor=2026.09.01 01:00:00|old_peak=78.00|new_peak=0.00`. `:127` `AG|2026.09.01 01:00:04|INFO|lock level|enforced_limit=114.74|peak=0.00|ratchet_level=-114.74|peak_level=-114.74|chosen=tie|pnl=0.00|realized=0.00|floating=0.00`, THE FIRST ACTIVE LOCK LEVEL LINE. THE EXPIRY WAS FLAT: `realized=0.00` and `floating=0.00` at the lock level line and at the first ACTIVE LIFE line, `:128` `AG|2026.09.01 01:00:30|LIFE|state=ACTIVE|seconds_in_state=27|waiting_on=-|anchor=2026.09.01 01:00:00|realized=0.00|floating=0.00|base=2294.76|limit=114.74|pnl_vs_limit=0.00 vs -114.74|server=2026.09.01 01:00:30|local=2026.09.01 01:00:27`, no position open, nothing given back and nothing to lose against the fresh floor. THIS ENTRY DOES NOT ARGUE WHAT THE EXPIRY PROVES OR FAILS TO PROVE BEYOND WHAT V1-A ALREADY CLOSED ON: V1-A closed on the 2026-08-31 16:52:46 breach declaration above, and this expiry is banked only as the event that entry's closure deferred.

V1-B CLOSES: RESTART RECONSTRUCTION WITH NO RE-INFLATION, INCLUDING THE FILE DELETED LEG. Evidence `docs/evidence/journal-20260827-v1b-v1c-input-dance.txt`, seven restarts inside nine minutes around the day's only peak-raising deal, `:2416` `INFO|realized peak raised|anchor=2026.08.27 01:00:00|old=0.00|new=2.32|deal_ticket=332259087|deal_time=2026.08.27 17:42:29`. THE ORDINARY LEG: boot at `:2474` `INFO|realized peak loaded|anchor=2026.08.27 01:00:00|peak=2.32|source=persisted|...`, reconciled at `:2505` `INFO|realized peak reconciled|anchor=2026.08.27 01:00:00|reconstructed=2.32|persisted=2.32|taken=2.32|source=equal_anchor_max`, repeated identically at `:2523`, `17:51:19`. THE FILE DELETED LEG: between the `17:51:06` and `17:52:51` restarts the peak file itself was removed, logged plainly at `:2539` `DEBUG|no realized peak file, first session on this account`, reconciling at `:2546` `INFO|realized peak reconciled|anchor=2026.08.27 01:00:00|reconstructed=2.32|persisted=0.00|taken=2.32|source=no_file`. `persisted=0.00` against `taken=2.32` is the file-deleted leg in one line: nothing was on disk, the fold walked the day's deal history alone and RECOVERED THE SAME 2.32 IT HAD BEFORE. Re confirmed a third time at `:3535`, `20:36:42`, `source=no_file` again, `taken=2.32` again, long after the input dance ended. NO PASS ACROSS SEVEN RESTARTS EVER READS `taken` ABOVE `2.32`, WHICH IS THE ROW'S OWN DEFINITION OF NO RE-INFLATION.
V1-C CLOSES: STRICTER WINS WITH BOTH MECHANISMS LIVE ACROSS A RAISE ATTEMPT. Evidence `docs/evidence/journal-20260827-v1b-v1c-input-dance.txt`, the 17:47:43 pass beside its own WARN one line above it. `:2479` `WARN|ratchet HOLDING against a raised limit (question SEVEN): live=109.47 floor=87.57; the floor is enforced until the next day anchor`, THE RAISE ATTEMPT: the day's live enforced limit had moved to `109.47`, looser than the `87.57` floor the ratchet locked in earlier, and per the Q7 FINAL the floor holds against it. `:2480` `INFO|lock level|enforced_limit=87.57|peak=2.32|ratchet_level=-87.57|peak_level=-85.25|chosen=peak|pnl=2.32|realized=2.32|floating=0.00`. THE SECOND CONTEST IS INSIDE THIS ONE LINE: with the raise already beaten by the ratchet, the peak mechanism is checked against the surviving ratchet term, and `peak_level=-85.25` sits ABOVE `ratchet_level=-87.57`, the stricter of the two by MathMax, `chosen=peak` recording exactly that. BOTH MECHANISMS WERE LIVE AND BOTH WERE BEATEN IN TURN, the raise by the ratchet and the ratchet by the peak, on one pass, at one timestamp.

2026-09-01 LABEL CORRECTION TO THE V1-C CLOSURE ABOVE. THE CITATION "PER THE Q7 FINAL" IS WRONG AND IS CORRECTED HERE WITHOUT EDITING THE CLOSURE, which stands. `Q7` unqualified names the Q7/F5 owner ruling of 2026-07-29 in DECISIONS, "All expiry and anchor decisions use TimeCurrent exclusively", the TimeCurrent clock ruling, and has nothing to do with the ratchet floor. The ratchet holding against a raised limit is governed by PHASE 2 OPEN QUESTION SEVEN, owner ruling 2026-08-18, exactly as the WARN line the closure itself quotes names it: `` `:2479` `WARN|ratchet HOLDING against a raised limit (question SEVEN): live=109.47 floor=87.57; the floor is enforced until the next day anchor` ``. The closure's own quoted evidence names the correct ruling; only the prose sentence beside it mislabeled it as Q7. Nothing about the V1-C row, its evidence, or its closure changes.

V1-D CLOSES: ROLLOVER RESETTING PEAK AND BASE TOGETHER ON ONE ANCHOR VALUE. Evidence `docs/evidence/journal-20260830-rollover-and-boot-breach.txt`, the 01:00:00 trio. `:241` `INFO|day rollover|old_anchor=2026.08.29 01:00:00|new_anchor=2026.08.30 01:00:00|jump=86400s`, `:242` `INFO|ratchet reseeded at rollover|old_anchor=2026.08.29 01:00:00|new_anchor=2026.08.30 01:00:00|old_floor=109.58|new_floor=127.14`, `:243` `INFO|realized peak reset at rollover|old_anchor=2026.08.29 01:00:00|new_anchor=2026.08.30 01:00:00|old_peak=351.20|new_peak=0.00`. ALL THREE LINES CARRY THE SAME `old_anchor` AND THE SAME `new_anchor`, ONE ROLLOVER EVENT MOVING BOTH MECHANISMS' WINDOWS TOGETHER, exactly as the FINAL's Reason requires: the reset is the walk's window moving, not an action taken on its own clock. `old_peak=351.20` is the weekend's closing figure, the same peak the incidental finding below traces to four BTCUSD.ecn deals, and it drops to `new_peak=0.00` in the same event that reseeds the floor from `109.58` to `127.14`.
SIX INCIDENTAL FINDINGS SURFACED WHILE CLOSING THE FOUR ROWS, NONE OF THEM PART OF ANY ROW'S OWN DEFINITION, EACH QUOTED SO A LATER SESSION CAN CHECK RATHER THAN TRUST. ONE, 2026-08-29: THE ENTIRE WEEKEND REALIZED PEAK RISE WAS BUILT ON BTCUSD.ecn. Every one of the day's four `realized peak raised` lines in `docs/evidence/journal-20260829-peak-rise-weekend.txt` names a deal ticket that `docs/evidence/terminal-20260829-btcusd-deals.txt` shows was a BTCUSD.ecn close and nothing else: `:3142` `old=0.00|new=4.50|deal_ticket=342597189`, terminal `:22` `deal #342597189 buy 0.3 BTCUSD.ecn at 77595.50`; `:3417`-`:3418` `old=4.50|new=8.70` and `old=8.70|new=76.70`, deal tickets `342639979` and `342639982`, terminal `:162` and `:166`, both BTCUSD.ecn closes at `77683.50`; `:3641` `old=76.70|new=351.20|deal_ticket=342659606`, terminal `:177`, BTCUSD.ecn buy at `77565.00`. ZERO NON BTCUSD.ecn DEAL EVER TOUCHES THE PEAK THIS DAY, carried to ISSUES below since no ruling addresses BTCUSD.ecn's presence, only the rollover timing it happened to sit beside.
TWO, 2026-08-29: THE LATE ROLLOVER, MEASURED. `docs/evidence/journal-20260829-peak-rise-weekend.txt` opens already frozen, `server=2026.08.28 23:57:59` on its first line, and stays frozen on every `lock level` and `LIFE` line for the next twelve and a half hours; `:3076`, local `12:48:40`, is the last frozen pass, still `server=2026.08.28 23:57:59`, exactly thirty seconds after the pass before it at `:3074`, `12:48:10`, the cadence RULING THREE describes. The unfreeze and the rollover trio land together at `:3077`-`:3079`, local `12:48:53`, server stamp `2026.08.29 12:48:51` on all three lines, a same-value floor reseed, `old_floor=109.58|new_floor=109.58`, because nothing had traded yet that anchor. Every BTCUSD.ecn deal in finding ONE follows the unfreeze; none falls inside the frozen window.
THREE, 2026-08-30: THE OFFLINE GAP RULING FOUR EXPLAINS, MEASURED TO ITS OWN THREE SECONDS. `docs/evidence/terminal-20260830-zero-positions.txt:19` `15:14:10.134|System|terminal stopped due to system shutdown` to `:20` `15:45:54.206|Terminal|MetaTrader 5 x64 build 6140 started`, 31 minutes 44 seconds offline. `docs/evidence/journal-20260830-rollover-and-boot-breach.txt:3663` `TRANSITION|BOOT->SYNCING|boot|weekly=on|timer=1s` at `15:45:56` to `:3667` `TRANSITION|SYNCING->LOCKED|boot derivation: DAILY_BREACH|locked_until=2026.08.31 01:00:00` at `15:45:59`, three seconds on the server stamp, boot witness at `:3666` reading `realized=-192.00|floating=0.00|running_min=-192.00|replay=1|tier=floor`. `replay=1` is the tell, the derivation replayed server side deal history rather than trusting a live snapshot, the only way a guardian that was not running could know what happened while it was not running. The terminal's own sync lines before and after the gap both read `0 positions`, `:6` at `01:52:54` and `:27` at `15:45:55`, so the mobile trading that produced the `-192.00` opened and closed entirely inside the outage. ZERO ENFORCEMENT EXISTED FOR THOSE 31 MINUTES BY CONSTRUCTION, carried to ISSUES below since ruling four accepts the catch and does not rule on the window.
FOUR, 2026-08-27: THE INPUT DANCE, COUNTED. `docs/evidence/journal-20260827-v1b-v1c-input-dance.txt` carries nine `INFO|init` lines across the day; seven fall inside nine minutes, `17:44:34` to `17:53:16`, four preceded by `deinit|reason=5` (parameter change) and three by `deinit|reason=9` (terminal close), the owner alternating between editing inputs and restarting outright while driving V1-B and V1-C. `chain=1/3 consecutive unclean` reads on every one of the nine boots, the R1 crash-loop primitive correctly reading these as clean, deliberate restarts rather than a real crash loop.
FIVE, 2026-08-31: THE `stale_anchor_reconstructed` RECONCILIATION SOURCE, NAMED IN THE 2026-08-27 DESIGN ENTRY BUT NOT SEEN LIVE UNTIL NOW. `docs/evidence/journal-20260831-flat-expiry-and-v1a-lock.txt:121`-`:126`: the 2026-08-30 lock expires clean at `01:00:01`, `:123` `SYNCING->ACTIVE|history stable|polls=3/3`, and `:124`-`:125` fire the day's rollover and the peak reconciliation in the same pass, `:125` `INFO|realized peak reconciled|anchor=2026.08.31 01:00:00|reconstructed=0.00|persisted=0.00|taken=0.00|source=stale_anchor_reconstructed`. The branch fired and reconciled correctly, landing on the degenerate all zero case since the prior anchor never carried a realized gain; a live exercise of the same branch with a NONZERO stale figure has still not occurred in any banked evidence, carried to ISSUES below.
SIX, 2026-08-31: THE PROFIT V1-A GIVES BACK, TRACED TO ITS OWN DEALS. `docs/evidence/terminal-20260831-v1a-lock.txt:30` and `:35` close the day's first position, `buy 1 XAUUSD.ecn at 4425.79` opened `16:49:59.586`, closed at `4426.64` at `16:50:06.009`, the earliest contributor to the day's realized figure; neither of the day's two XAUUSD.ecn deals visible in the banked terminal window is a BTCUSD.ecn deal, so the 2026-08-31 peak sits on the instrument set the 2026-08-01 FINAL actually authorizes, unlike the weekend figure findings ONE and TWO trace.
WHAT THIS ENTRY DOES NOT DO: it does not merge this branch, per the merge gate FINAL of 2026-08-03, and it does not bank or argue the V1-A lock's 2026-09-01 01:00:00 expiry, left to the follow-up session the ISSUES head below names.

2026-08-31. RAW EVIDENCE FOR FOUR DAYS OF LIVE VERSION 1 ACTIVITY IS BANKED READ ONLY, AND NOTHING HERE CLOSES ANY OF THE FOUR ACCEPTANCE ROWS. Per RULE A the owner produced the events and copied the four days of Experts and terminal logs out of the Terminal folder into a session tmp directory; the executor's whole part was to integrity check each tmp copy against the live Terminal folder original, read only, and bank the eight files under `docs/evidence/` with no line quoted and no row judged. THE LIVE SOURCE PATHS, confirmed this session at the real data folder whose first component after `Terminal\` is `D0E8209F77C8CF37AD8BF550E51FF075`: Experts journals at `MQL5\logs\<YYYYMMDD>.log`, terminal logs at `logs\<YYYYMMDD>.log`.
INTEGRITY METHOD, TWO SHAPES. Closed days, `20260827`, `20260829`, `20260830`: FULL md5 of the tmp copy against the full live original, both files no longer growing. The open day, `20260831`: BYTE PREFIX, the live original still being written by the running EA, so the tmp copy's own length was taken as `N` and the first `N` bytes of the live original were hashed and compared against the tmp copy's whole-file hash, per the same method the 2026-08-23 and 2026-08-25 entries above used for a day still in progress. ALL EIGHT PAIRS MATCHED, zero mismatches: `experts-20260827.log`/live md5 `58DDA5A665885E1A95763D28B6C50650`; `terminal-20260827.log`/live md5 `9BCF2FA3FFFA7C6AC78E805D6320E007`; `experts-20260829.log`/live md5 `EBFBCCFAEAB6B3DA799B91EDE91BAF37`; `terminal-20260829.log`/live md5 `9997858AA93280995029C84F57491A1C`; `experts-20260830.log`/live md5 `4C49914B71A8D44FC09A11C6952EFC1A`; `terminal-20260830.log`/live md5 `051CF03678EFA50952B17DE932E20FED`; `experts-20260831.log` (1997010 bytes) against the live original's first 1997010 bytes (live original itself 1998730 bytes and still growing), both hashing `5324386185D3156D00411C1646F406FA`; `terminal-20260831.log`, tmp and live equal length at 8830 bytes, both hashing `57771BAF0D4F765365BF535D7B430AA2`.
EIGHT FILES BANKED AT THIS ENTRY, WITH THE BANKED COPY'S OWN SIZE, LINE COUNT AND md5, EACH RE HASHED AFTER THE COPY AND EQUAL TO THE SOURCE VALUE ABOVE. `docs/evidence/journal-20260827-v1b-v1c-input-dance.txt`, 2441526 bytes, 4754 lines, md5 `58DDA5A665885E1A95763D28B6C50650`. `docs/evidence/terminal-20260827-v1b-v1c-input-dance.txt`, 16376 bytes, 92 lines, md5 `9BCF2FA3FFFA7C6AC78E805D6320E007`. `docs/evidence/journal-20260829-peak-rise-weekend.txt`, 2983838 bytes, 5767 lines, md5 `EBFBCCFAEAB6B3DA799B91EDE91BAF37`. `docs/evidence/terminal-20260829-btcusd-deals.txt`, 36818 bytes, 179 lines, md5 `9997858AA93280995029C84F57491A1C`. `docs/evidence/journal-20260830-rollover-and-boot-breach.txt`, 2279830 bytes, 4657 lines, md5 `4C49914B71A8D44FC09A11C6952EFC1A`. `docs/evidence/terminal-20260830-zero-positions.txt`, 8812 bytes, 45 lines, md5 `051CF03678EFA50952B17DE932E20FED`. `docs/evidence/journal-20260831-flat-expiry-and-v1a-lock.txt`, 1997010 bytes, 3990 lines, md5 `5324386185D3156D00411C1646F406FA`. `docs/evidence/terminal-20260831-v1a-lock.txt`, 8830 bytes, 44 lines, md5 `57771BAF0D4F765365BF535D7B430AA2`.
WHAT THIS ENTRY DOES NOT DO, so it is not mistaken for more than it is. NO LINE FROM ANY OF THE EIGHT FILES IS QUOTED HERE. NO ACCEPTANCE ROW, V1-A, V1-B, V1-C OR V1-D, IS CLOSED, SCORED OR EVEN ARGUED, and the ACTIONS entry of 2026-08-27 above stands unedited: acceptance is still pending on all four. THE `.ex5` BUILD IDENTITY THAT PRODUCED THIS ACTIVITY IS NOT RE DERIVED IN THIS ENTRY and is left to whichever session performs the actual row closures, which will need it beside the quoted lines exactly as the P3-1 and P49/P53 closures above did. THIS IS A BANKING ENTRY ONLY, the same class of entry as the 2026-08-23 vector run banking above.
`20260901` DOES NOT YET EXIST AND IS NOT BANKED HERE. THE V1-A LOCK, IF ONE STANDS AT THE 2026-08-31 CLOSE, REACHES ITS EXPIRY ANCHOR AT 2026-09-01 01:00:00, AND THAT EVENT, TOGETHER WITH WHATEVER IT PROVES OR FAILS TO PROVE ABOUT THE FOUR ROWS, IS EXPLICITLY DEFERRED TO A SEPARATE FOLLOW-UP SESSION. This entry states that deferral outright rather than leaving it to be inferred from an absent file.

2026-08-27. VERSION 1 OF THE REALIZED PEAK TRAILING FLOOR IS IMPLEMENTED IN SOURCE ON BRANCH `worktree-v1-realized-peak`, COMPILES CLEAN, AND IS ACCEPTANCE PENDING ON EVERY ONE OF ITS FOUR ROWS. Four commits off main at `de8d9c3`, in the order the units were built and each compiled before it was committed: `80cc46a` the fold extension in `Pnl.mqh`, `19e158e` the peak model and its file and `AgPeakUpdate` in `Persist.mqh`, `665ffb4` the boot load and the breach comparison in `AccountGuardian.mq5`, `428614c` the lock level journal line. THE DIFF IS 522 INSERTIONS AND 2 DELETIONS ACROSS THREE FILES, ALL UNDER `MQL5/`, and the two deletions are named here because a diff this large with two removed lines invites the question: they are the old `bool breach_now = (pnl <= -enforced_limit + AG_PNL_EPSILON);` and the old `double AgRealizedFold(...)` signature line, the latter re appearing unchanged twenty lines lower as the forwarder. NOTHING ELSE WAS REMOVED ANYWHERE.
WHAT THE MECHANISM IS, IN THE TERMS THE RULING SET USES. `AgRealizedRunFold` is the existing single sorted walk in `Pnl.mqh` with a running maximum added beside the running minimum it already carried, plus the ticket and time of the deal that last raised it. `AgRealizedFold` keeps its frozen signature and its two outputs exactly and is now a forwarder onto it, so the defect 3 shape 1 FINAL of 2026-08-20 is satisfied in letter and no second walk exists anywhere. The peak block in `Persist.mqh` mirrors the ratchet floor block above it function for function, `AgPeakPath`, `AgPeakSerialize`, `AgPeakSave`, `AgPeakResetModel`, `AgPeakQuarantine`, `AgPeakLoad`, `AgPeakEffectiveLevel` and `AgPeakUpdate`, with ONE deliberate difference and it is the whole of D3.1: a corrupt or missing floor reseeds from the live limit, a corrupt or missing peak RECONSTRUCTS FROM TODAY'S DEAL HISTORY, and nothing in the block ever seeds a peak from an input. In the breach tail `peak_level` is the peak minus the POST RATCHET `enforced_limit` per D7, `ratchet_level` is `-enforced_limit`, and the comparison is `pnl <= MathMax(ratchet_level, peak_level) + AG_PNL_EPSILON`, which is D2 with what `pnl` means left untouched, which is D1.2.
ONE DESIGN QUESTION THE SPEC DID NOT SETTLE WAS SETTLED BY THE CODE AND IS RECORDED RATHER THAN LEFT FOR A REVIEWER TO DISCOVER. `AgEvaluateActive` calls `AgRealized` and not `AgRealizedFold` above the breach tail, and that region is required to stay byte identical, so the running maximum CANNOT be threaded down from it. `AgPeakUpdate` therefore calls `AgRealizedRunFold` itself, every pass. THAT IS A SECOND CALL AND NOT A SECOND WALK: the fold exists once, with the F12 whitelist and the Q3 formula written down once, which is the property both the shape 1 FINAL and the fold's own comment protect. The cost is one more pass over one day's deals per timer tick and it is stated here rather than discovered later. The gain is that reconstruction is the ORDINARY path rather than a restart only path, so D3.1 has no separate code to be wrong on its own, and a dip and recovery between two samples cannot hide from a running maximum that sees every deal in order.
THREE READINGS WERE TAKEN THAT THE SPEC LEFT OPEN, ALL IN THE STRICT DIRECTION, ALL RECORDED AS READINGS AND NOT AS RULINGS. ONE, the peak's anchor MOVES FORWARD ONLY. On the D3.1 backward step case the anchor is held at the later day rather than rewound, so a clock rewind followed by a correction cannot present itself as a rollover and reset the peak in one act; that is the ratchet's NO RESEED ON A BACKWARD STEP clause applied to the peak for its own reason. TWO, on a backward step in steady state the peak is HELD and is NOT raised from that pass's reconstruction, because a rewound anchor widens the walk's window into the previous day and the running maximum it returns is a two day figure that no single day ever reached. Holding loosens nothing, so D2 is untouched: D2 governs the max BETWEEN the two mechanisms and neither term moves down there. THREE, `AgPeakUpdate` runs AFTER the `HistoryDealsTotal` read rather than before it, so the Q9 deferral's count is provably the one Phase 1 took; the fold re selects the same window with the same anchor, so the count could not differ either way.
THE FOUR JOURNAL LINES THE ACCEPTANCE ROWS DEPEND ON ALL EXIST. `realized peak reconciled|anchor|reconstructed|persisted|taken|source`, where `source` names the D3.1 case outright as `no_file`, `equal_anchor_max`, `stale_anchor_reconstructed` or `backward_step_max`, so the two legs of the restart row are separable on the artifact. `realized peak raised|anchor|old|new|deal_ticket|deal_time`. `realized peak reset at rollover|old_anchor|new_anchor|old_peak|new_peak`, fired from a branch that re stamps the anchor and logs and does nothing else, THE RESET ITSELF BEING THE WALK'S WINDOW MOVING exactly as the FINAL's Reason requires and NOT an action taken. And `lock level|enforced_limit|peak|ratchet_level|peak_level|chosen|pnl|realized|floating`, emitted UNCONDITIONALLY before every breach declaration and at the LIFE cadence on every ACTIVE pass that does not declare, the deferred breach pass included. ONE PROPERTY OF `chosen` IS WRITTEN DOWN NOW SO IT IS NOT READ AS A DEFECT LATER: the peak term can never sit BELOW the ratchet term, a running maximum starting at zero and both terms subtracting the same `enforced_limit`, so the field reads `tie` while the day has no realized gain and `peak` once it has one, and `ratchet` is UNREACHABLE while that invariant holds. Seeing `ratchet` in a live journal is a finding, not a normal reading.
THE COMPILE, AND THE BUILD IDENTITY IN THE FORM STANDING RULE 7 REQUIRES RATHER THAN AS AN `.ex5` HASH. Every one of the four units compiled `0 errors, 0 warnings` before it was committed, and both vector scripts were compiled as well because `Persist.mqh` is included standalone by them, `AgPhase1ClockVectors` and `AgPhase2StateVectors`, both `0 errors, 0 warnings`. The final build of the tip logged `Result: 0 errors, 0 warnings, 1021 ms elapsed, cpu='X64 Regular'`. THE `.ex5` FIGURES ARE RECORDED AND ARE EXPLICITLY NOT IDENTITY, per standing rule 7 of 2026-08-04: `2A3320AABF6C000CB151842E1FBEFA19` at 88438 bytes, mtime 2026-08-27 12:12:45. THE IDENTITY IS THE SOURCE SET: `AccountGuardian.mq5` `6CF5F6A78E8D9A333D84780C33DE475A`, `Pnl.mqh` `D43E85BFF6AC375932A55E4032B3F835`, `Persist.mqh` `0728E30210CAB55C7F83FD46E4F8D0A6`, and the three unchanged includes `Clock.mqh` `DC5A04004F0CB7BDB9D6C602E944CEC1`, `Log.mqh` `C4E803FB81EBE7F521CC020E8FA1CE82`, `State.mqh` `34792807C6B6B548943215866193EB3F`, `Sweep.mqh` `CF7D355A7C52265A7AFF5C465DA33C46`. THE COMPILE RAN ENTIRELY INSIDE THE WORKTREE: `metaeditor64.exe /compile /include` rooted at the worktree `MQL5` tree, and the log confirms every include resolved to a worktree path, so NO PATH UNDER THE MT5 TERMINAL DATA FOLDER WAS READ, WRITTEN OR NAMED by this session, per RULE A. As always the exit code was 1 and was ignored, standing rule 6.
THE CONSTRAINTS WERE VERIFIED BY MEASUREMENT AND NOT BY INSPECTION, WHICH IS WHY THE MEASUREMENTS ARE HERE. `AG_STATE_FORMAT_VERSION` is still 1 and `state_<login>.dat` is untouched: the `Persist.mqh` diff is a SINGLE HUNK, a pure insertion at line 846 after `AgRatchetUpdate` ends, and contains ZERO `AGSTATE` or `AGFLOOR` lines, so the state and floor serializers are byte identical and `floor_<login>.dat` keeps its format and the ratchet keeps all five ratified elements. The region above the breach tail in `AgEvaluateActive` is byte identical BY HASH: the function's opening line through the last line before the tail's own comment md5s `58cb52d324065a9b9fcde3f89524761c` on main and on the tip alike. THE PEAK IS PRE BREACH ONLY: `AgPeakUpdate` has exactly ONE call site in the whole tree, inside the breach tail, `AgEvaluateActive` has exactly one call site and it is under `g_ag_state == AG_STATE_ACTIVE`, and `AgDeclareLock` contains zero references to anything peak related, so D8 holds structurally rather than by care. ONE CLOCK: the entire peak block contains exactly one clock call, a `TimeLocal()` for the backward step WARN cadence which is the A1 exemption class the ratchet's own WARN already uses, and NO `TimeCurrent`, NO `AgDayAnchor`, NO date literal and NO seconds constant, the boundary reaching the peak only through the `window_anchor` the caller already computed, which is D3.2. EPSILON: the rise test is `reconstructed > g_ag_peak_currency + AG_PNL_EPSILON` and the breach test is the flat 2026-07-30 epsilon in the same place and direction it has always been; the anchor comparisons are datetime and exact by nature, and the one `<= 0.0` guard is copied in shape from the ratified `AgFloorEffectiveLimit` line above it and cannot move the level by more than a sub cent, which the breach epsilon absorbs. NO EM DASH AND NOTHING NON ENGLISH: the full diff was scanned byte by byte and contains ZERO non ASCII bytes.
ACCEPTANCE IS PENDING ON ALL FOUR ROWS AND NOTHING HERE CLAIMS OTHERWISE. V1-A profit then give back locking on full equity with the position still open, V1-B restart reconstruction with no re inflation including the file deleted leg, V1-C stricter wins with both mechanisms live across a raise attempt, and V1-D rollover resetting peak and base together on one anchor value. NOT ONE OF THEM HAS BEEN RUN. Each closes only on quoted journal evidence plus the build identity of the binary that produced it, and per RULE A the rows run on a build THE OWNER DEPLOYS, after owner review of this branch, with the executor's part being the read only harvest afterwards and no part of the live event.
WHAT WAS DELIBERATELY NOT TOUCHED, listed so a later diff is not re read as scope creep. NO MERGE, NO PUSH, no branch but this one. Nothing under the MT5 Terminal folder, not even a read. No `README.md`, no `docs/`, no `docs/status`. NO FINAL ENTRY WRITTEN OR EDITED, and no DECISIONS entry of any kind: the three readings recorded above are readings taken to implement a ruling that already exists, and if the owner disagrees with any of the three the fix is code, not a new ruling. The ISSUES head is left as it stands, because what it says is still true: version 1 is authorized and the D4 gate is open, and it stops being accurate only when the acceptance rows close, which is the owner's event and not this session's.

2026-08-27. THE CONSOLIDATION OF 2026-08-26 AND 2026-08-27. THE PHASE 3 DEFECT FIX BRANCH REACHED MAIN AND THE REPOSITORY WAS REDUCED TO ONE CHECKOUT ON ONE BRANCH. `worktree-phase3-defect-fixes` was merged into main as `962d6fb`, a true merge taken with `--no-ff` and carrying both parents, `e73874a` on the main side and `44ac449` on the branch side, message `merge: phase 3 defect fixes, P3-1 P49 P53 closed and the D4 gate opened`. EXACTLY ONE FILE CONFLICTED, `LEDGER.md`, and the resolution is stated here so it can be checked rather than trusted: a LOSSLESS THREE HUNK UNION, nothing dropped from either side, DECISIONS carrying 92 entries of which 89 are FINAL, which is the exact union of both parents and not a selection from either. ONE LINE IN THE WHOLE RESOLUTION WAS REWRITTEN RATHER THAN UNIONED, the `Status:` line of the D4 gate, now reading UNBLOCKED, and that rewrite is the recorded consequence of `P3-1`, `P49` and `P53` closing on the deployed build rather than an independent ruling. NO FINAL ENTRY WAS EDITED, ADDED OR REMOVED BY THE MERGE.
THE MERGED SOURCE WAS COMPILED FROM MAIN AND THE DEPLOYED BINARY WAS LEFT ALONE, which are two separate facts and are recorded as two. The compile was performed in MetaEditor against the post merge main working tree and returned 0 ERRORS AND 0 WARNINGS, so the union of the two branches is a building tree and not merely a merging one. THE TERMINAL WAS NOT TOUCHED: the deployed `AccountGuardian.ex5` remains `1C88C4A1` and NO BINARY WAS REPLACED, copied or removed, per RULE A. A clean compile is proof the tree builds and is not a deploy, and nothing in this entry moves the running build off `1C88C4A1`.
THE BRANCH AND WORKTREE ESTATE WAS RETIRED, AND ONE BRANCH WAS RETIRED ONLY AFTER IT WAS PROVEN TO CARRY NOTHING. `claude/readme-state-docs-ntmvuf` was deleted with `-D`, the forced form, and the force is justified on artifact rather than on judgement: the branch was first proven CONTENT IDENTICAL to main, its work having already reached main as the squash `dbb1705`, so `-D` discarded a pointer and not a change. Its remote counterpart was deleted as well. ALL 12 STALE WORKTREES WERE REMOVED and the 13 MERGED BRANCHES they carried were deleted both locally and on the remote. THE FINAL STATE IS ONE CHECKOUT ON `main`, ONE LOCAL BRANCH, AND `main` EQUAL TO `origin/main`.
ONE INCIDENT IS RECORDED RATHER THAN OMITTED, BECAUSE IT LOOKED WORSE THAN IT WAS. After the worktree cleanup the WORKING TREE COPY of `AccountGuardian.mq5` was found DELETED FROM DISK, showing as ` D` in `git status`. It was restored with `git checkout --` and the restored file was verified BYTE IDENTICAL TO HEAD. THE TRACKED CONTENT WAS NEVER AT RISK, the deletion having touched the working copy alone while the committed blob stood untouched throughout, which is why the restore is a checkout and not a recovery. THE CAUSE WAS NOT INVESTIGATED and is left open as a fact rather than closed as a theory; anyone who meets it again should read this line first and not assume history loss.
THE STANDING STATE AFTER THE CONSOLIDATION. The repository is MIRROR READY for the version 2 split described by the consolidation study, and VERSION 1 IMPLEMENTATION IS AUTHORIZED AND UNBLOCKED, the D4 gate having opened on the closures the merge carried onto main. WHAT WAS NOT TOUCHED IN THIS RECORDING SESSION, listed so a later diff is not read as scope creep: no file but `LEDGER.md`, no ISSUES entry, no DECISIONS entry, no FINAL entry, no MQL5 source, no README, no `docs/status`, nothing under the MetaTrader Terminal data folder, no merge, and no push.

2026-08-26. RUN B, THE HARVEST, AND THE CLOSURE OF BOTH DEFECT ROWS. THE LAST TWO LIVE ACCEPTANCE ROWS ON THIS BRANCH CLOSED ON THE ARTIFACTS THEY ASKED FOR, NOT ON REPORTS. Run A of 2026-08-25 had closed P3-1 and had explicitly given the defect 3 rows nothing, because the losing position was closed by hand well before the anchor and no expiry time re lock followed. Run B was the complement: the owner let a lock reach its anchor with the two losing sells STILL OPEN. The lock itself is `AG|2026.08.25 22:00:27|TRANSITION|ACTIVE->LOCKED|DAILY_BREACH|pnl=-108.84|limit=100.72|locked_until=2026.08.26 01:00:00` at `docs/evidence/journal-20260825-runb-boot-derived-lock.txt:753`. At 2026-08-26 01:00 that lock expired into a SYNCING exit with the loss still on the book, so the pass took the BOOT DERIVATION route P32 says it must under shape A, and twelve minutes later the owner performed one deliberate terminal restart while the new lock was live, which produced the P53 boot. One event, both rows.
THE HARVEST WAS READ ONLY AND WAS INTEGRITY CHECKED BEFORE ANYTHING WAS QUOTED, and that is the part worth keeping rather than the result. The owner copied the source logs out of the Terminal data folder into `tmp` per the evidence handling ruling of the run, and the executor read only from there; every executor touch of the Terminal folder across both sessions was a directory listing, a file hash or a byte prefix read, per RULE A. Five artifacts were banked at `c2678d2`, four journals transcoded UTF-16LE to UTF-8 character for character and the state file banked RAW. The two closed 2026-08-25 day logs hashed identical on both sides at `8CFE43ADCF828045796E20E2AD813BF8` and `6FC491F1020DADE02403EA7779C10779`; the two 2026-08-26 logs, still being written, were verified as exact byte prefixes at 76248 bytes hashing `6AA8248FECA711C5EF79F5CD90445B34` and 2652 bytes hashing `0B701C17C956662AEB0304A322623AE3`. THIS SESSION RE DERIVED ALL SIX HASHES BEFORE QUOTING A SINGLE LINE, which is why they are written into both closures: `journal-20260825-runb-boot-derived-lock.txt` `E689483DC31184CCE974EB1A7BF9BA34` at 249526 bytes; `journal-20260826-runb-p53-next-boot.txt` `52E41EB668F99CEE34C941B398B0D9B4` at 42374 bytes; `state_1200252169.dat.runb-p53-next-boot-2026-08-26` `B6E33C27EADA7D20C0A82D7713C51284` at 93 bytes; `terminal-20260825-runb-boot-derived-lock.txt` `601637015309B6BB5AAB4200366CEBCE` at 6591 bytes; `terminal-20260826-runb-p53-next-boot.txt` `9F7EA3CCADA024A09A0EE1B33B2FAD8F` at 3731 bytes; and the deployed `AccountGuardian.ex5` `1C88C4A155CF09AC38D71C3E5BFEF1D9` at 78676 bytes, hashed read only at the real data folder Experts path under `D0E8209F77C8CF37AD8BF550E51FF075`. The four journal values hash the BANKED UTF-8 files and the source side values above hash the UTF-16LE originals; the state file, banked raw, hashes the same on both sides, which is the corroboration that matters, since P49 is a byte comparison.
WHAT CLOSED P49 AND P53, in one paragraph, with the full quoting left to the entry itself. The state file written by the boot derived lock reads `L|1|1787792400|1787706010` and `N|101.39900000|2027.98000000` where the 2026-08-18 artifact under the retired build reads `L|1|1787101200|0` and `N|0.00000000|0.00000000`: non zero breach time, both snapshot figures non zero at eight decimals, and the limit stored as the full double `101.39900000` rather than the printed cent, which is P50 in passing. The 01:00 derivation at `:184` read `tier=live` because no snapshot existed yet; the 01:12 derivation at `:223` read `tier=snapshot` off the one the first pass wrote, which is P53 in one word. P56 reconciles with no other artifact: `breach_time` `1787706010` decodes to `2026.08.26 01:00:10` and the `SYNCING->LOCKED` line at `:185` carries the server stamp `AG|2026.08.26 01:00:10`, agreeing at ZERO seconds. Over the banked day, `ACTIVE->LOCKED` ZERO, `SYNCING->ACTIVE` ZERO, `LOCKED->ACTIVE` ZERO and `SYNCING->LOCKED` TWO. Neither row used a LIFE line and no source was read as proof of either.
P51 WAS GRADED HONESTLY RATHER THAN UNIFORMLY, and it is recorded here as a method note because the temptation was to report three proofs. Two of the three preserved fields are DIRECT: `breach_time` still reads the 01:00:10 instant after the 01:12 FILE witness boot wrote the file again, and `limit_snap` is read back on a line that says `tier=snapshot`, meaning the value was loaded rather than recomputed. The third, `base_snap`, is INFERENTIAL: no line prints the base on this path and no state file copy exists from between the two writes, so the field rests on the three being written together from one loaded snapshot on the same preserving branch the other two took. The owner ruled P51 SATISFIED on that basis and ruled the basis itself be recorded rather than hidden, which is what both the entry and this line do.
THE RAISED AND NOT RULED ITEM IN THE ISSUES HEAD WAS RESOLVED ON ARTIFACT, and it resolved in the direction the head named as the good one. P31 of the defect 1 plan, written 2026-08-19 against shape A alone, predicted an expiry time re lock writing `L|1|<until>|0` and `N|0.00000000|0.00000000`; the head recorded in advance that on the deployed binary the same pass should write P49's form instead, and that an all zeros file WOULD be evidence the defect 3 fix never reached the running binary. The file took the P49 shape. The owner therefore ruled the item RESOLVED ON ARTIFACT, with P31 SUPERSEDED ON THIS PATH by the one build coupling of 2026-08-20 rather than falsified. It is a live proof that the deployed `1C88C4A1` carries the fix, obtained without reading a line of source.
WHAT THE ISSUES HEAD LOST AND WHY. The head carried `limit=106.65` against `base=2133.00`, a 2026-08-23 reading, as the size a losing position had to clear. By the time the rows closed that pair had moved twice, to `limit=100.72` at the Run B lock and to `limit_cmp=101.40` against a base of `2027.98` at the derivation. The figures are not corrected in place, they are REMOVED, and replaced by the statement that the daily base and the enforced limit move every day by design so no figure pinned in a head survives; the two closures carry their own numbers tied to banked line numbers. The same head now records both rows closed, records the RAISED AND NOT RULED item as resolved, and carries one line stating that the D4 gate is open.
THE D4 GATE OF THE VERSION 1 RULING SET IS NOW OPEN. D4, FINAL 2026-08-24 in the main LEDGER, holds that no version 1 binary reaches MT5 while `P3-1` or `P49`/`P53` is open on the currently deployed build. Both are now closed on `1C88C4A1`, so the gate is open and version 1 implementation is authorized to proceed. Recorded with the fact the 2026-08-24 entry itself surfaced, because it still holds: `P3-1` and `P49`/`P53` do not exist in the main ledger, they live only on this unmerged branch, so a session reading only main cannot see what closed the gate. NO MERGE IS PERFORMED OR PROPOSED HERE, the merge being the owner's to instruct per the merge gate FINAL of 2026-08-03.
WHAT WAS DELIBERATELY NOT TOUCHED, listed so a later diff is not re read as scope creep. No MQL5 source, no README, no `docs/status`, nothing under the MT5 Terminal folder beyond the read only hash of the deployed `.ex5`, no position closed, no merge, no push, no branch but this one, and NO FINAL ENTRY WRITTEN OR EDITED. The defect 3 row was closed IN PLACE under the same convention P3-1 and the vector row use, a `CLOSED` line inserted above the `Status:` line with the `Issue:` and `Action:` lines left byte for byte, since this file has no DONE section and never has. NOTHING HERE CLOSES DEFECT 2, DEFECT 4, P2-A OR P2-H: Run B produced no `TRANSITION|ACTIVE->LOCKED` inside the measured window, which is P2-A's own row, and P2-H is not closable by code or by code reading at all per the FINAL of 2026-08-19.

2026-08-25. RUN A AND THE CLOSURE OF P3-1. THE DEFECT 1 FIX EXECUTED IN THE FIELD FOR THE FIRST TIME AND THE ROW CLOSED ON THE ARTIFACT IT ASKED FOR, NOT ON A REPORT. The owner produced the live event and the executor performed no part of it, per RULE A: a losing position on the demo breached the daily limit at 14:32:15 on 2026-08-24, `pnl=-111.70` against `limit=106.65`, locking to `2026.08.25 01:00:00`, and the position was closed by hand at 14:32:30 and 14:32:33, more than ten hours ahead of the anchor. The machine was then left alone across the anchor. The executor's part was the read only harvest afterwards, which is the same division that closed the vector row and the deploy.
THE HARVEST WAS READ ONLY AND WAS INTEGRITY CHECKED BEFORE ANYTHING WAS QUOTED, which is the part worth keeping rather than the result. RULE B alias audit run before any path inside the Terminal folder was named, hazard confirmed still live on all seven audited names, four resolving to the deletion cmdlet and the rest to the move, copy and content writing cmdlets, and every command used was a plain inline directory listing, file hash or shared read FileStream with no user defined helper in the call chain. Four logs were copied out with a shared read FileStream. THE CHECK WAS SPLIT BY WHETHER THE ORIGINAL WAS STILL BEING WRITTEN, because the two cases cannot be checked the same way: the two CLOSED 2026-08-24 day logs were hashed on BOTH sides and matched, `48E02C8A2345371A7BFAF75651AB0E27` for the Experts log and `979523CB2FEF7DF3C49DB7553A824E33` for the terminal log; the two 2026-08-25 logs were still open for writing, so each tmp copy was verified as an EXACT BYTE PREFIX of its original instead, hashing the original's first `<tmp length>` bytes and comparing, 181402 bytes at `56A11705961B66FD893894A28EEEEA83` and 2652 bytes at `D0D629763A0967FA03C32A272419F614`, both matching. A whole file hash would have been meaningless on a growing file and a size comparison would have proved nothing about content, which is why the prefix form is written down here rather than left to the next session to reinvent.
THE ROW CLOSED ON P26 AND P27 AND ON NO LIFE LINE. P25, P26 and P27 each landed EXACTLY ONCE in the window, P27 three seconds after P26 on the server stamp and 3.001 seconds on the log local stamp, and the four counts the row is judged on were all ZERO over the window: `TRANSITION|LOCKED->ACTIVE`, `RESYNC entered`, `RESYNC exited` and `waiting_on=RESYNC: polls=`. P28 was satisfied at zero `LIFE|state=SYNCING` lines and is recorded as satisfied rather than as evidence, per its own terms. The full quotation with banked line numbers lives in the defect 1 ISSUES entry and is not duplicated here.
THE MOST USEFUL FINDING IS WHAT THE RUN HAPPENED TO EXERCISE, and it was not the easy case. The server clock sat FROZEN at `2026.08.24 23:57:59` for the last hour before the anchor, 125 LIFE lines in the window carrying that frozen stamp and `quote_age` climbing to 3721s on the line immediately before expiry, then advancing in one step. The terminal journal carries NO entry between 22:41:04 on 2026-08-24 and 02:41:08 on 2026-08-25, so the terminal was connected across the whole freeze. Run A therefore took the FROZEN QUOTE WHILE CONNECTED straddle, which is the exact shape of the 2026-08-19 evidence that raised defect 1, rather than a quiet expiry that would have proven the fix runs without proving it covers the defect. THE ROW GOT MORE THAN ITS MINIMUM and that is stated plainly instead of being left for a reader to notice.
WINDOW BOUNDARY AND THE 02:41 RESTART, recorded because a later recount will otherwise read a contradiction. The banked window runs from the lock at `:33` to the `deinit reason=9` at `:1500` of `docs/evidence/journal-20260825-runa-lock-expiry.txt`. An owner terminal restart at 02:41 removed and re attached the EA, one hour forty one minutes after the expiry, done to surface the new day's log file in the viewer. It is banked BELOW the AFTER THE WINDOW marker at `:1503` and its own `SYNCING->ACTIVE|history stable|polls=3/3` sits at `:1512`. So the DAY FILE carries two such lines while the WINDOW carries one, and the difference is the boundary and nothing else. The restart is corroborated on artifact rather than on the report: `deinit reason=9` is a terminal close, against the `reason=3` chart change events of 2026-08-18 and of 2026-08-24 at 14:13, so the journal distinguishes the two on its own, and the terminal journal agrees with `exit with code 0` at 02:41:09.
STRUCTURAL NOTE ON WHERE THE CLOSURE WAS WRITTEN. The instruction for this session was to move the defect 1 entry to DONE. THIS FILE HAS NO DONE SECTION and never has: it carries ISSUES, ACTIONS and DECISIONS only, and every row closed so far, the vector row of 2026-08-23 and the whole Phase 1 set among them, was closed IN PLACE inside ISSUES with a `CLOSED <date>` paragraph inserted above a rewritten `Status:` line. The defect 1 entry was closed the same way, so the convention is unbroken and no reference by row name is invalidated. Creating a new section was not done on the executor's initiative. IF THE OWNER WANTS A DONE SECTION the relocation is a pure cut and paste of the entry and no text needs rewriting.
WHAT WAS DELIBERATELY NOT TOUCHED. The ISSUES head was edited ONLY where it named defect 1 as open, in three places: the opening clause that counted two live acceptance rows, the block that restated the P3-1 predictions as pending, and the joint sentence `BOTH ROWS NEED A LOCK AND THERE IS NO LOCK`, which had to move because it asserted a falsehood about defect 1; its defect 3 requirement is preserved and is now stated as needing a BOOT DERIVED lock specifically. The `Action:` line of the head at LEDGER.md:6, including the RAISED AND NOT RULED P31 item, is untouched, as are the defect 3 entry, P49, P53, every FINAL entry in DECISIONS, every code file and `docs/status`. RAISED HERE AND NOT ACTED ON, since it sits in the defect 3 half this session was told to leave alone: the head still reads `A lock needs a losing position clearing 106.65 against base 2133.00`, and both figures are now stale, the 01:00 rollover having reseeded the ratchet to `new_floor=100.72` against `base=2014.50`. It is a stale number and not a wrong ruling, and correcting it belongs to whoever next has authorization over that half.
NOTHING HERE CLOSES ANY DEFECT 3 ROW. Run A produced no boot derived lock, because the position was closed by hand before the anchor and no expiry time re lock followed, so P49 and P53 took nothing from it and still wait on their own event. The P31 question raised in the head on 2026-08-23 is likewise still unanswered, since answering it needs exactly the expiry time re lock Run A did not produce.

2026-08-25, THE 2026-08-11 MANUAL-TRADES EVIDENCE BRANCH WAS MERGED INTO MAIN, branch `evidence/manual-trades-20260811` at commit `d4e2869`, merged onto main at `dbb1705`, a true merge carrying both parents since the merge conflicted and therefore could not fast forward, message `merge: evidence/manual-trades-20260811, P1-I and Q7 closures into main`. No push was performed and none was authorized. WHAT IT CARRIES: the manual-trades acceptance session of 2026-08-11, three evidence files that merged cleanly, `docs/evidence/phase1-manual-trades-2026-08-11.md`, `docs/evidence/journal-20260811-manual-trades.txt` and `docs/evidence/terminal-journal-20260811-manual-trades.txt`, and one ACTIONS entry recording P1-I CLOSED on the silver partial close and Q7 CLOSED WHOLE with the A8 corroboration debt discharged. THE ORDERING POINT, which is the whole reason this entry exists rather than a line inside the merged one: the closure is dated 2026-08-11 and the work was performed on that date, while main advanced past the branch point and accumulated entries from 2026-08-14 through 2026-08-18 that describe P1-I as open and Q7 as still unread. THOSE ENTRIES ARE NOT ERRORS AND ARE NOT CORRECTED. Each was true on main when it was written, because the evidence closing Q7 existed only on an unmerged branch and reached main only today, so they are SUPERSEDED BY CHRONOLOGY rather than by correction, and no later session may cite the difference as a fabrication or a contradiction. RESOLUTION OF THE ONE CONFLICTED FILE, `LEDGER.md`, stated so it can be checked rather than trusted. ACTIONS was resolved as a union with nothing dropped from either side: every entry from both parents is present, the branch's 2026-08-11 manual-trades entry sits below the 2026-08-14 sleep survival entry and above the 2026-08-11 push entry, so newest first ordering holds and the entry sits at its own date. ISSUES was updated only on its current status surfaces, three of them: the Phase 1 entry's Status line, which now records P1-I and Q7 closed and the A8 debt discharged rather than carried into Phase 2; the Phase 2 kickoff questions entry, whose only open item was EIGHT, the Q7 read, now closed; and the factual clause of the Phase 3 size gate input, which cited the debt as still outstanding. The branch's own two 2026-08-11 ISSUES entries, the plan section 2.3 nightly freeze correction and the P1-N gating on P1-L, are carried onto main unchanged. NOTHING WAS DELETED AND NOTHING WAS REWRITTEN: no line of history was removed, no dated narrative line on either side was edited in place, and DECISIONS was not touched at all, FINAL entries included. No file other than `LEDGER.md` was edited by the resolution, the three evidence files having merged without conflict, and nothing under the MetaTrader Terminal data folder was read, written or approached, per RULE A.

2026-08-24, CORRECTION SESSION, ONE LINE ONLY. THE DEFECT 1 Action LINE CARRIED A FALSE DEPLOY CLAUSE AND IT IS CORRECTED HERE ON OWNER RULING. The clause as it stood read `NOT DEPLOYED AND NOT COMPILED: the running binary is still 74D666E9, which carries the defect`, and it is false on artifacts: the fix was built as `1C88C4A1` on 2026-08-20 and that build has been deployed and running since 2026-08-23 22:25:57, which the ISSUES head and the third ACTIONS entry of 2026-08-23 already record, so the line contradicted the rest of this file rather than merely being out of date. THE OWNER RULING OF THIS DATE IS THE BASIS AND IS QUOTED AS THE RULING PUTS IT: `1C88C4A1 IS THE DEPLOYED BUILD, ARTIFACTS GOVERN`. THE OWNER'S OWN MEASUREMENT IS QUOTED RATHER THAN RESTATED, taken by hand by the owner: `Get-FileHash` on the Experts path returned `1C88C4A155CF09AC38D71C3E5BFEF1D9`. That value is recorded exactly as the owner reported it and THE EXECUTOR DID NOT RE MEASURE IT, no path under the MetaTrader Terminal data folder having been read, written or named by this session, per RULE A.
WHAT CHANGED AND WHAT DID NOT: exactly one clause of the `Action:` line of the defect 1 entry is replaced, and every other clause of that line stands byte for byte, including the shape A description, the `AccountGuardian.mq5:489` and `:487-488` citations, the closing of shapes B and C, and the coupling clause that this fix does not ship alone and reaches the terminal in one build with defect 3. The Issue line above it and the Status line below it are untouched, the Status line already having carried the true deploy state, which is the shape of the error: the false clause sat one line above a true one. The rows P3-1, P49 and P53, the ISSUES head, every FINAL entry in DECISIONS and every code file are untouched. NOTHING HERE CLOSES ANY ROW. Defect 1 still closes only on live acceptance P3-1, on the artifact of one `TRANSITION|LOCKED->SYNCING|lock expired` line followed by one `TRANSITION|SYNCING->ACTIVE|history stable|polls=3/3` line across a real expiry, and a corrected ledger line is not evidence of anything running.
THE TRACEABLE SOURCE OF THE STALE FRAMING IS NAMED SO IT IS NOT HUNTED FOR AGAIN. `docs/status/index.html` is a mirror frozen at its own stamp, `Last updated: 2026-08-02` and `Last synced: 2026-08-02 13:53 IDT (UTC+3)`, roughly three weeks before the fixes, the build and the deploy. Its whole lock and deploy area therefore still carries `class="pending"` rows written before any of it, `Lock survives terminal restart` at line 139 and the end to end lock row at line 204 among them, and that pre deploy PENDING framing reads as current to anyone who opens the page and is the visible source a reader would reach for. THE MIRROR IS NOT CORRECTED IN THIS SESSION AND ITS CORRECTION IS DEFERRED, `docs/status` being outside the authorization of this session; it stays stale and is recorded here as KNOWN STALE until an authorized session syncs it. LEDGER.md is the source of truth and the mirror is not, which is why the mirror being wrong is a reporting defect and not a state defect.

2026-08-24, THE VERSION 1 RULING SET D1 THROUGH D8 WAS TAKEN AND RECORDED. Owner gave D1 through D5 in chat on this date and then D6, D7 and D8 as amendments in the same session, all recorded verbatim in the FINAL entry of this date in DECISIONS. WHAT WAS RULED, one line each: only trade closures including partial closures raise the day's realized peak and floating profit never does; the floor is checked against full account equity including floating, so an open position diving below it locks immediately rather than waiting to be closed; the peak rises only intraday and resets only at the day boundary; the stricter of the ratchet floor and the realized peak floor always wins, in every scenario including restart and day rollover, with no exceptions; a restart reconstructs the peak from today's deal history with the persisted value a cross check only and the stricter of the two taken on mismatch; the reset uses the day boundary already FINAL for the daily base with no new boundary defined anywhere; storage is a separate `peak_<login>.dat` so that `floor_<login>.dat` stays byte identical to what is deployed; the peak term uses the post ratchet `enforced_limit` rather than the raw live limit; and the peak never enters the lock snapshot, being PRE BREACH ONLY exactly as the ratchet is. THE VERIFICATION THAT PRECEDED THE RULING, performed read only in plan mode earlier this same date and recorded here because the ruling rests on it: twenty three FINAL entries touching the daily PnL formula, the daily base definition, the day boundary, the Phase 2 ratchet and the lock trigger conditions were quoted verbatim and checked one by one against the rulings, all twenty three found present exactly once and every quoted clause byte identical, ZERO CONFLICTS FOUND. One clause is superseded and one only, question FIVE's "NO new persisted artifact is added"; two entries are constrained but NOT superseded, question SEVEN and the 2026-08-20 defect 3 shape 1 ruling, both of which version 1 satisfies in letter. A GOVERNANCE FACT SURFACED BY THE SAME VERIFICATION AND RECORDED RATHER THAN LEFT IMPLICIT: `P3-1` and `P49`/`P53`, which ruling D4 makes the gate on deployment, DO NOT EXIST IN THIS FILE. They live only on the unmerged `phase3-defect-fixes` branch, together with three FINAL owner rulings of 2026-08-19 and 2026-08-20 that bear directly on this work, so this ledger cannot close the D4 gate from its own contents and a session reading only this file would not know the gate exists. LEDGER ONLY: no MQL5 source, no README, no docs and no MT5 terminal touched, and no existing FINAL entry edited, the supersession being carried entirely by the new entry's own text as the protocol requires. Committed on branch `worktree-ledger-v1-peak-ruling` rather than directly on main, this background session being unable to write the main checkout; the merge is the owner's to instruct, per the 2026-08-03 merge gate FINAL.

2026-08-24, README.md WAS BROUGHT CURRENT AGAINST THIS LEDGER AND THE MAIN-BRANCH SOURCE, REPLACING ITS PHASE 0 FRAMING. Every claim changed was checked against a named artifact rather than carried over: the phase and enforcement badges, the "at a glance" bullets, the core guarantee paragraph, the Status table, the state-diagram note and the `LOCKED` row of the states table, in both the English section and its Hebrew mirror in parallel. VERIFIED IMPLEMENTED AND LIVE ON THE MAIN BRANCH: the daily PnL engine, day anchor and ratchet (Phase 1, closed `c7170a8`), breach detection and `LOCKED` state, lock persistence with all three boot witnesses fired, and the `SYNCING` exit with `polls=3/3` observed live (Phase 2, closed `8e99778`), matching `docs/status/index.html`'s own banner, "Phases 0-2 CLOSED", last synced 2026-08-23. VERIFIED STILL UNBUILT ON MAIN: `Sweep.mqh` is read directly and is still the empty Phase 3 stub with zero trade API calls, so the "no trading calls" claim in the old README turned out to still be TRUE today and was rewritten as a deliberate current boundary (detect and lock, no sweep) rather than dropped; weekly reporting has no measurement code, only the `WeeklyReportEnabled` flag threaded to a boot log tag, confirmed by grep. TWO ITEMS IN THE TASK'S OWN ORIENTATION DID NOT VERIFY AND WERE NOT USED: the build hash `1C88C4A1` is listed as a PENDING deploy in `docs/status/index.html` itself, "Only after vector verification passes", so the actually-deployed hash on main is `74D666E9`; and lock expiry routes `LOCKED` directly to `ACTIVE` on main, per `AccountGuardian.mq5:457` and the live `TRANSITION|LOCKED->ACTIVE|lock expired|` line quoted earlier in this file, not through `SYNCING` as the orientation suggested. That SYNCING-routed fix exists only on the unmerged `worktree-phase3-defect-fixes` branch (commit `0a4c86b`), nineteen commits ahead of main and not reflected in this LEDGER. README's state diagram was left with its original `LOCKED --> ACTIVE` edge, which is the edge actually deployed. No MQL5 source and no MT5 terminal touched.

2026-08-23, THIRD ENTRY FOR THE DATE. BUILD `1C88C4A1` IS DEPLOYED AND RUNNING, AND `74D666E9` IS
RETIRED FROM THE LIVE TERMINAL. READ ONLY THROUGHOUT: nothing under the MetaTrader Terminal data folder
was written, deleted or moved, per RULE A, and the folder was read only, to hash two binaries and to
read the two journals. RULE B was satisfied because every command that named a path inside it was a
plain inline `Get-Item`, `Get-FileHash` or a `System.IO` call with no user defined helper anywhere in
the call chain. The executor performed NO part of the copy and NO part of the restart. No push, no
merge, no code change, no owner ruling, no live acceptance row closed, and neither defect 1 nor defect
3 was marked DONE. State re derived rather than carried: `git rev-parse HEAD` returned
`03ec0bf31cbd597dd8008ac06b6f48dc59898425` on branch `worktree-phase3-defect-fixes` with
`git status --porcelain` empty. LEDGER.md coverage was taken by the proven diff shortcut, on its three
checks: the full read of this date was against blob `d7fe17e1f79e03c1b3dab3c559c44defa463add6` at 1758
lines, `git log 751bcf0..HEAD -- LEDGER.md` lists exactly two commits and both are this executor's, and
`git diff --quiet HEAD -- LEDGER.md` exits clean.

THE DEPLOY IS SEALED ON ARTIFACTS AND EVERY ELEMENT THE OWNER REPORTED HOLDS. THE LIVE BINARY:
`C:\Users\www83\AppData\Roaming\MetaQuotes\Terminal\D0E8209F77C8CF37AD8BF550E51FF075\MQL5\Experts\AccountGuardian\AccountGuardian.ex5`,
md5 `1C88C4A155CF09AC38D71C3E5BFEF1D9`, 78676 bytes, which is the required hash and the required size,
at a path carrying the terminal id rather than any other string. `74D666E90C1749FD24374D0AEF8C8F7D` at
77396 bytes is GONE from that path. The vectors script is undisturbed at
`BE3E233CBC8A770CEFEEB382CAFA688F`, 49004 bytes.

THE THREE FILE STAMPS, READ TOGETHER, PLACE THE COPY BEFORE THE SHUTDOWN. LastWriteTime is 2026-08-20
20:33:18.681 and is IDENTICAL to the worktree source to the millisecond, so it is the BUILD time
carried over by the copy and says nothing about when the file landed. CreationTime is 2026-07-29
13:37:57.240, the original entry preserved through an overwrite in place. LastAccessTime is 2026-08-23
22:21:53.217 and is the usable stamp, write induced because last access is demonstrably not updated on
reads on this volume, and it places the copy THREE MINUTES AND FORTY SIX SECONDS BEFORE THE CLEAN
DEINIT. Order is copy, deinit, shutdown, start, init.

THE BOOT SEQUENCE, QUOTED IN FULL FROM THE EXPERTS JOURNAL.
`AG|2026.08.21 23:57:59|INFO|deinit|reason=9|session marked clean|timer_armed=1|timer_ticks=977` at
local 22:25:39.589. `AG|2026.08.21 23:57:59|INFO|init|build=Phase2|account=1200252169|server=JustMarkets-Demo3`
at 22:25:57.318. `AG|2026.08.21 23:57:59|TRANSITION|BOOT->SYNCING|boot|weekly=on|timer=1s` at
22:25:57.321. `AG|2026.08.21 23:57:59|TRANSITION|SYNCING->ACTIVE|history stable|polls=3/3` at
22:26:00.315. The first full ACTIVE LIFE line, at 22:26:28.315,
`AG|2026.08.21 23:57:59|LIFE|state=ACTIVE|seconds_in_state=28|waiting_on=-|anchor=2026.08.21 01:00:00|realized=0.00|floating=0.00|base=2133.00|limit=106.65|pnl_vs_limit=0.00 vs -106.65|server=2026.08.21 23:57:59|local=2026.08.23 22:26:28`.
Carried with it and not separated out: the mutex acquired at 22:25:57.318 with `id_set=1` and
`hb_set=1`, the lock state file loaded at `locked_until=1970.01.01 00:00:00`, the ratchet floor loaded
at `anchor=2026.08.21 01:00:00|floor=106.65`, the crash loop check at `chain=1/3`, and the timer armed
at `period=1s`.

THE DEINIT WAS CLEAN AND THE LOAD WAS FRESH, both established rather than asserted. CLEAN: the deinit
line carries `reason=9` and the words `session marked clean` in the emission itself, which is the
authoritative witness under standing observation rule 4, that the halt file and not the journal decides
shutdown behaviour, and here the journal line survived anyway. FRESH: the incoming transition reads
`BOOT->SYNCING`, which amendment A5 fixed on 2026-08-06 as the CANONICAL FRESH IMAGE FORM, against
`SYNCING->SYNCING` which is the in place re init form a properties dialog or a chart period change
produces. It is corroborated at the process level by the terminal journal, `22:25:39.609 Experts expert
AccountGuardian (XAUUSD.ecn,M15) removed`, `22:25:40.783 Terminal exit with code 0`,
`22:25:41.655 Terminal stopped with 0`, `22:25:41.658 Terminal shutdown with 0`,
`22:25:54.776 Terminal MetaTrader 5 x64 build 6140 started for MetaQuotes Ltd.` and
`22:25:55.867 Experts expert AccountGuardian (XAUUSD.ecn,M15) loaded successfully`. A process that
exited cannot carry globals across, so the image is fresh by construction as well as by transition
form.

THE TIMER TICK RESET IS DEMONSTRATED ON MEASURED BOUNDARIES RATHER THAN CLAIMED, and the arithmetic is
given because the counter is the discriminator the ledger has leaned on since 2026-07-30. The counter
is PER SESSION, and both closed sessions of this date prove it: init 20:46:14.542 to deinit 22:09:02.450
is 4967.9 seconds against a reported `timer_ticks=4967`, and init 22:09:22.201 to deinit 22:25:39.589 is
977.4 seconds against a reported `timer_ticks=977`. Both match to the second at the one second timer, so
each session's counter began at ZERO at its own init and nothing carried over. The outgoing session's
977 is therefore its own lifetime and not a running total, which is exactly the reset property an
inherited image would violate by continuing the previous count, as the 2026-08-06 chart change did when
it ran 124475 to 124480 across a pair of deinits.

THE RUNNING IMAGE IS `1C88C4A1`, AND THE LIMIT OF THAT CLAIM IS STATED RATHER THAN LEFT IMPLICIT,
because standing rule 7 says the ex5 hash is not build identity and that runtime behaviour in the
journal is the only measure of what is actually loaded. What is measured: the file on disk hashes
`1C88C4A1`, its last write is the copy at 22:21:53, the process exited and restarted, and the expert was
loaded at 22:25:55.867 which is AFTER that write, so the image loaded is the file that was on disk. What
is NOT available: a runtime discriminator. The defect 1 fix alters lock expiry and the defect 3 fix
alters what a boot derived lock persists, and this boot had NO LOCK, `locked_until=1970.01.01 00:00:00`,
so the two builds are behaviourally identical on a flat no lock boot and the journal cannot tell them
apart. THAT IS NOT A GAP IN THIS DEPLOY, IT IS THE REASON THE LIVE ACCEPTANCE ROWS EXIST, and it is why
neither defect is marked DONE here.

WHAT THE DEPLOY CHANGES AND WHAT IT DOES NOT. The guardian protecting account 1200252169 is now the
build carrying the defect 1 and defect 3 fixes. Defects 2 and 4 are NOT in this build and the live
terminal still has both: the GV lock mirror still erases itself on every cold boot, and LOCKED LIFE
lines still carry no numbers. The interim posture ruled 2026-08-18 is unchanged, so a real breach locks
the state machine and sends no order, and positions stay open until the owner closes them by hand.

CONDITIONS AT THE DEPLOY, recorded because they bound what can be observed next. Server clock still
frozen at `2026.08.21 23:57:59` and the anchor still pinned at `2026.08.21 01:00:00`, the weekend close
being unbroken; the account flat at zero positions and zero orders; base 2133.00 and enforced limit
106.65 with the ratchet floor loaded at the same 106.65.

THE ARTIFACT IS BANKED AT COMMIT `0101f04`. `docs/evidence/journal-20260823-deploy-1c88c4a1.txt`, 5383
bytes, 53 lines, md5 `5F29B7C030CE0476500F800ACE3677D6`, a derived excerpt labelled as one, carrying the
hash block with all three file stamps, the terminal process restart, the EA boot sequence, the first
full ACTIVE LIFE line and the tick arithmetic. The primary journals behind it are already banked whole
at `89e49f9`. Confirmed present with `git ls-files docs/evidence` rather than by the commit's exit code,
and named `.txt` because the bare `*.log` rule is still unchanged.

NOT DONE, AND NOT DONE DELIBERATELY: no push, no merge, no code change, no terminal write, P2-A
untouched and still OPEN, defects 1 and 3 still IN PROGRESS and neither marked DONE, no live acceptance
row closed, and no owner ruling recorded or implied. Per the FINAL remote state ruling of 2026-08-04
this entry makes no claim about where the remote stands. THE NEXT BEST ACTIONS ARE THE TWO LIVE
ACCEPTANCE ROWS and they are written at the head of ISSUES with the live window each needs and its
predicted lines.

WHICH PREDICTION THE NEXT NATURAL EVENT TESTS, stated because the deploy makes the question due and
NOTHING IS CLOSED BY STATING IT. THE NEXT NATURAL EVENT IS THE MONDAY REOPEN, expected at server
2026-08-24 01:00:00 when quotes resume and `TimeCurrent` unfreezes from `2026.08.21 23:57:59`. IT TESTS
NOTHING NEW. What it exercises is Q8 anchor behaviour, the high water latch and the day rollover across
a three day jump, which is already proven live three times over, on 2026-08-10 at `jump=259200s`, on
2026-08-13 at `jump=172800s` in the wake from sleep class and on 2026-08-17 at `jump=259200s`, and the
cadence capped ALERT that rides it is a permanent scope limit recorded FINAL. It is a REGRESSION CHECK
on the new binary and worth reading as one, since the deployed build touched the expiry path, but it
carries no P25 through P33 content because the account is flat and there is no lock to expire. THE
FIRST NEW PREDICTIONS TESTED ARE P25, P26 AND P27, AND THEY WAIT ON A LOCK'S EXPIRY, which needs a lock,
which needs a losing position that clears 106.65 against base 2133.00. None exists. ONE OF THE SET IS
ALREADY OBSERVED IN ITS FLAT ACCOUNT FORM AND IS NOT CLOSED BY IT: P30 predicts ZERO `boot witness`
lines of any kind at a SYNCING exit on a flat account, and the 22:25:57 boot emitted none, which is the
predicted observation but on a boot rather than on the expiry window the prediction is scoped to.

ONE CONFLICT BETWEEN THE TWO PLAN DOCUMENTS IS RAISED HERE AND IS NOT RULED, per the standing rule
against settling by inference what a ruling did not say. P31 of `docs/FIXPLAN_PHASE3_DEFECT1_2026-08-19.md`
predicts that an expiry time re lock writes `L|1|<until>|0` and `N|0.00000000|0.00000000`, and says in
terms that this is "defect 3 arriving on a path shape A newly makes routine". THAT WAS WRITTEN AGAINST
SHAPE A ALONE, on 2026-08-19, BEFORE the coupling ruled 2026-08-20 put defect 3 in the same build. On
the binary actually deployed, the same pass should instead write P49's form, a NON ZERO third field in
the `L` record and BOTH `N` figures non zero. The documents disagree only because P31 predates the
coupling, and the deployed build follows P49. RECORDED SO NO LATER SESSION READS A P49 SHAPED STATE
FILE AS P31 FAILING, and equally so that a P31 shaped file, all zeros, is read as the defect 3 fix NOT
having reached the running binary, which is the more useful direction of the same observation.

2026-08-23, SECOND ENTRY FOR THE DATE. THE CORRECTED COPY LANDED, THE VECTOR ROW FOR BUILD `1C88C4A1`
PASSES ON ARTIFACT, AND THE ROW AT THE HEAD OF ISSUES CLOSES. READ ONLY THROUGHOUT: nothing under the
MetaTrader Terminal data folder was written, deleted or moved, per RULE A, and the folder was read only,
to hash two binaries, to read the two journals and to list directories. RULE B was satisfied because
every command that named a path inside it was a plain inline `Get-Item`, `Get-ChildItem`,
`Get-FileHash`, `Get-Content` or a `System.IO` call with no user defined helper anywhere in the call
chain. No deploy, no push, no merge, no code change, no owner ruling, and neither defect 1 nor defect 3
was marked DONE. State re derived rather than carried: `git rev-parse HEAD` returned
`2217b55ea873d614689f7786ae94b2e21745378f` on branch `worktree-phase3-defect-fixes` with
`git status --porcelain` empty.

LEDGER.md WAS COVERED BY THE DIFF SHORTCUT AND THE SHORTCUT WAS PROVED RATHER THAN ASSERTED, which is
the only form in which it is acceptable. The full read of 2026-08-23 was taken against the working tree
at `751bcf0` with a clean tree, and `git rev-parse 751bcf0:LEDGER.md` resolves that version to blob
`d7fe17e1f79e03c1b3dab3c559c44defa463add6` at 1758 lines. `git log 751bcf0..HEAD -- LEDGER.md` lists
EXACTLY ONE commit, `2217b55`, which this executor wrote, and `git diff --quiet HEAD -- LEDGER.md`
exits clean so the working copy is the committed one, blob
`5342da5be39670853f33b75dd296301b98d25e8f` at 2003 lines. The 249 inserted and 4 deleted lines between
the two are therefore the whole of what a full read would have added, and they were read. THE SHORTCUT
IS VALID ONLY UNDER THOSE THREE CHECKS and a session that cannot produce all three reads the file in
full.

THE OWNER REPORT IS CONVERTED WHOLE THIS TIME, four claims and all four hold on artifacts.

CLAIM ONE, THE STRAY TREE IS DELETED. `C:\Users\www83\AppData\Roaming\MetaQuotes\Terminal\BE3E233CBC8A770CEFEEB382CAFA688F`
does not exist. The `Terminal` root now holds four directories, `9F913C9103D1CA7EBB9EA87B6406EA94`,
`Common`, `Community` and `D0E8209F77C8CF37AD8BF550E51FF075`, and no md5 shaped name among them.

CLAIM TWO, THE SCRIPT IS AT THE REAL PATH AND IS THE RIGHT BYTES.
`C:\Users\www83\AppData\Roaming\MetaQuotes\Terminal\D0E8209F77C8CF37AD8BF550E51FF075\MQL5\Scripts\AccountGuardian\AgPhase2StateVectors.ex5`,
md5 `BE3E233CBC8A770CEFEEB382CAFA688F`, 49004 bytes, LastWriteTime 2026-08-20 20:33:53. That is the
required hash and the required size, and the path contains the terminal id
`D0E8209F77C8CF37AD8BF550E51FF075` rather than the file's own md5, which is the check the corrected
dictation added and the exact thing that failed on 2026-08-23 at 20:39. The previous binary,
`A4FFA0D31651B3F425DABC084BE1C610` at 46996 bytes, IS GONE FROM THAT PATH: only one
`AgPhase2StateVectors.ex5` exists there and it hashes to the new value on two independent readings.

THE mtime IS THE BUILD TIME AND NOT THE LANDING TIME, and that is written down rather than glossed
because the row's ordering rests on it. 2026-08-20 20:33:53.537 at the real path is IDENTICAL to the
LastWriteTime of the worktree source to the millisecond, so the copy preserved the source stamp and the
mtime is a true but WEAK lower bound on the run: any run on 2026-08-23 postdates it trivially. The
directory mtime is no better, `MQL5\Scripts\AccountGuardian` still reading 2026-08-18 11:22:50, which
is the expected signature of an overwrite in place rather than a new directory entry. The USABLE stamp
is LastAccessTime, 2026-08-23 21:35:24.871. Last access is demonstrably NOT updated on reads on this
volume, since the executor's own `Get-FileHash` at 22:11 left it unmoved, so that value is write
induced and places the copy at 21:35:24, THIRTY THREE MINUTES BEFORE THE RESTART. CreationTime is
2026-08-17 22:13:28.197, the original entry preserved through the overwrite, which corroborates that
the file was replaced in place rather than the directory rebuilt.

CLAIM THREE, THE RESTART, AND CLAIM FOUR, THE RUN, IN ORDER AND FROM THE TERMINAL JOURNAL. Quoted in
sequence: `22:09:02.457 Experts expert AccountGuardian (XAUUSD.ecn,M15) removed`,
`22:09:03.618 Terminal exit with code 0`,
`22:09:03.974 Network '1200252169': disconnected from JustMarkets-Demo3`,
`22:09:04.058 Terminal stopped with 0`, `22:09:04.062 Terminal shutdown with 0`,
`22:09:19.734 Terminal MetaTrader 5 x64 build 6140 started for MetaQuotes Ltd.`,
`22:09:19.735 Terminal C:\Users\www83\AppData\Roaming\MetaQuotes\Terminal\D0E8209F77C8CF37AD8BF550E51FF075`,
`22:09:20.942 Experts expert AccountGuardian (XAUUSD.ecn,M15) loaded successfully`, then
`22:09:31.704 Scripts script AgPhase2StateVectors (XAGUSD.ecn,M5) loaded successfully` and
`22:09:33.180 Scripts script AgPhase2StateVectors (XAGUSD.ecn,M5) removed`. THE ORDER IS COPY, THEN
RESTART, THEN RUN, with the run starting 11.97 seconds after the process started and 33 minutes 7
seconds after the copy landed. The Navigator enumeration rule of 2026-08-04, extended to the Scripts
class on 2026-08-09, is satisfied on its own terms rather than by assumption.

THE SUMMARY LINE AND THE FAIL ACCOUNTING, FOR THE 22:09 RUN AND NOT THE 20:46 ONE. The summary reads
`AGVEC|SUMMARY|84/84`, emitted at 22:09:33.180 by `AgPhase2StateVectors (XAGUSD.ecn,M5)`. EIGHTY FIVE
AGVEC LINES in the run, eighty four vector lines and the summary, and the count of AGVEC lines not
matching PASS is EXACTLY ONE and it is the summary itself. FAIL LINES OVER THE RUN: ZERO. The
accounting is given in three parts rather than as a single number, the same separation the 20:46 entry
used: raw text hits for FAIL are FIVE; AGVEC lines carrying FAIL are ZERO; all five raw hits are the
substring inside `failed checksum` in expected WARN lines from the deliberate corruption vectors, four
`state file failed checksum or does not parse` and one `floor file failed checksum or does not parse`,
each naming a synthetic login in the 9900000xx range; and the residual set of raw hits that are neither
is EMPTY. CORROBORATION THAT THIS IS A FRESH RUN AND NOT A RE READ OF THE EARLIER OUTPUT, collected
free: the quarantine counters advanced, `state_990000002.dat.bad.11` and `.bad.12` against the 20:46
run's `.bad.9` and `.bad.10`, `state_990000005.dat.bad.6` against `.bad.5`,
`state_990000006.dat.bad.6` against `.bad.5`, and `floor_990000011.dat.bad.4` against `.bad.3`. A
replayed log could not increment a filename counter.

THE VECTOR ROW FOR BUILD `1C88C4A1` IS RECORDED AS PASSED ON ARTIFACT. The chain, in the four links the
row requires: the script at the real data folder path hashes `BE3E233CBC8A770CEFEEB382CAFA688F` at
49004 bytes, with the stray tree gone and the previous binary no longer at that path; the terminal shut
down at 22:09:03.618 and started at 22:09:19.734 against the data folder it names itself; the script
loaded at 22:09:31.704 and was removed at 22:09:33.180, inside that process and after that restart; and
the run emitted eighty four PASS lines and `AGVEC|SUMMARY|84/84` with zero FAIL. Evidence:
`docs/evidence/journal-20260823-vector-run-corrected.txt`,
`docs/evidence/terminal-20260823-vector-run-corrected.txt` and
`docs/evidence/agvec-20260823-corrected-run.txt`.

WHAT THE ROW DOES AND DOES NOT PROVE, restated here rather than left to the earlier entry, because a
green row is exactly the thing a later session will over read. IT PROVES the suite is green on the
binary built from the tree carrying both fixes, which is the row's whole claim and is now met. IT DOES
NOT PROVE DEFECT 1 OR DEFECT 3 IS FIXED, and it cannot: the finding of the earlier entry of this date
is unchanged and was re confirmed by hash there, that the vectors source differs between the two trees
by one corrupted byte inside a header comment, that all six includes are md5 identical across them, and
that the script carries exactly one include, `#include <AccountGuardian/Persist.mqh>` at line 34, while
both fixes live inside `AccountGuardian.mq5`, which no script can include. So the eighty four vectors
exercise code that is byte identical on both builds, and the 84/84 of 22:09 and the 84/84 of 20:46 are
the same result from the same code reached by two different binaries. THE TWO DEFECTS STILL CLOSE ONLY
ON LIVE ACCEPTANCE, P3-1 for defect 1 and P49 and P53 for defect 3, exactly as their own entries state,
and no reading of this row may be offered as closing either.

THE GUARDING BINARY IS UNCHANGED AND THAT IS CORRECT. The live EA is still
`AccountGuardian.ex5`, md5 `74D666E90C1749FD24374D0AEF8C8F7D`, 77396 bytes, mtime 2026-08-18 11:27:16,
which is build `74D666E9` and carries all four defects. `1C88C4A1` is nowhere in the terminal tree.
Deploying it is not authorized to this session and was not done; whether it is deployed is the owner's
call, since the script copy changes nothing about what guards the live account and the EA copy changes
exactly that.

THE ARTIFACTS ARE BANKED AT COMMIT `89e49f9`, THREE FILES, the two journals copied byte for byte so the
banked md5 equals the source md5. `docs/evidence/journal-20260823-vector-run-corrected.txt`, 1636028
bytes, 2798 lines, md5 `53A447779D8F698D3956B570E9CFDDB1`, which is the whole day Experts journal and
carries BOTH runs of the date. `terminal-20260823-vector-run-corrected.txt`, 12572 bytes, 68 lines, md5
`B95518DFCE67C9C62182E21169F39F24`. `agvec-20260823-corrected-run.txt`, 12654 bytes, 124 lines, md5
`CD1F3A6E79D10E7F20701B0D8DA963B0`, a derived extract naming the four links of the tie in order and
labelled as an extract.

CONTINUITY OF THE RECORD WAS CHECKED AND NOT ASSUMED. The copy banked earlier this date as
`journal-20260823-weekend-sunday-vector-run.txt` is a STRICT BYTE PREFIX of the new full day snapshot,
all 1505052 of its bytes comparing equal to the first 1505052 of 1636028, so the two artifacts are one
continuous record and the earlier one was not superseded by a rewrite. That is the same test the
recovered Sunday journal of 2026-08-17 was put to. All three new files were confirmed present by
`git ls-files docs/evidence` rather than by the commit's exit code, per the standing lesson of
2026-08-19, and they are named `.txt` because the bare `*.log` rule that swallowed thirteen journals is
still unchanged.

NOT DONE, AND NOT DONE DELIBERATELY: no deploy, no terminal write, no push, no merge, no code change,
P2-A untouched and still OPEN, defects 1 and 3 still IN PROGRESS and neither marked DONE, defects 2 and
4 untouched, and no owner ruling recorded or implied. Per the FINAL remote state ruling of 2026-08-04
this entry makes no claim about where the remote stands. THE NEXT BEST ACTION IS THE OWNER'S AND IT IS
A DECISION RATHER THAN A CHORE: whether to deploy `1C88C4A1` to the live terminal, which is the only
route to the live acceptance rows that close defects 1 and 3, and which replaces the binary currently
guarding the account. The dictation is prepared in the ISSUES entry at the head of that section and is
not performed here.

2026-08-23, VERIFICATION SESSION. THE VECTOR RUN IS REAL AND IT RAN THE WRONG BINARY, AND THE WEEKEND
HARVEST IS BANKED. READ ONLY THROUGHOUT: nothing under the MetaTrader Terminal data folder was written,
deleted or moved, per RULE A, and the folder was read only, to list directories, to hash the live tree
and to read two journals. RULE B was satisfied because every command that named a path inside it was a
plain inline `ls`, `find`, `Get-ChildItem`, `Get-FileHash`, `Get-Content` or a `System.IO` call with no
user defined helper anywhere in the call chain. No deploy, no push, no merge, no code change, no owner
ruling, and neither defect 1 nor defect 3 was marked DONE. State re derived rather than carried:
`git rev-parse HEAD` returned `751bcf03ab1c570999a08671accc51e0a56edba0` on branch
`worktree-phase3-defect-fixes` with `git status --porcelain` empty, and LEDGER.md was read in full,
all 1758 lines, before any of the work below, per execution rule 2.

THE OWNER REPORT IS CONVERTED IN PART AND FAILED IN PART, ON ARTIFACTS. Reported: the vectors script
copied with md5 verified, the terminal restarted, 84 vectors run, an AGVEC summary line present. Three
of those four hold. THE COPY DID NOT LAND IN THE TERMINAL DATA FOLDER, and the md5 that was verified
was verified against the file at the wrong destination, so it confirmed the bytes and not the place.

WHERE THE FILE ACTUALLY WENT, quoted as measured. `AgPhase2StateVectors.ex5`, 49004 bytes, md5
`BE3E233CBC8A770CEFEEB382CAFA688F`, mtime 2026-08-20 20:33:53, sits at
`...\MetaQuotes\Terminal\BE3E233CBC8A770CEFEEB382CAFA688F\MQL5\Scripts\AccountGuardian\`. THE FIRST
PATH COMPONENT AFTER `Terminal\` IS THE FILE'S OWN MD5 WHERE THE TERMINAL ID BELONGS. The real data
folder is `D0E8209F77C8CF37AD8BF550E51FF075`, named by the terminal itself in its own journal at
`20:46:12.491 Terminal C:\Users\www83\AppData\Roaming\MetaQuotes\Terminal\D0E8209F77C8CF37AD8BF550E51FF075`.
The directories carry the timestamps of the mistake: `MQL5` and `MQL5\Scripts` both at 2026-08-23
20:39:36 and `MQL5\Scripts\AccountGuardian` at 20:39:52, so an empty tree three levels deep was created
by the copy and the file landed at the bottom of it. THE DICTATION NAMED BOTH A DESTINATION PATH AND AN
MD5 FOR THE SAME FILE and the two were transposed; that is the mechanism, stated once and not argued.

WHAT THE TERMINAL STILL HOLDS, hashed this session rather than assumed. The Scripts folder of the real
data folder carries `AgPhase2StateVectors.ex5` at 46996 bytes, md5
`A4FFA0D31651B3F425DABC084BE1C610`, mtime 2026-08-18 11:27:01, which is the PREVIOUS build and is
untouched today. The live EA is `AccountGuardian.ex5` at 77396 bytes, md5
`74D666E90C1749FD24374D0AEF8C8F7D`, mtime 2026-08-18 11:27:16, which is build `74D666E9` and carries
all four defects; that is correct and intended, since whether the EA rode the same step was left as the
owner's call. `1C88C4A1` IS NOWHERE IN THE TERMINAL TREE. Corroborated on a second field: the terminal
side `metaeditor.log` was last written 2026-08-20 20:33, so no compile ran in the terminal tree today.

THE RESTART IS CORRECTLY TIED AND IT IS NOT WHAT FAILED. Terminal journal, quoted in order:
`20:45:31.569 Terminal exit with code 0`, `20:45:32.625 Terminal stopped with 0`,
`20:45:32.635 Terminal shutdown with 0`,
`20:46:12.491 Terminal MetaTrader 5 x64 build 6140 started for MetaQuotes Ltd.`. The EA side brackets
it exactly: `AG|2026.08.21 23:57:59|INFO|deinit|reason=9|session marked clean|timer_armed=1|timer_ticks=73932`
at local 20:45:30.432 and `AG|2026.08.21 23:57:59|INFO|init|build=Phase2|account=1200252169|server=JustMarkets-Demo3`
at 20:46:14.542, then `TRANSITION|BOOT->SYNCING|boot|weekly=on|timer=1s` at 20:46:14.558 and
`TRANSITION|SYNCING->ACTIVE|history stable|polls=3/3` at 20:46:17.555. The vectors ran at 20:46:30.311
to 20:46:30.422, eighteen seconds after the process started, so the run is genuinely inside the post
restart image. The Navigator enumeration rule was honoured. It simply enumerated the folder that still
held the old file.

THE SUMMARY LINE AND THE FAIL COUNT, BOTH REAL AND BOTH ATTRIBUTED TO THE OLD BUILD. The summary reads
`AGVEC|SUMMARY|84/84`, emitted at 20:46:30.422 by `AgPhase2StateVectors (XAGUSD.ecn,M5)`. Eighty five
AGVEC lines in the file, eighty four vector lines and the summary, and EVERY ONE OF THE EIGHTY FOUR
READS PASS: the count of AGVEC lines not matching PASS is exactly one and it is the summary itself.
FAIL LINES OVER THE WHOLE RUN: ZERO. A bare text search for FAIL returns five hits and all five are the
substring inside `failed checksum` in expected WARN lines from the deliberate corruption vectors, four
`state file failed checksum or does not parse` and one `floor file failed checksum or does not parse`,
each naming a synthetic login in the 9900000xx range. That distinction is written out rather than
reported as a count, because a raw count of five would have read as five failures.

THE VECTOR ROW FOR BUILD `1C88C4A1` IS NOT RECORDED AS PASSED AND MAY NOT BE. The run proves the old
build still passes its own suite. It says nothing about the build carrying the defect 1 and defect 3
fixes, exactly as the 2026-08-20 entry warned when it refused to quote the 2026-08-18 summary for the
new build.

AND THE SUITE COULD NOT HAVE DISCRIMINATED THE TWO BUILDS EVEN IF THE COPY HAD LANDED, which is the
more useful half of this finding and is measured rather than argued. Three facts. ONE, the vectors
source is the same source: `AgPhase2StateVectors.mq5` in the worktree and in the terminal differ by a
SINGLE LINE, the second line of the header comment box, where the terminal copy carries one corrupted
byte inside the comment marker and nothing else differs in 30364 bytes. TWO, all six includes are md5
identical across the worktree and the terminal, `Persist.mqh` `776f3ba0a3197138645305a1e743822c`,
`Pnl.mqh` `4da4810100691ffc135d6073917b7855`, `State.mqh` `34792807c6b6b548943215866193eb3f`,
`Clock.mqh` `dc5a04004f0cb7bdb9d6c602e944cec1`, `Sweep.mqh` `cf7d355a7c52265a7aff5c465da33c46` and
`Log.mqh` `c4e803fb81ebe7f521cc020e8fa1ce82`. THREE, the vectors script carries exactly ONE include,
`#include <AccountGuardian/Persist.mqh>` at line 34, while both defect fixes live inside
`AccountGuardian.mq5`, which is an EA file no script can include. SO THE EIGHTY FOUR VECTORS EXERCISE
CODE THAT IS BYTE IDENTICAL ON BOTH BUILDS, and a correct copy followed by a correct run would have
produced the same 84/84 and would have proven the same nothing about the fixes. The vector row is not
merely unproven for `1C88C4A1`, it is the wrong instrument for it, and what those fixes wait on is
unchanged and is live acceptance, P3-1 for defect 1 and P49 and P53 for defect 3.

THE OWNER'S tmp FOLDER IS NOT WHERE IT WAS REPORTED TO BE, and the correction matters because of what
sits on it. It is at `C:\Users\www83\Downloads\AI\AccountGuardian\tmp`, the REPOSITORY root, not the
Terminal data folder root. Searched rather than guessed: no directory matching tmp or temp exists
anywhere under the Terminal data folder root, whose only subdirectories are `MQL5`, `Tester`, `bases`,
`config`, `llm-agent`, `logs`, `sweep` and `temp`, and that `temp` holds only `EBWebView` and a three
file `logs` directory of 882 byte terminal stubs. THE FOLDER HOLDS FOUR EA JOURNALS, `20260820.log`,
`20260821.log`, `20260822.log` and `20260823.log`, AND IT IS UNTRACKED AND IGNORED: every file in it is
named `.log`, which the bare `*.log` rule under the Junk heading of `.gitignore` matches, which is the
same unchanged mechanism the OPEN `.gitignore` entry in ISSUES describes. A `git clean` would take all
four. They are banked below and the exposure is recorded on that entry rather than here.

THE ARTIFACTS ARE BANKED AT COMMIT `c843be5`, SEVEN FILES, and each was copied byte for byte rather
than transcoded so that the banked md5 EQUALS the source md5 and can be checked against the terminal at
any time. `docs/evidence/journal-20260820-weekend-thursday.txt`, 1690582 bytes, 2882 lines, md5
`A440C1A18C1FBCA2D22A1F5FEC3196F9`. `journal-20260821-weekend-friday.txt`, 997548 bytes, 1704 lines,
md5 `253C84D894D2A16D94991D34F11E5C21`. `journal-20260822-weekend-saturday.txt`, 1774064 bytes, 2887
lines, md5 `78EE269577460E340A137DFB273655B1`. `journal-20260823-weekend-sunday-vector-run.txt`,
1505052 bytes, 2519 lines, md5 `73355E6B3D99B664660DE996DCFCB728`, and this is the Task 1 vector run
artifact as well as the Sunday journal, since the owner's copy was taken at 20:47:03 and the run is
inside it. `terminal-20260823-restart-and-vector-run.txt`, 9582 bytes, 51 lines, md5
`05CCEB34DA43F6FC6F91583083CFCBC6`. Two derived extracts, both named as extracts:
`journal-20260823-restart-window-extract.txt`, 13321 bytes, 53 lines, md5
`4D0BFAB27E7BDA277EA50B418193D824`, and `agvec-20260823-run.txt`, 11789 bytes, 100 lines, md5
`DCC94F5824CB5F8E1147F61C352761B8`.

AUTHENTICITY OF THE FOUR COLLECTED COPIES WAS VERIFIED AND NOT ASSUMED. Three of them are md5 IDENTICAL
to the live terminal files, so they are the files themselves. The Sunday copy cannot be, because the
terminal was still writing that file, so it was byte compared instead: all 1505052 bytes of the copy
compare EQUAL to the first 1505052 bytes of the live file, making it a STRICT PREFIX and therefore
continuous with the live record rather than a substitute. That is the same test the recovered Sunday
journal of 2026-08-17 was put to, reused because it is the right one.

THE FILES WERE NAMED `.txt` AND THE TRACKED LIST WAS QUOTED RATHER THAN THE COMMIT'S EXIT CODE. All
seven were confirmed present by `git ls-files docs/evidence`, per the standing lesson of 2026-08-19
that a successful `git add -A` is not evidence a file was added.

A MEASUREMENT TRAP WAS HIT AND CAUGHT THIS SESSION, recorded because it is the fourth of its class in
this project and it very nearly produced a false negative on the whole harvest. The first search for
AGVEC lines returned ZERO on all three candidate journals. `iconv` DOES NOT EXIST on this machine, and
the shell reported that on stderr while the pipeline's `grep -c` printed a clean zero on stdout, so the
absence was manufactured entirely by a missing decoder. Standing observation rule 5 is what caught it:
the decoder was made to prove it had read the file, the same command returned zero LINES as well as
zero matches, and the files are 1.5 megabytes. Re read through PowerShell with an explicit
`-Encoding Unicode` the same files give 1704, 2887 and 2538 lines and 85 AGVEC hits. This sits with the
CRLF regex trap of 2026-08-06, the stale directory entry trap of 2026-08-16 and the alias shadowing of
2026-08-17. THE REUSABLE PART: a text tool that is absent behaves exactly like a text tool that found
nothing, so rule 5's requirement to confirm the decoder read the file covers the case where the decoder
was never there at all.

ENVIRONMENT FACT, logged with the date as the 2026-08-01 build entry was. THE TERMINAL IS NOW BUILD
6140, quoted from its own journal at `20:46:12.491`, where this ledger last recorded 6090 on
2026-08-01. Nothing is re verified against 6140 by this entry and nothing is claimed about it. The same
line records the host as `Windows 11 build 26200, 10 x 12th Gen Intel Core i5-1235U, AVX2, 7 / 15 Gb
memory, 158 / 346 Gb disk, UAC, GMT+2`.

TASK 2, THE WEEKEND WINDOW, ITS BOUNDARIES AND WHAT THE RESTART TRUNCATED.

THE WINDOW OPENS AT THE FRIDAY CLOSE AND IS STILL OPEN AT THE END OF THE ARTIFACTS. The server clock
stops at `2026.08.21 23:57:59` and every LIFE line from that instant to the last line banked carries
that same server stamp, across Saturday and Sunday alike. The LOCAL instant of the last tick is derived
from the guardian's own measure rather than from the clock: Saturday's first LIFE line at local
00:00:08.996 reads `quote_age=131s`, giving a last tick at local 2026-08-21 23:57:57, and Saturday's
last pre restart line at 23:29:35.548 reads `quote_age=84698s`, giving the same 2026-08-21 23:57:57 to
the second. Two independent readings 23 hours apart agree exactly. THE ANCHOR IS PINNED at
`2026.08.21 01:00:00` for the whole weekend, since a frozen `TimeCurrent` never crosses, and the last
genuine rollover in the harvest is Friday's,
`AG|2026.08.21 01:00:05|INFO|day rollover|old_anchor=2026.08.20 01:00:00|new_anchor=2026.08.21 01:00:00|jump=86400s`
at local 01:00:03.347. THE WINDOW DOES NOT CLOSE INSIDE THESE ARTIFACTS. The market had not reopened
when the last line was written and the Monday reopen is not in the harvest, which bounds what any row
can close on today.

THERE ARE TWO RESTARTS IN THE WINDOW, NOT ONE, and the earlier one was not reported. SATURDAY:
`AG|2026.08.21 23:57:59|INFO|deinit|reason=9|session marked clean|timer_armed=1|timer_ticks=336575` at
local 23:29:57.392, then init at 23:30:15.552, `BOOT->SYNCING` at 23:30:15.574 and
`SYNCING->ACTIVE|history stable|polls=3/3` at 23:30:18.580. SUNDAY, the reported one, is the restart
bracket quoted above, deinit 20:45:30.432 and ACTIVE again at 20:46:17.555. Both are clean deinits at
reason=9 and both come up through the canonical `BOOT->SYNCING` fresh image form.

WHAT THE RESTARTS TRUNCATED, stated exactly, because it is a measurement loss and not a data loss. NO
JOURNAL WAS LOST: the EA journal is per day and append only and every line either side of both restarts
is present. What was cut is the guardian's OWN continuous measure of the frozen quote. `quote_age` is a
LOCAL tick age accumulator and it reseeds at every init, so the single closed market condition was
sampled as THREE separate runs instead of one. Run one ended at `quote_age=84698s` at local 2026-08-22
23:29:35, 23 hours 31 minutes 38 seconds, cut by the Saturday restart. Run two ended at
`quote_age=76498s` at local 2026-08-23 20:45:14, 21 hours 14 minutes 58 seconds, cut by the Sunday
restart. Run three was still climbing when the artifacts end. `seconds_in_state` was truncated with it,
76496 on the last line before the Sunday restart and 28 on the first ACTIVE line after it, and the
ACTIVE episode that began Saturday 23:30:18 ended. NOTHING THE ROWS BELOW DEPEND ON WAS TRUNCATED,
because each run reports the same branch and no verdict rests on any one run's length.

ROW ONE, THE MARKET CLOSED DISTINCTION. IT DOES NOT CLOSE, AND WHAT THE WEEKEND SUPPLIES IS THE
STRONGEST NEGATIVE THE ROW HAS EVER HAD. `AgObservabilityNote` returns the quote age string when
`AgQuoteSessionOpen()` is true and the fixed string `market closed` when it is false, so exactly one of
two strings can appear. THE STRING `market closed` OCCURS ZERO TIMES IN ALL FOUR JOURNALS, counted over
the whole files and not sampled. The other branch occurs 120 times Thursday, 23 times Friday, 2874
times Saturday and 2393 times Sunday. SATURDAY IS THE DECISIVE FILE: a complete market closed day, 2878
LIFE lines, and 2874 of them carry the OPEN session branch with the age climbing monotonically to
`AG|2026.08.21 23:57:59|LIFE|state=ACTIVE|seconds_in_state=340175|waiting_on=quote_age=84698s|anchor=2026.08.21 01:00:00|realized=0.00|floating=0.00|base=2133.00|limit=106.65|pnl_vs_limit=0.00 vs -106.65|server=2026.08.21 23:57:59|local=2026.08.22 23:29:35`.
The four Saturday LIFE lines without the note are fully accounted for and none of them is a
`market closed` line: one SYNCING line at 23:30:16 reading
`waiting_on=history stability poll not yet run this session`, and three post boot ACTIVE lines at
23:30:46, 23:31:16 and 23:31:46 reading `waiting_on=-` because the age had not yet reached the ruled
120 second threshold. SO WHAT THE BROKER SESSION STATE REPORTED ACROSS THE CLOSED MARKET IS: OPEN,
CONTINUOUSLY, THROUGH THE ENTIRE WEEKEND. The 2026-08-19 observation that narrowed this row on one
night's nightly break now holds across a full weekend close, on three independent accumulations
totalling more than 44 hours of frozen feed, and the narrowing extends: on this broker and this Market
Watch the `market closed` branch is not reached during a weekend either. THAT IS AN OBSERVATION AND NOT
A RULING. Whether the row is reclassified as unreachable by construction, in the class amendment A5 and
P1-K already occupy, is the owner's to declare and no reclassification is applied here.

A SECOND PROPERTY OF THE NOTE WAS MEASURED FOR FREE AND IS RECORDED BECAUSE IT BEARS ON THE SAME ROW.
THE NOTE IS SUPPRESSED WHOLE ON A DEGRADED PASS. The twelve Sunday LIFE lines lacking the note are nine
DEGRADED lines, one SYNCING line and two under threshold ACTIVE lines, and every one of the nine reads
`waiting_on=DEGRADED: disconnected, no breach decisions|DEGRADED` with NEITHER the quote age string NOR
`market closed` anywhere on the line. So the note reports only on an ACTIVE, connected, non degraded
pass whose age has passed 120 seconds. A reader holding a DEGRADED line therefore cannot tell whether
the market was open or shut, which is a fourth suppression case beyond the three the row was written
against, and it is stated as a measurement with no fix proposed.

ROW TWO, P2-H, PER ITS STANDING INSTRUCTION. IT DOES NOT CLOSE, AND IT DID NOT FAIL EITHER, BECAUSE ITS
PRECONDITION NEVER AROSE. The row's claim is that a limit raised while locked produces a loud WARN
while the Q6 snapshot holds, so it needs a LOCKED evaluation pass to observe. THE ACCOUNT WAS NEVER
LOCKED AT ANY POINT IN THE WINDOW. Every state value appearing in the four journals is ACTIVE or
SYNCING and nothing else: Thursday and Friday ACTIVE only, Saturday and Sunday ACTIVE and SYNCING.
Stated precisely so no later session miscounts, a plain search for LOCKED returns exactly one hit in
each of Saturday and Sunday and BOTH are the substring inside `locked_until` in the boot line
`INFO|lock state file loaded|reason=-|locked_until=1970.01.01 00:00:00|weighed by the boot derivation at the SYNCING exit`,
which is the epoch value a file with no lock carries. There were no positions and no exposure: every
LIFE line in the window reads `realized=0.00|floating=0.00|base=2133.00|limit=106.65` and the terminal
journal confirms `0 positions, 0 orders` at each authorization. The string `input changed` occurs ZERO
times in all four files. So the weekend market close the 2026-08-19 ruling named as the window this row
waits for HAS NOW PASSED WITHOUT SUPPLYING IT, and it did not supply it for a reason that has nothing
to do with the market being shut: the row needs a lock, and a lock needs a losing position, and there
was none. NO SUBSTITUTE WAS ATTEMPTED and none may be, per the standing instruction of 2026-08-18. The
ruling's other clause is untouched and is restated rather than acted on: the row may prove unreachable
through the terminal UI altogether, since a reload always reseeds the witness first, and that outcome
is the owner's to declare rather than the executor's to infer. Nothing here declares it.

ROW THREE, WHAT THE BROKER SESSION STATE REPORTED, is answered inside row one above and is not
repeated.

COLLECTED FREE FROM THE HARVEST AND NOT LOAD BEARING ON ANY ROW. The RESYNC gate, which had never once
been observed live through Phase 1 and was ruled an observation channel limitation on 2026-08-17, now
has SEVEN paired event lines in this window: `RESYNC entered` and `RESYNC exited` at Friday 00:04:58 to
00:48:44, 00:48:55 to 00:48:56 and 01:41:11 to 10:36:47, and Sunday 00:44:57 to 01:20:03, 01:40:09 to
01:42:09, 01:52:32 to 01:52:33 and 04:39:05 to 04:40:53. These are the Stage 6 RESYNC EVENT LINES doing
exactly what they were authorized to do under ruling SIX of 2026-08-18, announcing entry and exit
instead of relying on a 30 second sampler to catch a 3 second prefix. The Friday pair spanning 01:41:11
to 10:36:47 is an 8 hour 55 minute gap and is a machine sleep by shape rather than a network event, and
nothing is claimed about it here beyond noting it, which is why Friday carries 1696 LIFE lines against
Saturday's 2878. The nine Sunday DEGRADED lines pair with the four connection losses the terminal
journal records at 00:44:56, 01:40:08, 01:52:31 and 04:39:04.

NOT DONE, AND NOT DONE DELIBERATELY: no deploy, no terminal write, no push, no merge, no code change,
no vector run, P2-A untouched and still OPEN, defects 1 and 3 still IN PROGRESS and neither marked
DONE, and no owner ruling recorded or implied. Per the FINAL remote state ruling of 2026-08-04 this
entry makes no claim about where the remote stands. THE NEXT BEST ACTION IS THE OWNER'S AND IT IS THE
CORRECTED COPY, dictated in the ISSUES entry that heads that section.

2026-08-23, THE NOTION ROADMAP STATUS PAGE WAS RE-SYNCED. The Notion page, now titled "Account Guardian Road-Map", was re-fetched read only and mirrored into docs/status/index.html, replacing the 2026-08-02 version. All current subjects are reflected (Now, Defect build, Open not closable by code, Later, and Phases 0, 1, 2 shown CLOSED), every checkbox state copied verbatim with no rewording and no additions, 6 of 18 open tracker items done. Notion remains the source of truth for status; this file is a dated mirror and is stale the moment the Notion page moves. Documentation only, no MQL5 source and no MT5 terminal touched. Committed on main.

2026-08-20, THIRD ENTRY FOR THE DATE. DEFECT 3 IS IMPLEMENTED AS SHAPE 1 AND BOTH BINARIES COMPILE
CLEAN. NOTHING WAS DEPLOYED, NO MERGE, NO PUSH, AND NOTHING UNDER THE MetaTrader TERMINAL DATA FOLDER
WAS WRITTEN, per RULE A. The folder was READ ONLY, to list the Scripts directory and to re hash the
live tree, and RULE B was satisfied because every command that named a path inside it was a plain
inline `Get-ChildItem`, `Get-FileHash` or `ls` with no user defined helper in the call chain. State re
derived rather than carried: `git rev-parse HEAD` returned
`dd0b5071a6f686f0b45cf852a6d31e972bf84691` with `git status --porcelain` empty and LEDGER.md at 1645
lines after the third ruling landed.

THE THIRD RULING OF THE DATE IS FINAL IN DECISIONS, commit `3a4972c`, and it settles the derived
question the shape 1 ruling left open: "recorded as such" is satisfied by the ledger, the plan and the
journal's existing distinction, the state file format stays frozen, and reading 2 stays available as a
design item for a future build rather than being closed against.

THE IMPLEMENTATION, commit `4d6b5e2`, ONE FILE, 86 insertions and 9 deletions. `AgBootDerivation`
gains four out parameters, `have_snapshot_out`, `breach_time_out`, `limit_out` and `base_out`, all
initialised at the head of the function so the two NOT EVALUABLE returns leave them defined rather
than indeterminate, and all four assigned immediately after the three tier cascade. They are published
UNCONDITIONALLY rather than only when a witness fires, and that is deliberate: the caller decides on
`have_snapshot` and not on which witness won, so keeping the publication at one site keeps the
decision at one site. `AgEnterLockFromBoot` takes the same four and its DAILY_BREACH site is now a
three way branch, CORRUPT_STATE to `AgStateSetCorrupt` untouched, `have_snapshot` true preserving the
loaded triple byte for byte, and `have_snapshot` false writing the derivation's own. THE NON BLANKET
RULE IS THE WHOLE OF THE FIX and the build instruction named it explicitly: writing the derivation's
values unconditionally would have been a no op for the limit, since the cascade sets
`limit_cmp = g_ag_state_limit_snap` at tier 1, and would still have overwritten the base and the breach
time with freshly computed values, which is the substitution Q6 forbids.

WHY `limit_out` IS THE ENFORCED LIMIT AND NOT THE RAW LIVE ONE, recorded because getting it wrong
would be silent. `limit_cmp` is what the disjuncts compare against and is therefore what Q6 means by
the snapshot, and it is already the ratchet floor when the floor is the stricter value. Snapshotting
the raw live limit instead would have loosened every boot derived locked window by exactly the amount
the floor was holding back, which is the same trap the Stage 5 entry of 2026-08-18 recorded for the
ACTIVE path and which is now closed on this path by construction.

THE SCOPED DIFF ROW PASSES IN THE STRONGER FORM. `AgDeclareLock` is byte identical across the change
at sha256 `a23e38c2031f05107a55af657b59158f911bd389ce845aaf2d2e83b75aa0bc31`, `AgEvaluateLocked` at
`25888a59eb8a185c24dc483d3b09703730bce6f347de77ea3e6450d01c5a5cc8`, and `AgEvaluateActive` at
`1a795e285268a4e731808ecc6423e804ecebdeca5b8e5f0fb25f82cad4f2c01c`, WHICH IS THE SAME HASH IT CARRIED
BEFORE SHAPE A, so the Phase 1 evaluation path is byte identical across BOTH defect fixes and not
merely across this one. ALL FIVE INCLUDES THE INSTRUCTION NAMED ARE UNCHANGED BY MD5: `Persist.mqh`
`776f3ba0a3197138645305a1e743822c`, `Pnl.mqh` `4da4810100691ffc135d6073917b7855`, `State.mqh`
`34792807c6b6b548943215866193eb3f`, `Clock.mqh` `dc5a04004f0cb7bdb9d6c602e944cec1`, `Sweep.mqh`
`cf7d355a7c52265a7aff5c465da33c46`, and `Log.mqh` is unchanged too at
`c4e803fb81ebe7f521cc020e8fa1ce82`. Twelve hunks, every one inside `AgBootDerivation`,
`AgEnterLockFromBoot` or the single call site in the SYNCING branch, which is the shared table's ranges
plus the shape 1 delta and nothing else. S1 and S2 rerun clean, the one tree wide hit being the header
comment in `Sweep.mqh`, so the build still carries zero trade API calls. Zero `TimeLocal`, `TimeGMT` or
`TimeTradeServer` occurrences were added, counted over the added lines.

THE COMPILES, BOTH QUOTED VERBATIM AND BOTH RUN INSIDE THE WORKTREE. EA:
`Result: 0 errors, 0 warnings, 948 ms elapsed, cpu='X64 Regular'`. Vectors:
`Result: 0 errors, 0 warnings, 675 ms elapsed, cpu='X64 Regular'`. Per standing rule 6 the process exit
code was read and DISCARDED, 1 on both, inverted exactly as that rule records, and the Result line and
the ex5 timestamp are what the claim rests on. THE INCLUDE RESOLUTION WAS MEASURED AND NOT ASSUMED,
which is the check the Stage 2 entry of 2026-08-18 established when it found MetaEditor rooting against
the deployed terminal tree: every `including` line in both logs names a path under
`...\.claude\worktrees\phase3-defect-fixes\MQL5\Include\AccountGuardian\`, and no path under the
Terminal data folder appears in either log at all.

BUILD IDENTITY. `AccountGuardian.ex5` md5 `1C88C4A155CF09AC38D71C3E5BFEF1D9`, 78676 bytes, mtime
2026-08-20 20:33:18. `AgPhase2StateVectors.ex5` md5 `BE3E233CBC8A770CEFEEB382CAFA688F`, 49004 bytes,
mtime 2026-08-20 20:33:53. Both figures are recorded because the build instruction asked for them, and
STANDING RULE 7 IS RESTATED WITH THEM RATHER THAN LEFT FOR A LATER SESSION TO REMEMBER: the ex5 size
and hash are NOT build identity, since five compiles of byte identical source once produced five
different binaries spanning about 1500 bytes. These two values identify THIS artifact for the copy that
follows, so the owner can verify what landed is what was named; they cannot be used later to argue that
some other binary was or was not built from this source. Both files are untracked by design,
`.gitignore:8` carrying `*.ex5`, so no binary enters history.

RULE A VERIFIED ON A SECOND FIELD RATHER THAN ASSERTED. Every file in the live terminal tree still
carries its pre existing mtime with nothing from today: `Clock.mqh`, `Pnl.mqh` and `AccountGuardian.mq5`
at 2026-08-18 00:20:02, `Persist.mqh` at 2026-08-18 11:02:31, `Log.mqh` and `State.mqh` at 2026-08-09,
`Sweep.mqh` at 2026-07-30 22:39:18, and the live `AccountGuardian.ex5` still at 2026-08-18 11:27:16,
which is build `74D666E9`. THE RUNNING GUARDIAN IS THEREFORE STILL THE UNFIXED BUILD and carries all
four defects, which is the correct and intended state until the owner performs the copy.

THE 84 VECTORS DID NOT RUN, AND THE INSTRUCTION THAT ASKED FOR THEM CANNOT BE SATISFIED IN THE SAME
SESSION THAT FORBIDS THE COPY. Stated plainly rather than worked around, because a substitute would
prove something else while carrying this row's name. A vector run needs `AgPhase2StateVectors.ex5`
inside `MQL5\Scripts\AccountGuardian\` under the Terminal data folder and needs the running terminal to
execute it; that copy is a WRITE under that folder, which RULE A forbids the executor and which ruling
ONE of 2026-08-18 assigns to the owner at the executor's dictation, and this instruction both lists it
as not authorized and assigns it to the next step. This is the same wall the Stage 2 entry of
2026-08-18 hit and recorded in the same terms, where 42 vectors were COMPILED and NEVER EXECUTED. No
summary line and no FAIL count is quoted here because none exists, and none may be invented: the last
`AGVEC|SUMMARY|84/84` on the record is the 2026-08-18 run of the PREVIOUS build and it says nothing
about this one.

THE DICTATION FOR THE NEXT STEP IS PREPARED AND IS NOT PERFORMED. Two copies, both owner performed, one
file at a time with the md5 verified after each landing: `AgPhase2StateVectors.ex5` from the worktree
Scripts directory to the terminal's `MQL5\Scripts\AccountGuardian\`, expected md5
`BE3E233CBC8A770CEFEEB382CAFA688F`; and `AccountGuardian.ex5` to the terminal's
`MQL5\Experts\AccountGuardian\`, expected md5 `1C88C4A155CF09AC38D71C3E5BFEF1D9`. A TERMINAL RESTART IS
MANDATORY BETWEEN THE SCRIPT COPY AND THE RUN, not a precaution: the Navigator enumeration rule
established 2026-08-04 and extended to the Scripts class on 2026-08-09 means a running terminal never
enumerates a file added to its data folder after it started. Whether the EA binary is copied in the
same step or held is the owner's call and is not assumed here, since deploying the EA changes what
guards the live account while deploying the script alone does not.

BOOKKEEPING. The defect 3 ISSUES entry moves to IN PROGRESS with its `Issue` line left verbatim, and
its Status names what closes it, LIVE ACCEPTANCE ON P49 AND P53 AND NOTHING ELSE: a `state_<login>.dat`
written by a boot derived lock whose `L` record third field is non zero and whose `N` record carries
both figures non zero, and a following boot whose witness line reads `tier=snapshot`. The defect 1
entry is unchanged and still IN PROGRESS on P3-1. No FINAL entry was edited, nothing was marked DONE,
and defects 2 and 4 were not touched.

NOT DONE, AND NOT DONE DELIBERATELY: no deploy, no terminal write, no push, no merge, no vector run.
Per the FINAL remote state ruling of 2026-08-04 this entry makes no claim about where the remote
stands. THE NEXT BEST ACTION IS THE OWNER'S: perform the two copies at the dictation above, restart the
terminal, run the vectors, and report the summary line, after which the executor reads the journal read
only and banks it.

2026-08-20, SECOND ENTRY FOR THE DATE. THE DEFECT 3 SHAPE IS RULED AND RECORDED, AND NOTHING WAS
IMPLEMENTED. Ledger only: no source file was written, no compile was run, nothing was built or
deployed, no merge, no push, and nothing under the MetaTrader Terminal data folder was read, written
or approached, per RULE A. No command this session named a path inside that folder, so RULE B's alias
audit was not reached. State re derived rather than carried: `git rev-parse HEAD` returned
`f8972cddc96408cee5b748606ae75c93dbb05233` on branch `worktree-phase3-defect-fixes` with
`git status --porcelain` empty, and LEDGER.md stood at 1582 lines.

THE OWNER RULING IS FINAL IN DECISIONS, quoted there in full and not restated here. Shape 1 of the
defect 3 plan is taken, so the boot derived path persists the derivation's own `limit_cmp` and `base`
as the Q6 snapshot and takes the derivation instant as `breach_time`. Shape 2 is not taken, and the
consequence worth carrying is that `Pnl.mqh` IS NOT WIDENED: `AgRealizedFold` keeps its signature and
its two outputs, no second history walk is added, and the whole fix stays inside the EA. That also
means the file Phase 1 froze, and which carries its own static acceptance row, is untouched by defect
3, which is a smaller and safer diff than the alternative would have been.

NOTHING WAS BUILT, AND THAT IS A READING OF THE INSTRUCTION RATHER THAN AN OMISSION, so it is written
down as one. The owner's message is a ruling and carries no task list, and this project's standing
discipline is explicit and repeated that a ruling is not a build instruction: the kickoff entry of
2026-08-18 records that defining what Stage 5 and Stage 6 ARE "is not the same as an instruction to
build them", the fix order FINAL of 2026-08-19 says in terms that it "IS NOT AN AUTHORIZATION TO WRITE
CODE", and the defect 3 plan's own closing paragraph says the same of itself. All three questions on
defect 3 are now ruled, the order, the coupling and the shape, and the executor reads three rulings as
three rulings. IF THE OWNER INTENDED THIS AS THE BUILD INSTRUCTION, one line saying so is all that is
needed and the work is ready to start from the plan as written.

ONE DERIVED QUESTION IS RAISED BY THE RULING'S OWN WORDS AND IS DELIBERATELY NOT ANSWERED. "Recorded
as such" is satisfied today by this ledger and by the plan, both of which now state that `breach_time`
means the derivation instant on the boot derived path and a true breach instant on the `AgDeclareLock`
path. It MAY instead or additionally be read as requiring the distinction to be marked in the ARTIFACT,
so a reader holding only `state_<login>.dat` can tell the two apart. The state file cannot carry that
today: its record layout is fixed at `Persist.mqh:352-359` and a new field means a format version
change, which touches `Persist.mqh`, the loader, `AG_STATE_FORMAT_VERSION` and the round trip vectors,
none of which is inside the ranges the defect 3 plan names. The executor names the fork and stops
rather than choosing the cheaper reading by default, per the standing rule against settling by
inference what a ruling did not say. The journal already distinguishes the two paths without any format
change, since a boot derived lock is preceded by a `boot witness DERIVED fired` line and an ACTIVE
declaration is preceded by `breach arithmetic`, so a third option is to rely on that and add nothing.

BOOKKEEPING DONE THIS SESSION AND NOTHING ELSE. The defect 3 ISSUES entry keeps its `Issue` line
verbatim, since that is the record of the defect as found together with its live evidence, and its
`Action` line now names the ruled shape, points at the plan, states that no build is authorised, and
carries the shape A interlock in the entry itself rather than only in the plan, because a session
reading ISSUES alone must know that since `0a4c86b` this defect would bite at every expiry rather than
only at a boot. Its `Status` stays OPEN, since nothing is implemented, and now names what it waits on.
No FINAL entry was edited, no defect was marked DONE, defects 2 and 4 were not touched, and the defect
1 entry is unchanged and still IN PROGRESS pending the live acceptance row P3-1.

NOT DONE, AND NOT DONE DELIBERATELY: no code, no compile, no build, no deploy, no merge, no push. The
running binary is still `74D666E9` and carries all four defects. Per the FINAL remote state ruling of
2026-08-04 this entry makes no claim about where the remote stands. THE NEXT BEST ACTION IS THE
OWNER'S AND IT IS NOW A SINGLE INSTRUCTION: authorise the defect 3 build, which under the coupling
ruled this date is one build carrying both fixes, and say which reading of "recorded as such" it
carries.

2026-08-20, DEFECT 1 IS FIXED IN SOURCE AS SHAPE A AND THE DEFECT 3 FIX PLAN IS WRITTEN. NO COMPILE
WAS RUN, NOTHING WAS BUILT OR DEPLOYED, NO MERGE, NO PUSH, AND NOTHING UNDER THE MetaTrader TERMINAL
DATA FOLDER WAS READ, WRITTEN OR APPROACHED, per RULE A. No command this session named a path inside
that folder, so RULE B's alias audit was not reached. State re derived before anything rather than
carried: `git rev-parse HEAD` returned `2894ad3a7288a4fc83e5c5a466d700a051fa9ed9` on branch
`worktree-phase3-defect-fixes` and `git status --porcelain` was empty.

LEDGER.md COVERAGE, STATED PRECISELY BECAUSE THE FILE WAS NOT RE READ FROM BYTE ZERO THIS SESSION AND
THE ENTRY MUST NOT IMPLY OTHERWISE. All 1344 lines of LEDGER.md at `f9b8b47` were read in full in the
immediately preceding session of this same conversation, and the only change since is the 96 line
ACTIONS entry that session authored, whose text is held verbatim. That coverage is complete and it is
PROVEN rather than asserted: `git diff -U0 f9b8b47 HEAD -- LEDGER.md` returns exactly one hunk header,
`@@ -319,0 +320,96 @@`, an insertion and nothing else, and the tree was clean, so the file on disk is
the file already read plus that one block. Execution rule 2 is satisfied on every current line. A
session that cannot make that showing re reads the file.

THE OWNER RULING OF THIS DATE IS FINAL IN DECISIONS, quoted there in full and not restated here. Two
things about it are worth a line in the dated log. The shape is chosen, so shapes B and C of the
defect 1 plan are closed rather than left standing. And DEFECT 1 AND DEFECT 3 ARE COUPLED TO ONE
DEPLOYMENT, which is the ruling's operative half: neither ships alone, and the ground is that shape A
would otherwise widen defect 3 from a boot only case to every expiry.

TASK 1, SHAPE A IMPLEMENTED, commit `0a4c86b`, ONE FILE, 42 insertions and 5 deletions. The
behavioural change is one call: `AgTransition(AG_STATE_ACTIVE, "lock expired", "")` becomes
`AgTransition(AG_STATE_SYNCING, "lock expired", ...)` at `AccountGuardian.mq5:489`. Everything else is
the reset the new destination needs and comment.

A DEFECT IN THE APPROVED PLAN'S OWN PHRASING WAS FOUND WHILE IMPLEMENTING IT, AND IT IS THE MOST
IMPORTANT THING IN THIS ENTRY. The plan's shape A table said the expiry block "gains whatever reset
the new destination needs", which reads as boilerplate and is not: WITHOUT THE RESET THE FIX DOES
NOTHING AT ALL. `g_ag_stable_polls` and `g_ag_last_history_total` are declared at `Pnl.mqh:250-251`
and are advanced or cleared ONLY inside `AgHistoryStable`, which is called from exactly two sites, the
SYNCING branch and the Q10 RESYNC gate, and neither runs while LOCKED. So at an expiry they still hold
whatever the last SYNCING occupancy left, which on a session that booted normally is
`g_ag_stable_polls = 3` with the deal total latched. Entering SYNCING in that state, on an account
whose deal count has not moved, takes `AgHistoryStable`'s count unchanged branch at `Pnl.mqh:273-274`,
increments to 4, returns `4 >= 3` true on the FIRST poll, and the EA leaves SYNCING on the very tick it
entered. The three second discipline the whole fix exists to buy would have been bought and not spent,
and the artifact would have read `polls=4/3`, which is neither the `polls=3/3` prediction P27 names nor
the `polls=<n>/3` for n below 3 it excludes. The expiry block therefore sets `g_ag_stable_polls = 0`
and `g_ag_last_history_total = -1` at `:487-488`, which are byte for byte the two assignments
`Pnl.mqh:257-258` already performs on a disconnect, so the semantics are borrowed from the file that
owns those globals rather than invented here.

TWO SMALLER CHOICES INSIDE THE SAME BLOCK, both recorded rather than left in the code to be found. The
transition now carries a detail field, `history stability required before the first post-expiry breach
decision|polls=0/<HistoryStablePolls>`, which is what P26's `<detail>` placeholder anticipated. And
`g_ag_dynamic_waiting_on` is set to `polls=0/<HistoryStablePolls>` instead of the empty string it was
set to before, because `AgWaitingOn`'s SYNCING default at `State.mqh:96-98` reads "history stability
poll not yet run this session", which is FALSE after an expiry since the boot occupancy already ran
one. A LIFE line that happens to land inside the three second window now says something true.

THE SCOPED DIFF ROW PASSES AND IS THE STRONGER FORM, sha256 over the function bodies rather than
inspection. `AgEvaluateActive` is byte identical across the change at
`1a795e285268a4e731808ecc6423e804ecebdeca5b8e5f0fb25f82cad4f2c01c`, 139 lines, and `AgBootDerivation`
at `424c0553eeb1d5e82677c3ea1734d456342c5432c371088acc753a667160d9ae`, 119 lines. `OnTimer` is byte
identical at `455df9d2b762648ea4c8f4e1908b2f3efb32334ac5dee7b8c6e2a24ee562a7e8`, and so is the whole
globals block at `31a19216bc7a2e001ed30c749673487e3668fe6a15731f566393a404da81b082`, which is what
carries the Stage 6 observability globals. ALL SIX INCLUDES ARE UNCHANGED BY MD5 against their values
before the edit: `Clock.mqh` `dc5a04004f0cb7bdb9d6c602e944cec1`, `Log.mqh`
`c4e803fb81ebe7f521cc020e8fa1ce82`, `Persist.mqh` `776f3ba0a3197138645305a1e743822c`, `Pnl.mqh`
`4da4810100691ffc135d6073917b7855`, `State.mqh` `34792807c6b6b548943215866193eb3f`, `Sweep.mqh`
`cf7d355a7c52265a7aff5c465da33c46`. `git diff` reports three hunks and all three sit inside the two
ranges the instruction permitted. S1 and S2 rerun clean, the single hit tree wide being the header
comment in `Sweep.mqh`, so the build still contains zero trade API calls. Zero `TimeLocal`, `TimeGMT`
or `TimeTradeServer` occurrences were added, counted over the added lines rather than over the file.

ONE THING A LATER GREP WILL FIND AND SHOULD NOT MISREAD: the new comment block mentions
`g_ag_resyncing` by name at `:423`. That is prose explaining what the fix closes, not a reference, and
`g_ag_resyncing` is the Q10 gating flag rather than one of the two Stage 6 observability globals, so
the Stage 6 static acceptance row is untouched in both letter and substance.

TASK 2, THE DEFECT 3 FIX PLAN, commit `1c655ca`, `docs/FIXPLAN_PHASE3_DEFECT3_2026-08-20.md`, and IT
RECOMMENDS NOTHING. Two candidate shapes are laid out side by side and they agree on everything except
one field's meaning, so the choice is isolated to that one disagreement: shape 1 writes the derivation
instant into `breach_time`, shape 2 recovers the true breach instant from the replay when the replay
disjunct fired and falls back to the derivation instant when only the live disjunct did. Shape 2 is the
only one that reaches outside the EA, into `Pnl.mqh`'s shared fold, and the plan names two sub variants
of that reach without choosing between them either. TWO FURTHER VARIANTS ARE NAMED AND EXPLICITLY NOT
PROPOSED: writing the snapshot from inside `AgBootDerivation`, which would put a persistence side
effect in a function that also returns NOT EVALUABLE at `:250` and `:257` on passes where nothing
should be written; and redefining `have_snapshot` so it no longer tests `limit_snap > 0.0`, which fixes
nothing because with zeros on disk there is no value for tier 1 to use and the change would turn a
missing protection into a wrong one.

THE PLAN SEPARATES WHAT THE DEFECT ACTUALLY COSTS, by enumerating every consumer rather than
characterising them. `g_ag_state_limit_snap` is a BEHAVIOUR GATE, since `have_snapshot` at `:273-275`
tests it and tier 1 at `:279` is its only consumer, so zeros make tier 1 unreachable for the whole class
of boot derived locks. `g_ag_state_breach_time` IS READ BY NOTHING in the running logic, its only
consumers being the serializer, the loader and the vectors, so losing it is a pure loss of record.
`g_ag_state_base_snap` is read outside persistence in exactly one place, the Q6 input change WARN at
`:451-452`, where a boot derived lock today prints `limit=0.00 base=0.00` and tells the operator the
opposite of the truth about what governs their locked window.

THE SHAPE A INTERLOCK IS STATED EXPLICITLY, as the instruction required, and it is now a fact about
this branch rather than a prediction. After `0a4c86b` the expiry block clears `g_ag_locked_until` at
`:463` and calls `AgStateResetModel()` at `:464` before transitioning, and `OnTimer` rewrites the GV
mirror from memory every tick at `:918`, so at the SYNCING exit the FILE witness at `:211` cannot fire,
the GV witness at `:224` cannot fire, and the DERIVED witness is the only one left, with `have_snapshot`
false for the same reason. EVERY EXPIRY TIME RE LOCK IS THEREFORE A DERIVED WITNESS LOCK WITH NO
SNAPSHOT, which is exactly the case defect 3 gets wrong, so on this branch as it stands every expiry
time re lock would persist zeros. That is the window the owner's coupling closes. Once defect 3 is
fixed the same path persists reason 1, the derivation's own `limit_cmp` as the limit, its own `base`,
and a non zero breach time, and the NEXT boot then reads `tier=snapshot` where today it is structurally
incapable of reading anything but `tier=floor` or `tier=live`.

PREDICTIONS RUN P47 TO P60 and continue the defect 1 plan's numbering unbroken. P49 is the row the fix
closes and it is written field by field against the state file, `L|1|<until>|<breach_time non zero>`
and `N|<limit>|<base>` both non zero at eight decimals, against the `L|1|1787101200|0` and
`N|0.00000000|0.00000000` the 2026-08-18 artifact carries. P53 is the cheapest proof that tier 1 is
reachable again, one word in one journal field. P51 and P52 are the two negatives that stop the fix
overreaching, preservation of a loaded snapshot on the file witness path and zeros still written on the
CORRUPT_STATE path.

A CONSTRAINT THE PLAN FOUND RATHER THAN ASSUMED, AND IT CUTS BOTH WAYS. The eight existing vectors that
assert on these fields, `c5` through `c8` at `AgPhase2StateVectors.mq5:200-207`, `e4` through `e6` at
`:237-239` and `g4` at `:287-289`, all exercise the `Persist.mqh` model layer and not the EA caller, so
since neither shape touches `Persist.mqh` all eight should pass unchanged and any movement in them means
the fix reached further than intended. The other edge is that for the same reason NO EXISTING VECTOR
EXERCISES THE FIX AT ALL, because `AgEnterLockFromBoot` and `AgBootDerivation` are EA functions and a
script cannot include an EA. Whether defect 3 gets the treatment owner ruling C of 2026-08-18 gave the
two bound helpers, a move into an include so a vector can reach it, is raised in the plan and
deliberately not answered, since it would widen the diff well beyond the ranges the fix needs.

TASK 3, THE LEDGER SYNC. The defect 1 ISSUES entry moves to IN PROGRESS and NOT to DONE, its `Issue`
line left verbatim because it is the record of the defect as found together with its live evidence, its
`Action` line rewritten to record the implementation, and its `Status` naming what closes it: THE LIVE
ACCEPTANCE ROW P3-1 AND NOTHING ELSE, whose artifact is one `LOCKED->SYNCING` transition followed by one
`SYNCING->ACTIVE|history stable|polls=3/3` across a real expiry. No code reading closes it. Defects 2
and 4 were not touched this session and neither was any FINAL entry.

NOT DONE, AND NOT DONE DELIBERATELY: no compile, no build, no deploy, no merge, no push, defect 3 not
implemented, defects 2 and 4 untouched. The running binary is still `74D666E9` and it carries defect 1,
so nothing about the live instance changed today. Per the FINAL remote state ruling of 2026-08-04 this
entry makes no claim about where the remote stands. THE NEXT BEST ACTION IS THE OWNER'S: choose a shape
for defect 3, which is the only thing standing between this branch and a build that satisfies the
coupling ruled today.

2026-08-19, DEFECT 1 FIX PLAN WRITTEN, AND THE OWNER'S ONE WORD RULING OF THIS DATE IS RECORDED
HERE RATHER THAN IN DECISIONS. NO CODE WAS WRITTEN, NO COMPILE WAS RUN, NOTHING WAS DEPLOYED, AND
NOTHING UNDER THE MetaTrader TERMINAL DATA FOLDER WAS READ, WRITTEN OR APPROACHED, per RULE A. No
command this session named a path inside that folder, so RULE B's alias audit was not reached.
State re derived before anything rather than carried: `git rev-parse HEAD` returned
`f9b8b479f743415ec605018c0f067af56b64dcd3` on branch `worktree-phase3-defect-fixes` and
`git status --porcelain` was empty. LEDGER.md was read in full, all 1344 lines, before any of the
work below, per execution rule 2.

THE OWNER RULING OF THIS DATE, QUOTED AS GIVEN IN FULL: "FINAL". IT CONFIRMS THE FINAL MARKER ON
RULINGS ONE AND THREE OF 2026-08-19, the defect fix order and P2-H not being closable by code. IT
IS RECORDED AS THIS DATED NOTE AND NOT AS A NEW DECISIONS ENTRY, per the instruction, because it
adds no new content to either entry and creating a third entry to say that two existing entries
mean what they say would put a decision in DECISIONS that decides nothing. THE PROVENANCE
SENTENCE INSIDE EACH OF THOSE TWO ENTRIES IS UNTOUCHED AND WAS DELIBERATELY NOT EDITED. Both
entries state that the owner said nothing about a marker for them and that FINAL was applied by
transcription rather than by explicit owner words. That sentence is now historically accurate
about how the marker was FIRST applied, and this note is the record that the owner has since
confirmed it. Verified rather than asserted: `git diff` over LEDGER.md for this session shows no
change inside the DECISIONS section at all, only an insertion at the head of ACTIONS.

THE FIX PLAN IS WRITTEN AND IT RECOMMENDS NOTHING. `docs/FIXPLAN_PHASE3_DEFECT1_2026-08-19.md`
lays out the two shapes the ISSUES entry already names, side by side, plus one third shape that
is mechanically distinct from both. For each: the exact functions and line ranges touched, what
it changes on the running path, what it does not change, and its interaction with every FINAL it
touches, each cited by its DECISIONS location. THE EXECUTOR MAKES NO CHOICE BETWEEN THEM and the
document says so in its own opening, because the owner reserves the design.

EVERY LINE RANGE IN THE DOCUMENT WAS RE DERIVED FROM SOURCE THIS SESSION RATHER THAN COPIED FROM
THIS LEDGER'S OWN NARRATIVE, per the FINAL of 2026-08-05, AND SEVEN CITATIONS WERE WRONG IN THE
DRAFT AND WERE CORRECTED BEFORE COMMIT. Recorded rather than silently fixed, because a fix plan
whose line numbers are approximate is exactly the artifact a later session would act on without
rechecking: `AgEnterLockFromBoot`'s `AgStateSetBreach` call is at `:393` and not `:396`, which is
the line the defect 3 ISSUES entry already named correctly; the boot derivation's live disjunct is
at `:284` and not `:281`; the LOCKED dispatch comment block is `:406-417` and not `:411-417`; the
expiry block's state file reset and write is `:446-451` and not `:449-453`; the SYNCING branch is
`:914-944` and not `:913-943`; the RESYNC edge detector is `:960-968` and not `:959-968`; and the
Stage 6 comment above it is `:950-959` and not `:952-958`. Everything else checked out exactly,
including the three that carry the most weight: `g_ag_resyncing` at `:50` written true only at
`:531` and cleared only at `:555`, the coherence gate at `:547-556`, and the expiry transition at
`:457`.

TWO VARIANTS ARE NAMED AND EXPLICITLY NOT PROPOSED, both because they contradict a FINAL. Arming
the coherence gate from the quote age measure would close the frozen quote route and would delay
a decision on quote freshness grounds, which the stale quote ruling of 2026-08-18 forbids in
terms. And arming it by reading `g_ag_obs_connected`, the Stage 6 observability global, breaks
that stage's own static acceptance row as written and, under the broad reading of question SIX of
2026-08-18, contradicts that ruling too. Shape B is therefore laid out in the form that reads
`TerminalInfoInteger(TERMINAL_CONNECTED)` fresh and touches no Stage 6 global, which is otherwise
identical and which needs neither reading resolved.

THE MOST CONSEQUENTIAL THING IN THE DOCUMENT IS NOT A PREFERENCE, IT IS A COVERAGE FACT, and it
is recorded here so a later session does not have to re derive it. THE TWO SHAPES DO NOT COVER
THE SAME GROUND. Shape A routes every expiry through SYNCING unconditionally and therefore
covers both straddle routes, the disconnect and the frozen quote while connected. Shape B arms on
connection state and covers the disconnect route only, so on the one instance this project has
actually observed live, the 61 minute 32 second frozen quote of 2026-08-19 with the terminal
CONNECTED throughout, shape B is predicted to change nothing at all. That prediction is written
down as P39 rather than left as an argument. Two costs are recorded on the other side: shape A
makes `AgBootDerivation` run at every expiry, so an expiry time re lock arrives through
`AgEnterLockFromBoot` and persists an empty Q6 snapshot, which is defect 3 landing on a path
shape A newly makes routine and which the ruled order fixes THIRD; and shape B's flag is cleared
only inside `AgEvaluateActive`, so any disconnect anywhere in a locked window latches the gate
until expiry rather than only a straddling one.

POST FIX PREDICTIONS RUN P25 TO P46 and continue the numbering of
`docs/PREDICTIONS_PHASE3_DEFECTS_2026-08-19.md` unbroken, in the same format and with the same
notation. P25 to P33 are shape A, P34 to P41 are shape B, P42 to P46 are shape C. Every one names
the exact journal lines and the counts over the expiry window. TWO OF THEM ARE PREDICTIONS ABOUT
THE SAMPLER RATHER THAN ABOUT THE BUILD and are marked as such: a three second SYNCING window and
a three second RESYNC window are both about one tenth of the thirty second LIFE cadence, so ZERO
LIFE lines carrying either is the EXPECTED observation and closes nothing either way. The rows
therefore close on the event lines, which are not sampled, exactly as the P1-O ruling of
2026-08-17 established.

THE REPRODUCTION IS WRITTEN FOR THE OWNER AND WAS NOT PERFORMED. Only the precondition has ever
been observed, so the document specifies the run that exercises the consequence: a live lock, an
open losing position carried across the anchor and sized so its floating loss clears the new day's
enforced limit, and either a staged adapter disconnect held from about 00:58 to about 01:02 or the
nightly frozen quote, with the chart, the timeframe, the properties dialog, the template and the
terminal untouched throughout. THE SIZING IS DELIBERATELY NOT DICTATED: the Q7 symbol
specification tables are still unread and still carry the A8 corroboration debt, so the executor
has no tick value or contract size to compute from and will not invent one. FOUR NEGATIVES MAKE A
RUN VALID and a run failing any of them is discarded rather than interpreted: zero `deinit` lines,
zero `init` lines, zero unexpected `TRANSITION` lines, and a continuous `seconds_in_state` within
each occupancy. ONE HALF OF THE CONSEQUENCE MAY NOT BE PRODUCIBLE ON DEMAND and the document says
so rather than promising it: that the gate does not arm is provable on any straddle, while that a
wrong decision follows from unsettled history needs the history to be genuinely unsettled at the
expiry instant, which only a reconnect can produce and which no procedure can guarantee.

NOT DONE, AND NOT DONE DELIBERATELY: no source file touched, no build, no deploy, no merge and no
push. A ruled fix order is not an instruction to build and no separate build instruction has been
given. Per the FINAL remote state ruling of 2026-08-04 this entry makes no claim about where the
remote stands. THE NEXT BEST ACTION IS THE OWNER'S: choose a shape, or perform the reproduction,
in either order.

2026-08-19, PHASE 3 OPENED. BOOKKEEPING, BRANCH, WRITTEN PREDICTIONS AND ONE OBSERVATION PROCEDURE, ON BRANCH `worktree-phase3-defect-fixes`. NO CODE WAS WRITTEN, NO COMPILE WAS RUN, NOTHING WAS DEPLOYED, AND NOTHING UNDER THE MetaTrader TERMINAL DATA FOLDER WAS READ, WRITTEN OR APPROACHED, per RULE A. Every artifact this session read is a repository copy under `docs/evidence`, which is the sanctioned side of that boundary, so RULE B's alias audit was not reached because no command named a path inside the Terminal folder at any point. State re derived before anything rather than carried from the previous entry: `git rev-parse` returns `8e99778efeab1a5612369d65671df5856deb5518` for HEAD, for main and for the local ref origin/main alike, and `git status --porcelain` was empty. LEDGER.md was read in full, all 1317 lines of it, before any of the work below, per execution rule 2.

THE BRANCH AND ITS BASE, both quoted because the instruction asked for them and because the Stage 7 branch needed correcting on exactly this point one day earlier. Branch `worktree-phase3-defect-fixes`, created by `git worktree add` so nothing runs in the owner's own checkout, cut from `8e99778efeab1a5612369d65671df5856deb5518`, whose subject is "ledger: record the Stage 7 push and the UNRULED main-checkout proposal". Verified after creation rather than assumed: `git merge-base HEAD main` returns that same hash, so the merge base EQUALS main's head and there is no second side for a union to reconcile today; `git diff --stat main HEAD` returned EMPTY at creation; and `git status --porcelain` was empty in the new worktree. THE HARNESS CUTS FROM `origin/main` BY DEFAULT AND THAT WAS HARMLESS THIS TIME, which is worth writing down rather than leaving to luck: on 2026-08-18 the Stage 7 branch had to be moved forward because the harness cut it from an `origin/main` that predated every Phase 2 stage, and here origin/main and local main both stood at `8e99778`, so the two agreed and no correction was needed. Re derive it next time rather than trusting this paragraph.

THREE OWNER RULINGS OF THIS DATE ARE RECORDED FINAL IN DECISIONS, quoted as given, and NO FOURTH RULING WAS RECORDED. ONE, the defect fix order for this build: the expiry straddle first, the GV mirror self erase second, the empty Q6 snapshot third, the LOCKED LIFE numbers fourth. TWO, the reseed on reload stays as is, on reload the state takes the current input values, and a record of an input change made while locked is not required, marked FINAL by the owner in terms. THREE, P2-H stays OPEN until the coming weekend market close and is not closable by code or by code reading. The reseed entry references the two standing arguments BY THEIR EXISTING LOCATION and does not restate either, per the instruction: both sit in the ACTIONS entry of 2026-08-18 headed PHASE 2 STAGE 7 OPENED, in the paragraph beginning THE CONSEQUENCE THE OWNER ASKED TO BE RECORDED ON THIS ROW, which carries the argument against and names where the argument for it already sits, the Stage 3 note of 2026-08-18. That paragraph closed on the words "Both are recorded and neither is decided"; the ruling is what decides it. PROVENANCE RECORDED AT THE POINT IT MATTERS, per the correction FINAL of 2026-08-18: the owner marked ruling TWO FINAL in terms and said nothing about a marker for ONE and THREE, and since the protocol offers a DECISIONS entry only FINAL or REVISIT, FINAL was applied to those two by transcription. That distinction is written inside each entry rather than left for a later session to assume.

THE ISSUES SECTION IS REWRITTEN AROUND THE FIX ORDER and the next best action now sits at the top, which is defect 1, the expiry straddle. The four defects are condensed to one three line entry each, in the ruled order, every one Status OPEN and every one citing the live evidence that proves it by journal name, date, build and quoted line. Three items not closable by code follow them, each OPEN with the live window it needs named: P2-A needs one uninterrupted ACTIVE breach, P2-H needs the coming weekend market close per the ruling, and the market closed distinction needs a weekend or a holiday. THE MARKET CLOSED ENTRY REPLACES THE STAGE 4 BOOT DERIVATION ENTRY, which had already narrowed itself to that single item once all three boot witnesses and both degraded channels were measured during Stage 7; its full history survives in the ACTIONS entries of 2026-08-18 and 2026-08-19 and nothing of it is lost. Counted rather than eyeballed: the section held 73 entry blocks before and holds 75 after, five removed and seven added, and all seven entries in it carrying Status FINAL are untouched, as is every entry in DECISIONS below the three new ones.

ONE INSTRUCTION HAD NOTHING TO ACT ON, AND IT IS RECORDED AS A NEGATIVE RATHER THAN REPORTED AS DONE. The instruction was to remove the reseed item from ISSUES. NO SUCH ENTRY EXISTS AND NONE EVER DID. A grep of the whole ISSUES section for the string returns hits in exactly two pre existing entries and neither is an entry about the reseed on reload: one is the kickoff questions entry summarizing ruling SEVEN's "reseed at rollover, no reseed on a backward step", and the other is the SAFE_HALT bypass entry describing a halt file reseeded by the next healthy attach. The reseed on reload question has only ever lived inside the ACTIONS narrative of 2026-08-18, and in the 2026-08-19 Stage 7 close which named it design item 5 and classed it a design item rather than a defect. Nothing was removed. The FINAL entry recorded above is what closes it, and this paragraph exists so a later session comparing the instruction against the diff does not read the absence as an omission.

THE EVIDENCE UNDER EVERY DEFECT ENTRY WAS RE DERIVED FROM THE ARTIFACT AND NOT COPIED FROM THIS LEDGER'S OWN NARRATIVE, per the FINAL of 2026-08-05 that binds an entry to what its own session measured. The five banked journals were decoded from UTF-16LE to UTF-8 into the session scratch directory, read only and outside the repository, and the lines were then quoted from the decode. FOUR THINGS WERE CHECKED THAT THE NARRATIVE ALONE WOULD NOT HAVE SETTLED. The expiry file `journal-20260819-stage7-lock-expiry.txt` carries 127 lines and EXACTLY THREE non LIFE lines, the expiry INFO, the `LOCKED->ACTIVE` transition and the ratchet reseed, so the absence of any SYNCING or RESYNC line in that window is a count over the whole file rather than a sample. The forced kill file carries all three boots in one artifact and `boot witness GV fired` appears in it exactly ONCE, at 15:36:27, on the one boot whose transition reads `LOCKED->SYNCING`, while the two cold boots at 13:46:41 and 16:01:24 both read `BOOT->SYNCING` and both fire FILE and DERIVED only; that is the defect 2 split established on one file instead of two. `ACTIVE->LOCKED` returns ZERO occurrences and `SYNCING->LOCKED` returns four across the same file, which is the P2-A negative. And `state_1200252169.dat.live-breach-2026-08-18` was read byte for byte and matches the four records the ledger quotes exactly, `AGSTATE|1|1200252169`, `L|1|1787101200|0`, `N|0.00000000|0.00000000`, `C|2087926071`. One line the ledger had not previously quoted was recovered and is now in the defect 4 entry: the LOCKED LIFE line covering the 13:38:07 liquidation itself, `AG|2026.08.18 13:38:02|LIFE|state=LOCKED|seconds_in_state=2121|waiting_on=expiry: TimeCurrent >= locked_until|server=2026.08.18 13:38:02|local=2026.08.18 13:38:03`.

TWO DOCUMENTS WERE WRITTEN UNDER `docs/`, both dated, both explicitly requested, and neither is a fourth markdown file in the workspace root. `docs/PREDICTIONS_PHASE3_DEFECTS_2026-08-19.md` carries twenty four numbered predictions, P1 to P24 with the numbering running unbroken across all four defects, of the exact journal lines the CURRENT UNFIXED build `74D666E9` is predicted to emit or not to emit when each defect is reproduced, together with the acceptance row wording that will close each defect. POST FIX PREDICTIONS ARE DELIBERATELY ABSENT and ride each fix plan later, per the instruction. `docs/PROCEDURE_P2A_OBSERVATION_2026-08-19.md` carries the observation procedure the owner performs on the demo to see `ACTIVE->LOCKED` with no reload in between: the preconditions, the eight input values, the sizing arithmetic with its question SEVEN debt named, the predicted lines in order, the five negatives that make a run valid, and the seven outcomes that would convert the row into a numbered defect. STRINGS NEVER OBSERVED ON THE ACTIVE DECLARATION PATH ARE DESCRIBED RATHER THAN QUOTED IN THAT DOCUMENT, the transition's reason field and the ALERT's wording among them, because pre quoting an unobserved line trains a reader to see what the document said instead of what the terminal wrote, and this row exists precisely to catch that difference.

NOT DONE, AND NOT DONE DELIBERATELY: no source file touched, no build, no deploy, no merge and no push. The session's authorization was bookkeeping, a branch, written predictions and one procedure, and nothing else was reached for. Per the FINAL remote state ruling of 2026-08-04 this entry makes no claim about where the remote stands. Five commits sit on `worktree-phase3-defect-fixes` and it is unmerged. THE NEXT BEST ACTION IS DEFECT 1 and it is a fix plan rather than a fix: the ruled order is recorded, and a ruled order is not an instruction to build, so the executor writes no code until a later and separate owner instruction says so.

2026-08-19, THE STAGE 7 WORK WAS PUSHED, TWO REFS IN ORDER, BRANCH FIRST AND MAIN SECOND. Owner merged `4c44fac` into main as `4021519` with a second `--no-ff` merge rather than a cherry-pick, then pushed `worktree-phase2-stage7` to `4c44fac` and main to `4021519`. THIS IS THE FIRST PUSH OF MAIN SINCE PHASE 2 BEGAN: origin/main stood at `cef9127` beforehand, which predates every Phase 2 stage, so the remote moved across the whole of Stages 2 through 7 in a single step. Verified read only by the executor afterwards and quoted rather than summarised: `git log --oneline -1 origin/main` returns `4021519 merge: Stage 7 ledger record of its own merge`; `git log --oneline -1 origin/worktree-phase2-stage7` returns `4c44fac ledger: record the Phase 2 Stage 7 merge to main`; and `git diff --stat origin/main origin/worktree-phase2-stage7` returns EMPTY, so both pushed refs carry identical trees. Per the FINAL remote state ruling of 2026-08-04 this is a DATED OBSERVATION and nothing more: the executor read local remote-tracking refs, not the remote itself, and this line is stale the moment either ref moves. This paragraph was committed directly on main by the owner as a ONE TIME EXCEPTION, to avoid a third merge commit whose only content would be a record of the second.

2026-08-19, PHASE 2 STAGE 7 MERGED TO MAIN, merge commit `1ea7c2d`, parents `49ea967` and `6219dfe`, twenty seven branch commits carried, seventeen files changed, 273 insertions and 3 deletions. PERFORMED BY THE OWNER in the main checkout, from the command and the message file the executor dictated, because a worktree cannot check out a branch that is already checked out elsewhere and this session is isolated to its worktree by design. The executor verified it read only afterwards and did not perform it.

VERIFICATION, all three commands run against the merged tree rather than against this narrative, and their output quoted rather than summarised. `git log --oneline -1 main` returns `1ea7c2d merge: Phase 2 Stage 7, deploy and live acceptance`. `git diff --stat main worktree-phase2-stage7` returns EMPTY, so main's tree is byte identical to the branch and nothing was dropped in the merge. `git log 49ea967..main --format='%an %ae' | sort -u` returns exactly one line, `www8351 www8351@gmail.com`. An attribution scan over the same range for co-authored, generated with, anthropic, ai assistant and claude returns ZERO on all five.

TWO MECHANICS CHECKS THE EXECUTOR PROMISED AT PREPARATION TIME AND THEN RAN. The commit has TWO parents, `49ea9679d19c58bfadb9fc7e73b1b1c30bb018c4` and `6219dfe07d4e19437f7b7838b8938ec393bc98df`, so `--no-ff` held and no fast forward occurred despite the merge base equalling main's head, which is exactly the case where git would otherwise have fast forwarded. AND THE COMMIT CARRIES THE COMPOSED MESSAGE rather than git's bare default MERGE_MSG. That is the third consecutive merge to survive the window that lost the messages of `937c604` on 2026-08-08 and `d52f063` on 2026-08-11, and the reason is the mitigation adopted on 2026-08-17 and repeated here: merge with `--no-ff -F <message file>` in ONE command, never `--no-ff --no-commit` followed by a separate commit, so no prepared index sits uncommitted for a periodic committer to take. Still a mitigation and still not a diagnosis; the cause of the two original losses remains unestablished.

THE ANTICIPATED LEDGER.md HAND UNION WAS NOT NEEDED AND THE EXPECTATION IS NOW DISCHARGED BY MEASUREMENT. The owner's standing instruction was that this merge would need the union per the `ce6964d` precedent. Re derived at merge time as that precedent itself demands: the merge base was `49ea967`, equal to main's head, with `HEAD..main` at zero commits, so main had not moved since the branch was cut and there was no second side for a union to reconcile. No conflict was possible on any file. This is the second time the anticipation has proved unnecessary, after 2026-08-17, and the lesson both times is the same one: re derive the base at merge time rather than trusting either the instruction or a prior entry.

WHAT THE MERGE CARRIED, and the count is only right because a defect was caught first. The branch's own `git diff --stat main HEAD` reported FOUR files at first inspection, against a ledger narrative claiming a dozen banked journals, and that discrepancy is what exposed the `.gitignore` swallow now recorded as its own OPEN issue. After the thirteen journals were renamed from `.log` to `.txt` the merge carries seventeen files: LEDGER.md, the ratchet epsilon hotfix in `Persist.mqh`, the eight new vectors in `AgPhase2StateVectors.mq5`, the live breach state file, and thirteen evidence journals. Git classifies the journals as binary because MT5 writes them UTF-16LE, so they archive faithfully and will not text diff. HAD THE MERGE RUN BEFORE THAT CATCH, main would now cite evidence paths that do not exist in it, and the files themselves would have gone with the worktree.

2026-08-19, A REPORTED MACHINE SLEEP WAS INVESTIGATED AND NO SUCH EVENT EXISTS IN ANY ARTIFACT. Owner reported that the PC entered sleep by accident and was woken, describing it as a third clock shape after a disconnect and a kill, with an approximate window of 13:00 to 15:00, and asked for the surrounding lines and the state file comparison. THE INVESTIGATION RETURNED A NEGATIVE AND IT IS RECORDED AS ONE, because the 2026-08-05 rule binds an entry to what its own session measured and a reported event that leaves no trace must be written down as absent rather than assumed and narrated. THE WINDOW WAS FIRST READ AS 2026-08-19 AND THE OWNER CORRECTED IT TO 2026-08-18, so the first pass dismissed it on the wrong ground, that 13:00 to 15:00 had not yet happened when the check ran at 10:22 on the 19th. That ground is withdrawn and the correct window was then scanned directly at a tighter threshold. RESULT ON THE CORRECTED WINDOW: across 13:00 to 15:00 on 2026-08-18 there is exactly ONE discontinuity in the guardian's LIFE sequence over 35 seconds, and it is 69 seconds at `13:45:33 -> 13:46:42`, which is the owner's own P2-B terminal restart via Task Manager, already banked in this entry. The line count corroborates it: 239 LIFE lines across the 13:00 and 14:00 hours against 240 expected at a 30 second cadence, the single missing tick being the one that 69 second gap swallowed. A two hour suspension would have left a two hour hole; the largest hole is 69 seconds. THREE INDEPENDENT FACTS PLACE THE MACHINE AWAKE THROUGHOUT: the P2-C adapter disconnect at 14:58:54 to 15:05:12 produced CONTINUOUS LIFE lines carrying `DEGRADED: disconnected`, which is the disconnect signature and not a suspension; the executor read the live log at 13:41, 13:47 and 15:04 and each read returned fresh lines; and the broker liquidation at 13:38:07, the relaunch at 13:46:41 and the reconnect at 15:04 all fall inside the window and are all evidenced.

THE ARITHMETIC, which is stronger than an absence of notices. On 2026-08-19 the LIFE lines run at their 30 second cadence from `00:00:00` to `10:22:00` with ZERO gaps exceeding 60 seconds. The expected line count across that span is 37320 seconds divided by 30 plus one, which is 1245, and the ACTUAL count is 1245: every tick is present and none is missing. The counter agrees independently: `seconds_in_state=33720` on the `10:22:01` line, and 10:22:01 less the 01:00:01 entry into ACTIVE is exactly 33720 seconds, so it tracks wall clock with no jump. The only non LIFE lines in the entire day are the three expiry lines at 01:00. No `deinit`, no `init`, no `TRANSITION` beyond the expiry, no `DEGRADED`, no `quote_age` after the nightly break closed. THE FILES AGREE: `state_1200252169.dat` is unchanged at md5 `793A00F2931A3563EE890EDA20E30BFE` since 01:00:00, and `halt_1200252169.dat` has not been written since 2026-08-18 16:01:24, which is decisive on its own because `AgHaltAppendSession` writes that file at EVERY `OnInit`, so there has been no EA initialisation of any kind for eighteen hours. On 2026-08-18 the same gap scan finds exactly three, all already banked and all explained: 25788 seconds at 03:28:35, the unattended Windows Update reboot; 69 seconds at 13:45:33, the terminal restart; and 103 seconds at 15:59:42, the two forced kills. NOTHING between 13:00 and 15:00 on either day.

CONFIRMED FROM OUTSIDE MT5 AT THE OWNER'S REQUEST, WHICH IS THE STRONGER FORM OF THIS NEGATIVE. `Get-WinEvent` over the Windows System log for event ids 42 and 107, sleep and resume, across 2026-08-18 00:00:00 to 2026-08-19 23:59:59 returned "No events were found that match the specified selection criteria". So the operating system recorded no suspension either, from a source entirely independent of the guardian's own logging. Owner accepted the negative: no sleep occurred.

WHAT A SLEEP WOULD LOOK LIKE, recorded so a future session can recognise the third shape rather than re deriving it. A suspended machine cannot run the MT5 timer, so a sleep leaves TWO simultaneous signatures that neither of the other two shapes produces together: a gap in the LIFE sequence, like a kill, AND a `seconds_in_state` that JUMPS across that gap rather than resuming, because the counter is derived from a stored entry timestamp rather than accumulated tick by tick. A disconnect freezes the server clock while local advances and the lines keep coming. A kill ends the lines and starts a new session with a fresh `init` and a halt file entry. A sleep would keep the same session, skip the lines, and step the counter. Neither signature is present anywhere in the two days examined. The only machine level event on record remains the 03:28 to 10:38 Windows Update reboot of 2026-08-18, which is a reboot and not a suspension, and it is named here as the nearest thing rather than as an identification of what the owner recalls.

2026-08-19, THE LOCK EXPIRED ON SCHEDULE AND THE 01:00 ROW IS CLOSED ON THE OWNER'S CONFIRMATION. The row was held open at the owner's instruction until they confirmed it; that confirmation was given 2026-08-19 with the wording that it closes on the quoted evidence, so the row is CLOSED and the evidence it closes on is quoted in full below rather than referenced. All five artifacts the owner asked for, quoted: `AG|2026.08.19 01:00:01|INFO|lock expired|locked_until=2026.08.19 01:00:00|server=2026.08.19 01:00:01`; `TRANSITION|LOCKED->ACTIVE|lock expired|`; the state file md5 moving off `1CF59DF8CFA804E636576DAB4ECB2E19`, held since 13:02:42 through four boots, to `793A00F2931A3563EE890EDA20E30BFE` with content `L|0|0|0`, a reset model exactly as `AgStateResetModel` then `AgStateSave` should produce; `INFO|ratchet reseeded at rollover|old_anchor=2026.08.18 01:00:00|new_anchor=2026.08.19 01:00:00|old_floor=106.66|new_floor=106.65` with the floor file rewritten to `F|1787101200|106.65000000`; and the first ACTIVE LIFE line carrying numbers again, `AG|2026.08.19 01:00:31|LIFE|state=ACTIVE|seconds_in_state=30|waiting_on=-|anchor=2026.08.19 01:00:00|realized=0.00|floating=0.00|base=2133.00|limit=106.65|pnl_vs_limit=0.00 vs -106.65`. Every one of the executor's four published predictions for this moment held: the md5 changed, realized reset to 0.00, the ratchet reseeded at the new anchor, and base did not read as a doubled balance.

THE NIGHTLY FREEZE STRADDLED THE EXPIRY ANCHOR, WHICH IS THE PRECONDITION OF AN OPEN ISSUES ROW ARRIVING LIVE ON THE FIRST NIGHT IT COULD. The server clock stopped at `2026.08.18 23:57:59` and stayed there while local time advanced normally, with the Stage 6 note climbing from `quote_age=121s` at 00:00:00 to `quote_age=3691s` at 00:59:30, then jumped in one step to `2026.08.19 01:00:01`. That is a 61 minute 32 second freeze across the 01:00 anchor, the same shape measured on the night of 2026-08-13 into 14. THE LOCK EXPIRED ONE SECOND AFTER THE CLOCK CAUGHT UP, so in wall clock terms it expired on time and the P2-I delay was zero, because the freeze happened to end exactly at the anchor rather than after it. THE HAZARD DID NOT BITE AND ITS PRECONDITION IS NOW OBSERVED: the guardian went LOCKED to ACTIVE directly, with no SYNCING pass and no resync gate, on a clock that had just jumped an hour, and the first ACTIVE evaluation ran ungated. It was harmless because the account was flat, zero positions and zero deals in the new window. The ISSUES row for this is updated from derived-and-unobserved to precondition-observed rather than being closed, since the consequence still has not been exercised.

THE `quote_age` BRANCH OF THE STAGE 6 NOTE FIRED FOR THE FIRST TIME, AND THE `market closed` BRANCH STILL HAS NOT. `AgObservabilityNote` returns `AgQuoteSessionOpen() ? ("quote_age=" + age + "s") : "market closed"`, so the two are mutually exclusive and the artifact now carries the OPEN-session branch across a full hour of frozen quotes. Worth recording because it bears on the one item still keeping the Stage 4 entry open: throughout the entire nightly break the broker's session table reported the quote session OPEN, which is why the note read `quote_age` and never `market closed`. If that holds generally then the `market closed` branch cannot be reached during a nightly break at all and needs a weekend or a holiday, which narrows how that last item can ever be closed. Stated as one night's observation and not as a rule.

THE RECONCILIATION BELOW IS WRONG AND IS WITHDRAWN, CORRECTED 2026-08-19 BY THE OWNER'S DIRECT READING OF THE HISTORY TAB. THE DEPOSIT LANDED AT 2026-08-18 16:51:35, as the owner stated and as planned; the executor's claim that the 2133 was already present at 16:01:27 is FALSE. RECORDED AS OWNER READ FROM THE UI, because no artifact corroborates it and the executor states that plainly: a grep of the whole 2026-08-18 terminal journal for balance and deposit deal lines returns ZERO, and the journal after 16:30 carries only network lines, so MT5 never wrote a balance record anywhere the executor can read.
THE ERROR, NAMED PRECISELY RATHER THAN GLOSSED, and there were two faults stacked. FAULT ONE, the load bearing step. The executor read `tier=floor` as meaning the live limit MEANINGFULLY exceeded the floor. It licenses no such thing: the label comes from `limit_cmp < live_limit`, an EXACT double comparison, and the ratchet epsilon defect fixed the day before proves that exact comparisons in this codebase are routinely decided by a one ULP round trip artifact. `tier=floor` is satisfied by a margin of 1e-14 and cannot carry a quantitative conclusion. FAULT TWO, the arithmetic, and this is the one that should have been caught. Base is INVARIANT under trading deals, since a loss reduces `ACCOUNT_BALANCE` and the all deals sum by the same amount, so with `base=2133.13` and `realized=-1893.50` the balance at 16:01:27 was `2133.13 - 1893.50 = 239.63`, the liquidation residue. That is the SAME 239.63 the executor's own bound produced, and it was treated as a floor beneath a supposed pre existing 2133 instead of being recognised as the residue itself. The executor then compounded it by assuming the owner's 2133 would be ADDED to that figure, expecting about 2372.63, seeing 2133.00 at the 01:00 rollover, and concluding the deposit was absent. THE CORRECT READING: the balance operation at 16:51:35 took the account FROM 239.63 TO 2133.00, and today's `base=2133.00` is the post deposit balance rather than evidence of anything pre existing. Every artifact is consistent with the owner's timestamp.
ALSO NOTED FROM THE SAME JOURNAL PULL AND NOT PREVIOUSLY RECORDED: an unplanned connection loss on 2026-08-18 at 18:02:53, `connection to JustMarkets-Demo3 lost`, with reauthorization at 18:09:04 and resynchronisation reporting `0 positions, 0 orders`. About six minutes, while the account was LOCKED, nobody's doing, and it passed without incident.

THE WITHDRAWN RECONCILIATION IS PRESERVED BELOW AS WRITTEN, since a ledger that edits away its wrong turns cannot be audited.
A RECONCILIATION THE EXECUTOR CANNOT CLOSE AND IS HANDING TO THE OWNER. `base=2133.00` with `limit=106.65`, and the floor file storing `106.65000000` exactly, pins `DailyLossPercent` at 5 in the running EA beyond inference, since 2133.00 at five percent is 106.65 exactly. With percent 5 established, the 16:01:27 witness line, `realized=-1893.50` with `limit_cmp=106.66|tier=floor`, REQUIRES `base > 2133.13` at that moment and therefore `ACCOUNT_BALANCE > 239.63`. Had the owner's 2133 deposit landed at about 16:52 onto a zero balance, the balance at 16:01:27 would have been 0.00, base would have been 1893.50, the live limit 94.68, and that line would have printed `limit_cmp=94.68|tier=live`. IT DID NOT. So the 2133 now in the account was ALREADY PRESENT at 16:01:27, and no further 2133 landed after it, or today's base would read about 4266 rather than 2133.00. The executor cannot see balance operations at all: the terminal journal records no balance deal on any date this session. The owner is asked to check the timestamp of the balance operation in the account history, and the discrepancy is recorded unresolved rather than explained away.

2026-08-19, PHASE 2 STAGE 7 PART 3 IS CLOSED, WITH ONE ROW UNTESTED AND FIVE ITEMS WAITING ON THE NEXT BUILD. Owner declared the status in these terms and it is recorded as given. CLOSED ON ARTIFACTS: the deploy and its four verified copies; the compile at 0 errors 0 warnings and the binary hash; the boot path, re closed against the hotfix binary `74D666E9`; the ratchet epsilon hotfix, 84 of 84 vectors and 271 false WARNs before against zero after; the ratchet seed; the observability row on the 12:09 ACTIVE disconnect; the live breach and its lock declaration; ruling TWO, on the broker's own `0 positions, 0 orders` line; P2-B, relaunch while locked; P2-C, disconnect while locked; the crash path on two forced kills with the chain advancing to 2 of 3 and the mutex takeover firing; and the 01:00 expiry row, closed this date on the owner's confirmation. ALL THREE BOOT WITNESSES FIRED, which is what five stages of ISSUES entries had been waiting for. UNTESTED AND DELIBERATELY NOT SUBSTITUTED: P2-H, the input change while locked, which the owner instructed be left untested rather than approximated, because the reload reseeds the witness before any LOCKED pass can compare and the row may not be reachable through the terminal UI at all. STILL OPEN AND NOT CLOSABLE HERE: the market closed distinction, which needs an out of session window. WAITING ON THE NEXT BUILD, four defects and one design item: the empty Q6 snapshot, the LOCKED numbers gap, the expiry straddle whose precondition was observed live on 2026-08-19, the GV mirror self erase confirmed on two independent cold boots, and design item 5, the reseed that erases the record of an input change while locked. NO CODE WAS WRITTEN THIS DATE and the build under acceptance is unchanged since the hotfix.

2026-08-18, PHASE 2 STAGE 7 OPENED, DEPLOY AND LIVE ACCEPTANCE, on main at `3ad7ef4`. Authorized explicitly by the owner with RULE A standing unchanged: the owner performs every copy at the executor's dictation per ruling ONE, and the executor writes nothing under the Terminal data folder at any point. Ledger work runs on branch `worktree-phase2-stage7`, created by `git worktree add` so nothing is written in the owner's own checkout, and moved to local main's head `49ea967` immediately after creation because the harness created it from `origin/main` at `cef9127`, which predates every Phase 2 stage and carried a 924 line ledger. The branch now reads 1125 lines and `git diff --stat main HEAD` is empty. This entry is appended to as each part closes, so a reader should take the last line of it as the current position rather than the first.

DEFECT FOUND BEFORE ANY WORK AND RECORDED RATHER THAN WORKED AROUND. The instruction directed the executor to read the acceptance row table from the Stage 7 section of the kickoff plan as recorded in this ledger. THAT TABLE IS NOT IN THIS LEDGER. A grep for the row codes across all 1125 lines returns exactly one hit, `P2-I` in the kickoff entry of this date, in the sentence that classifies the weekend expiry overshoot as a row rather than a defect. There is no P2-A, P2-B, P2-C, P2-H, P2-J, P2-K or P2-N anywhere in the file. The cause is already on the record and is not a loss: the kickoff entry states that the plan itself was deliberately NOT captured to a file, its capture having been made conditional on an authorization that was never given, so the coded table exists only in the uncaptured plan. Three Phase 2 rows survive here in prose without their codes, in the DECISIONS entries of 2026-08-18: the two day jump row, the one deferral row, and the DEGRADED through a disconnect row. The executor therefore runs Stage 7 against the row definitions the Stage 7 instruction itself states, and every row banked below is banked against that wording rather than against a table the executor could not read. Named and stopped, not unlocked and not reconstructed.

PART 1 PRECONDITIONS, all read only, measured this session rather than carried. The live halt file `MQL5\Files\AccountGuardian\halt_1200252169.dat` under `%APPDATA%\MetaQuotes\Terminal\D0E8209F77C8CF37AD8BF550E51FF075` reads 475 bytes, mtime 2026-08-18 00:09:50, md5 `0333854E9FE0E3B835D0953F4B5053AF`. THE OWNER'S BACKUP IS VERIFIED AGAINST IT rather than assumed: `C:\Users\www83\OneDrive\Desktop\halt_1200252169.dat`, 475 bytes, mtime 2026-08-18 00:09:50, md5 `0333854E9FE0E3B835D0953F4B5053AF`, identical on all three. The backup sits on the Desktop, outside the Terminal folder, and was made by hand by the owner. NO LIVE STATE FILE EXISTS TO BACK UP: there is no `state_1200252169.dat` on disk. The eleven `state_9900000xx.dat` and `floor_990000011.dat` files in that directory are vector fixtures on synthetic logins written by the owner's 76 of 76 run, not live state, and they are left alone. The absence of a state file on the live login is also the clean account precondition the boot path row and the first breach row both need, so it is recorded as evidence and not only as a housekeeping note.

THE DEPLOY SET IS FOUR FILES, NOT SEVEN, AND THE THREE EXCLUSIONS ARE PROVEN RATHER THAN ASSUMED. Every file in the live include tree and the live Experts tree was hashed read only this session and compared against the source at `3ad7ef4`, whose tree is verified equal to the main checkout by an empty `git diff --stat 3ad7ef4 HEAD -- MQL5` and a clean `git status --porcelain`. Differing and therefore queued for copy: `Clock.mqh` live `31BAD26357DDF6E973DFAA1D49C337DB` at 6349 bytes against source `dc5a04004f0cb7bdb9d6c602e944cec1` at 10151; `Persist.mqh` live `BA6AB1092B0A86979974ACECEC643BF0` at 13393 against `e821fa71ddbb4e5201cf0545a4856cff` at 40714; `Pnl.mqh` live `BC81EEEB8A839F3D168A838958E9B578` at 10115 against `4da4810100691ffc135d6073917b7855` at 14025; and `AccountGuardian.mq5` live `EB646E20A17BF806C803C12A4C6878F4` at 23520 against `c1e12b30ae8fddd76ccaebcf2eef7069` at 53542. Identical and therefore NOT copied: `Log.mqh` at `C4E803FB81EBE7F521CC020E8FA1CE82` and `State.mqh` at `34792807C6B6B548943215866193EB3F`, both byte identical live to source. `Sweep.mqh` is the third exclusion and it is the one that needed an argument rather than a hash: the two files differ by md5, live `CF7D355A7C52265A7AFF5C465DA33C46` at 1944 bytes against source `600b083c69c7d4b121303c2590685414` at 1913, but a diff of both with carriage returns stripped is empty and the byte gap is exactly the 31 carriage returns the live copy carries and the source does not. The content is the same, the line endings are not, so copying it would write the Terminal folder for a line ending change and it is skipped. Git agrees with the hashes independently: `git log --name-only` over `3ad7ef4` shows Phase 2 touched only `AccountGuardian.mq5`, `Persist.mqh`, `Clock.mqh`, `Pnl.mqh` and the vector script, and `State.mqh` has not changed since `fb58788` in Phase 1.

A MEASUREMENT TRAP FOUND IN THE DICTATION ITSELF AND CORRECTED BEFORE A SINGLE PATH WAS NAMED. The instruction says to dictate md5 from `git show` at `3ad7ef4`. That value is WRONG for this purpose on five of the seven files, because `.gitattributes` sets `* text=auto` and `core.autocrlf` is `true`, so `git show` emits the LF normalized blob while the file the owner actually copies carries CRLF. `AccountGuardian.mq5` is the clearest case: blob `da184295730b31a994866d55ca7a46de`, file on disk `c1e12b30ae8fddd76ccaebcf2eef7069`. Dictating the blob hash would have made every landed file fail its own verification. What is dictated is therefore the md5 of the source file on disk, which is what `Get-FileHash` reports at both ends of the copy, with the blob hash recorded here only for provenance: `Clock.mqh` blob `6c74f366bf04f4376659465d7e53b9db`, `Persist.mqh` blob `6985e83a4189aad6264cf120c6bd197b`, `Pnl.mqh` blob `f26dd45fd7355374b836de71a9d56cc1`.

PART 1 DEPLOY, THE FOUR COPIES, ALL PERFORMED BY THE OWNER AT THE EXECUTOR'S DICTATION AND ALL VERIFIED READ ONLY AFTER LANDING. The executor named SOURCE, DESTINATION and expected md5 for one file at a time, waited for the owner's confirmation on each, and hashed the landed file before dictating the next. Landed and matching, in the order copied, includes first so the EA compile would pull them: `Clock.mqh` `DC5A04004F0CB7BDB9D6C602E944CEC1` at 10151 bytes; `Persist.mqh` `E821FA71DDBB4E5201CF0545A4856CFF` at 40714; `Pnl.mqh` `4DA4810100691FFC135D6073917B7855` at 14025; `AccountGuardian.mq5` `C1E12B30AE8FDDD76CCAEBCF2EEF7069` at 53542. Every one equals the dictated source hash exactly. A FULL RE HASH OF THE TREE was then run rather than trusting the four individual checks, and it confirms both halves of the plan: the four copied files carry mtime 2026-08-18 00:20:02, which is the source file's own timestamp carried by `Copy-Item` and not the 2026-08-04 to 2026-08-09 stamps they held before, so the overwrite is witnessed on a second field independent of the hash; and the three excluded files still carry their old mtimes untouched, `Log.mqh` at 2026-08-09 00:35:48, `State.mqh` at 2026-08-09 00:35:24 and `Sweep.mqh` at 2026-07-30 22:39:18, which is the evidence that the exclusion argument was acted on and not merely written down.

THE BINARY IS STILL PHASE 1 AT THIS POINT AND THAT IS STATED RATHER THAN LEFT TO INFERENCE. `AccountGuardian.ex5` reads `AC19EAEA1A65E3C4D052E151C21502E9` at 48244 bytes with mtime 2026-08-09 19:10:59, unchanged by any of the four copies. Between the deploy and the compile the source tree and the running guardian deliberately disagree, and no acceptance row can be read from a journal until the compile and the restart close that gap.

PART 1 COMPILE AND RESTART, CLOSED ON ARTIFACTS. Owner compiled `AccountGuardian` in MetaEditor and reported the Result line verbatim: `0 errors, 0 warnings, 921 ms elapsed, cpu='AVX2 + FMA3'`. THE BINARY IS VERIFIED TO HAVE MOVED rather than taken from the Result line alone: `AccountGuardian.ex5` now reads `EAB9C0F5D8ABDB306849722C40624605` at 76632 bytes with mtime 2026-08-18 01:59:56, against `AC19EAEA1A65E3C4D052E151C21502E9` at 48244 bytes and mtime 2026-08-09 19:10:59 before it. Owner then restarted the terminal at 02:01:32 local with the EA attached to XAUUSD.ecn M15 on account 1200252169.

THE BOOT PATH ROW IS CLOSED, quoted from `MQL5\Logs\20260818.log`, read read only through a shared read `FileStream` because the live log's directory entry is unreliable while MT5 holds the handle, and decoded as UTF-16LE. The build line: `AG|2026.08.18 02:01:36|INFO|init|build=Phase2|account=1200252169|server=JustMarkets-Demo3`. The path: `TRANSITION|BOOT->SYNCING|boot|weekly=on|timer=1s` at 02:01:36, then `LIFE|state=SYNCING|seconds_in_state=1|waiting_on=history stability poll not yet run this session`, then `TRANSITION|SYNCING->ACTIVE|history stable|polls=3/3` at 02:01:39. NO WITNESS FIRED, and that is read from the source rather than from silence alone: every witness in `AgBootDerivation` logs a `boot witness ... fired` line only when it fires and the function returns 0 quietly when none does, while a `NOT EVALUABLE` path would have returned 2 and held the EA in SYNCING with a WARN. ACTIVE was reached with no such WARN and no witness line, so the derivation ran, evaluated all three witnesses and found nothing, which is the correct result on an account with no state file. The file witness also says so directly: `DEBUG|no lock state file, first session on this account`.

THREE SESSIONS ARE IN TODAY'S LOG AND THE MIDDLE ONE WAS NOT ORDERED BY ANYONE, which matters because it is where the first Phase 2 ACTIVE pass actually happened. `init|build=Phase1` at 00:09:50 is the pre deploy session. `init|build=Phase2` at 01:59:57 is an EA reload the terminal performed by itself the moment MetaEditor wrote the new `.ex5`, seven seconds before any human acted, and its `deinit|reason=9` at 02:01:19 is the owner's restart taking it down. `init|build=Phase2` at 02:01:36 is the ordered restart. Nothing is wrong with this, and it is recorded because the seed half of the ratchet row belongs to the middle session and a later reader looking only at the ordered restart would not find it.

THE RATCHET SEED ROW IS CLOSED on the first Phase 2 ACTIVE pass, both halves quoted from the middle session: `AG|2026.08.18 01:59:57|DEBUG|no ratchet floor file, first session on this account` at init, then `AG|2026.08.18 02:00:02|INFO|ratchet seeded|anchor=2026.08.18 01:00:00|floor=106.66` on the first pass after `SYNCING->ACTIVE` at 02:00:01. The floor file on disk corroborates it and is quoted whole: `AGFLOOR|1|1200252169`, `F|1787014800|106.65650000`, `C|3897115903`, 63 bytes, mtime 2026-08-18 02:00:00. Banked against the wording of the Stage 7 instruction rather than against a coded row, per the defect recorded at the head of this entry.

AN UNPLANNED CRASH SURVIVAL LANDED BETWEEN PART 1 AND THE HOTFIX, AND IT IS BANKED AS EVIDENCE BECAUSE NOBODY DESIGNED IT. The machine was restarted at about 03:29 by an unattended Windows Update. This was reported by the owner and then VERIFIED FROM THE JOURNAL rather than taken on report, which matters because a clean shutdown and a killed process look identical in a summary and completely different in the log. The 02:01:36 session's last line is `AG|2026.08.18 03:28:37|WARN|ratchet HOLDING ...` and there is NO `deinit` line after it, against three clean `deinit|reason=` lines elsewhere in the same file, so the session was killed rather than closed. Recovery, quoted: `AG|2026.08.18 10:38:22|INFO|init|build=Phase2|account=1200252169|server=JustMarkets-Demo3`, then `DEBUG|no lock state file, first session on this account`, `INFO|ratchet floor loaded|anchor=2026.08.18 01:00:00|floor=106.66`, `DEBUG|crash-loop check|chain=1/3 consecutive unclean, gap bound 300s`, `TRANSITION|BOOT->SYNCING|boot|weekly=on|timer=1s` at 10:38:22 and `TRANSITION|SYNCING->ACTIVE|history stable|polls=3/3` at 10:38:25. A grep for SAFE_HALT across the whole day's log returns ZERO. The crash-loop chain reading 1 of 3 rather than 2 is correct and not a miss: the gap bound is 300 seconds and the outage was about seven hours, so the chain did not extend, which is the negative-gap discipline behaving as ruled. The floor survived the reboot and reloaded from file, which is the persistence half of the ratchet proven by an event rather than by a vector. Owner has since set the `NoAutoRebootWithLoggedOnUsers` policy and Active hours 02:00 to 20:00, so this is not expected to recur, and the observation is kept because an unplanned kill is evidence no scheduled test produces.

THE SAME LOG QUANTIFIES THE DEFECT AND THE NUMBER IS WORSE THAN THE FINDING FIRST SUGGESTED. `ratchet HOLDING` appears 221 times in `20260818.log`. One is the legitimate vector case from the 00:09 run, `live=150.00 floor=80.00`. The other 220 are the live account between 02:01:40 and 10:39:26, every one of them reading `live=106.66 floor=106.66`, across two separate sessions and one unplanned reboot, against a limit nobody touched all day.

2026-08-18, PHASE 2 STAGE 7 HOTFIX, THE RATCHET EPSILON, committed on `worktree-phase2-stage7` under the owner ruling of this date recorded in DECISIONS. Two files, 92 insertions and 2 deletions. The two deletions are the whole of the behavioural change: `if(live_limit < g_ag_floor_currency)` becomes `if(live_limit < g_ag_floor_currency - AG_PNL_EPSILON)` and `else if(live_limit > g_ag_floor_currency)` becomes `else if(live_limit > g_ag_floor_currency + AG_PNL_EPSILON)`. Everything else added is comment and vectors. `Persist.mqh` gains `#include <AccountGuardian/Pnl.mqh>` because `AG_PNL_EPSILON` is defined there, and that include was checked for cycles before it was written rather than after: `Pnl.mqh` includes only `Clock.mqh`, declares its own globals and names `DailyLossPercent` and `DailyLossCurrency` only inside message strings, so it drags in no EA-only symbol and the vectors script, which includes `Persist.mqh` alone, still stands on its own.

THE COST OF THE FIX IS STATED RATHER THAN BURIED, since a one cent band is not free. A genuine limit change smaller than one cent now moves neither the floor nor the journal, and in the lowering direction that errs LOOSE by up to a cent, which is the first place in Phase 2 where a tolerance does. It is accepted on the same grounds the breach disjuncts already use `AG_PNL_EPSILON` for money comparisons, and it is named here so a later session weighs it instead of rediscovering it.

VECTORS, section K, eight new checks taking the set from 76 to 84. `k1` reloads a floor that was seeded from the live arithmetic exactly as the guardian computes it, `AgLimitCurrency(2133.13, 5.0, 0.0)`, rather than from a round number, because a round number does not exercise the bug. `k2`, `k2b` and `k2c` are the row the ruling asked for: one pass at the same live limit immediately after the reload, with no branch permitted to fire. The two assertions are chosen so they cannot false pass. A `g_ag_last_ratchet_warn` still at zero proves the HOLD branch was not taken, because that branch always warns when the stamp is zero; a bit identical `g_ag_floor_currency` proves the LOWER branch was not taken, because that branch is the only writer of the global. `k3` and `k4` then do the same thing WITHOUT depending on how any particular value rounds, by placing the floor half a cent above and half a cent below the live limit, which makes the row deterministic on any platform. `k4`'s direction is the mirror hazard the finding named and it is now covered: under the old exact comparison a floor a hair ABOVE the live limit selected the LOWER branch, and that branch calls `AgFloorSave`, so the guardian would have rewritten the file on every pass forever. `k5` and `k6` guard the fix from being a blanket mute, at two cents, the smallest move that clears a one cent band: a two cent raise must still warn and a two cent decrease must still lower the floor.

STATIC ROWS AND SCOPE, re run against the modified tree rather than carried from Part 1. S1 and S2: a tree wide grep for OrderSend, CTrade, PositionClose, OrderDelete and Trade.mqh over `MQL5` returns exactly ONE hit, `Sweep.mqh:7`, which is that file's own header comment asserting the rule, not a call. Zero trade API calls in the build, unchanged. Scope: `git diff --stat` names two files and no others, so `AccountGuardian.mq5` is untouched and the pre breach tail of `AgEvaluateActive` is byte identical to `3ad7ef4` by construction rather than by inspection. NOT YET DONE AND NOT CLAIMED: the compile is the owner's and has not run at the time this paragraph was written, and the 84 vectors have not executed. Both are recorded as pending below rather than as passing.

THE HOTFIX IS DEPLOYED, COMPILED, MEASURED AND PROVEN LIVE, and every claim in this paragraph is read from an artifact rather than from the owner's report. Two copies, both owner performed at dictation and both verified read only after landing: `Persist.mqh` `776F3BA0A3197138645305A1E743822C` at 41929 bytes, and `AgPhase2StateVectors.mq5` `4D1FE9F17471DD9519325D34398D4588` at 30364 bytes, each equal to its dictated source hash. The vectors script was NOT in the owner's original instruction, which named `Persist.mqh` and only `Persist.mqh`; the executor named the collision rather than resolving it silently, since MetaEditor compiles from the terminal tree and the eight new checks could otherwise never run, and the owner authorized the second copy. Compiles, both reported by the owner: EA `0 errors, 0 warnings, 966 ms elapsed, cpu='AVX2 + FMA3'`, vectors `0 errors, 0 warnings, 654 ms elapsed, cpu='AVX2 + FMA3'`. THE VECTOR RUN IS VERIFIED FROM THE JOURNAL AND NOT FROM THE REPORT: `AGVEC|SUMMARY|84/84` at 11:27:54, zero lines matching FAIL anywhere in the file, and all eight new checks present by name and PASS, `k1_roundtrip_floor_reloads`, `k2_no_hold_warn_on_the_reloaded_floor`, `k2b_reloaded_floor_is_not_rewritten`, `k2c_enforced_value_tracks_the_live_limit`, `k3_half_cent_above_does_not_lower_the_floor`, `k4_half_cent_below_does_not_warn`, `k5_two_cent_raise_still_warns`, `k6_two_cent_decrease_still_lowers_the_floor`. The set moved 76 to 84 with no regression.

THE LIVE PROOF IS A TIMESTAMP EIGHT SECONDS WIDE AND IT IS THE STRONGEST EVIDENCE IN THIS ENTRY. Counting only lines emitted by the EA rather than by the vectors script, `ratchet HOLDING` appears 271 times in `20260818.log`, and the LAST of them is `AG|2026.08.18 11:25:56|WARN|ratchet HOLDING against a raised limit (question SEVEN): live=106.66 floor=106.66`. The first init of the fixed build is at 11:26:04. Every one of the 271 belongs to the defective build and not one belongs to the fixed one, across three subsequent sessions, each of which logged `INFO|ratchet floor loaded|anchor=2026.08.18 01:00:00|floor=106.66` and then said nothing further about the ratchet. The same-day anchor matters and is why the restart was dictated for today rather than left to the owner's convenience: had it happened after tomorrow's 01:00 the floor would have reseeded and the reload path, which is the only path the defect lives on, would never have been exercised. The three warns that remain in the file from the vectors script are correct holds and not noise: one from the 00:09 run and two from the 11:27 run, including k5's deliberate `live=106.66 floor=106.64`, which is the two cent raise the band must still report.

THE BOOT PATH ROW IS RE CLOSED AGAINST THE NEW BINARY, superseding the Part 1 close, which was proven on `.ex5` `EAB9C0F5D8ABDB306849722C40624605` and is now historical. The running binary is `AccountGuardian.ex5` `74D666E90C1749FD24374D0AEF8C8F7D` at 77396 bytes, mtime 2026-08-18 11:27:16. On the ordered 11:29 restart: `AG|2026.08.18 11:29:31|INFO|init|build=Phase2|account=1200252169|server=JustMarkets-Demo3`, `DEBUG|no lock state file, first session on this account`, `INFO|ratchet floor loaded|anchor=2026.08.18 01:00:00|floor=106.66`, `DEBUG|crash-loop check|chain=1/3 consecutive unclean, gap bound 300s`, `TRANSITION|BOOT->SYNCING|boot|weekly=on|timer=1s` at 11:29:31, `LIFE|state=SYNCING|waiting_on=history stability poll not yet run this session`, then `TRANSITION|SYNCING->ACTIVE|history stable|polls=3/3` at 11:29:34. A grep for `boot witness` across the whole day returns ZERO and a grep for SAFE_HALT returns ZERO, so no witness fired on the clean account and nothing halted. Recorded so a later reader is not confused by the session count: the day carries two extra unordered inits, 11:26:04 and 11:27:16, which are the terminal reloading the EA by itself as MetaEditor wrote each new `.ex5`, the same behaviour first seen at 01:59:56. The ordered restart is the 11:29 one and it is the one the row closes on.

THE RATCHET EPSILON ISSUES ENTRY IS CLOSED AND REMOVED from ISSUES, per the convention this ledger already applied to the Stage 2 unrun row. Its full history is preserved here and in the FINAL of this date: found live, quantified at 271 false WARNs, fixed under owner ruling, measured by eight new vectors, and proven by the absence of the line on the fixed build.

PART 2, THE OBSERVABILITY ROW, CLOSED ON QUOTED JOURNAL. Owner performed the adapter disconnect and reconnected at about 12:13:40; everything below is measured from `20260818.log` rather than taken from that report, and it is measured against the fixed build `74D666E9` running since 11:29. The gate, both ends: `AG|2026.08.18 12:08:42|INFO|RESYNC entered|reconnect coherence gate armed, evaluation waits for history stability before the first post-disconnect breach decision`, logged locally at 12:09:07, and `AG|2026.08.18 12:13:43|INFO|RESYNC exited|history stable again, evaluation resumes`, logged locally at 12:13:44. The marker: NINE consecutive LIFE lines from 12:09:33 to 12:13:33, every one reading `waiting_on=DEGRADED: disconnected, no breach decisions` with a `|DEGRADED|` field in the numbers block, and the first LIFE line after the exit, 12:14:03, back to `waiting_on=-` with no marker. This is the first time the RESYNC lines, the DEGRADED marker and the market closed distinction have been observed at all: the ISSUES entry of this date recorded them as a class no synthetic fixture can produce, closable only by a deploy and a deliberate disconnect, and that is what just happened.

THE SERVER CLOCK FROZE FOR THE WHOLE WINDOW AND THAT IS A SECOND INDEPENDENT WITNESS TO THE SAME EVENT. Every one of the nine DEGRADED lines carries `server=2026.08.18 12:08:42` while its `local=` field advances normally from 12:09:33 to 12:13:33, and the first line after the reconnect reads `server=2026.08.18 12:14:02`. So the disconnect is visible in the log twice over, once through the guardian's own gate and once through TimeCurrent standing still, which is the same freeze behaviour the 2026-08-13 and 2026-08-15 harvests measured. Duration measured rather than reported: about 5 minutes 1 second of frozen server time, and 4 minutes 37 seconds between the RESYNC lines. The owner's four minutes is the right order and the exact figures are these.

ONE ORDERING DETAIL IS RECORDED BECAUSE IT LOOKS LIKE A GAP AND IS NOT ONE. The LIFE line at local 12:09:03 already carries the frozen `server=2026.08.18 12:08:42`, yet it reads `waiting_on=-` with NO DEGRADED marker, and the RESYNC gate arms four seconds later. So for roughly one pass the guardian was working from a quote that had already stopped advancing while still reporting itself healthy. That is the ruled behaviour rather than a miss, and it is the first live sighting of it: the stale quote FINAL of 2026-08-18 put the whole boundary on the connection state and explicitly refused to gate on quote freshness, so a pass that sees `TERMINAL_CONNECTED` true is a normal pass no matter how old the quote is. Banked here so a later session reading this window does not mistake the four second lag for a defect in the marker.

WHAT THE ROW DOES NOT PROVE, stated so the evidence is not read as more than it is. No breach decision was suppressed during the window because there was nothing to suppress: realized and floating were 0.00 on every line and the account was flat throughout. The row proves the gate arms, the marker renders and the gate releases. The suppression itself is the row that needs a breach to land inside a frozen window, which is by design and does not run tonight. Also confirmed clean across the same window and the 48 minutes around it: zero `TRANSITION` lines after the 11:29 boot, so the EA held ACTIVE and RESYNC behaved as a gate inside ACTIVE rather than as a state of its own; zero SAFE_HALT; zero ALERT; and zero EA emitted `ratchet HOLDING` lines, which extends the hotfix's proof through a disconnect and a reconnect.

OPERATING CONSTRAINTS FOR PART 3, given by the owner before the live breach and recorded because they explain what this session did NOT close as much as what it did. The executor stops and waits for an explicit go before each of four owner actions: placing manual losing trades on the demo, restarting the terminal, disconnecting the adapter, and changing any EA input. The executor also stops AFTER the lock event and waits for the owner to confirm that positions are still open, rather than inferring the position state from the journal, which is the correct discipline for ruling TWO since the journal reports the guardian's decision and not the broker's book. Four things are NOT authorized: closing or modifying any position, writing anything under the MT5 Terminal folder, merging to main, and pushing. Three of those four were already standing, RULE A of 2026-08-17 for the folder and CLAUDE.md for merge and push, and the position one is moot from the executor's side since no trade API is reachable from a dictation. A FIFTH ITEM IS NEW AND IS THE ONE A LATER READER WILL NEED: the 01:00 row is NOT to be marked DONE today, whatever the expiry window does tonight. So if that row is found open in this ledger with the mechanism apparently working, that is an owner instruction being honoured and not an omission.

WHERE MAIN STANDS, CORRECTED BY THE OWNER AND THEN RE DERIVED RATHER THAN ACCEPTED. Owner confirmed that `49ea967` on main is the ledger record of the Phase 2 merge and accepted it, so main's head is `49ea967` and NOT `3ad7ef4`. `3ad7ef4` is the merge commit itself and it is main's second most recent commit, which is why the Stage 7 instruction naming main "at `3ad7ef4`" is right about the deployed tree and one commit short of main's head. Measured this session: `git rev-parse main` returns `49ea9679d19c58bfadb9fc7e73b1b1c30bb018c4`, its subject is "ledger: record the Phase 2 merge to main", and the deploy is unaffected either way because `git diff 3ad7ef4 HEAD -- MQL5` was empty when the copies were dictated, so the two commits carry the same source tree and differ only in LEDGER.md.

THE ANTICIPATED LEDGER.md HAND UNION IS RECORDED AS ANTICIPATED, AND THE MEASUREMENT AS OF NOW SAYS IT WOULD NOT BE NEEDED. The owner's instruction is that the Stage 7 merge will need the LEDGER.md hand union per the `ce6964d` precedent, and that is written here as the standing expectation for whoever performs that merge. What this session measured against it, in the same discipline the 2026-08-17 entry used when it recorded that an anticipated union turned out unnecessary: `git merge-base main HEAD` returns `49ea9679d19c58bfadb9fc7e73b1b1c30bb018c4`, which EQUALS main's head, so main has not moved since this branch was cut and there is no second side for a union to reconcile. On today's refs the merge would therefore resolve LEDGER.md with no hand work at all. THE EXPECTATION IS NOT DISCHARGED BY THAT and is deliberately left standing, because it becomes correct the moment main moves, which is exactly what happens if the owner commits to main independently before the Stage 7 branch merges, and because the merge is not being performed now. Re derive the merge base at merge time rather than trusting either this paragraph or the instruction; that is the whole lesson of the `ce6964d` precedent and of the entry that found it unnecessary.

NOT DONE, AND NOT DONE DELIBERATELY: no merge and no push. The owner's instruction on both is explicit, it agrees with CLAUDE.md, and per the FINAL remote state ruling of 2026-08-04 this entry makes no claim about where the remote stands. Branch `worktree-phase2-stage7` sits at `82e1fba` with a clean tree, carrying the Stage 7 ledger record, the ratchet epsilon hotfix and the banked journals, unmerged.

PART 3, THE LIVE BREACH. A LOCK WAS DECLARED ON THE REAL ACCOUNT AT 13:02:42 AND THE ROW AS SPECIFIED DID NOT CLOSE. Both halves of that sentence are the entry. The instruction's row asked for exactly one `TRANSITION ACTIVE to LOCKED` line with full arithmetic. There is no such line: `ACTIVE->LOCKED` appears ZERO times in `20260818.log` and `SYNCING->LOCKED` appears once. The lock is real, correct and fully evidenced, and it arrived through the boot derivation rather than through the ACTIVE breach tail, so the row is recorded as what happened rather than as what was asked for.

THE SEQUENCE, quoted, because the interleaving is the whole story. At 13:02:33 the M1 session was at `realized=574.00|floating=-368.00|pnl_vs_limit=206.00 vs -106.66`, still in profit. At 13:02:37 the ACTIVE breach tail detected the breach and refused to act on it yet: `AG|2026.08.18 13:02:37|WARN|breach deferred one pass: no new deal visible, count=21`. That is the one deferral mechanism of the 2026-08-18 DECISIONS entry behaving exactly as ruled. At 13:02:39, two seconds later, the owner changed the chart period again and the session was torn down mid deferral: `INFO|deinit|reason=3|session marked clean|timer_armed=1|timer_ticks=5586`. THE SECOND PASS NEVER RAN, so the declaration the deferral was waiting to make never happened on that path. Three seconds after that the replacement session reached the SYNCING exit and caught the same breach independently: `AG|2026.08.18 13:02:42|INFO|boot witness DERIVED fired|live=1|replay=0|realized=574.00|floating=-920.50|running_min=-7.00|limit_cmp=106.66|tier=floor|bounded=2026.08.19 01:00:00`, then `TRANSITION|SYNCING->LOCKED|boot derivation: DAILY_BREACH|locked_until=2026.08.19 01:00:00`.

THE ARITHMETIC IS CHECKED RATHER THAN QUOTED. `realized=574.00` plus `floating=-920.50` is `-346.50`, which clears `-106.66` with room. `live=1` and `replay=0` is the correct pair and not an inconsistency: the replay folds CLOSED deals and its `running_min=-7.00` never approached the limit, while the entire loss sat in open positions, so the live disjunct is the only one that could fire. `tier=floor` because no state file existed at that moment, so the cascade skipped tier 1 and took the ratcheted floor, which equals the live limit today. `locked_until=1787101200` is 2026-08-19 01:00:00, exactly one day anchor forward, which is `AgNextDayAnchor` and nothing else.

COUNTS, ALL EXACT AND ALL FROM THE FILE: one `SYNCING->LOCKED`, zero `ACTIVE->LOCKED`, one `|ALERT|` event, one `Alert:` popup which is that same event surfacing to the terminal rather than a second declaration, one `boot witness` line, one `breach deferred` line. LIFE lines from 13:03:03 read `state=LOCKED|waiting_on=expiry: TimeCurrent >= locked_until`.

THE OLDEST OPEN ITEM IN THIS LEDGER CLOSED BY ACCIDENT AND IT IS THE MOST VALUABLE THING IN THIS ENTRY. The three boot witnesses have been unmeasurable since Stage 4 and have kept an ISSUES entry open through five stages, on the argument that each needs a real state file, a real GV or real broker deals that no synthetic fixture can produce. THE DERIVED HISTORY WITNESS, witness 3, HAS NOW FIRED ON A REAL BREACH with its arithmetic logged. It was not produced by the test plan: it was produced by the owner changing the chart period at the exact moment the ACTIVE path had deferred, which is a two second window nobody could have scheduled. The defense in depth argument that justified building three witnesses is no longer an argument, it is an observation: the primary path was interrupted mid decision and the account was still locked five seconds later.

A DEFECT IN WHAT THE LOCK PERSISTED, found by reading the file rather than the log. `state_1200252169.dat`, 79 bytes, mtime 13:02:42, quoted whole: `AGSTATE|1|1200252169`, `L|1|1787101200|0`, `N|0.00000000|0.00000000`, `C|2087926071`. THE Q6 SNAPSHOT IS ZEROS AND THE BREACH TIMESTAMP IS ZERO. Cause, from source at `AccountGuardian.mq5:393`: `AgEnterLockFromBoot` calls `AgStateSetBreach(until, g_ag_state_breach_time, g_ag_state_limit_snap, g_ag_state_base_snap)`, passing the EXISTING model values, which is right when the FILE witness fires and an earlier snapshot must be preserved, and wrong when the DERIVED witness fires with no file at all, because the model is then default constructed and the derivation's own freshly computed limit and base are discarded rather than recorded. SEVERITY, both halves stated. Not an enforcement hole today: the file witness re-fires on reason plus an unexpired `locked_until` and never consults the snapshot, so the lock survives restarts, and tier 2 supplies the same 106.66 on this account. It IS a permanent loss of record: the breach's timestamp is gone, and `have_snapshot`, which tests `g_ag_state_limit_snap > 0.0`, will be false on every future boot for this lock, so the tier 1 protection Q6 exists to give is absent for every boot derived lock rather than for this one only. NOT FIXED, and deliberately: the build under acceptance is the one that just locked a live account, and the executor does not change it mid row.

RULING TWO IS NOT VERIFIED AND IS DELIBERATELY LEFT OPEN AT THIS POINT. The ALERT text asserts that open positions stay open, and the journal cannot corroborate that, because it reports the guardian's decision and not the broker's book. Per the owner's operating constraints the executor stops here and waits for the owner to confirm the position state by hand. The 01:00 expiry row is likewise NOT marked done, per the same constraints, even though `locked_until` is now set and observable.

RULING TWO HOLDS, AND THE EXECUTOR IS NOT THE WITNESS FOR IT. Owner reported that thirteen XAUUSD sell positions of 0.5 lot each were open at 13:02, corrected from an earlier count of four, that the broker force closed all of them on margin, and that the guardian closed nothing. What the executor can corroborate from artifacts, read only: the terminal journal records thirteen closing buy deals of 0.5 XAUUSD.ecn, tickets `#279906117` through `#279906181`, ALL at price 4397.35 and all inside 65 milliseconds at 13:38:07, which is consistent with a forced liquidation; and the broker's own resynchronisation line after the later relaunch reads `terminal synchronized with Just Global Markets Ltd.: 0 positions, 0 orders`. WHAT THE ARTIFACTS DO NOT SAY IS ALSO RECORDED: a grep of the terminal journal for stop out, stopout and margin returns ZERO hits, so the CLOSE REASON is nowhere in any log. The broker's reason is the owner's account and is not corroborated. NO ORDER WAS ISSUED BY THE EA and that rests on three independent artifacts, the strongest first: the build contains zero trade API calls, re-verified this session by the tree wide grep that returns only the `Sweep.mqh` header comment; the EA log carries no order or trade line of any kind after the lock, only LIFE lines; and the thirteen deals carry no expert attribution in the terminal journal.

AN OBSERVABILITY GAP FOUND WHILE ANSWERING THE OWNER'S QUESTION, recorded because it cost real evidence today. LOCKED-state LIFE lines carry NO numbers field: they print state, seconds_in_state and waiting_on and nothing else. Through the entire liquidation of thirteen positions at 13:38:07 the guardian's log said `state=LOCKED|waiting_on=expiry` and not one figure. The owner asked for balance and equity at that moment and the executor cannot supply either from any readable artifact, because no artifact carries them. That is not a defect in enforcement and it is a hole in the record exactly where the record matters most.

HOW A `DEAL_TYPE_BALANCE` DEAL IS TREATED, answered from source at the owner's request and quoted rather than summarised, because a deposit was contemplated. IN THE DAILY SUM IT IS EXCLUDED, `Pnl.mqh:109`, the F12 whitelist: `if(type != DEAL_TYPE_BUY && type != DEAL_TYPE_SELL) continue;`, so a balance deal contributes nothing to `cumulative` and nothing to `running_min`, and a deposit does not reduce the day's realized loss. IN THE DAY BASE IT IS NEUTRAL BY CONSTRUCTION, `AgDayBase` returning `AccountInfoDouble(ACCOUNT_BALANCE) - all_deals` where `AgDealsSumAll` counts all deal types: a deposit raises Balance and raises the all deals sum by the same amount, so Base is unchanged, which is the Q2 identity the function's own comment states. CONSEQUENCE, stated as fact and with no recommendation either way, per the owner's instruction: base and limit are unmoved by a deposit, the day's realized loss stays fully counted, and the lock holds to its expiry regardless. One thing DOES change: at 13:02:42 the derived witness fired with `replay=0` because the loss was floating, and after the liquidation made it realized the same witness fires with `replay=1` from broker history alone, independently of the state file and the GV mirror.

P2-B CLOSES AND TWO OF THE THREE BOOT WITNESSES FIRED ON ONE BOOT. Owner killed and relaunched the terminal while locked. Quoted: `INFO|lock state file loaded|reason=DAILY_BREACH|locked_until=2026.08.19 01:00:00|weighed by the boot derivation at the SYNCING exit`, `TRANSITION|BOOT->SYNCING|boot|weekly=on|timer=1s`, then at 13:46:44 `INFO|boot witness FILE fired|reason=DAILY_BREACH|raw=2026.08.19 01:00:00|bounded=2026.08.19 01:00:00` and `INFO|boot witness DERIVED fired|live=1|replay=1|realized=-1893.50|floating=0.00|running_min=-1893.50|limit_cmp=106.66|tier=floor|bounded=2026.08.19 01:00:00`, then `TRANSITION|SYNCING->LOCKED` and LIFE lines at `state=LOCKED`. THE FILE WITNESS HAS NOW FIRED FOR THE FIRST TIME, joining the derived witness from 13:02:42, so two of the three paths the Stage 4 entry called unmeasurable are measured on artifacts. The `BOOT->SYNCING` label also settles a question this session raised: the `ACTIVE->SYNCING` lines seen earlier belong to the chart change path and not to a cold start, and a cold start does print `BOOT`.

THE `replay=1` FIRING WAS PREDICTED IN ADVANCE AND THEN MEASURED, which is why it is banked as a measurement and not as an expectation. Before P2-B ran, the executor stated that the derived witness would fire with `replay=1` this time, on the reasoning that the liquidation had converted a floating loss into a realized one and `running_min` folds realized deals only. The 13:02:42 firing had `live=1|replay=0|running_min=-7.00`; the 13:46:44 firing has `live=1|replay=1|running_min=-1893.50`. The owner later asked for this to be recorded as an expectation attached to the P2-B row; it is recorded as the stronger thing, a prediction made before the event and confirmed by the artifact after it. THE SIGNIFICANCE IS NOT THE FLAG BUT WHAT IT MAKES INDEPENDENT: with `replay=1` the lock is now derivable from broker deal history ALONE, so deleting both the state file and the GV mirror would not unlock the day.

THE KILL WAS NOT A KILL, AND THE EXECUTOR'S OWN PREDICTION IS WHAT CAUGHT IT. Before the row ran the executor predicted the crash-loop chain would advance past `1/3` and stated in advance that a reading of `1/3` would need explaining rather than shrugging at. It read `chain=1/3`. The halt file gives the reason: the killed session is recorded `S|1787058159|1`, MARKED CLEAN. Task Manager's End task sends a close message to a GUI application first, so MT5 shut down gracefully and `OnDeinit` ran. So P2-B proves the lock survives a RESTART and the crash path remains untested; a genuine test needs a forced termination. A SECOND RESULT FALLS OUT OF THE SAME EVIDENCE AND IS WORTH MORE THAN THE FIRST: the journal has NO `deinit` line for that session, because the log buffer died with the process, while the halt file already carried the clean marker, having been flushed by the atomic write. The halt file's record survives a shutdown that the journal does not.

A FACTUAL CORRECTION AGAINST THE OWNER'S REPORT, derived from the log rather than asserted. The owner reported the demo balance as 0. The 13:46:44 witness line prints `tier=floor`, and that label is emitted only when `limit_cmp < live_limit`. Since `limit_cmp` is the ratchet floor at 106.6565, the live limit must EXCEED 106.6565, so `base > 2133.13`. With `base = ACCOUNT_BALANCE - all_deals` and the day's deals summing to `-1893.50` with no balance deals, ACCOUNT_BALANCE is above roughly 239.63. Had the balance truly been 0, base would be 1893.50, the live limit 94.68, and the line would have read `limit_cmp=94.68|tier=live`. It does not. So the liquidation left a residue. No exact figure is claimed here, only the bound the artifact forces, and the owner should read the terminal rather than take this number.

P2-C CLOSES, DISCONNECT AND RECONNECT WHILE LOCKED, and the executor's published prediction was WRONG ON ONE OF FIVE POINTS. The prediction was written and given to the owner BEFORE the row ran, which is the only reason the error is visible at all, and it is recorded here in full rather than quietly corrected. Correct: zero `RESYNC entered` and zero `RESYNC exited` lines; zero TRANSITION lines; zero ALERT lines; no state file write, `state_1200252169.dat` still at mtime 13:46:44 and md5 `1CF59DF8CFA804E636576DAB4ECB2E19` throughout; the numbers field absent on every line, since `AgPnlNumbersString` returns empty whenever the state is not ACTIVE; and the server clock frozen while local advanced. WRONG: the executor predicted ZERO DEGRADED markers and there are ELEVEN, on LOCKED LIFE lines from 14:59:42 to 15:04:42.

WHY THE PREDICTION FAILED, stated as a reading error rather than as a surprise. The executor traced `g_ag_degraded` and `g_ag_resyncing`, found both set in exactly one place, the `TERMINAL_CONNECTED` guard at the head of `AgEvaluateActive`, correctly concluded that neither can be set while LOCKED, and stopped there. It missed `AgObservabilityNote()` at `AccountGuardian.mq5:500`, a Stage 6 addition that returns `"DEGRADED: disconnected"` from a SEPARATE global, `g_ag_obs_connected`, which the timer samples every tick independently of the state machine. The comment at lines 102 to 110 states that these globals are kept deliberately separate from `g_ag_degraded` "precisely so the two cannot be confused"; the executor read that comment and drew the opposite conclusion from it.

THE ERROR PROPAGATED INTO A WRONG FINDING BEFORE IT WAS CAUGHT, which is the part worth recording. On the strength of the wrong prediction the executor told the owner that a disconnect while locked would be "almost invisible" and that this WIDENED the LOCKED observability issue opened earlier this session. That was wrong and is withdrawn: Stage 6 covers connection state under a lock perfectly well. The observability issue stands for the NUMBERS and only the numbers, which is what it says, so no entry needs correcting. It is recorded because the wrong conclusion was one sentence away from being written into that issue.

WHAT THE ROW ACTUALLY PROVES, and it is a design claim holding up rather than a null result. The two degraded channels are distinguishable in the artifact, exactly as the Stage 6 comment claims. ACTIVE emits `waiting_on=DEGRADED: disconnected, no breach decisions`, from the Q10 gate at line 532, as seen at 12:09. LOCKED emits `waiting_on=expiry: TimeCurrent >= locked_until` plus a separate `DEGRADED: disconnected` field, from the Stage 6 note at line 502, as seen now. Different strings from different sites, so a reader can always tell which fired. The absence of RESYNC under a lock is likewise correct rather than missing: the gate exists to hold the first post-disconnect BREACH DECISION until history is stable, and a locked guardian takes no breach decisions. Window measured from the frozen clock: 14:58:54 to 15:05:12, six minutes eighteen seconds. THE ONE PASS LAG APPEARS FOR THE THIRD TIME: at 14:59:12 the server stamp had already frozen at 14:58:54 while no marker was present yet, the same shape as 12:09:03 and for the same ruled reason.

A GAP DERIVED FROM THIS ROW AND EXPLICITLY NOT OBSERVED, recorded as a reading rather than as a finding, and deliberately NOT opened as an ISSUES row without the owner. Because `g_ag_resyncing` is set only inside `AgEvaluateActive`, a disconnect that begins AND ends inside a locked window arms no resync gate at all. Lock expiry then runs `AgTransition(AG_STATE_ACTIVE, "lock expired", "")`, entering ACTIVE directly rather than through SYNCING, and the coherence gate is `if(g_ag_resyncing)`, so with the flag false the first post expiry pass performs no stability check. For this to matter the disconnect must STRADDLE the expiry boundary: reconnect after `locked_until`, the clock jumps past it, expiry fires within a tick or two, and ACTIVE is entered while history may still be synchronising. That is the nightly break and weekend freeze shape this ledger already documents at length. It did not happen tonight and could not have: the reconnect was 15:05 and expiry is 01:00, about ten hours apart.

P2-H DID NOT CLOSE, AND THE REASON WAS PUBLISHED BEFORE THE ROW RAN. Owner raised `DailyLossPercent` from 5 to 10 while locked. There are ZERO `input changed while LOCKED` lines in the journal. The row's claim, that a limit raised while locked produces a loud WARN while the snapshot holds, is therefore UNTESTED rather than passed or failed. The cause is the one the executor put in writing before the owner touched the input: MT5 reloads the EA on an input change, `INFO|deinit|reason=5`, and `AgEnterLockFromBoot` reseeds the Q6 input witness from the LIVE inputs at `AccountGuardian.mq5:386` to `:388`, so the witness captured percent 10 as the locked-in value and now agrees with the input. Nothing remains to warn about. The executor's other published prediction for this row, that the WARN would read `limit=0.00 base=0.00` and expose the empty snapshot in operator-facing text, is likewise untested since the WARN never fired. WHAT THE ROW DID PROVE, and it is not nothing: the lock held across the input change without a flicker, `locked_until` unchanged at 2026-08-19 01:00:00, the state file rewritten byte identical at md5 `1CF59DF8CFA804E636576DAB4ECB2E19`, no transition out of LOCKED, and LIFE lines resuming at `state=LOCKED` with `seconds_in_state` restarting from 15, which is the reload and not a state change. HOW TO ACTUALLY TEST IT, named and not performed: the WARN needs a LOCKED evaluation pass that observes an input differing from the captured witness, which an input change through the dialog can never produce because the reload always reseeds first. That is a row that may not be reachable through the terminal UI at all, and it is left for the owner rather than worked around. OWNER INSTRUCTED 2026-08-18: leave the row UNTESTED and do not manufacture a substitute test, since a substitute would prove something else while carrying this row's name.

THE INPUT CHANGE WAS ERASED A SECOND TIME, BY THE CRASH, AND NO ARTIFACT ANYWHERE NOW RECORDS THAT IT HAPPENED. Owner observed after the forced kills that `DailyLossPercent` reads 5 on the chart, having set it to 10 at 15:36 and never restored it, and asked what the EA actually booted with. ANSWERED FROM ARTIFACTS, with the negative results stated first. B1, the 16:00:46 boot, left ZERO journal lines, so nothing whatever can be said about its inputs. B2, the 16:01:24 boot, is no better served directly: the EA logs no input value at any point, verified by grep over `AccountGuardian.mq5`, where `DailyLossPercent` appears only as the input declaration at line 19 and inside arithmetic and comparisons. The current LIFE lines imply nothing either, since LOCKED lines carry no numbers, which is the second time in one session that the observability gap has blocked a direct answer. AND THE OBVIOUS CHECK FAILS: the `tier=floor` label cannot discriminate, because with base above 2133.13 both five percent, about 106.7, and ten percent, about 213, exceed the 106.6565 floor, so both print `limit_cmp=106.66|tier=floor` identically.
THE DECISIVE ARTIFACT IS THE CHART STATE FILE, read only: `MQL5\Profiles\Charts\Default\chart01.chr`, mtime 2026-08-18 13:46:03, containing under its `<expert>` block `DailyLossPercent=5.0` and `DailyLossCurrency=0.0`. That mtime PREDATES the 15:36 input change by nearly two hours. MT5 persists chart state, EA inputs included, on clean shutdown, and `taskkill /F` denied it that chance on both kills, so the file still holds the pre change value and the relaunch restored 5 from it. Corroborated independently by the Q6 witness itself: `AgEnterLockFromBoot` captures the live input at boot and `AgEvaluateLocked` warns on any divergence, and with ZERO `input changed while LOCKED` lines all day the live input has equalled the boot captured value continuously since 16:01:24, which with a chart reading 5 means it booted at 5. Restored from the PROFILE's chart state file, not from a template; `Default.tpl` is dated 2026-08-03 and is not implicated.
SO THE OPERATOR ACTION WAS ERASED TWICE OVER, by two independent mechanisms, and this is the finding rather than the input value. Once from the guardian's own witness, by the reseed on reload at 15:36. Once from the terminal's persisted chart state, by a process kill that prevented the save. There is now NO artifact anywhere, in the journal, in the state file, or in the chart, recording that `DailyLossPercent` was ever set to 10 on a locked account. It survives only in the owner's memory and in this ledger. Enforcement never moved and was never at risk; what was lost is the record.

THE CONSEQUENCE THE OWNER ASKED TO BE RECORDED ON THIS ROW, and it is larger than the row itself. THE RESEED ON RELOAD ERASES THE RECORD OF AN INPUT CHANGE MADE WHILE LOCKED. `AgEnterLockFromBoot` sets `g_ag_locked_in_percent` and `g_ag_locked_in_currency` from the LIVE inputs on every boot, so after any reload the witness matches whatever the inputs now say, and the guardian can no longer tell that they ever differed. An operator who raises a limit during a locked window leaves NO trace of having done so in the journal: no WARN at the time, because the reload preempts the first comparing pass, and none afterwards, because the witness has been re-seeded to agree. Today's run is the demonstration: `DailyLossPercent` went 5 to 10 at 15:36 on a locked account and the journal contains zero `input changed while LOCKED` lines. Enforcement did not move and that is not the point; the audit trail did not record an operator action on a locked account. OWNER INSTRUCTED 2026-08-18: the reseed question JOINS THE NEXT BUILD AS DESIGN ITEM 5, classed as a DESIGN ITEM AND NOT AS A DEFECT. That classification is the owner's and is recorded as given: the four items before it are defects with a wrong behaviour to correct, while this one is a deliberate behaviour whose correctness is open. WHETHER THE RESEED IS CORRECT BEHAVIOUR IS A QUESTION FOR THE NEXT BUILD AND IS NOT RULED HERE. The argument for it is on the record already, in the Stage 3 note the reseed was written to answer: without it the first LOCKED pass compares live inputs against a default-constructed 0.0 and warns about a change nobody made. The argument against it is this paragraph. Both are recorded and neither is decided.

ALL THREE BOOT WITNESSES HAVE NOW FIRED, AND THE STAGE 4 ENTRY THAT WAITED FIVE STAGES FOR THIS IS ANSWERED. On the 15:36:27 boot, in one block: `boot witness FILE fired|reason=DAILY_BREACH|raw=2026.08.19 01:00:00|bounded=2026.08.19 01:00:00`, `boot witness GV fired|raw=2026.08.19 01:00:00|bounded=2026.08.19 01:00:00`, and `boot witness DERIVED fired|live=1|replay=1|realized=-1893.50|floating=0.00|running_min=-1893.50|limit_cmp=106.66|tier=floor|bounded=2026.08.19 01:00:00`. The GV path had never fired before today. Counting the whole day: DERIVED fired at 13:02:42 with `replay=0` and again at 13:46:44 and 15:36:27 with `replay=1`, FILE fired at 13:46:44 and 15:36:27, GV fired ONCE, at 15:36:27 only. THE SINGLE GV FIRING IS NOT A COINCIDENCE AND IS THE SUBJECT OF ITS OWN ISSUES ROW opened this session: the mirror is overwritten with zero by `OnTimer` on every cold boot before the witness reads it, so it can only fire on a reload that preserved memory, which is what 15:36:26 was and what 13:46:41 was not.

THE CRASH PATH CLOSES ON TWO FORCED KILLS, and the executor's published table was right on every row it named and incomplete on one it did not. Owner ran `taskkill /F /IM terminal64.exe` twice, relaunching after each, with the two inits deliberately placed inside the 300 second window so the crash-loop chain could advance. Measured init timestamps from the halt file rather than from the owner's report, which said 15:59 and 16:01: B1 is `1787068846`, 16:00:46, and B2 is `1787068884`, 16:01:24, a gap of THIRTY EIGHT seconds. THE HALT FILE IS THE PROOF THE KILLS WERE HARD and it is quoted whole at the tail: `S|1787060801|1`, the 13:46:41 session that Task Manager's End task shut down gracefully, then `S|1787067386|0`, `S|1787068846|0`, `S|1787068884|0`, three consecutive UNCLEAN entries. The contrast between the `|1` and the three `|0` in one file is the discriminator the P2-B entry said was needed and could not supply.

B2 VERIFIED AGAINST THE PREDICTION, LINE BY LINE: `DEBUG|crash-loop check|chain=2/3 consecutive unclean, gap bound 300s`, the chain advancing for the first time in the project's history; `TRANSITION|BOOT->SYNCING`, a true cold start; `boot witness FILE fired`; `boot witness DERIVED fired|live=1|replay=1|realized=-1893.50|floating=0.00|running_min=-1893.50|limit_cmp=106.66|tier=floor`; `TRANSITION|SYNCING->LOCKED`; one ALERT; and `state_1200252169.dat` rewritten byte identical at md5 `1CF59DF8CFA804E636576DAB4ECB2E19` for the fourth boot running. `limit_cmp=106.66|tier=floor` was predicted specifically and holds even though `DailyLossPercent` is now 10: the live limit is roughly 213 and the ratchet floor at 106.6565 is still the stricter value, so the floor governs.

THE GV WITNESS DID NOT FIRE ON THIS COLD BOOT EITHER, WHICH IS THE SECOND INDEPENDENT CONFIRMATION AND TURNS THE DEFECT FROM AN OBSERVATION INTO A PATTERN. Count for the whole day: `boot witness GV fired` appears exactly ONCE, at 15:36:27, the parameter reload that preserved memory. It is absent from the 13:46:41 cold boot and absent from the 16:01:24 cold boot, both of which reached LOCKED on FILE and DERIVED alone. Two cold boots, two non firings, one warm reload, one firing: the self erase mechanism recorded in its own ISSUES row predicts exactly that split and nothing else explains it.

A RESULT NEITHER THE OWNER NOR THE EXECUTOR PUT ON THE LIST, and it is a clean pass on a path that had never run live. `AG|2026.08.18 16:01:24|WARN|stale mutex heartbeat (11s), taking over crashed-instance mutex`. The killed instance died holding the mutex and never released it, and the F8 stale heartbeat takeover did precisely what it exists for, measured at an 11 second staleness against the 10 second threshold. One occurrence in the whole day's log, and it took a genuine process kill to produce it; every earlier restart this session was graceful enough to release the mutex cleanly.

B1 LEFT NO JOURNAL LINES AT ALL, AND THAT CORRECTS SOMETHING THE EXECUTOR WROTE EARLIER THIS SESSION. There are ZERO EA lines between 16:00:00 and 16:01:20; the log runs from `15:59:42` straight to the B2 init. B1 lived 38 seconds and lost its entire boot block, `chain` line included. Before the flush the executor told the owner that a hard kill "loses only seconds of journal, not minutes" and that this SOFTENED the P2-B finding about the halt file outlasting the journal. THAT WAS WRONG AND IS WITHDRAWN: it generalised from the 15:36:26 session, which had lived 23 minutes and had therefore already flushed, to a case with no flush at all. The P2-B finding stands undiminished. B1 IS THEREFORE ATTESTED BY TWO ARTIFACTS AND ZERO LOG LINES: its halt file entry `S|1787068846|0`, and B2's own `chain=2/3`, which could only reach 2 by walking back onto an unclean B1 entry inside the 300 second window. The chain arithmetic is itself evidence of the session that left no trace. B1's own `chain=1/3` line is UNVERIFIABLE and is recorded as such rather than assumed from the arithmetic that says it must have read that way.

THE 2133 DEPOSIT IS INVISIBLE IN EVERY ARTIFACT THE EXECUTOR CAN READ, AND THAT IS TWO DIFFERENT THINGS AT ONCE, ONE A GAP AND ONE THE DESIGN WORKING. Owner deposited 2133 on the demo at about 16:52 while the account was LOCKED, having asked in advance to be told what would be visible. THE LIFE LINES DID NOT MOVE, as predicted before the deposit: sixteen lines from 16:44:25 to 16:51:55 are identical in every field except `seconds_in_state` and the two clocks, `state=LOCKED|waiting_on=expiry: TimeCurrent >= locked_until`, with no numbers block at all. The state file stayed at md5 `1CF59DF8CFA804E636576DAB4ECB2E19`, the halt file at `18A82E7E498B038D7A80295B62D4985F`, the floor file untouched since 02:00:00. NO CORROBORATION EXISTS: the terminal journal ends at 16:01:24 and records no balance deal, so the deposit rests on the owner's report alone and the executor states that rather than implying otherwise.
THE TWO CAUSES MUST NOT BE CONFLATED AND ARE SEPARATED HERE. That the LIFE lines carry no numbers under a lock is the OPEN observability gap, and this is the fourth time in one session it has blocked a direct reading. That the deposit will never appear in the guardian's numbers, not even after expiry returns it to ACTIVE, is the DESIGN WORKING AS RULED and is not a gap at all: the F12 whitelist excludes balance deals from realized, and the Q2 day base identity makes them neutral, verified on tonight's own figures, `base = B + 1893.50` before and `base = (B + 2133) - 239.50 = B + 1893.50` after. The only quantity a deposit moves is `ACCOUNT_BALANCE`, which the guardian logs in no state. So the guardian is BUILT blind to deposits WITHIN THE DAY THEY LAND, deliberately.
CORRECTED 2026-08-19 AT THE EXPIRY, AND THE ORIGINAL WORDING WAS WRONG. This paragraph first said that `base` and `limit` after 01:00 "should read as though nothing was deposited". THAT IS FALSE AND IS WITHDRAWN. The Q2 neutrality holds only inside the day the deposit lands, because `all_deals` counts it there and cancels it out of the identity. AFTER THE ROLLOVER THE NEW ANCHOR EXCLUDES YESTERDAY'S DEALS ENTIRELY, so `all_deals` returns to about zero and `base` collapses to `ACCOUNT_BALANCE` itself, deposit included. Measured at 01:00:31: `base=2133.00`, which IS the account balance and not a coincidence. So a deposit is invisible for the remainder of its own day and fully visible from the next anchor onward. The executor wrote the wrong half of that before the event and is recording the correction here rather than editing the claim away.

2026-08-18, PHASE 2 STAGES 2 THROUGH 6 MERGED TO MAIN, merge commit `3ad7ef4`, parents `06d92ee` and `35628f6`, eleven branch commits carried, nine files changed, 2117 insertions and 28 deletions. Owner ordered. Mechanics per the standing plan the 2026-08-03 ruling carried forward unchanged: a real merge commit forced with `--no-ff`, no rebase, no fast forward only, no side picking strategy.

THE HAND UNION WAS NOT EXERCISED AND IS RECORDED AS NOT NEEDED RATHER THAN SKIPPED, which is the distinction the `ce6964d` precedent asks for. Re derived before merging rather than assumed: `git merge-base main worktree-phase2-lock` returns `06d92ee`, which equals main's head exactly, so main had not moved since the branch point and LEDGER.md had no second side to reconcile. No conflict arose in any file.

GATE POSITION, stated plainly because this is the first Phase 2 merge and because every prior merge entry that failed to state it left a later session to discover the position by reading diffs. The owner instruction the ruling requires: given. COMPLETE: Stages 2, 3, 4, 5 and 6 are implemented and every acceptance row their own instructions named passes on artifacts, with compile at 0 errors 0 warnings at each stage, S1 and S2 clean throughout, and the pre breach tail region of `AgEvaluateActive` byte identical to the branch point across all five stages. PROVEN: 76 of 76 vectors measured in one journal run, verified from the terminal's own journal rather than from a report, with evidence banked at stated paths and md5s in the entries above. THE BRANCH CARRIES ONE OPEN ISSUE and the reading taken is written down rather than left implicit: the three boot witnesses and every Stage 6 condition need a real state file, a real GV, real broker deals or a live disconnect, so they are evidence that structurally requires Stage 7, a separate stage the owner has not authorized, and not an unfinished item of this branch's own work. The contrary reading, that any open entry disqualifies under the all of it clause, is recorded here so a later session can weigh it rather than rediscover it.

VERIFICATION, all against the merged tree rather than against this narrative. `git diff 35628f6 HEAD` returns EMPTY, so main's tree is identical to the branch tip. Per file blob hashes were compared individually as the instruction required, and all eleven match: LEDGER.md at `825311ee3236`, `AccountGuardian.mq5` at `010f4c8f16a8`, `Clock.mqh` at `54bdbc7c90d8`, `Persist.mqh` at `b6e117796141`, `Pnl.mqh` at `db484e2ef9cc`, `Log.mqh` at `60d355d522c3`, `State.mqh` at `efbf57b8622c`, `Sweep.mqh` at `817349043b11`, `AgPhase2StateVectors.mq5` at `f5761d17012b`, `AgPhase1ClockVectors.mq5` at `1ee754a69ef2` and `docs/SPEC_v0.1.md` at `debbbc2d433b`. Conflict markers across every tracked file: zero. S1 and S2 rerun on main: one comment only hit in `Sweep.mqh`, so main still carries zero trade API calls. `docs/SPEC_v0.1.md` is unchanged by the merge, so amendment A7 remains drafted in this ledger and uncommitted to the SPEC, and no session may cite it as applied.

TWO THINGS CHECKED BECAUSE THIS PROJECT HAS BEEN BITTEN BY EACH BEFORE. The merge commit was produced by THIS session's own `git merge` and carries the message this session composed, not git's bare default; the window that lost the composed messages of `937c604` and `d52f063` was closed the same way the 2026-08-17 merge closed it, by using `--no-ff -F` in one command so no prepared index sat uncommitted for a periodic committer to take. And the attribution scan over `06d92ee..HEAD` returns ONE textual hit which is not an attribution: it is the literal string `CLAUDE.md` inside a ledger commit message discussing the markdown file rule, the same class of false positive the 2026-08-08 merge recorded. Real attribution count is zero and the range carries a single author.

The executor performed NO PUSH. Per the FINAL remote state ruling of 2026-08-04 no claim is made here about where the remote stands.

2026-08-18, THE FULL VECTOR SET RUNS CLEAN, `AGVEC|SUMMARY|76/76`, verified from the terminal's own journal per the FINAL of 2026-08-05. RULE B alias audit rerun first, hazard still live on all seven names; RULE A read only throughout, and every command a plain inline `Get-ChildItem`, `Get-FileHash` or shared read `FileStream` with no user defined helper.

THE EVIDENCE, QUOTED. Artifact is a NEW file, `MQL5\Logs\20260818.log`, the first journal of the new local day, since the run landed at 00:09:57 rather than before midnight. `AgPhase2StateVectors (XAGUSD.ecn,M5)` contributes 87 lines: 76 PASS, ZERO FAIL, and 10 INFO or WARN lines from the code under test, closing on `AGVEC|SUMMARY|76/76` at 00:09:57.640. Banked to `docs/evidence/agvec-phase2-stage56-vectors-20260818.txt`, 87 lines, md5 6C4F9433F8D9A4FBFF11BA21DBC4F6D4. The EA re initialized at 00:09:50 with a clean deinit at 00:09:39 before it, which is the restart the Navigator enumeration rule requires, and was back in ACTIVE at 00:09:53 before the script ran.

ALL TWENTY NEW RATCHET VECTORS PASS, counted as twenty and named in full because each carries part of question SEVEN: `j1_seed_takes_the_live_limit`, `j1b_seed_stores_the_floor`, `j1c_seed_stores_the_day_anchor`, `j2_lowers_on_a_decrease`, `j2b_enforces_the_lowered_value`, `j3_holds_the_floor_against_a_raised_limit`, `j3b_enforces_the_floor_not_the_raised_limit`, `j4_reseeds_at_rollover`, `j4b_reseed_moves_the_day_anchor`, `j4c_reseed_enforces_the_new_live_limit`, `j5_no_reseed_on_a_backward_step`, `j5b_backward_step_does_not_move_the_anchor`, `j5c_backward_step_still_enforces_the_held_floor`, `j6_floor_file_reloads`, `j6b_floor_survives_a_restart`, `j7_stale_floor_is_declined`, `j7b_same_day_floor_is_applied`, `j8_corrupt_floor_is_quarantined`, `j8b_corrupt_floor_resets_to_nothing`, `j8c_corrupt_floor_file_was_not_deleted`. The five elements the ruling names are therefore measured rather than argued: seed, lower, hold, reseed at rollover, and no reseed on a backward step, with persistence and quarantine measured alongside them.

THE RATCHET'S OWN LINES ARE THE RUNTIME WITNESS and they read exactly as ratified. `ratchet seeded|anchor=2026.02.10 01:00:00|floor=100.00`. `ratchet HOLDING against a raised limit (question SEVEN): live=150.00 floor=80.00; the floor is enforced until the next day anchor`, which is the inflation defence firing on a real pass. `ratchet reseeded at rollover|old_anchor=2026.02.10 01:00:00|new_anchor=2026.02.11 01:00:00|old_floor=80.00|new_floor=150.00`. And `floor file failed checksum or does not parse, quarantined as AccountGuardian\floor_990000011.dat.bad, the ratchet reseeds from the live limit on the next completed pass (no lock follows: the floor is not lock state)`, which measures the deliberate difference from the state file's corruption policy. Recorded because its absence would otherwise read as a defect: the backward step case emitted NO WARN, because the cadence cap had just fired on the HOLD line milliseconds earlier, and `j5` and its two companions passed on the state assertions regardless. The cap working is the correct outcome, not a missing line.

A MEASUREMENT TRAP, THIRD VARIANT OF THE SAME FAMILY, AND THIS ONE NEARLY PRODUCED A FALSE ACCUSATION. The first read of `20260818.log` returned 11356 bytes, 21 lines and ZERO AGVEC lines, and the directory entry agreed at 11356 with a last write time of 00:09:41. The run is stamped 00:09:57, SIXTEEN SECONDS LATER. The read had raced the event. A second read minutes later returned 36682 bytes and the complete run. Had the executor stopped at the first read it would have reported that no 76/76 run exists, which is a direct contradiction of the owner's report and the most damaging kind of wrong answer this ledger can produce, since the evidence rule exists to catch fabrication and would here have manufactured an accusation instead. What prevented it was not care but procedure: the next check was of the deployed binary, which was already the 76 check build at md5 221AAAD649DEE151443485F493E463E8, and a binary in place with no output demanded an explanation rather than a conclusion. THE RULE THIS YIELDS, alongside the stale directory entry of 2026-08-16 and the growing stream length of this date: A NEGATIVE RESULT FROM A LIVE LOG IS NEVER EVIDENCE OF ABSENCE. It is only ever evidence about the moment of the read. Absence counts only after the writer is known to be finished, or after a second read at a later time agrees.

WHICH BINARY RAN. The terminal's `AgPhase2StateVectors.ex5` is 46364 bytes at mtime 2026-08-17 23:55:54 with md5 221AAAD649DEE151443485F493E463E8, byte identical to the build this branch compiled at commit `0db69a0`, and the mtime is this executor's own compile stamp carried across by the copy. Still no Phase 2 source in the terminal and the live guardian still logs `build=Phase1` at the 00:09:50 init, so nothing on this branch is in the running EA.

WHAT IS NOW MEASURED AND WHAT IS NOT. Measured: the whole state file family, the `locked_until` bounds including the rewound clock precedence, and now the ratchet whole. Not measured and unchanged: the three boot witnesses, which need a real state file, a real GV or real broker deals, and every Stage 6 condition, the RESYNC lines, the DEGRADED markers and the market closed distinction, all of which are live terminal conditions no fixture can produce. Those remain the branch's open evidence and they need Stage 7, which is not authorized.

2026-08-18, PHASE 2 STAGES 5 AND 6 IMPLEMENTED, THE RATCHET AND THE OBSERVABILITY PASS, commit `0db69a0`, three files, 493 insertions and 7 deletions. The three governing FINAL entries were re read in full before any code was written, as the instruction required: question SEVEN, question SIX, and the floor versus clamp precedence. RULE A held, nothing under the Terminal data folder was written or read. Stage 7 deploy remains unauthorized and nothing was deployed.

STAGE 5, THE RATCHET, exactly the five ratified elements and no more. Single scalar `floor_currency` persisted as `floor_<login>.dat` through `AgAtomicWrite`, carrying the same never loaded guard and the same free name quarantine the state file family uses. It SEEDS on the first completed ACTIVE computation, LOWERS on a decrease as a running minimum, HOLDS on an increase with a cadence capped WARN naming both the live value and the floor, RESEEDS AT ROLLOVER, and does NOT RESEED ON A BACKWARD STEP. The ratcheted quantity is the DERIVED `limit_currency` and never the raw inputs, which is the correction the 2026-07-30 addendum made and the ruling ratified: under Q8's min of enabled a leg that is not binding can be disabled with zero effect on the enforced limit, and an input level ratchet blocks that as a false positive on the owner's own worked case.

`min(limit, floor)` IS PRE BREACH ONLY and the code makes that structural rather than careful. It is applied in the live comparison, called from the breach tail and from nowhere else, and as TIER 2 of the boot cascade in the order the instruction named, snapshot then floor then live. Q6's snapshot governs the locked window unconditionally and the floor never touches it, the two being disjoint by construction since the ratchet governs pre breach ACTIVE while the snapshot governs post breach LOCKED. ONE CONSEQUENCE WORTH STATING because getting it wrong would be silent: the snapshot taken at breach is the ENFORCED limit, not the raw live one, so it is automatically the floor at that instant exactly as the 2026-07-30 addendum predicted, and snapshotting the live value instead would have loosened the locked window by precisely the amount the ratchet had been holding back.

TWO DESIGN CALLS INSIDE STAGE 5 THAT THE RULING DID NOT SPELL OUT, both recorded rather than left in the code to be discovered. FIRST, a backward step HOLDS the floor and KEEPS ENFORCING IT. The alternative, treating a floor whose anchor is later than the window as stale, would let one clock rewind drop the floor in a single act, which is the same one step disarm the negative gap ruling of 2026-08-04 refused in the crash loop chain; holding is also the strict direction. SECOND, a corrupt floor file is quarantined and reset but LOCKS NOTHING, unlike a corrupt state file which locks via CORRUPT_STATE. The floor is a pre breach tightening and not lock state, so losing it errs toward the live limit, and the residual that follows, that destroying the floor file resets it on the next seed, is the one the 2026-07-30 ruling already accepted on the record when it ordered the ratchet built.

`AgRatchetUpdate` was placed in `Persist.mqh` beside the floor family rather than in the EA. That is the owner's ruling C of this date applied to a second case rather than a new decision: a script cannot include an EA, so while the function sat there the six vectors question SEVEN calls for could not be written at all. The WARN cadence arrives as a parameter rather than by relocating the EA's `AG_LIFE_INTERVAL_SECONDS`, which keeps Phase 1's structure untouched.

STAGE 6, OBSERVABILITY, ALL THREE ITEMS AND ALL OF IT LOGGING. RESYNC ENTRY AND EXIT INFO LINES, edge triggered on the flag the Q10 reconnect coherence gate already maintains, emitted from the timer rather than from inside the gate, which keeps the evaluation path untouched and is also the better instrument: the RESYNC window is about three seconds against a thirty second LIFE cadence, which is why five live disconnects produced zero observations and why the row could only ever close by luck, and an event line is not sampled. DEGRADED MARKER ON SYNCING AND LOCKED LIFE LINES while `TERMINAL_CONNECTED` is false, closing the measured 2026-08-14 gap where seven SYNCING lines emitted entirely offline were indistinguishable from healthy ones to any reader; ACTIVE already carries Q10's own marker and is deliberately not double marked. QUOTE AGE NOTE when the tick age exceeds the ratified 120 seconds, with `SymbolInfoSessionQuote` drawing the market closed distinction so the marker does not fire across every weekend and every nightly break until nobody reads it, and with a symbol that reports no quote session at all treated as OPEN and never as closed, since claiming market closed without evidence would explain away a genuinely frozen feed.

THE STAGE 6 STATIC ROW PASSES and was run as the instruction specified rather than asserted. Every reference in the tree to either new observability global is enumerated: two declarations, one write in `OnTimer`, one read in `AgObservabilityNote`, and one read plus one write in the RESYNC edge detector. ZERO references inside `AgEvaluateActive`, `AgEvaluateLocked`, `AgBootDerivation`, `AgDeclareLock`, `AgEnterLockFromBoot` or `AgRatchetUpdate`, verified by line range against the function boundaries rather than by reading the grep and trusting it. The globals are kept deliberately separate from `g_ag_degraded`, which the Q10 gate sets and the numbers field reads, so the gating one and the logging one cannot be confused.

ACCEPTANCE, ALL ROWS. Compile PASSES twice: the EA at `Result: 0 errors, 0 warnings, 935 ms elapsed`, ex5 written 2026-08-17 23:55:03, and the vectors at `Result: 0 errors, 0 warnings, 770 ms elapsed`, ex5 written 2026-08-17 23:55:54, sizes not quoted per standing rule 7. S1 and S2 rerun clean, the single comment only hit in `Sweep.mqh`, so Phase 2 still holds zero trade API calls with sweep and flatten still Phase 3. SCOPED DIFF PASSES against the branch point: the pre breach tail region of `AgEvaluateActive` is byte identical to `06d92ee` at sha256 `0fe8e845d2777a6f0aa464c64309c9355ca9fa8d0a165de41790dc85a8924839`, 96 lines, unchanged across all five stages. VECTORS EXTENDED BY 20 TO 76, covering the six the ruling named, seed, lower, hold under inflation, reseed at rollover, no reseed on backward step and corrupt file quarantine, plus two the ruling implies rather than names, that the floor survives a restart and that a prior day floor is declined as stale, since a floor that does not persist is not a ratchet at all.

SPEC AMENDMENT A7, ADDITIONS DRAFTED AND NOT COMMITTED TO THE SPEC, extending the A7 draft already held in the Stage 3 entry. Section 3 gains the floor file: magic `AGFLOOR`, format version, login, `floor_day_anchor`, `floor_currency` and checksum, with the statement that its quarantine policy differs from the state file's in exactly one respect, that a corrupt floor locks nothing because it is not lock state. Section 4.4 gains the pre breach only rule and the sentence that the breach snapshot is the enforced limit and therefore already the floor. Section 4.6 gains the three tier cascade, snapshot then floor then live, replacing the two tier wording. Section 5 gains the ratchet's own rules, seed on the first completed computation, lower on a decrease, hold on an increase with a loud line, reseed at rollover, no reseed on a backward step. Section 6 gains the LIFE line's new `waiting_on` notes, the DEGRADED marker on SYNCING and LOCKED, `quote_age=<n>s`, and `market closed`, together with the RESYNC entry and exit event lines. Section 9's threat model gains one line recording that destroying the floor file resets the floor on the next seed, which is the residual the 2026-07-30 ruling accepted when it ordered the ratchet built. NOTHING WAS WRITTEN TO `docs/SPEC_v0.1.md` and no session may cite A7 as applied.

WHAT IS NOT MEASURED. The 76 vectors COMPILE and have never RUN, which is the same position every stage has reached at its review point. Unchanged from Stage 4 and still the largest gap on the branch: the three boot witnesses are not vector reachable at all, since each needs a real state file, a real GV or real broker deals. Stage 6 adds a second unmeasurable class of its own, and it is worth naming rather than leaving implicit: the RESYNC lines, the DEGRADED markers and the market closed distinction are all conditions of the live terminal, so no synthetic fixture can produce them and they will only ever be proven by a deploy and a disconnect.

2026-08-18, THE EXTENDED VECTORS RUN CLEAN, `AGVEC|SUMMARY|56/56`, verified by the executor from the terminal's own journal and not from the owner's report, per the FINAL of 2026-08-05 that binds every channel including the owner's. RULE A read only throughout, RULE B alias audit rerun before touching the folder and the hazard confirmed still live, all seven of `rd`, `del`, `ri`, `rm`, `mv`, `cp` and `sc` resolving to destructive cmdlets; every command was a plain inline `Get-ChildItem`, `Get-FileHash` or shared read `FileStream` with no user defined helper in the call chain.

THE EVIDENCE, QUOTED. Same artifact as the Stage 2 run, `MQL5\Logs\20260817.log`, now 1662192 bytes by stream read against 1644322 by directory entry, the stale metadata trap firing a third time and again resolved by reading the stream. The file holds THREE vector runs and they are distinguished by timestamp rather than by name: `AgPhase1ClockVectors` at 22:09:25.614 with `AGVEC|SUMMARY|26/26`, the first `AgPhase2StateVectors` at 22:13:45.779 with `42/42`, and THIS run, `AgPhase2StateVectors (XAGUSD.ecn,M5)` spanning 23:02:41.585 to 23:02:41.620, 63 lines, 56 PASS, 6 WARN and ZERO FAIL, closing on `AGVEC|SUMMARY|56/56`. Banked to `docs/evidence/agvec-phase2-stage4-vectors-20260817.txt`, 63 lines, md5 52E568146E1F4BC43A88143E5B2072F4.

THE FOURTEEN NEW VECTORS ALL PASS, and they are the point of the run because they are what the owner ruling of this date made reachable at all. Named in full since each carries a FINAL ruling: `i1_q1_base_is_next_day_anchor`, `i2_latch_floor_is_zero_while_unseeded`, `i3_ruling_three_frozen_adds_a_full_day`, `i4_ruling_three_is_exactly_one_extra_day`, `i5_pre_anchor_breach_does_not_lock_for_minutes`, `i6_pre_anchor_unfrozen_still_takes_the_imminent_anchor`, `i7_ruling_four_floors_a_stale_computed_value`, `i8_ruling_four_is_a_floor_not_a_replacement`, `i9_witness_value_beyond_the_ceiling_is_clamped`, `i10_witness_value_inside_the_bounds_is_untouched`, `i11_rewound_clock_floor_sits_above_the_clamp_ceiling`, `i12_rewound_clock_floor_wins_over_the_clamp`, `i13_rewound_clock_floor_wins_even_for_an_inflated_witness` and `i14_computed_path_is_not_clamped`. THE THREE THAT MATTER MOST are the last of those: `i12` and `i13` measure the precedence ruling of 2026-08-18 in the exact geometry it was made for, a latch floor sitting above the clamp ceiling, and show the floor winning both against an ordinary witness value and against an inflated one; `i14` measures the domain split itself by proving a value the guardian computes for itself is never clamped. Those three rulings were argued from source at the Stage 3 review and are now measured.

WHICH BINARY RAN, established and not inferred. The terminal's `AgPhase2StateVectors.ex5` is 37262 bytes at mtime 2026-08-17 22:53:52 with md5 63D7A89B3FB869298E470A8AB8461A2F, byte identical to the build this branch compiled at commit `922dff1`, and the mtime is this executor's own compile stamp carried across by the copy. Standing rule 7's asymmetry is used the same way as before: a mismatch would have proved nothing, an exact match can only mean the file was copied rather than rebuilt. Still no Phase 2 SOURCE in the terminal: its `Persist.mqh` remains md5 BA6AB1092B0A86979974ACECEC643BF0, the Phase 1 copy of 2026-08-04, so the include tree the live guardian would compile against is untouched by everything on this branch.

THE LIVE INSTANCE, checked rather than assumed. A fourth init today at 23:02:18, the restart the Navigator enumeration rule requires, giving four clean `BOOT->SYNCING` then `SYNCING->ACTIVE` pairs across the day and eight transitions in total, no SAFE_HALT, ZERO WARN from the EA itself, and the tail at 23:02:47 reading ACTIVE with `seconds_in_state=28` and the full numbers group. ALL FOUR INITS STILL LOG `build=Phase1`, which is the standing rule 7 channel and is the direct evidence that none of Stage 2, 3 or 4 is in the running guardian. Today's single ALERT remains the routine 01:00 anchor jump of this morning, unrelated.

WHAT THIS RUN DOES AND DOES NOT CLOSE, stated because the distinction is the whole of the branch's remaining risk. It closes the `locked_until` bounds: Q1's base, ruling THREE's extra day, ruling FOUR's floor, the clamp's domain and the precedence between them are now measured on artifacts rather than argued. IT CLOSES NOTHING ABOUT THE THREE BOOT WITNESSES. The file, GV and derived history paths are still unmeasured and are still not reachable from any synthetic fixture, since each needs a real state file, a real GV or real broker deals, and `AgRealizedFold`'s running minimum needs real deals likewise. The entry that was Stage 3 item B stays open on exactly that.

2026-08-18, THE THREE STAGE 3 ITEMS ARE RULED AND PHASE 2 STAGE 4 IS IMPLEMENTED, commit `922dff1`, five files, 471 insertions and 82 deletions. RULE A held, nothing under the Terminal data folder was written or read this stage. Stage 5, Stage 6 and the Stage 7 deploy remain unauthorized and nothing was deployed.

THE RULINGS APPLIED. ITEM A, `AG_QUOTE_FROZEN_SECONDS = 120` ratified: the UNRULED marker is gone from the source and the grounds now sit at the constant, FINAL in DECISIONS of this date, ISSUES entry closed. ITEM B, noted with no ruling, entry kept open and rewritten below rather than closed. ITEM C, the two bound helpers moved into `Clock.mqh`, FINAL in DECISIONS, ISSUES entry closed; the move brought a single `AgLatchFloor` and `AgApplyLatchFloor` pair with it so ruling FOUR's floor has one definition instead of a copy inside each helper, and CLOCK.MQH'S STATIC ACCEPTANCE ROW WAS RERUN AS THE RULING REQUIRED AND PASSES, zero forbidden clock calls in code, the four textual hits all being the header contract and comments citing it.

STAGE 4, THE BOOT DERIVATION. `AgBootDerivation` runs at the SYNCING exit and ORs three witnesses with STRICTEST WINNING on duration. FILE witness fires on `locked_until > now` rather than on the reason field, so an expired lock's stale record reads as not-locked and needs no clear-on-unlock rewrite. GV witness carries a bare timestamp and therefore defaults to DAILY_BREACH, a GV having no CORRUPT_STATE concept to express. DERIVED witness replays broker history and is the AUTHORITY, precisely because a local machine attacker cannot forge or delete server side history while the file and the GV are both locally writable and are therefore accelerators rather than authority. CORRUPT_STATE takes the reason on a tie, because a DAILY_BREACH label would hide the one thing that finding says, that the record cannot be trusted. THE OR ONLY ADDS A LOCK: no witness unlocks, unlock remains exclusively the `TimeCurrent >= locked_until` comparison, and an unevaluable replay returns a third code that holds the EA in SYNCING rather than licensing an unlock, which is the F6 obligation made structural rather than advisory.

ONE SHARED REPLAY, which is what the instruction asked for and is worth stating precisely. `AgRealizedFold` folds the whitelisted deals once and returns BOTH the realized sum and the running minimum, so the F12 whitelist and the Q3 per deal formula exist in exactly one place and cannot drift between the live path and the replay. `AgRealized` is now that same fold with the minimum discarded. The refactor is behaviour preserving BY CONSTRUCTION and the argument is stated rather than assumed: sorting cannot change a sum, so every Phase 1 figure this function has produced is reproduced exactly. The fold sorts `(DEAL_TIME, DEAL_TICKET)` ascending per design item 1's tiebreak, and the reason it must is that a running minimum is order sensitive where a sum is not, MQL5 does not document `HistoryDealGetTicket`'s order, and an intra second dip is exactly the case where ordering decides whether the dip is visible at all. The insertion sort is deliberate and bounded: this runs at boot only, never per tick, which is the design's own scope note about not replaying the whole history every second.

THE GV MIRROR is now written from memory on every tick beside the mutex heartbeat, unconditionally, which is what makes it self healing: a GV cleared through the terminal's own Global Variables window while the EA is alive is restored within one tick. The state FILE is deliberately NOT rewritten per tick and stays event triggered, at breach, at expiry and at corruption handling, matching the halt file's pattern.

VECTORS EXTENDED BY 14 TO 56, covering exactly what ruling C unblocked and nothing more: Q1's base, ruling THREE's extra day including the measured 00:58 pre anchor case where the unfrozen path locks for 120 seconds and the frozen path locks for more than a day, ruling FOUR's floor and its no-op when the computed value already clears it, the witness path's clamp, and the rewound clock geometry where the latch floor sits ABOVE the clamp ceiling and must win. That last case is constructed by moving the latch rather than the clock, which reproduces the exact geometry deterministically without needing a machine whose clock has been stepped backward. Vector `i14` asserts the domain split head on by proving the computed path is never clamped, which is the one way the two domains could silently collapse into each other.

ACCEPTANCE, ALL ROWS THE INSTRUCTION NAMED. Compile PASSES twice: the EA at `Result: 0 errors, 0 warnings, 849 ms elapsed`, ex5 written 2026-08-17 22:52:21, and the vectors at `Result: 0 errors, 0 warnings, 580 ms elapsed`, ex5 written 2026-08-17 22:53:52, sizes deliberately not quoted per standing rule 7. S1 and S2 rerun clean, one comment only hit in `Sweep.mqh`, so Phase 2 still contains zero trade API calls with sweep and flatten still Phase 3. SCOPED DIFF PASSES and against the stronger reference than before: the pre breach tail region of `AgEvaluateActive` is byte identical to THE BRANCH POINT `06d92ee`, not merely to the previous commit, at sha256 `0fe8e845d2777a6f0aa464c64309c9355ca9fa8d0a165de41790dc85a8924839`, 96 lines, so the Phase 1 evaluation path is untouched across the whole branch even though `AgRealized` was refactored underneath it; the call site inside that region is unchanged and the refactor lives entirely in `Pnl.mqh`.

WHAT IS NOT MEASURED, stated plainly because Stage 4 is the stage where the gap matters most. The 56 vectors COMPILE and have never RUN. Beyond that, the three witness paths are not vector reachable at all: a witness needs a real state file, a real GV and real broker deals, so no synthetic fixture inside the existing script can exercise them, and `AgRealizedFold`'s running minimum likewise needs real deals. So the boot derivation's logic is argued from source and from a clean compile, and the only vectors that touch Stage 4 at all are the `locked_until` bounds ruling C made reachable. The entry that was Stage 3 item B is rewritten to carry this rather than being closed on the code alone.

2026-08-18, PHASE 2 STAGE 3 IMPLEMENTED, LOCK SEMANTICS, commit `7621ce7`, one file, 267 insertions and 16 deletions. RULE A held, nothing under the Terminal data folder was written; the only terminal contact this stage was the read-only journal verification recorded in the entry above. Stage 4 is authorized and NOT started, per the instruction to stop after each stage.

WHAT CHANGED. The breach tail of `AgEvaluateActive` no longer shouts and stays ACTIVE; it declares a lock. `AgDeclareLock` snapshots limit and base into the state file per Q6, computes `locked_until`, transitions to LOCKED and raises an ALERT whose text says in terms that no order is sent and open positions stay open, so the operator is not left expecting a flatten that ruling TWO explicitly defers to Phase 3. The LOCKED case in `OnTimer`, an empty block since Phase 1, now calls `AgEvaluateLocked`.

THE BOUNDS, all three rulings of this date in one function so the composition order is stated once and cannot drift. Q1 is the base, next day anchor from the breach instant. RULING THREE takes the anchor AFTER the imminent one when the quote is frozen at the moment of breach, a full trading day, which is what stops the measured 62 minute pre anchor freeze of 2026-08-13 producing a lock that expires minutes later at the reopen. RULING FOUR floors the result at `AgNextDayAnchor` of Q8's high water latch, which never recedes, so a breach declared after a backward clock step cannot compute an already past expiry. THE 2026-07-30 CLAMP IS DELIBERATELY ABSENT from the computed path and present in the witness path, exactly as the precedence ruling of this date directs by giving each bound a domain; `AgLockedUntilFromWitness` applies clamp first as the upper bound and floor last as the lower bound, which is the mechanism by which the floor wins under a rewound clock. That witness helper is written now and is NOT REACHED in this stage, since no witness is read until Stage 4; it sits beside the computed one so the two domains cannot be re-derived differently later.

EXPIRY IS THE ONLY UNLOCK PATH and compares `TimeCurrent` per Q7. Nothing else leaves LOCKED: no input, no absence of evidence, no reconnect. A frozen or backward clock therefore delays an unlock, which errs locked and needs no special case, and the Friday breach that outlives its computed expiry by about 48 hours across the weekend freeze is that behaviour working rather than a defect.

THE FROZEN QUOTE MEASURE, and where it sits is a deliberate choice worth recording. It is a purely local tick age delta, never clock arithmetic, so it inherits nothing from the broker DST peg this ledger still has open until the October harvest. It lives in `OnTimer` and not inside `AgEvaluateActive` for two reasons: it must keep measuring in EVERY state including LOCKED, since ruling THREE reads it at a breach that may follow any state history, and putting it in the evaluation path would have broken the byte identical requirement for no gain. IT GATES NOTHING. The breach is declared from the frozen quote exactly as the stale quote ruling of this date requires, and the measure is read only afterwards to size the consequence, which is the boundary ruling THREE itself draws.

A DEFECT IN THIS STAGE, FOUND BY THE EXECUTOR BEFORE COMMIT AND FIXED, recorded because it would have been invisible until the first live breach. `g_ag_state_loaded` is set only by `AgStateLoad`, and Stage 3 as first written never called it, so `AgStateSave` would have refused on every breach under the never-loaded-never-written rule and the lock would have held in memory only, silently failing to persist. `AgStateLoad` is now called in `OnInit` and its result is DELIBERATELY NOT ACTED ON, which is the smallest correct fix: loading is what makes a later write legitimate, while acting on what the file says is Stage 4's boot derivation. A second defect was fixed the same way: the input change witness compared live inputs against values a fresh image has never set, so a Stage 4 boot restoring a lock from the file would have emitted a change WARN naming a change nobody made. It is now guarded by an explicit captured flag rather than by an accident of initialization.

ACCEPTANCE. Compile PASSES, `Result: 0 errors, 0 warnings, 784 ms elapsed, cpu='X64 Regular'`, `AccountGuardian.ex5` written 2026-08-17 22:24:45, size deliberately not quoted per standing rule 7. THE SCOPED DIFF PASSES and is the row the instruction named: the region of `AgEvaluateActive` from its opening brace to the Q9 coherence comment, which is everything above the breach tail, is BYTE IDENTICAL to HEAD at sha256 `0fe8e845d2777a6f0aa464c64309c9355ca9fa8d0a165de41790dc85a8924839`, 96 lines, compared as text rather than by eye. S1 and S2 rerun clean, one comment only hit in `Sweep.mqh`, so Phase 2 still contains zero trade API calls. Clock discipline rechecked line by line: `TimeLocal` appears in the new code only in the tick age measure and in the input change WARN cadence, both A1/A3 liveness class, and every anchor and expiry decision reads `AgServerNow` alone. CHANGES OUTSIDE THE PROTECTED REGION, listed rather than left for a reader to diff: the `#property description`, the new globals block, the five new functions, the breach tail itself, the `AgStateLoad` call in `OnInit`, the tick tracker in `OnTimer`, the LOCKED dispatch line, and the init line's build label moving `Phase1` to `Phase2`, that last one because standing rule 7 makes the journal the only measure of what is actually running and Phase 1 already had to fix this exact defect once.

SPEC AMENDMENT A7, DRAFTED AND NOT COMMITTED TO THE SPEC, per the instruction. It is recorded here because CLAUDE.md permits no fourth markdown file without explicit authorization, which is the same route the Phase 2 kickoff plan took. Section 2, the transition table, gains two rows: ACTIVE to LOCKED on a confirmed DAILY_BREACH after the Q9 deferral, and LOCKED to ACTIVE on expiry, with the From column for the LOCKED entry row left at ACTIVE only, since Stage 4's boot derivation will add SYNCING to LOCKED as a separate row rather than widening this one. Section 4.5 is rewritten from the Phase 1 interim posture to: a confirmed breach declares the lock, `locked_until` is computed as Q1's next day anchor, raised to the following anchor when the quote is frozen at the breach instant per ruling THREE, and floored at `AgNextDayAnchor` of the Q8 high water latch per ruling FOUR; a witness supplied value takes the 2026-07-30 clamp first and the same floor last; expiry is `TimeCurrent >= locked_until` and is the only unlock path. Section 3 gains the state file's field list exactly as design doc item 5 states it, with the note that the file carries no freshness field and no floating baseline, per the stale quote ruling and question FIVE of this date. Section 6 gains the LOCKED line's `waiting_on` string, which already reads `expiry: TimeCurrent >= locked_until`. Section 8 gains the Q6 sentence that an input change while locked is logged loudly and ignored. A7 is NOT written into `docs/SPEC_v0.1.md` and no session may cite it as applied until the owner rules the wording.

THREE THINGS RAISED AND NOT DECIDED, each with its own ISSUES entry: the frozen quote threshold, which ruling THREE did not name and which is implemented at 120 seconds carrying an explicit UNRULED marker; the window between this stage and Stage 4 in which a persisted lock is read at boot, reported, and NOT honoured; and the fact that the two bound helpers live in the EA rather than an include, so no script can call them and the highest risk arithmetic in this stage is not vector testable as it stands.

2026-08-18, STAGE 2 CLOSES ON A LIVE VECTOR RUN, `AGVEC|SUMMARY|42/42`, verified by the executor from the terminal's own journal and not from the owner's report, per the FINAL of 2026-08-05 which binds the owner's channels as well as the executor's. RULE A held: read only throughout, RULE B alias audit run first and recorded because the hazard is live, `rd`, `del`, `rm` and `ri` all resolving to `Remove-Item` and four of the seven AllScope, and every command used was a plain inline `Get-ChildItem`, `Get-FileHash` or shared read `FileStream` with no user defined helper anywhere in the call chain.

THE EVIDENCE, QUOTED. Artifact: `MQL5\Logs\20260817.log` in terminal `D0E8209F77C8CF37AD8BF550E51FF075`, read through a shared read handle because the running terminal holds it open. The stream reports 1588804 bytes where the directory entry reports 1574072, which is the 2026-08-16 stale metadata trap firing again in real time and is why the stream is the instrument; a second read minutes later returned 1590540, so a live log's own hash is stale the moment it is taken and the banked extract, not the source md5, is the stable artifact. Whole file counts: 70 AGVEC lines, 68 PASS, ZERO FAIL, 2 SUMMARY. The run itself, `AgPhase2StateVectors (XAGUSD.ecn,M5)`, spans 22:13:45.705 to 22:13:45.779 and contributes 49 lines, 42 PASS, 6 WARN and one summary, with zero FAIL. First line `AGVEC|v0_synthetic_login_range_cannot_hit_live_account|PASS`, last line `AGVEC|SUMMARY|42/42`. Banked to `docs/evidence/agvec-phase2-state-vectors-20260817.txt`, 76 lines, md5 5F87EAB8D6BE1A2F4294752839156FF6, which also carries the Phase 1 clock vectors' own `AGVEC|SUMMARY|26/26` at 22:09:25.614, an unplanned regression pass collected free in the same restart.

THE SIX WARN LINES ARE THE POINT OF THE RUN AND NOT NOISE, because they are the only runtime witness that the branches under test actually executed rather than merely compiling. In order: the never loaded guard refusing a write, "state file NOT written: the lock state model was never loaded this session"; a checksum failure quarantined as `state_990000002.dat.bad`; a SECOND checksum failure on the same login quarantined as `state_990000002.dat.bad.2`, which proves the non destructive quarantine deviation live and is the whole reason that deviation was taken; a login mismatch naming both sides, "carries login 990000004 but this account is 990000003"; a rejected format version; and a halt file placed at the state path being rejected. EVERY ONE of the five quarantines reports "locking via CORRUPT_STATE until 2026.08.18 01:00:00" against a server clock reading 2026.08.17 22:13:47, so Q1's fixed 01:00 offset and `AgNextDayAnchor` are now measured inside Phase 2 code rather than inherited from Phase 1's.

HOW IT RAN, ESTABLISHED RATHER THAN ASSUMED, AND IT MATTERS FOR SAFETY. The terminal's `MQL5\Include\AccountGuardian\Persist.mqh` is UNCHANGED at 13393 bytes, mtime 2026-08-04 14:12:46, md5 BA6AB1092B0A86979974ACECEC643BF0, which is the Phase 1 deployed copy and differs from this branch's 3D1AC16B73963AE509003A0EC808B44D. No Stage 2 source entered the terminal at all. What was deployed is ONE binary: `MQL5\Scripts\AccountGuardian\AgPhase2StateVectors.ex5`, 34252 bytes, mtime 2026-08-17 21:23:13, md5 16CCF9D22353911415C05B3538EA3536, BYTE IDENTICAL to the ex5 this branch compiled from commit `7856e9f`. Standing rule 7 says an ex5 hash is not build identity, and the asymmetry is used correctly here: a MISMATCH would have proved nothing, while an exact match can only mean this file was copied rather than rebuilt, so the run exercised code built from the committed source. THE CONSEQUENCE WORTH CARRYING: a vector acceptance needs one ex5 copy and no source deploy, the live guardian's own include tree is never touched, and all three of today's EA inits still log `build=Phase1`, so Stage 2 code is NOT in the running guardian and cannot get there without a deliberate recompile. That is a lighter and safer acceptance loop than Phase 1's full source deploy and Stages 3 and 4 can reuse it.

THE LIVE INSTANCE WAS CHECKED RATHER THAN ASSUMED UNDISTURBED. Three inits today at 22:09:04, 22:11:44 and 22:13:39, each `BOOT->SYNCING` then `SYNCING->ACTIVE|history stable|polls=3/3` within about three seconds, six transitions in three clean pairs, no SAFE_HALT anywhere, zero WARN from the EA itself, and the tail at 22:17:40 reading ACTIVE with `seconds_in_state=238` counting back exactly to the 22:13:42 entry. Those restarts are the terminal restart the dictation required, since a running terminal never enumerates a script added after it started. Recorded because it is one short of a trip and nobody should discover that later: three inits inside 300 seconds with adjacent gaps of 160 and 115 seconds would have reached `CrashLoopMaxInits=3` had any session been unclean, and none was, which is A7's proven clean reset behaving exactly as ruled. Today's single ALERT is the routine Monday anchor jump at 01:00:01, `jump=259200s`, unrelated to this work.

DATE NOTE, so the two do not look inconsistent to a later reader: the machine and server clocks read 2026-08-17 and every artifact above is quoted with its own stamps, while this ledger's entries for this working day are dated 2026-08-18 per the owner's convention. Nothing is reconciled or adjusted; the artifact stamps are the measurement and the entry date is the record.

STAGE 2 IS THEREFORE COMPLETE ON ALL THREE ACCEPTANCE ROWS: compile 0 errors 0 warnings with the Result line and ex5 mtimes quoted, S1 and S2 static rows clean, and all vectors PASS in one journal run. The ISSUES entry that carried the unrun row is closed and removed.

2026-08-18, PHASE 2 STAGE 2 IMPLEMENTED, THE LOCK STATE FILE FAMILY, ON BRANCH `worktree-phase2-lock`. First code written in Phase 2 and the first code of any kind since 2026-08-09. Branch created by `git worktree add` from local main at `06d92ee`, so nothing ran in the owner's own checkout; one code commit, `7856e9f`, 615 insertions across two files. Authorization was explicit and bounded: Stages 2, 3 and 4 only, Stages 5 and 6 waiting on a separate instruction, Stage 7 deploy NOT authorized, and a stop for owner review after each stage. This entry covers Stage 2 alone and no Stage 3 work was started. RULE A held throughout and is verified rather than asserted: the MetaTrader Terminal data folder was READ, never written, and the six files in its `MQL5\Include\AccountGuardian` still carry their 2026-07-30 to 2026-08-09 mtimes with nothing from today.

WHAT WAS BUILT. `Persist.mqh` gains the `state_<login>.dat` family per design doc item 5, reusing `AgAtomicWrite` and `AgChecksum` unchanged: `AgStatePath`, `AgStateSerialize`, `AgStateSave`, `AgStateLoad`, `AgStateResetModel`, `AgStateSetBreach`, `AgStateSetCorrupt`, `AgStateQuarantine`, `AgStateQuarantinePath`, and the model globals including `g_ag_state_loaded`. Magic is `AGSTATE`, deliberately distinct from `AGHALT` so the two formats cannot cross-read. Load returns 0 loaded, 1 missing, 2 corrupt, 3 login mismatch; 2 and 3 are handled identically per the 2026-07-30 FINAL and are distinguished only so the journal can say which happened. `MQL5\Scripts\AccountGuardian\AgPhase2StateVectors.mq5` is new, in the `AGVEC` contract the Phase 1 clock vectors established.

THE RULINGS EACH FUNCTION IMPLEMENTS, quoted at the point they bind rather than listed in a header: Q6 for the snapshot governing the locked window; never-loaded-never-written, which names the lock state file explicitly and is why `AgStateSave` refuses loudly rather than silently; the login mismatch FINAL of 2026-07-30; Q7 for `locked_until` and `breach_time`, with the A1 TimeLocal exemption explicitly stopped at the halt file and the mutex; and Amendment 2a keeping the file charter-constrained to lock state only, which is why it gains no SAFE_HALT field, no freshness field per the stale quote ruling of this date, and no floating baseline per question FIVE of this date.

TWO DELIBERATE DEPARTURES, both recorded because a later session must not read either as an accident. ONE, quarantine picks a FREE name rather than the design document's plain `.bad`. The document says to reuse the halt file's `FileMove` pattern; that pattern uses `FILE_REWRITE`, so a second corruption would overwrite the first quarantine, which the FINAL of 2026-07-29 forbids in terms, lock artifacts are never deleted, only quarantined. The FINAL outranks the document's convenience wording, so `.bad`, `.bad.2`, `.bad.3` accumulate and a vector asserts it. The halt file's own single-name path is untouched, being out of Stage 2's scope and not a lock artifact. TWO, money fields are stored at 8 decimals, not the cent. Base 1985.97 at the ruled five percent is 99.2985, which the banner prints as 99.30, and Q6 makes the snapshot the enforced limit, so storing the printed value would change enforcement on every restart. The 0.01 epsilon of 2026-07-30 is a comparison rule and not a storage format; a vector asserts the stored limit is NOT 99.30.

THE MEASUREMENT OF THE SESSION, AND IT CORRECTS THIS LEDGER: A WORKTREE COMPILE IS POSSIBLE AFTER ALL, WITH NO DEPLOY AND NO TERMINAL WRITE. The 2026-08-09 implementation entry records that MetaEditor's include resolution rooted against the deployed terminal tree and that no compile log could exist for that branch, and the Stage 5 deploy entry later explained the error 106 as "a consequence of compiling a file outside any MQL5 root". That explanation is INCOMPLETE, measured today in both directions. Compiling `AgPhase2StateVectors.mq5` from inside the worktree's own complete MQL5 root still resolved every include to `C:\Users\www83\AppData\Roaming\MetaQuotes\Terminal\D0E8209F77C8CF37AD8BF550E51FF075\MQL5\Include\AccountGuardian\`, the terminal data folder, and produced `Result: 101 errors, 12 warnings`, every error an undeclared identifier naming a Stage 2 symbol, because it had compiled the new script against the DEPLOYED Phase 1 `Persist.mqh` of 2026-08-04. So the include root follows MetaEditor's configured data folder and not the compiled file's tree. Adding `/inc:` pointing at the worktree's own `MQL5` directory fixes it completely: the log then names the worktree's own include paths and both compiles pass. This removes a limitation Phase 1 carried from Stage 2 to Stage 5 and it means every future Phase 2 stage can bank a real compile result before any deploy exists.

ACCEPTANCE, STATED HONESTLY AND NOT ALL OF IT PASSES. Compile row PASSES: `AgPhase2StateVectors.mq5` gives `Result: 0 errors, 0 warnings, 770 ms elapsed, cpu='X64 Regular'` with `AgPhase2StateVectors.ex5` written at 2026-08-17 21:19:26, and the EA itself, recompiled because `Persist.mqh` gained two includes, gives `Result: 0 errors, 0 warnings, 1072 ms elapsed` with `AccountGuardian.ex5` at 2026-08-17 21:19:57. Per standing rule 7 the ex5 mtimes are quoted as build identity and the sizes deliberately are not. Per standing rule 6 the process exit codes were read and discarded, 0 for the failing run and 1 for the clean one, inverted exactly as that rule records. S1 and S2 RERUN CLEAN: a tree-wide grep for OrderSend, CTrade, PositionClose, OrderDelete and Trade.mqh returns exactly one hit in one file, the static-structure rule comment inside `Sweep.mqh`, so the build still contains zero trade API calls, which is the baseline Phase 2 must leave untouched since sweep is Phase 3. A third static row was added and passes: zero TimeLocal, TimeGMT or TimeTradeServer appear anywhere in the state family's code, the single textual hit being the comment that warns against exactly that. THE VECTOR RUN ROW DOES NOT PASS AND COULD NOT: "all vectors PASS in one journal run" needs the script inside the terminal's Scripts folder, that copy is a write into the Terminal data folder which RULE A forbids the executor and which ruling ONE assigns to the owner at the executor's dictation, and the deploy stage is explicitly not authorized this session. The 42 vectors have therefore been COMPILED and NEVER EXECUTED, which is recorded as its own ISSUES entry rather than left implicit.

ONE CORRECTION TO THIS SESSION'S OWN COMMIT MESSAGE, recorded here rather than by rewriting an existing commit: `7856e9f` says the vector script carries 40 checks. It carries 42, counted from the source rather than from memory, one interlock vector plus 41. The message is left as written and this line is the correction, which is the same discipline the provenance FINAL of this date requires.

2026-08-18, THE FLOOR VERSUS CLAMP PRECEDENCE IS RULED AND THE LAST DERIVED QUESTION FROM THE KICKOFF IS CLOSED. Ledger only: no source file was written, no compile was run, nothing was deployed, and nothing under the MetaTrader Terminal data folder was read, written or approached, per RULE A. THE FLOOR WINS. When Q8's latch next day anchor is later than `AgNextDayAnchor(AgServerNow())`, `locked_until` takes the floor value. The ruling settles it by giving each bound a domain rather than by ordering them: the 2026-07-30 clamp keeps its full force as an upper bound on WITNESS SUPPLIED values against a forged or inflated input, and the question FOUR floor governs the guardian's own COMPUTED value plus any witness value that would otherwise fall below it. Recorded FINAL in DECISIONS above the question FOUR entry it completes, and the ISSUES entry that carried it is closed and removed. Two dependent lines were corrected to match rather than left stale, which is the kind of thing that rots quietly: the question FOUR entry's own text already flagged this as raised and not ruled and now points at the answer, and the kickoff questions entry no longer claims a derived question is open. The derived composition order was written into the ruling because Stage 5 will need it in that exact form, clamp first as the upper bound and floor second as the lower bound for a witness value, floor alone for a computed one, which follows from the ruling rather than adding to it. Nothing from the Phase 2 kickoff is now open except question EIGHT, the symbol specification tables, which is an owner action at the terminal. IMPLEMENTATION REMAINS UNAUTHORIZED and no instruction to build has been given.

2026-08-18, PHASE 2 OPEN QUESTIONS THREE THROUGH SEVEN ARE RULED FINAL AND ONLY EIGHT REMAINS OPEN. Ledger only, as every session of this date has been: no source file was written, no compile was run, nothing was deployed, and nothing under the MetaTrader Terminal data folder was read, written or approached, per RULE A. Five FINAL entries added to DECISIONS, THREE the anchor straddle floor, FOUR the `locked_until` floor at Q8's latch, FIVE the total exposure framing, SIX observability inside Phase 2 with Stage 6 authorized, SEVEN the ratchet ratified with Stage 5 existing. EIGHT stays in ISSUES because it is an owner action at the terminal and not a ruling, and it is now the only item left from the kickoff. Two other ISSUES entries move with these rulings: the carry drag entry, open since 2026-08-14 and scoped FINAL on 2026-08-17 with its framing left unruled, is closed by ruling FIVE and marked RULED in place with the chosen framing and its accepted consequence spelled out; and the eight questions entry is rewritten to carry only EIGHT. Numbering was preserved throughout the whole sequence and nothing was ever renumbered as questions closed, so every earlier reference by number still resolves. WHAT WAS VERIFIED RATHER THAN TAKEN ON TRUST, because ruling SEVEN ratifies a mechanism by citation and a FINAL entry that cites something unresolvable is the exact failure this ledger ruled on earlier today: `AgAtomicWrite` exists at `MQL5/Include/AccountGuardian/Persist.mqh:67` on main; the ratchet proposal exists on main in `docs/DESIGN_PHASE1_2.md` under `Active-state limit inflation: ratchet defense` in the 2026-07-30 addendum, carrying the single scalar `floor_currency`, the `floor_<login>.dat` file, the rollover reseed and the pre breach only application in those words. TWO DISCREPANCIES FOUND AND RECORDED IN THE RULING ITSELF RATHER THAN SMOOTHED OVER, neither of which changes what was ruled, since the owner enumerated all five elements explicitly. First, the ruling cites "section 6 of the kickoff plan" and no such file exists: the Phase 2 kickoff plan was never captured to a file, by an earlier deliberate decision recorded in this ledger, so the citation resolves to the design document section named above and to nothing else. Second, NO RESEED ON A BACKWARD STEP appears nowhere in that section, which is silent on backward clock movement entirely, so that element is an addition ruled here for the first time rather than a ratification of existing text. ONE NEW OPEN ITEM RAISED, not ruled and not guessed at: ruling FOUR's floor and the 2026-07-30 clamp point in opposite directions under a rewound clock, which is the only condition FOUR was made for, and neither FINAL entry says which wins. It has its own ISSUES entry. IMPLEMENTATION REMAINS UNAUTHORIZED and the owner's message says so in terms; that Stage 5 and Stage 6 now exist and are defined is not an instruction to build them, and none has been given.

2026-08-18, PHASE 2 OPEN QUESTIONS ONE AND TWO ARE RULED, BOTH FINAL IN DECISIONS, AND SIX REMAIN. Ledger only, as every session of this date has been: no source file was written, no compile was run, nothing was deployed, and nothing under the MetaTrader Terminal data folder was read, written or approached, per RULE A. ONE, the RULE A deploy collision, is closed by moving the hand rather than the rule: the owner performs every copy into the Terminal data folder while the executor dictates source file, destination path and md5 per file, and RULE A is explicitly NOT amended. TWO, the Phase 2 interim posture, is sanctioned: the build locks the state machine and sends no order, a real breach shows LOCKED with Alerts while positions stay open until the owner closes them by hand, and sweep, flatten and pending order deletion stay in Phase 3. The eight questions entry in ISSUES is updated to six, with ONE and TWO summarized as closed and pointed at their DECISIONS entries. THE REMAINING SIX ARE DELIBERATELY NOT RENUMBERED and the list now starts at THREE, because earlier entries of this date cite "question seven" for the ratchet mechanism by number and renumbering would silently break every one of those references. Consequence of these two rulings on what is now blocking, stated so no later session infers more than was ruled: the deploy stage and the enforcement window are unblocked, question seven remains the only one of the six that gates a stage, and IMPLEMENTATION IS STILL UNAUTHORIZED. Both rulings remove blockers; neither is an instruction to begin building, and none has been given.

2026-08-18, THE UNPUSHED LOCAL HISTORY IS REWRITTEN SO `CLAUDE.md` NEVER ENTERED IT, route one of the three named in the ISSUES entry this closes, authorized by the owner. HASH MAPPING, and it is the first thing recorded because every earlier entry of this date cites the old hashes and those objects no longer exist: `b4e5da2` became `9f494bc`, `476d42b` became `22f1a7a`, and `4e42fce` and everything below it are untouched. Any reference to `b4e5da2` or `476d42b` anywhere above in this ledger, including inside the FINAL reversal entry in DECISIONS, now names a commit that cannot be looked up; the mapping here is the only way to resolve them and the entries themselves are left as written, since ACTIONS is append only and DECISIONS is locked. METHOD, stated because it is not what the instruction literally named: `git rebase -i` is unavailable in this environment, so the equivalent was done with `git filter-branch --index-filter 'git rm --cached --ignore-unmatch CLAUDE.md'` over the range `4e42fce..HEAD`, which drops the file from every tree in the range rather than editing one commit by hand. Both commits survive as commits and neither went empty, since each still carries its `.gitignore` and `LEDGER.md` changes. VERIFIED against baselines captured before the rewrite, not by eye: `LEDGER.md` at the new HEAD is byte identical to the old HEAD, sha256 `75992de7d7fc7d12fc02b0cda028b9b75ff072bcc4bd301e1341a1a1b56ef1b2`, 952 lines; `.gitignore` is byte identical, sha256 `c9c0f902405f1295fa1de5da2c20d782d2eaf515d0113e1bcbf732c446c0cf1f`, 37 lines with `# ccode-managed` first; `git log --all -- CLAUDE.md` returns nothing; and the working copy of `CLAUDE.md` is untouched at sha256 `dfc7329cc9288d9f9636da04d2488f5f67f4266786f3efdfaff15d6eac9e1772`, 4548 bytes, with its original mtime. THE PURGE WAS A SECOND STEP AND WITHOUT IT THE REWRITE WOULD HAVE BEEN COSMETIC: `filter-branch` keeps the original history at `refs/original/refs/heads/main`, which `git log --all` still reads, so that ref was deleted, all reflogs were expired with `--expire-unreachable=now`, and `git gc --prune=now` was run. Confirmed by object lookup rather than by log output: `git cat-file -e` on both old commits now fails with `Not a valid object name`, so the blob carrying the protocol text is gone from this repository rather than merely unreferenced. COST, recorded so it is not discovered later as a surprise: expiring all reflogs removed the undo history for every branch in this repository, not just for this rewrite. No work was lost, since every other branch is still reachable through its own ref, but the safety net that would let a future session recover an accidental reset is empty and will refill only as new operations happen. Nothing was pushed, and no force push was performed or attempted.

2026-08-18, `CLAUDE.md` IS UNTRACKED AGAIN, REVERSING THE TRACKING CHANGE MADE EARLIER THE SAME DAY. The owner unlocked the FINAL tracking entry with the explicit `unlock decision` phrase and the entry name, which is the only route protocol rule 5 allows, and the reversal is now FINAL in DECISIONS above the entry it reverses, with that entry left intact. What was done: `!CLAUDE.md` removed from `.gitignore`, the `# ccode-managed` sentinel left as the first line, and `git rm --cached CLAUDE.md`. Verified rather than assumed, since `git rm` is the one step here that could have destroyed the file: the working copy is byte identical across the change, sha256 `dfc7329cc9288d9f9636da04d2488f5f67f4266786f3efdfaff15d6eac9e1772` before and after, 4548 bytes, mtime unchanged, and `git check-ignore` now matches it against `*.md` again with no negation. RECOVERABILITY, which is the whole reason the tracking ruling existed and is now recorded so no future session reopens the question on that ground: the owner's profile copies `CLAUDE.md` to `~/.claude/backups` on every launch, so the file survives an overwrite or a lost working copy without git holding it. FLAGGED, not acted on, and it is the one loose end this reversal does not close by itself: `CLAUDE.md` remains in this repository's history at commit `b4e5da2`, where it was first added, and `git rm --cached` removes it from the index and from future commits only. Nothing has been pushed, so the file is not public yet and the reversal is fully effective as long as that stays true, but a push of the current history would publish it despite this ruling. Two ways out exist, dropping or rewriting that commit, and both rewrite history, so neither was performed on the executor's own initiative. Owner's call.

2026-08-18, `CLAUDE.md` IS TRACKED, `.gitignore` IS DEDUPLICATED, AND THE ROOT CAUSE OF THE DUPLICATION IS NAMED. Owner ruled tracking, FINAL in DECISIONS of this date, and the ISSUES entry that raised it is closed by that ruling. `CLAUDE.md` is committed for the first time; the proof it is genuinely no longer ignored is that `git add` accepted it without `-f`, which git refuses for an ignored path. `NOTES.md` was probed as a control and is still ignored, so the blanket rule still does its job and only the one negation changed. ROOT CAUSE OF THE DUPLICATION, supplied by the owner and recorded because nothing inside this repository could have shown it: the managed block in `.gitignore` was missing its sentinel first line, and the owner's profile re appends the whole block on every launch when the sentinel is absent, so each launch added another copy. Fixed by the owner outside this repository. The repository side of the fix is the sentinel itself, now the first line of the block and of the file, exactly `# ccode-managed`, which is what stops the next launch from appending a seventh copy. `.gitignore` went from 186 lines to 38. Verified by set comparison rather than by eye: not one ignore rule was lost, the only rule added is `!CLAUDE.md`, and the union of everything the six copies carried is preserved, including the two blocks that existed in only one copy each, the `.claude/` local tooling scaffolding and the `/Monitor_Plan.html` root duplicate. The old descriptive comment `# MD files stay local except README, versioned docs, and the governance record` was dropped, since the sentinel must be the block's first line and an externally managed block is not the place for decoration; its content is preserved by this sentence and by the DECISIONS entry. CORRECTION TO THIS SESSION'S OWN EARLIER ENTRY, appended rather than edited since ACTIONS is append only: the `.gitignore` correction entry below states the block appears three times, at lines 99, 129 and 159, and the ISSUES entry closed today repeated that number. IT WAS SIX, at lines 1, 38, 68, 98, 128 and 158. The wrong count came from reading a `tail -20` of grep output that clipped the first half of the file and then reporting the visible three as the total, which is the same class of error as the provenance defect ruled on today: a partial reading stated as a complete one. The count is corrected here and the dedup was performed against the real six.

2026-08-18, THE THREE LEGACY MANAGEMENT FILES ARE CONSOLIDATED TO ONE LINE AND DELETED, per the owner ruling of this date. The preserved line, and it is the whole of what survives them: the token usage tracker was installed on 2026-08-02 and ruled removed on 2026-08-17. No FINAL is carried across, and the 2026-08-02 entry that marked the tracker design FINAL is deliberately not migrated, because the 2026-08-17 owner ruling in DECISIONS supersedes it and two conflicting FINALs on one subject is exactly the failure the ledger exists to prevent. `STATUS.md`, `PROGRESS.md` and `DECISIONS.md` were then deleted from the workspace root. Recorded because git cannot witness it: all three were untracked and ignored by the `*.md` rule in `.gitignore`, so the deletion appears in no diff and this line is the only record that they existed and are gone. Protocol rule 5 is now satisfied and no legacy management file remains in the root. Unchanged by this and still open, the badge writer itself, which lives outside this repository and keeps its own ISSUES entry. One thing was carried out of the removed entry rather than deleted with it: the observation that the same `*.md` rule leaves `CLAUDE.md` untracked, which is unrelated to the three files and was never ruled, now stands as its own ISSUES entry. It is split out because the `.gitignore` correction entry of this date says it is recorded in ISSUES, and closing its host entry would otherwise have made that line false.

2026-08-18, THE PROVENANCE CORRECTION IS AUTHORIZED AND MOVES FROM ISSUES INTO DECISIONS AS FINAL. The owner authorized it explicitly after the executor raised the defect, declined to write it into DECISIONS unratified, and waited. The entry now sits at the head of DECISIONS, immediately above the `f814df2` stale quote ruling it corrects, and carries the standing rule that no session may cite its own draft as an already-given ruling. The `f814df2` entry itself was not altered and not struck, verified rather than asserted: the DECISIONS text as committed at `f814df2` is byte identical under `diff` to the same range at HEAD, and every commit this session was insertion only against that section. The substance of the stale quote ruling is untouched and is not reopened. The corresponding ISSUES entry is closed and removed from ISSUES by this move, which is where it went and why it is no longer listed there.

2026-08-18, CORRECTION APPENDED TO THE MERGE ENTRY BELOW, ON THE `.gitignore` OBSERVATION ONLY. Appended rather than edited in place, since ACTIONS is append only. The entry below says the working copy of `.gitignore` was modified before the session and describes the duplication as an uncommitted working copy state. First half accurate at the time it was written, `git status` returned ` M .gitignore` twice, before the merge and again immediately after it, and the diff was a 30 line block appended past line 186. What is wrong is the implication that the duplication lives only in the working copy. Measured after the commits: the file tracked at HEAD is itself already duplicated, the block that starts `# MD files stay local except README, versioned docs, and the governance record` and its `*.md` rule appear at lines 99, 129 and 159, three copies committed, and the working copy had briefly carried a fourth. Second correction, and it is the one worth flagging: the working copy modification disappeared during the session without any executor action. `git diff --stat .gitignore` now returns empty against HEAD and the file is back to 186 lines. The executor ran no `checkout`, no `restore`, no `stash` and no `reset`, staged only LEDGER.md at each of the two commits, and cannot account for the revert; the plausible causes are another process holding the main checkout or the owner's own edit being undone outside this session, and neither was verified, so neither is claimed. The `*.md` rule and its effect on CLAUDE.md is unchanged by any of this and is recorded in ISSUES. Nothing in `.gitignore` was written by this session.

2026-08-18, MERGE OF THE PHASE 2 KICKOFF LEDGER AND A PROVENANCE CORRECTION RAISED AGAINST IT. Ledger only again: no source file was written, no compile was run, nothing was deployed, and nothing under the MetaTrader Terminal data folder was read, written or approached, per Rule A. `git merge --no-ff worktree-worktree-phase2-kickoff-ledger` was run from the main checkout and brought in the two ledger commits `f814df2` and `c7030a4`, merge commit `cef9127`, 26 insertions and 1 deletion in LEDGER.md, no conflicts. The executor performed no push, so per the FINAL remote state ruling of 2026-08-04 no claim is made here about where the remote stands. The working copy of `.gitignore` in the main checkout was already modified before this session and was deliberately left untouched and unstaged; for the record it duplicates the last five blocks of the file, including an `*.md` rule with negations for README.md, docs and LEDGER.md, which as written also ignores CLAUDE.md. Raised as an observation, not fixed, not ruled. Second half of the session: the owner raised that the DECISIONS entry written at `f814df2` misstates its own provenance, since it claims the ruling was recorded at the kickoff before any design work while the kickoff instruction had in fact ordered the executor to surface the question and wait, and the owner's confirmation of the content arrived in a separate channel after the entry already existed. The correction was recorded as an OPEN ISSUES entry awaiting owner authorization rather than written straight into DECISIONS, because DECISIONS carries ruled entries only. The original FINAL entry was not altered and not struck, and its substance is not in question. Naming the ambiguity in the instruction rather than hiding the choice: the instruction opened by saying to append the correction to DECISIONS and then specified a DECISIONS-adjacent ISSUES entry with status OPEN and owner to authorize; the second reading was taken.

2026-08-18, PHASE 2 KICKOFF, a planning session only. No source file was written, no compile was run, nothing was deployed, and nothing under the MetaTrader Terminal data folder was read, written or approached, per Rule A. Work happened on branch `worktree-worktree-phase2-kickoff-ledger`, created by `git worktree add` from local main and re derived this session rather than carried: `git rev-parse` returns `c7170a809833ebd44cbdb482b6077c39a5f3ff7b` for HEAD, for main and for the local ref origin/main alike, so the branch point equals main's head and no second side exists yet. Two commits, both ledger only. Nothing is merged and the executor performed no push; per the FINAL remote state ruling of 2026-08-04 no claim is made here about where the remote stands.

Commit one, `f814df2`, records the owner ruling of 2026-08-18 as FINAL in DECISIONS and updates the stale quote ISSUES entry to RULED. The ruling goes against the four part mechanism this ledger has carried as a proposal since 2026-08-14. Enforcement is not gated on quote freshness while the terminal is connected, the connection state is the entire boundary, and the mechanism survives as an observability proposal that may log but may never gate. The Phase 2 consequence recorded with it: a Q6 breach snapshot taken from a floating leg priced off a frozen quote is valid by design, so the state file gains no freshness field.

Investigation banked with this entry, and the reuse surface below is RE DERIVED BY GREP AND READ THIS SESSION rather than quoted from the kickoff narrative, because the 2026-08-05 rule binds an entry to what its own session measured. `AgAtomicWrite(const string path, const string content)` is at Persist.mqh:67 and its single existing caller is Persist.mqh:107, `return AgAtomicWrite(AgHaltPath(), AgHaltSerialize());`, which is the halt file path the state file will reuse unchanged. `AgEvaluateActive()` opens at AccountGuardian.mq5:118 and its Q10 connection gate is the third line of it, AccountGuardian.mq5:121, `if(!(bool)TerminalInfoInteger(TERMINAL_CONNECTED))`, with the same guard repeated at Pnl.mqh:178. The dispatch tail reads exactly as the Phase 1 close described it: the SAFE_HALT early return sits at AccountGuardian.mq5:405 to 406 with the comment "closes nothing, sweeps nothing, no expiry", ACTIVE dispatches to `AgEvaluateActive()` at :422, and the LOCKED case at :423 to :426 is an empty block whose only content is the comment "Phase 2 adds expiry and lock-witness reconciliation here." So the branch Phase 2 fills is present, empty, and positioned after the halt return and beside the SYNCING and ACTIVE branches rather than inside them. `AgNextDayAnchor` is a one line function at Clock.mqh:45, `return AgDayAnchor(t) + 86400;`, which is the whole of the Q1 expiry arithmetic. `AG_STATE_LOCKED = 3` is declared at State.mqh:16 with its name string at :39 and its waiting_on case at :101, so the state exists and renders today with no code able to enter it.

The S1 and S2 static rows were re run in the same pass and still hold, which matters because Phase 2 is the first phase that could break them. A tree wide grep for OrderSend, CTrade, PositionClose, OrderDelete and Trade.mqh over MQL5 returns exactly ONE occurrence in ONE file, Sweep.mqh, and reading it shows the hit is the static structure rule in the file's own header comment, "this is the ONLY file in the project permitted to reach the trade API", not a call. The build therefore still contains zero trade API calls, which is the baseline Phase 2 must leave untouched since sweep, flatten and pending deletion are Phase 3.

Expiry arithmetic under the measured clock regimes, assembled from artifacts already on this ledger rather than measured again today, and stated here because Phase 2 is where each one starts to matter. A Friday breach sets locked_until to Saturday 01:00, but TimeCurrent freezes at Friday 23:57:59 for the whole weekend, measured most recently across the 2026-08-15 and 2026-08-16 files, so the comparison stays false until the Monday reopen steps the clock and the lock outlives its computed expiry by about 48 hours. That errs locked and is acceptance row P2-I rather than a defect. The nightly break straddle is the one that produces a NEW question: 62 continuous minutes of frozen TimeCurrent while connected were measured on the night of 2026-08-13 into 2026-08-14, the freeze straddles the 01:00 anchor, and a breach evaluated inside it, now valid under today's ruling, computes locked_until as the anchor of the day that is about to end, so the lock expires minutes later at the reopen and the new day opens carrying the same losing position. Backward clock steps are the mirror hazard: a 25 hour rewind was measured 2026-08-01, and while locked a rewind only delays expiry, which errs locked, but a breach declared AFTER a rewind computes an already past locked_until, and the clamp ruling of 2026-07-30 bounds locked_until from above only. Expiry cannot fire at all while disconnected, since TimeCurrent does not advance, which errs locked by construction and needs no new rule.

Governance collision found at kickoff and recorded because it blocks a stage rather than a decision. RULE A of 2026-08-17 makes the Terminal data folder read only for every executor session, and every prior deploy had the executor copy sources into the terminal tree and vector files into Presets. Those copies are now prohibited executor writes, so Phase 2 cannot deploy and its vector scripts cannot reach the terminal until the owner either performs each copy at the executor's dictation or amends RULE A with an explicit deploy carve out. This blocks nothing before the deploy stage and it is NOT proposed as an unlock of anything; the executor names it and stops.

Not measurable yet and flagged as exactly that, so no later session reads silence as absence. The broker DST peg harvest opens 2026-10-25 and may land inside Phase 2 implementation, with the Q1 REVISIT riding on it. Whether MQL5\Files\AccountGuardian survives a terminal update rests on ONE data point, the halt file surviving build 6061 to 6090, which is a data point and not proof. The live breach rows all need manual losing trades, the same events P1-I, P1-L and P1-N still wait on, so one trading session can feed both row sets. The Q7 symbol specification tables are STILL unread and still carry the A8 corroboration debt, inherited from Phase 1 and now carried by Phase 2.

Not done and deliberately so: the kickoff plan itself was NOT captured to a file. Its capture was made conditional on explicit owner authorization, that authorization was not given, and CLAUDE.md permits no fourth markdown file without one. Everything from the plan that binds a later session, the ruling, the investigation and the open questions, is therefore in this ledger and nowhere else, which is where the protocol says the roadmap lives anyway.

2026-08-17, fourth entry, the housekeeping branch merged to main and pushed, closing the block the third entry opened. Merge commit `dd4727b19f4f431103a130b38db76e522b09a582`, created with `git merge --no-ff`, two parents `0babf84` and `1cb3c57`, carrying the two branch commits `b8678f1` and `1cb3c57`. Two files changed, 22 insertions and 11 deletions, LEDGER.md and README.md. INSIDE the merge gate of 2026-08-03 rather than outside it, and stated that way deliberately because the four merges before this one were all owner ordered exceptions: the branch carried no unfinished item, both of its commits are complete work with their evidence quoted in the third entry, so the all of it clause is satisfied on the branch's own proofs and the owner instruction the gate also requires was given.

Verification, all re derived against the merged tree rather than against this narrative. The merge conflicted on nothing, which is structural rather than lucky: main had not moved since the branch point, so the merge base equalled main's head and there was no second side, and no hand union was needed on LEDGER.md. `git diff --stat main worktree-post-phase1-housekeeping` returns empty, so main's tree is byte identical to the branch. Conflict markers across ALL 73 tracked files, read file by file rather than sampled: zero. Attribution scan over `0babf84..main` for co-authored, generated with, anthropic, ai assistant and claude: zero on all five, single author www8351. README.md opens on `<div align="center">` with the token block gone. Ledger section counts moved 61, 67 and 128 to 62 Issue blocks, 69 Decision blocks and 131 Status lines, which is the one new ISSUES entry, the two new FINAL decisions and their three Status lines and nothing else.

Recorded because it did NOT recur and both prior occurrences are on the record as unexplained: the merge commit was produced by this session's own `git merge`, carrying the full message this session composed rather than git's bare default MERGE_MSG. The 2026-08-08 merge `937c604` and the 2026-08-11 merge `d52f063` were both committed by something else while MERGE_HEAD existed, losing their composed messages. The window was deliberately closed this time by merging with `--no-ff -F <message file>` in one command instead of `--no-ff --no-commit` followed by a separate commit, so no prepared index sat uncommitted for a periodic committer to take. That is a mitigation and not a diagnosis; the cause of the two earlier occurrences is still not established.

PUSH PERFORMED, the second of this session, on the same owner authorization. Command: `git push origin main`. Output quoted in full: "To https://github.com/www8351/Account_Guardian_V2.git" followed by "   0babf84..dd4727b  main -> main", exit 0. Per the FINAL remote state ruling of 2026-08-04 this is a dated observation and nothing more: at 2026-08-17, immediately after this push, local `main` read `dd4727b19f4f431103a130b38db76e522b09a582`. No claim is made about where the remote stands now, and this line is stale the moment either ref moves. This entry itself lands after that push and therefore rides a third one, which is the same shape as the 2026-08-17 second entry and its `0babf84` record.

One observation the executor cannot explain and is recording rather than resolving, because the 2026-08-05 rule binds it to report what it measured and nothing more. At the start of this session `git status --porcelain` in the main checkout reported one modified file, ` M .gitignore`, the owner's uncommitted work that the 2026-08-17 second entry also recorded and deliberately left alone. At merge time the same command reported a CLEAN tree, `git diff` against HEAD on that path returns empty, and the file's last write time reads 2026-08-17 11:24:51, which falls inside this session. The executor never staged it, never reverted it, never checked it out and never wrote to it; no command run this session names that path. So the file now byte matches the committed version and whatever uncommitted variant stood there this morning is gone. Candidate, named as a candidate and not as a finding: `Initialize-GitWorkspace` in the owner's PowerShell profile rewrites `.gitignore` on every workspace init, which the 2026-08-08 and 2026-08-11 entries both record it doing, and a rewrite that happens to reproduce the committed content exactly would make the modification disappear without anyone reverting anything. Nothing in the repository was lost, since the tracked content is unchanged; what may have been lost is an owner edit that was never committed.

2026-08-17, third entry, post Phase 1 housekeeping on branch `worktree-post-phase1-housekeeping`, created from local main at `0babf84` by `git worktree add` so nothing ran in the owner's own checkout. Three items in the order the owner gave them: the outstanding push, the README token block ruling, and this ledger sync. State re-derived before anything and quoted rather than carried from the previous session's entry: local main `0babf84848402c8b89a7f3a93712452d67ba7f30`, branch main, `git status --porcelain` reporting one modified file, `.gitignore`, which is the OWNER's uncommitted work and was left untouched, never staged and never reverted, and which stays in the main checkout rather than following the worktree.

THE PUSH SUCCEEDED. The 2026-08-17 second entry recorded it as attempted and not performed, denied by the executing environment's permission classifier. Re-authorised by the owner and run again this session, it was not denied, no workaround was used and none was needed. A fetch was run first because a local remote tracking ref proves nothing until refreshed, and it observed `origin/main` at `0eb13cf`, the same value the previous entry recorded as outstanding, so nothing had moved the remote in between. Command: `git push origin main`. Output quoted in full: "To https://github.com/www8351/Account_Guardian_V2.git" followed by "   0eb13cf..0babf84  main -> main", exit 0. That carried the three badge commits, the Phase 1 merge `112db9c`, the README conflict marker fix `ca27980` and the ledger record `0babf84`. Per the FINAL remote state ruling of 2026-08-04 this is recorded strictly as a dated observation and never as a standing property: at 2026-08-17, immediately after the push, `git rev-parse origin/main` and `git rev-parse main` both returned `0babf84848402c8b89a7f3a93712452d67ba7f30`. That is what two refs said at one instant. It is stale the moment either ref moves and no claim is made here about where the remote stands now.

README TOKEN USAGE BLOCK REMOVED, owner ruling, FINAL in DECISIONS of this date. Commit `b8678f1`, eleven lines deleted and nothing else changed in the file: the `<!--TOKENS-->` opener, the dev tool usage header, the totals line, the five row model table, the `<!--/TOKENS-->` closer, and the blank line that followed. The file now opens on `<div align="center">`. Verified after the edit rather than assumed, and the checks were chosen because this project has been bitten by each of them before: the file carries no BOM, first bytes `3C 64 69`; line endings are unchanged CRLF, 714 carriage returns against 714 line feeds where the pre edit file read 725 and 725; zero occurrences of the string TOKENS survive anywhere in it; zero conflict markers; 6178 Hebrew characters, so the bilingual structure is intact and the decoder demonstrably read the file, per standing rule 5; and zero em dashes, the one the badge header carried having left with it.

The badge writer was found, it lives OUTSIDE this repository, and the executor stopped there rather than disabling it, per the instruction. Full location, mechanism and the two disable routes are in the ISSUES entry now at the top of this ledger, so they are not duplicated here. The one fact worth repeating in the dated log because it explains the incident the previous entry recorded: `Update-Readme` in `C:\Users\www83\.claude\token-tracker\Update-TokenReport.ps1` PREPENDS the whole badge when the delimiters are absent, at line `:203`, and only replaces in place when they are present, at `:199`. Deleting the block by hand is therefore the exact input that makes the job re author it above whatever remains, which is how a hand deletion and a bot rewrite came to be committed together as conflict markers. Repository side searched and clean: no `.github` directory exists, `.git/hooks` holds only the 212 byte `commit-msg` attribution stripper plus the stock samples, and the owner's PowerShell profile writes no badge. Nothing under the MetaTrader Terminal folder was touched, read or approached at any point in this session, per Rule A of 2026-08-17.

THE GATEGUARD FACT FORCING HOOK IS A PROJECT TOOL FEATURE AND IS DELIBERATELY KEPT ON, stated by the owner and recorded FINAL in DECISIONS of this date so no future session reports it as a defect, works around it, or turns it off. It fired twice this session and both firings are recorded as normal operation rather than as friction: once before the first edit to README.md and once before the first Bash command, each refusing the operation until the executor presented the facts it names, importers of the file, affected public symbols, the data structure and date format written, and the user's instruction quoted verbatim. Both times the facts were presented and the same operation then succeeded unchanged. The recovery hint the hook prints, setting `ECC_GATEGUARD=off` or adding `pre:edit-write:gateguard-fact-force` to `ECC_DISABLED_HOOKS`, is NOT to be used in this project by any session, and it is written out here rather than left unnamed precisely so that a later session reading the hook's own output does not treat the escape as sanctioned. The value it delivered this session is concrete rather than notional: answering its first question is what put the executor into `Update-TokenReport.ps1` far enough to find the prepend branch at `:203`, which is the fact that turns "delete the block" into "delete the block and disable the job".

2026-08-17, second entry, the owner ordered merge of the Phase 1 evidence branch into main. Merge commit `112db9c`, created with `git merge --no-ff`, two parents `1aa9a15` and `aedbc61`, no rebase and no side picking. Sixteen files changed, 9472 insertions, the one deletion being main's own README line. Authorised by explicit owner instruction, which the merge gate ruling requires, and merged on the branch's own proven work, which that ruling also requires.

The union resolve the owner anticipated was NOT NEEDED, and that is recorded rather than glossed because the instruction expected one per the `ce6964d` precedent. Re derived before merging rather than assumed: the merge base is `0a49130`, and since that point main had touched exactly ONE file, README.md, from the badge bot commits, while the branch had touched LEDGER.md and fifteen files under docs/evidence. Zero overlap, so git merged clean with no conflict and nothing to resolve on LEDGER.md at all. Verified afterwards rather than argued: LEDGER.md on main byte compares IDENTICAL to LEDGER.md on the branch, so nothing was dropped, with 61 Issue blocks, 67 Decision blocks and 128 Status lines on both sides.

One thing the verification DID find, unrelated to this merge and pre existing on main: README.md carried three live git conflict markers and an empty alternative at the top of the file, committed in `0eb13cf`. This merge never touches README.md, so the markers were already sitting on main and would have been pushed to a public repository in that state. Hand resolved at commit `ca27980`. The HEAD side carried the token usage block and the other side carried an empty line; the block is live content, since the badge job had kept rewriting it as recently as the same day despite the markers above it, so the block was kept and the markers and the empty alternative dropped, 729 lines to 725, with the `<!--TOKENS-->` delimiters preserved exactly so the badge job keeps matching. No other content in that file changed.

Post merge verification, all re derived: conflict markers across every tracked file, zero. Attribution scan over the full merged range `0eb13cf..main` for co-authored, generated with, anthropic, ai assistant and claude code, zero on all five, single author www8351. SPEC_v0.1.md carries A5 five times and A6 fourteen times. 39 files present under docs/evidence on main. Working tree carries one modification, `.gitignore`, which is the OWNER's uncommitted work and was deliberately left untouched, never staged and never reverted.

PUSH: authorised by the owner for this merge, ATTEMPTED, and NOT PERFORMED. The push was denied by the executing environment's permission classifier, not by git, not by the remote and not by anything in the repository. No workaround was attempted. Per the FINAL remote state ruling of 2026-08-04, no claim is made here about where the remote actually stands; recorded strictly as a dated observation, at 2026-08-17 the remote `refs/heads/main` was last observed at `0eb13cf`, and local main stands at `ca27980`, which means the remote is behind by the three badge commits, the merge and the README fix. The push remains outstanding and is the owner's to run.

2026-08-17, the Monday reopen block, and an executor error that destroyed a primary artifact. The destruction is put first because it is the most important fact in this entry and because a later session must not discover it by finding a missing file. THE EXECUTOR DELETED the source artifact 20260816.log, the whole Sunday EA journal, 1466866 bytes. Cause, stated exactly so the trap is reusable: a PowerShell helper function was named `Rd`, and `rd` is a built in AllScope ALIAS for `Remove-Item`. PowerShell resolves aliases BEFORE functions, so the helper never ran and the call `$l=Rd "$ea\20260816.log"` executed `Remove-Item` against the journal. It failed silently and returned nothing, which read as an empty file rather than as a deletion, and the deletion was only identified on the next call when the path reported as not existing. `Remove-Item` does not use the Recycle Bin. Recovery was attempted and failed: `Win32_ShadowCopy` returns an initialization failure and `Get-ComputerRestorePoint` returns access denied, both for want of elevation. The file is gone. Standing rule added, alongside the CRLF regex trap of 2026-08-06 and the stale directory entry trap of 2026-08-16: NEVER name a helper with a short token, and never with one that collides with a built in alias, since `rd`, `del`, `ls`, `cp`, `mv`, `sc` and others are aliases that silently win over any function of the same name. Prefer inline code over helpers when touching evidence, and never let a read path and a destructive cmdlet share a name.

What was lost and what survived, stated precisely rather than reassuringly. Lost: the Sunday EA journal from local 11:14:03 to end of day, which is the segment containing the sleep entry, the wake, and the single DEGRADED line the owner reported. Survived, because they had already been snapshotted and committed: docs/evidence/journal-20260816-p1f-close-binding.txt, 1348 lines through 11:13:33, at commit 6c6bced, and docs/evidence/journal-20260816-weekend-freeze-sunday-partial.txt through 11:04:33 at a61373e. Survived on disk and snapshotted this session before any further work: the TERMINAL side journal for 2026-08-16, which the deletion did not touch, now at docs/evidence/terminal-20260816-fullday-sleep-window.txt. Also unaffected: Saturday's complete file and Monday's. Net effect on the record is that one specific line, the DEGRADED line at 15:51:05, can no longer be quoted from an artifact. Everything else the owner reported about the sleep is independently reconstructible and is reconstructed below.

Sleep survival number two, unplanned, VERIFIED without reference to the deleted file. Cause per the owner and not measurable from here: the machine was unplugged and Windows sleep on battery was still at its default, the Never setting from 2026-08-13 having applied to plugged in only. Two independent witnesses establish the sleep. First, the terminal journal brackets it exactly: `connection to JustMarkets-Demo3 lost` at 12:33:14.992 and `authorized on JustMarkets-Demo3` at 15:51:11.765, a gap of 11876.773 seconds, with the following line reading `previous successful authorization performed from 5.29.18.75 on 2026.08.16 05:31:49`, which proves no intervening authorization and therefore no terminal restart inside the gap. Second, and this is the load bearing one, the state counter is continuous ACROSS the sleep and can be checked from MONDAY's file alone. Every one of the 1164 LIFE lines emitted on 2026-08-17 implies the same ACTIVE start when `seconds_in_state` is subtracted from the local stamp: 2026-08-14 12:32:18, one distinct value with zero variance across the whole day. That origin reproduces both of the owner's Sunday figures to the second, 172857 seconds after it landing on 2026-08-16 12:33:15 and 184727 seconds after it landing on 2026-08-16 15:51:05. So the ACTIVE run has been unbroken since the 2026-08-14 reconnect, straight through a 3 hour 17 minute 50 second suspension, with no re init, no SAFE_HALT, no takeover and no state reset. The process itself corroborates it, PID 2032 still reporting a start time of 2026-08-14 11:55:58. The counter therefore advances on wall clock rather than on ticks or timer callbacks, which is the same mechanism the 2026-08-05 mutex heartbeat entry predicted and the 2026-08-13 sleep first showed.

One honest limit on the above, recorded rather than glossed. The owner reports a single DEGRADED line at the wake and that is almost certainly right, since the EA resumed and emitted at 15:51:05 while the network did not reauthorize until 15:51:11.765, so that line was emitted disconnected and the Q10 gate at AccountGuardian.mq5:121 must have marked it. But it CANNOT BE QUOTED, because the executor deleted the file that held it. The claim is corroborated by timing and by code, not by artifact, and no future session may cite it as a quoted line.

This sleep is a DIFFERENT CLASS from the 2026-08-13 one and the difference is worth keeping. That sleep woke into a live market and produced a Q8 anchor jump on the first pass. This one began and ended entirely inside the weekend freeze, with `TimeCurrent` pinned at 2026.08.14 23:57:59 and the anchor pinned at 2026.08.14 01:00:00, so nothing needed to be caught up at the wake and no Q8 firing occurred or should have. Sleep survival is therefore now observed in both classes, inside a freeze and across a live boundary.

The Monday reopen, and the corrected prediction lands EXACTLY. Quoted from docs/evidence/journal-20260817-monday-reopen.txt. Last frozen line at 00:59:35 reads `anchor=2026.08.14 01:00:00|realized=-52.84|floating=0.00|base=1985.97|limit=99.30` with `server=2026.08.14 23:57:59`. Then at 00:59:59.487, four lines at one stamp: `INFO|anchor jump accepted: previous_high=2026.08.14 01:00:00, accepted=2026.08.17 01:00:00, jump=259200s`, the matching ALERT carrying the Q8 amendment text, the terminal Alert popup, and `INFO|day rollover|old_anchor=2026.08.14 01:00:00|new_anchor=2026.08.17 01:00:00|jump=259200s`. Then at 01:00:05 the first new day line reads `anchor=2026.08.17 01:00:00|realized=0.00|floating=0.00|base=2133.13|limit=106.66` with the server clock unfrozen to 2026.08.17 01:00:07. The prediction corrected at commit 59f6d21 named base 2133.13 and limit 106.66 in advance and both are exact; the superseded figure 2033.13 appears nowhere in the day, which is the discriminating check. Q8 LIVE FIRING NUMBER THREE, weekend class, and exactly one ALERT for the day against the cadence cap. `seconds_in_state` runs 217637 to 217667 straight across the boundary, so the rollover was absorbed inside ACTIVE with no transition, the same absorption P1-A and P1-M recorded. Day totals for 2026-08-17 to 09:41:35: 1164 LIFE lines, no gap above 31 seconds, DEGRADED 0, RESYNC 0, WARN 0, ALERT 1.

P1-B, stated plainly at owner request. The row reads "one weekend harvest: anchor holds Friday through the freeze, adopts Monday at reopen, no Sat/Sun rollover lines | journal scan, quoted lines" at PLAN_PHASE1_PNL_CORE.md:251. The PROPERTY is established and the ARTIFACT SET is not clean, and those are different things. Clause by clause. "Adopts Monday at reopen" is fully met and quoted above. "Anchor holds Friday through the freeze" is directly evidenced for all of Saturday, 2880 lines with the anchor pinned and zero non LIFE lines, and for Sunday to 11:13:33. "No Sat/Sun rollover lines" is directly evidenced for all of Saturday and for Sunday to 11:13:33, and for the remaining Sunday window it is established INDIRECTLY but rigorously, from an intact artifact: Monday's reopen reports `previous_high=2026.08.14 01:00:00` and `old_anchor=2026.08.14 01:00:00` and `jump=259200s`. Q8's latch is a HIGH WATER MARK, so any rollover that had fired on Saturday or Sunday would have raised previous_high to that later value and would have shortened the jump below three days. Neither happened, so no anchor movement occurred anywhere between 2026.08.14 01:00:00 and 2026.08.17 01:00:00, including inside the sleep and inside the deleted window. The property holds. What is NOT clean is the harvest itself, and for two independent reasons only one of which is the executor's fault: a genuine 3 hour 18 minute hole where the machine was asleep and emitted nothing at all, and the deleted remainder of the Sunday file. RECOMMENDATION, and it is a recommendation and not a ruling: re harvest next weekend. It costs nothing, since the machine sits idle through a freeze anyway, it removes a defect the executor introduced, and P1-B is precisely the row whose subject is an uninterrupted observation, so closing it on a reconstruction argues against the thing it exists to test. What this weekend has already proven should stand recorded so that next weekend's run is confirmatory rather than exploratory. P1-B is left OPEN and the owner may overrule this and close it on the inference above, which is sound.

Evidence, snapshotted this session before any analysis was run, because protecting the surviving files came first. docs/evidence/terminal-20260816-fullday-sleep-window.txt, 4839 bytes, md5 5626D3652E12375F0D00C02ACE863B50, 49 lines, carrying the sleep bracket at lines 34 and 35. docs/evidence/journal-20260817-monday-reopen.txt, 342137 bytes, md5 08AE0F7444B3F5943E9F5A7757DBC48F, 1168 lines, a partial since the day is still running, last LIFE line local 2026.08.17 09:41:35. docs/evidence/terminal-20260817-monday.txt, 143 bytes, md5 731D09C9F8687810E71FA0B3EFD70426, 2 lines. Same UTF-16LE to UTF-8 transcoding note as the 2026-08-16 entry applies, so line counts and not byte counts are the integrity comparison.

2026-08-16, the weekend freeze block, harvested with the machine untouched as instructed. The owner reports a second deposit of 100.00 made inside the weekend freeze, for P1-F. P1-F does NOT close on this session's artifacts, and the reason is the most useful thing in this entry, because it is a property of the acceptance row rather than a shortfall in the harvest: EVERY FIELD THE EA LOGS IS INVARIANT UNDER A CORRECTLY HANDLED DEPOSIT, so the journal alone can never witness that a deposit happened at all. Q2's base identity is `AccountInfoDouble(ACCOUNT_BALANCE)` minus the all deals sum, read at Pnl.mqh:116-122, and the source comment states the neutrality outright: a deposit raises Balance and raises the sum by the same amount. So base holds. Realized holds too, since balance deals are whitelisted out of it per Q3 and F12. Floating is 0.00 with no positions open. Limit is derived from base. Nothing moves. The 2026-08-14 deposit was only witnessed because two independent external readings existed that day, the owner's terminal balance of 2033.13 and the restart reconstruction, and neither exists this weekend by design, since the machine is deliberately untouched for P1-B.

What IS established, and it reduces P1-F to a single missing number rather than a missing run. The freeze is real and measured, not assumed: the server stamp is pinned at `2026.08.14 23:57:59` on every LIFE line from Friday's close through local 2026-08-16 11:04:33, while the local stamp advances normally on the same lines, and the anchor is consequently pinned at `2026.08.14 01:00:00` with no rollover fired across either weekend night. That is a genuinely frozen `TimeCurrent`, which is the precondition P1-F exists to test. Base reads 1985.97 on all 2880 Saturday lines and all 1330 Sunday lines, counted rather than sampled. The arithmetic is therefore already forced, conditional on one owner reading. Base is `balance` minus `deals since anchor`, and deals since anchor are the carry close of -52.84 plus the deposits. If the second deposit landed AND its deal is inside the history window, balance is 2133.13, the sum is 147.16, and base is 1985.97, which is what the artifact shows. If the deposit landed and its deal fell OUTSIDE the window, the failure P1-F hunts, base would read 2085.97. If the deposit has not landed yet, base reads 1985.97 for the trivial reason. So the owner's balance figure discriminates all three, and no other number is needed: 2133.13 closes P1-F, 2033.13 means the deposit has not arrived, and a base of 2085.97 would have failed the row outright and did not appear.

Recorded alongside it, the upper bound clause of P1-F is PASS BY CONSTRUCTION in the source and the live run only corroborates it. `#define AG_HISTORY_SELECT_TO D'3000.01.01'` at Pnl.mqh:21, used by both `AgRealized` at :55 and `AgDealsSumAll` at :88, with a header comment stating the obligation and discharging it: the bound is not derived from TimeCurrent, so neither a frozen nor a backward stepping clock can truncate the window. This weekend supplied the frozen clock in reality and the window did not truncate, since base never drifted to a stale value across 4210 lines. The row's phrase "proving the upper bound" is thus satisfied on the code side already, and the live half is the corroboration.

P1-B lattice harvest, collected clean. Saturday 2026-08-15 is a complete 24 hour file: exactly 2880 LIFE lines from 00:00:03 to 23:59:33, every consecutive interval 30 seconds, zero gaps above 31 seconds, zero non LIFE lines of any kind, zero DEGRADED, zero WARN, zero ALERT, base constant. Sunday to 11:04:33 is 1330 lines with the same zero gap property. A count of 1324 taken earlier in the session is superseded by 1330 and is noted here only because the difference was the live file growing between two reads, which is why the continuity check was re run rather than reported from the first pass.

P1-O advances on artifacts nobody staged, and two of its four clauses are now met on runs that CANNOT be contaminated, since the machine was untouched all weekend. The terminal journal records four connection losses on 2026-08-16. Two were long enough for the 30 second LIFE cadence to sample: a 93.8 second gap from 01:40:07.063 to 01:41:40.874, and an 80.9 second gap from 05:30:17.549 to 05:31:38.409. Both produced exactly three DEGRADED lines each, at 01:40:33, 01:41:03, 01:41:33 and at 05:30:33, 05:31:03, 05:31:33, carrying the marker on BOTH the `waiting_on` field and the numbers field group, reading `waiting_on=DEGRADED: disconnected, no breach decisions|DEGRADED`. The boundaries are exact in both windows: the last clean line precedes the loss and the first clean line follows the reauthorization, with no clean line inside either gap and no DEGRADED line outside one. Zero ALERTs fired, against a whole day non LIFE count of zero. So P1-O's "DEGRADED prefix on both the numbers field group and waiting_on throughout the gap" and its "zero ALERTs during it" are both MET here, and met twice, which is more than the contaminated 2026-08-14 run delivered.

The RESYNC clause is still not met and this session upgrades the diagnosis, which matters because the previous entry's framing was probably wrong. Across four live disconnects now, the 2026-08-13 wake, the 2026-08-14 deliberate cut, and these two natural weekend losses, the RESYNC count is ZERO. The earlier entry read that as the gate never executing. The likelier reading, and the one the numbers support, is that THE GATE IS UNOBSERVABLE THROUGH THE ONLY CHANNEL THAT REPORTS IT. The timer runs at 1 second, confirmed by `timer=1s` on the boot line, and `HistoryStablePolls` is 3, so a RESYNC episode lasts about 3 seconds. The RESYNC prefix appears only on LIFE lines, which are emitted every 30 seconds. The chance of any one reconnect being sampled during its RESYNC window is therefore roughly one in ten, and four events at that rate are far more likely to yield zero observations than one. There is no event driven line to fall back on: the whole weekend contains zero non LIFE lines, so RESYNC entry and exit are never announced, only sampled. Reinforcing the same point from the other side, the two SHORT losses this morning, 0.6 seconds at 01:52:24.723 and 0.7 seconds at 05:31:48.284, produced no DEGRADED line at all, because both opened and closed entirely between two LIFE emissions. The condition existed and the artifact does not contain it. As written, P1-O asks for a roughly 3 second signal to be caught by a 30 second sampler, which makes the row close by luck rather than by procedure, and that is a defect in the acceptance row and arguably in the observability of the build, not evidence about the gate's correctness either way. Nothing is proposed as authority here and no code is touched.

One methodology trap banked, because it very nearly produced a false alarm of the worst kind. `Get-ChildItem` reported the live EA log `20260816.log` as 0 bytes with a last write time of 00:00:42, which read as the guardian having died at midnight with the terminal still running. That was FALSE. Windows does not update a file's directory entry while a writer holds the handle open, and MT5 holds its log open all day. Opening the same path with `FileShare.ReadWrite` and reading `Stream.Length` returned 781774 bytes. The EA was alive and had been logging normally the whole time. The rule this yields, and it belongs with the CRLF regex trap of 2026-08-06: never judge a live MT5 log by its directory metadata, always read the stream. Every byte count in this entry comes from the stream.

Evidence, all under docs/evidence, snapshotted this session and hashed. journal-20260815-weekend-freeze-saturday.txt 847642 bytes md5 91ABD2E1EA526567519FD3C38A992DDE, 2880 lines. journal-20260816-weekend-freeze-sunday-partial.txt 393246 bytes md5 14EB8AD174BBB578B75465C34ABE0B2F, 1332 lines, explicitly a partial since the day was still running at capture, last LIFE line local 2026.08.16 11:04:33. terminal-20260815-weekend.txt 572 bytes md5 F9CD9EFD80926963D85CDBBB2AC15E7F. terminal-20260816-weekend-disconnects.txt 3122 bytes md5 FB19AA079634ED90375AC78CD94EC2A2, carrying the four connection loss and reauthorization pairs. Note for any later integrity check so a size mismatch is not misread as loss: MT5 writes these logs UTF-16LE and the snapshots are written UTF-8, so a snapshot is close to half its source in bytes while line for line identical, and the line counts above are the figure to compare, not the byte counts. Source read this session at Pnl.mqh:15-21, :52-58, :85-91 and :108-122.

2026-08-16, closing addendum to the entry above, written the same session once the missing witness arrived. P1-F CLOSES. The owner's terminal balance reads 2133.13, which is the value this entry named in advance as the closing one, and the conditional above becomes a measurement. It is bound to a live line rather than to the earlier harvest, so that the reading and the artifact refer to the same instant rather than to a stale one: base reads 1985.97 on the LIFE line stamped local 2026.08.16 11:13:33, taken seconds after the owner's reading, with the server stamp still pinned at 2026.08.14 23:57:59 and the anchor still at 2026.08.14 01:00:00. The identity is then forced with no free parameter left in it. 2133.13 minus 1985.97 is 147.16, and the only deals since the anchor are the carry close of -52.84 and the two deposits of 100.00 each, which sum to exactly 147.16 and to nothing else. Both balance deals are therefore inside the history window under a clock that has not advanced since Friday's close, which is exactly what the row asks for and what the constant bound at Pnl.mqh:21 predicts. Checked for a transient rather than only at the endpoints, because a momentarily wrong base would be a defect even if it corrected itself before the next emission: all 1348 LIFE lines of the day carry base=1985.97 and realized=-52.84 and no other value appears at any point, so the deposit was absorbed with no observable discontinuity whatsoever. That also means the artifact cannot date the deposit, which is the same invariance this entry opened with and is worth stating plainly rather than leaving as a gap someone later tries to close. Collected free alongside the close, P1-G's whitelist clause is re-proven a second time and under a frozen clock this time, since realized held at -52.84 across 200.00 of cumulative balance operations. Evidence: docs/evidence/journal-20260816-p1f-close-binding.txt, 397966 bytes, md5 9331F4C301D893F4912E51503E5031DB, 1348 lines. The earlier partial snapshot was byte verified to be a strict prefix of it rather than assumed to be one, its 393246 bytes comparing equal.

2026-08-14, second entry, the morning terminal block with the owner at the machine: carry closed, deposit run, restart reconstruction, disconnect. Two rows CLOSE, P1-E and P1-G. One row is met on its acceptance criterion but not on its procedure and is deliberately NOT closed, P1-C. Two rows do NOT close, P1-K and P1-O, and P1-K's failure is structural rather than procedural, which is the most important thing in this entry. Every figure below is re-derived by the executor from the terminal's own journals; the owner's readings are quoted only where they are the GUI side of a cross check.

Baseline at 11:39, four independent witnesses agreeing. Owner read balance 1985.97, equity 1933.72, margin 2.20, free margin 1931.52, carry ticket 411788798 buy 0.01 XAUUSD.ecn opened 2026.08.11 16:40:34 at 4399.90, current 4347.65, profit -52.25, banner -52.18. Journal at 11:39:32 read floating=-52.26. Checks: equity minus balance is -52.25, equal to the ticket profit exactly, so the EA's floating reproduces the platform's own equity arithmetic; free margin 1933.72 minus 2.20 is 1931.52 exactly; the banner figure sits between the 11:39:02 and 11:39:32 LIFE lines, which is the 30 second grid and not a discrepancy. Base agreement on three witnesses, journal base=1985.97, terminal balance 1985.97, personal area balance 1985.97, two of them off terminal and independent, the same class of check as P1-D on a second day and a different value.

SWAP IS ZERO ON THIS INSTRUMENT AND ACCOUNT, measured twice, and it corrects a claim this ledger and the owner both carried. 4399.90 minus 4347.65 is 52.25 and the profit is -52.25, exact to the cent at 1 oz per 0.01 lot, leaving zero residual for swap after three nights held. The close an hour later confirmed it independently on the realized side, 4399.90 minus the 4347.06 fill is 52.84 against realized -52.84. Equity carries swap, so the equity check is a second witness rather than a restatement. The owner's instruction of 2026-08-13 described the day's PnL as the carry's floating "including two nights of swap", and the earlier 2026-08-14 entry below repeated "swap included"; both are measurably wrong and the latter is corrected in place. Any commission was booked into balance at open rather than carried in floating.

Carry closed 11:42:38.298, deal #268572111 sell 0.01 XAUUSD.ecn at 4347.06 on order #440372679, closing position #411788798. Six predictions were stated in advance and all six held: realized moved 0.00 to -52.84; floating went to 0.00; pnl_vs_limit stayed CONTINUOUS across the event, -52.92 on the last pre close line at 11:42:32 against -52.84 on the first post close line at 11:43:02, the 0.08 being six seconds of price movement and not a step; base held 1985.97; limit held 99.30; and no TRANSITION and no ALERT anywhere. The continuity is the measurement that matters, because converting floating into realized without a step is what the Q2 identity requires and it had never been observed on a real deal. Lattice unbroken, 1351 LIFE lines on the day at that point. Note for the record that the close closes no row by itself: it supplies P1-C's precondition, deals present, which this account had never had.

Deposit of 100.00 through the personal area at 11:51, no fee, balance 1933.13 to 2033.13 exactly. P1-E CLOSES on its criterion, base and limit unchanged within 0.01, and they were unchanged exactly rather than within tolerance, 1985.97 and 99.30 across 11:50:32 through 11:52:32. P1-G's first clause CLOSES, realized moves by exactly 0, holding at -52.84 to the cent straight through; the discriminating counterfactual is that a deposit mistaken for a trading deal would have moved realized to +47.16. Recorded because it affects how this artifact is found later: the deposit produces NO terminal journal line at all, so its evidence is the balance delta and the deal history, not a log entry. The Q3 DEAL_FEE term is zero here and therefore untested by this deposit.

P1-G CLOSES WHOLE on the restart reconstruction, which collected its second clause immediately rather than deferring it to Monday. Clean deinit at 11:55:21.060 reading reason=9 session marked clean timer_armed=1 timer_ticks=41147, which is one tick per second across a session of 41149 seconds and an eleven and a half hour corroboration of the tick arithmetic. Fresh init 11:56:02.280, chain=1/3, BOOT->SYNCING at 11:56:02.284, SYNCING->ACTIVE history stable polls=3/3 at 11:56:05.285, first ACTIVE LIFE at 11:56:33.296 reading realized=-52.84, floating=0.00, base=1985.97, limit=99.30, anchor=2026.08.14 01:00:00. The image inherited nothing, globals reset and 0 positions on sync, so base was rebuilt from history alone and the arithmetic it had to get right was 2033.13 minus -52.84 minus 100.00 equals 1985.97. It produced exactly that. The wrong answers are what make this discriminating: ignoring the deposit when backing out the day's balance movements gives 2085.97, and counting it as a trading deal gives realized +47.16. Neither appeared, so the 100.00 sits in the base sum at the right magnitude and the right sign, which is P1-G's second clause "the base sum by the exact amount" proven on the reconstruction path.

P1-C is NOT CLOSED and the reason is recorded rather than smoothed over. The row's procedure is "kill mid-day with deals present, relaunch"; the owner performed a CLEAN CLOSE. The artifacts discriminate twice: the deinit logged "session marked clean", and the boot logged chain=1/3 where a kill would have produced chain=2/3, which was the executor's stated prediction beforehand. The acceptance criterion IS met, PnL equal before and after within 0.01, measured -52.84 at 11:55:02 and -52.84 at 11:56:33 for a difference of exactly 0.00, and the reconstruction path was genuinely exercised since a terminal restart sits in the fresh image family beside a kill. What was not exercised is the unclean variant, and the difference between the two is confined to the halt file clean flag and the crash chain, neither of which touches the PnL rebuild. Recorded as CRITERION MET, PROCEDURE DEVIATES, left for owner ruling, because this project's standard is that a row closes on the evidence its plan names and an executor may not widen that on its own authority.

P1-K CANNOT CLOSE BY ITS NAMED PROCEDURE, AND THIS IS STRUCTURAL. Detailed in its own ISSUES entry above. Measured: zero WARN lines exist in the whole day's journal, and the row requires a loud WARN from a false HistorySelect. Source read gives the mechanism: AccountGuardian.mq5:121 returns on !TERMINAL_CONNECTED before any history call is made, and Pnl.mqh:178 guards a second time, so a disconnect sets DEGRADED and exits early and the false return the row wants is never reachable that way. Same category as the Q8 cadence cap's permanent scope limit, unfalsifiable by construction through the named route.

P1-O does NOT close, and the run was additionally contaminated. The genuine disconnect ran 12:27:40.793 to 12:32:15.828 on the terminal log, about four minutes thirty five seconds, roughly nine LIFE lines. Of the row's four clauses, two are met and two are not. Met: the DEGRADED prefix appears on BOTH the waiting_on field and the numbers field group, on the lines at 12:28:03 and 12:28:33; and zero ALERTs fired during the gap, confirmed against a whole day count of zero. Not met: "throughout the gap" fails, because only 2 DEGRADED lines exist against roughly nine lines of gap; and the RESYNC clause fails outright, with zero RESYNC lines in the day, so the reconnect coherence gate at AccountGuardian.mq5:140 has now survived two live disconnects without ever executing. The contamination was three chart period changes at 12:28:36, 12:28:39 and 12:28:42, M5 to M30 to H1 to M15, each logging deinit reason=3 REASON_CHARTCHANGE, which forced the EA out of ACTIVE into a fresh SYNCING entry and so routed the reconnect through the ordinary stability counter instead of the RESYNC path. The owner first reported the machine untouched and corrected it to timeframe changes on being shown the lines; nothing changed the chart autonomously.

A SECOND FINDING FELL OUT OF THE CONTAMINATION AND IS INDEPENDENT OF IT: while disconnected but in SYNCING, the EA carries NO DEGRADED marker at all. The seven SYNCING lines from 12:29:03 to 12:32:03 read waiting_on=polls=0/3 with no degraded indication, and the terminal did not reconnect until 12:32:15, so every one of those lines was emitted offline. A reader of those lines cannot tell the terminal was down. Recorded in its own ISSUES entry with the P1-O remainder.

Free gains from the contaminated run, none of them planned. A7 is RE-PROVED LIVE on the deployed Phase 1 build: three re-inits six seconds apart, far inside the 300 second gap bound, and the chain stayed 1/3 throughout because every deinit logged session marked clean; had the clean flag not reset the chain the walk would have reached CrashLoopMaxInits=3 and fired SAFE_HALT, and it did not. The inherited image property is confirmed again, timer_ticks continuing 1953 to 1955 to 1958 across all three re-inits with no reset. The first ever observation of TRANSITION ACTIVE->SYNCING|boot was recorded and then checked against the SPEC rather than reported as a finding: docs/SPEC_v0.1.md:47 post A5 reads "boot in a fresh image, or the surviving prior state in an in-place re-init (A5)", ACTIVE is the surviving prior state, so the line is CONFORMING and the executor's initial suspicion of a table gap is withdrawn. And the reconnect gating worked in spirit even though the RESYNC path did not run: polls held at 0/3 for three minutes offline, the EA never resumed ACTIVE on absent history, and it transitioned at 12:32:18, exactly three seconds and three polls after the 12:32:15 reconnect, matching HistoryStablePolls=3.

State at the close of the block, verified at 14:15:03: ACTIVE, seconds_in_state=6165 continuous since the 12:32:18 transition, realized=-52.84, floating=0.00, base=1985.97, limit=99.30, day totals DEGRADED 2, RESYNC 0, WARN 0, ALERT 0. THE CHART IS NOW M15 AND WAS NOT RESTORED TO M5. That is functionally irrelevant to an account level guardian but it changes the journal's chart label, so it is recorded here so no future session reads the weekend harvest's M15 lines as a different instance. Standing predictions banked for Monday: base should recompute to 2033.13, and the reopen should produce Q8 live firing number three, weekend class, jump=259200s. SUPERSEDED 2026-08-16 on the base figure only, and corrected here rather than left standing, because a stale prediction of this kind is exactly what gets read as a failure on Monday morning when the build is in fact correct. The second deposit of 100.00 landed inside the weekend freeze and the owner's balance now reads 2133.13, so Monday's rollover, which moves the anchor to 2026.08.17 01:00:00 with zero deals after it, should recompute base to 2133.13 and the limit with it to 106.66 at the ruled five percent. A Monday base of 2033.13 would now be the WRONG answer and would mean the second deposit had been dropped from the balance side. The Q8 half of the prediction is UNCHANGED and stands as written, since the anchor has been pinned at 2026.08.14 01:00:00 throughout the freeze, making Monday's crossing a three day jump of exactly 259200s. Weekend plan agreed with the owner: the machine stays on and untouched for the P1-B lattice harvest, and deposit two, the frozen clock one for P1-F, is due Saturday inside the freeze, which the owner is aware of and which must be run before any staleness change ships since a DEGRADED gate would rewrite that row's expected artifact.

Evidence for this entry, snapshotted at 14:1x while both files were still being written and committed alongside it: docs/evidence/journal-20260814-morning-block.txt at 979066 bytes md5 8536F55A0DEAF4B09C853531F9D0FEBF, the EA journal carrying the whole day including the close, the deposit window, the restart and the disconnect; and docs/evidence/terminal-20260814-morning-block.txt at 9628 bytes md5 DCB08A178F0B83A7D9BB1D3E47ED7381, the terminal journal carrying the trade lines for deal #268572111, the two boots, and the connection lost and reauthorized stamps that bracket the true disconnect gap. The earlier docs/evidence/journal-20260814-observed-rollover.txt at md5 D543C8B918C1C18BF373FBE0337AC8E5 is retained unchanged as the narrower snapshot taken at 01:09 for the rollover itself and is a strict prefix of the morning block file, kept because the entry below cites it.

2026-08-14: Sleep survival measured, Q8 live firing number two banked, and the first OBSERVED 01:00 day rollover captured live, closing P1-H. Four things happened across three calendar days and they are recorded as one entry because they are one continuous run of the same process, broken only by the owner's firmware restart. Everything below is re-derived by this session from the terminal's own journals, not from the owner's report, and the point where the report and the artifacts disagree is stated as the artifacts have it.

First, the sleep. The machine slept on 2026-08-11 at server 19:17:10 and woke on 2026-08-13 at local 11:44:59. The last pre-sleep line stamps local 19:30:22 against a server clock already frozen at 19:17:10, and the first line back stamps local 2026.08.13 11:44:59 against that same frozen server value, so the freeze is visible on both edges of the hole. The state survived intact: seconds_in_state reads 18437 on the last line and 163314 on the first line back, a delta of 144877 seconds, and the local wall clock across the same hole is 144877 seconds exactly. That reproduces the 2026-08-05 finding on a second and much longer sleep, so seconds_in_state is a wall clock difference and the state entry timestamp is not disturbed by suspend and resume. There is no init line, no TRANSITION, no mutex takeover and no SAFE_HALT anywhere in the whole of 2026-08-13 up to the deinit, established by grep over the entire file rather than by sampling, so the process never died and the in memory Q8 anchor latch was never reconstructed. That is the load bearing point of the whole entry: the latch survived because the image survived, and this is the first time that has been shown across a sleep rather than across a quiet period.

Second, and recorded as an unplanned gain, the first line back is a DEGRADED line and nobody scheduled it: "LIFE|state=ACTIVE|seconds_in_state=163314|waiting_on=DEGRADED: disconnected, no breach decisions|DEGRADED|anchor=2026.08.11 01:00:00" carrying server=2026.08.11 19:17:10 against local=2026.08.13 11:44:59 on the one line. That is the Q10 DEGRADED path firing live for the first time, with the frozen server stamp and the live local stamp printed side by side, and it is evidence toward P1-O obtained for free from a wake rather than from the deliberate disconnect that row is still waiting on.

Third, Q8 live firing number two, and it is the wake from sleep class rather than the weekend reopen class of 2026-08-10. At 11:45:13.031 the EA emitted exactly four lines for one event: an INFO twin, the ALERT "anchor jump accepted: previous_high=2026.08.11 01:00:00, accepted=2026.08.13 01:00:00, jump=172800s (jump exceeds one day; evaluation continues, Q8 amendment 2026-08-09)", the ungated terminal Alert popup carrying the same string, and "INFO|day rollover|old_anchor=2026.08.11 01:00:00|new_anchor=2026.08.13 01:00:00|jump=172800s". One ALERT, cadence cap intact, evaluation continued, no state change. The question of whether Wednesday's anchor was ever seen is answered in the strongest available form: there is NO 20260812.log in the terminal's MQL5 Logs directory at all, verified by Test-Path returning False, so the EA emitted not one line during the whole of 2026-08-12 and the jump went straight from 08.11 to 08.13 with nothing in between. Every 2026-08-13 LIFE line reads realized=0.00, base=1985.97 and limit=99.30, and 1985.97 at five percent is 99.2985 which rounds to the printed 99.30, so the recomputed base and the derived limit both check out arithmetically rather than being taken on report.

Fourth, the timeline correction, recorded because the ledger must not carry a wrong one forward. The owner's instruction described the firmware restart as happening on the afternoon of 2026-08-13. The artifacts place it nearly ten hours later. The EA deinit is at 14:45:13.023 reading "reason=9|session marked clean|timer_armed=1|timer_ticks=28452", the terminal logged "expert AccountGuardian (XAUUSD.ecn,M5) removed" and then "exit with code 0" at 14:45:14, and the machine's LastBootUpTime is 2026-08-14 00:28:26, with the terminal restarting at 00:29:29 and the expert loading at 00:29:30.693. So the guardian was down for nine hours and forty three minutes, from 14:45:14 on 2026-08-13 to 00:29:30 on 2026-08-14, and that window is unprotected and unobserved. The shutdown itself was clean and the restart healthy: crash loop chain 1 of 3, TRANSITION BOOT to SYNCING at 00:29:31.758, SYNCING to ACTIVE on "history stable|polls=3/3" at 00:29:34.767, and the fresh image re seeded the anchor to 2026.08.13 01:00:00 from a server clock reading 2026.08.13 23:57:59, which is the correct anchor for that server day and confirms the rollover seeding fix on a fresh boot.

Fifth, the observed rollover, which is what P1-H was held open for. At local 01:00:00.763 on 2026-08-14, server stamp 2026.08.14 01:00:01, the EA emitted "INFO|day rollover|old_anchor=2026.08.13 01:00:00|new_anchor=2026.08.14 01:00:00|jump=86400s" and nothing else. This is the negative control the Q8 amendment has never had in the live channel: the jump is exactly one day, so the ALERT branch correctly did NOT fire, and a grep of the entire 2026-08-14 file for ALERT and for the Alert popup returns zero lines. There is no TRANSITION at the event either, so the rollover was absorbed inside ACTIVE exactly as the Monday reopen was, and seconds_in_state steps 1798 to 1828 straight through the boundary with no reset. A carry position was open across the crossing, which is what makes the row meaningful rather than a bare clock test.

Sixth, a consequence measured at the rollover and flagged rather than ruled, because it is a design question and not a defect. The carry's floating PnL did NOT reset at the day boundary: the last line of the old day reads floating=-49.45 and the first line of the new day reads floating=-45.65, the difference being live price movement and not a reset. realized went to 0.00 as designed and base stayed at 1985.97 because there was no realized amount to fold in. So a position held across midnight drags its entire accumulated unrealized loss, swap included, into every new day's limit comparison. The new day opened at -45.65 against a limit of -99.30, which is forty six percent of the day's allowance consumed at second zero on a loss earned on previous days. This is a direct consequence of the Q2 identity as written and it is not a bug against any current ruling, but a Phase 2 build would be able to lock a day on a loss that day did not produce, so it wants an owner ruling before enforcement lands.

Evidence, all four artifacts committed with this entry under docs/evidence: journal-20260811-sleep-entry.txt at 347998 bytes md5 8BD4CFBB408C3304E98093558ABD2D83 carrying the sleep entry edge; journal-20260813-sleep-survival-q8-second-firing.txt at 214030 bytes md5 F68D75742CA9DB0E8C776AC850562F75 carrying the wake, the DEGRADED line, the Q8 firing and the clean deinit; journal-20260814-observed-rollover.txt at 43852 bytes md5 D543C8B918C1C18BF373FBE0337AC8E5 snapshotted while the file was still being written, carrying the fresh boot and the observed crossing; and journal-20260814-rollover-window-extract.txt at 6343 bytes md5 EED5743E6FBF90DD16DC7F15C043801D, the machine generated extract of the event lines plus the 00:55 to 01:06 window. Corroboration that the evidence directory is intact rather than assumed: the existing journal-20260810-monday-reopen.txt still hashes to 244CB38E4E5CA75AE13140FCDBC84A30, matching the value the 2026-08-10 entry recorded for it.

2026-08-11: MANUAL-TRADES ACCEPTANCE, P1-I CLOSED AND Q7 CLOSED WHOLE. Joint session, owner at the terminal for every order and every specification window, executor verifying each one from artifacts before it entered the record. Full evidence in docs/evidence/phase1-manual-trades-2026-08-11.md.

State re-derived first and quoted rather than carried from a report: `main` at `6be8d534784d2b68ab9f2f3faf66877cbede722e`, working tree carrying only the known bot-appended `.gitignore` block, commit-msg hook first bytes `23 21 2f 62 69 6e 2f 73` so BOM free. Running process `terminal64` pid 4816 with StartTime `2026-08-11 14:22:58`; loaded ex5 unchanged at mtime `2026-08-09 19:10:59` md5 `AC19EAEA1A65E3C4D052E151C21502E9`; all seven terminal sources md5-matching the tree pairwise. Build identity therefore rests on the three instruments standing rule 7 permits and on none it forbids. Primary artifacts snapshotted through shared-read handles and decoded UTF-16LE explicitly: docs/evidence/journal-20260811-manual-trades.txt, 165966 bytes, md5 6D013CC77F8685A3DB3085C0D43EBB38, 289 non-empty lines local 14:23:02.951 to 16:44:03.936, and docs/evidence/terminal-journal-20260811-manual-trades.txt, 9416 bytes, md5 95836779C50E7BB6B6006FE8C2BD5F4E. Both banked as `.txt` deliberately, since `.gitignore` matches `*.log` and a `.log` copy would be ignored, which is the phantom-pointer failure mode already recorded three times here.

THE SESSION INSTRUCTION'S BOOT NARRATIVE WAS PARTLY FALSE AND THE FALSE PART IS STRUCK. Confirmed as given: `init|build=Phase1` at server 14:23:04, `chain=1/3`, `BOOT->SYNCING` then `SYNCING->ACTIVE|history stable|polls=3/3` three seconds later, and the anchor seeded fresh to `2026.08.11 01:00:00` with zero ALERT lines and zero rollover lines all day, which is plan 4.1a's ruled first-pass behaviour. STRUCK: "mutex takeover over yesterday's stale heartbeat". No takeover occurred, established three ways rather than argued. The takeover discriminator is `Persist.mqh:280`, an `AgWarn` naming the measured age, and the day carries ZERO WARN lines and ZERO occurrences of "stale". `Persist.mqh:265` records that a heartbeat of 0 is a deliberate release by a clean OnDeinit and the branch is gated at `:275` on `hb > 0.5`; yesterday's final line is `AG|2026.08.10 14:12:44|INFO|deinit|reason=9|session marked clean|timer_armed=1|timer_ticks=25`. And the terminal process itself started four seconds before the EA init, so this was a cold start onto a released mutex. Recorded precisely because `id_exists=1|hb_exists=1` on the acquire line reads like a pre-existing holder and is not: both are sampled AFTER the writes at `Persist.mqh:293,295`.

Eight deals ran: gold round trips `#251330873`/`#251333949` at 4395.87/4395.46 and `#251394323`/`#251396880` at 4390.90/4391.10, a silver sequence `#251754278` buy 0.02 at 65.081 then `#251774078` PARTIAL close 0.01 at 64.896 then `#251789582` closing the remainder at 64.911, and the carry `#251916522` buy 0.01 XAUUSD.ecn at 4399.90 LEFT OPEN. The whole day's realized reconciles to the cent from inputs the EA does not write, broker fill prices plus the contract sizes and 7 USD/lot commission from the owner's specification read: `-0.07 + 0.41` then `-0.07 + 0.20` then `-0.14 - 9.25 - 8.50` gives `-17.42`, which is what the EA prints from 16:28:04, and `-17.49` from 16:41:04 once the carry's entry commission lands. Measured rather than assumed along the way: commission is ROUND-TURN CHARGED WHOLLY AT ENTRY, not per deal as "instant by deal volume" reads at a glance, since deal `#251333949` moved realized by exactly `+0.41` which gross profit alone accounts for.

P1-I CLOSES and it closes on margin rather than on tolerance, which matters because 30-second LIFE sampling cannot resolve small effects. Bracketing lines `16:25:04 realized=0.33|floating=-18.50|pnl_vs_limit=-18.17` and `16:25:34 realized=-8.92|floating=-9.00|pnl_vs_limit=-17.92`. Realized moved by `-9.25`, exactly `(64.896 - 65.081) x 50 oz`, and floating halved to track the remaining 0.01 alone. Both failure modes were enumerated in advance: a double-count would read about `-27.4` and a gap about `-8.7`, so the two sit roughly 9 currency units away in opposite directions while the observed drift between samples is 0.25, a discrimination margin of about 37x. The full close, deal `#251789582`, then moved realized by exactly `-8.50` with floating to `0.00`.

Two obligations earlier entries named as still owed are now discharged. The Q2 base identity is measured NON-DEGENERATELY for the first time: `base=2003.46` and `limit=100.17` held unchanged across every state of the day while Balance moved on eight deals, so the all-deals sum tracked it exactly. The 2026-08-10 entry drew that boundary in terms, "with zero deals the reconstruction reduces to base equals balance identically, so this proves the REDUCTION and not the minus-all-deals arithmetic, which still needs deals present", and deals are now present. And Q3's per-deal formula is exercised live on `DEAL_PROFIT` and `DEAL_COMMISSION`; `DEAL_SWAP` awaits the carry's close tomorrow, while `DEAL_FEE` is INFERRED zero and not READ zero, since neither specification carries a fee line and every deal decomposes completely into profit plus commission with nothing left over. The History-tab decomposition that would upgrade it was requested twice and not supplied.

Q7 CLOSES WHOLE on the owner's dialog read of all three specification windows, transcribed verbatim into the evidence document, which is the instrument the plan designates in Stage 1 and the same channel that closed the Q6 last-used gate. The A8 corroboration debt is discharged with it. The read immediately produced a finding that contradicts the plan, logged as its own ISSUES entry: no subscribed symbol bridges midnight, so the nightly no-quote window is about 62 minutes ending exactly on the anchor second, and plan section 2.3's "5.4-minute midnight freeze ... sits a clear 55 minutes below the 01:00 anchor and does not span it" cannot be reproduced under the FINAL Market Watch composition. Every weeknight rollover therefore fires at a resume boundary exactly like the Monday reopen. Second, narrower: the Monday reopen tick at server `2026.08.10 01:00:00` cannot have come from gold, whose Monday quote session opens 01:01, so it came from silver or US100, which excludes the chart symbol the EA runs on.

P1-N was found to be GATED on P1-L rather than merely coincident with it, by source read at `AccountGuardian.mq5:217-223` where the deferral emit sits strictly inside `if(breach_now)`. Eight deals produced zero deferral lines, exactly as the gating predicts. Logged as its own ISSUES entry so no future session spends a pass hunting one during ordinary trading.

P1-H's day-N leg is OPEN and running. The carry was placed last and deliberately: direction chosen on swap grounds and NOT on any price expectation, since F11 needs a losing position at the rollover, price is unpredictable, and swap is the one deterministic lever, which also exercises Q3's remaining measurable term. The rule given was BUY unless `Swap short` was more negative than `Swap long`; THE SWAP VALUES WERE REQUESTED AND NEVER SUPPLIED, so the choice is recorded as CONVENTION-SELECTED rather than swap-selected, and tomorrow's closing deal settles which. Gold was chosen over silver on exposure measured this session rather than argued: 0.01 gold is 1 oz needing a 100-unit move to breach, 0.01 silver is 50 oz needing about 2, and silver was observed covering a tenth of that in three minutes. Falsifiable expectations for the 2026-08-12 01:00:00 rollover are recorded IN ADVANCE in the evidence document section 11, seven of them, following the practice the Stage 5 deploy document set for the Monday reopen. The sharpest are that `jump=86400s` must NOT raise an ALERT, since 86400 is not more than the 86400 bound, and that `base` must move `2003.46 -> 1985.97` with `limit` moving `100.17 -> 99.30`, the first time either figure has moved in this project.

Recorded as a process observation, not a fault: the owner's reports and the artifacts diverged three times, each caught before it entered the record. Trade 1 was reported closed while the executor's snapshot predated the fill by 16 seconds; trade 2 was reported closed while the terminal journal showed it open with no closing deal; and the carry was reported open before the order had landed. In every case the FINAL 2026-08-05 rule held and the artifact decided. The instance was left running deliberately, since a terminal closed at 01:00 would lose the rollover line and the whole day-N half of F11.

Nothing about the deployed build changed: no source file was modified, the EA was never stopped or reconfigured, and the halt file was not touched.

2026-08-11: Push to origin, executed by this session, authorized by the owner in the same instruction that ordered the merge above. Command: git push origin main. Output quoted: "To https://github.com/www8351/Account_Guardian_V2.git" followed by "cabeb59..a2f3a8e  main -> main", exit 0, so the push carried sixteen commits, the two bot badge commits, the thirteen-commit merge and its ledger entry. Remote position at the moment of this push, a dated observation and nothing more, per the FINAL remote-state ruling of 2026-08-04: local main and origin/main both read a2f3a8e immediately after. That is what two refs said at one instant; it is not a standing property of the repository and it is stale the moment anything moves either ref, which is exactly why the ruling forbids carrying it forward as fact. GitHub Desktop needs no separate action: it reads the same local repository and the same origin, so it reflects this push on its next refresh rather than requiring a second one.

2026-08-11: Merge of worktree-phase1-pnl-core into main, owner ordered, plus a push, both authorized in the same instruction. State re-derived before anything and quoted rather than carried from a report: main stood at 42ede70, the branch tip at c4d363f, the working tree carried one modified file (.gitignore, see below), and the commit-msg hook's first four bytes read 23 21 2f 62 ("#!/b"), BOM free. A fetch was run first because a local remote-tracking ref proves nothing until refreshed: origin/main read cabeb59 both before and after the fetch, so the remote had not moved, and main stood two commits ahead of it on the two bot token-badge commits 07e6169 and 42ede70. Three of the four other worktree branches were confirmed already merged; worktree-phase0-plan-capture no longer resolves as a local ref at all and its worktree is gone from git worktree list, leaving worktree-phase1-pnl-core as the only unmerged work.

Merge commit d52f063, parents 42ede70 (main) and c4d363f (branch tip), thirteen branch commits carried: cb0251d, e7c1560, fb58788, 8dc2daf, e5d32bc, 2d8a400, fb82a70, 0685529, 3f47813, 301fc85, f84ac33, 8ebf756 and c4d363f. Mechanics per the standing plan: git merge --no-ff --no-commit, no rebase, no fast-forward-only, no side-picking strategy. ZERO conflicts, and the reason is structural rather than lucky: the branch never touched README.md and main's only two commits since the branch point touched nothing else, so the two sides were disjoint by file. No hand resolve was required and none is recorded as performed.

Gate position, stated plainly because this merge does not satisfy the 2026-08-03 standard on the branch's own proofs and the owner ordered it knowing that. This is the FOURTH owner-ordered merge outside the gate, after Stage 5a on 2026-08-04, phase0-close on 2026-08-05 and a9-phantom-audit on 2026-08-08. What the branch carries complete and evidenced: Stages 2 through 5 implemented against all ten Phase 1 rulings, SPEC amendment A6, the static and grep acceptance rows in docs/evidence/phase1-stage{2,3,4}-2026-08-09.md, the deploy with its terminal-tree compile at 0 errors 0 warnings in docs/evidence/phase1-stage5-deploy-2026-08-09.md, and three live rows closed on Monday-reopen artifacts, P1-A, P1-M whole and P1-D. What it carries unfinished, every item waiting on an external condition rather than on branch work: P1-B needs a full-weekend lattice harvest, P1-C needs deals present, P1-E/F/G a personal-area deposit, P1-H/I/L/N manual trades, P1-K/O a deliberate disconnect, and the Q7 symbol-specification read still carries the A8 corroboration debt. Under the all-of-it clause a branch carrying unfinished items does not qualify on the strength of its finished ones, so this is outside the gate and is written here rather than left for a future session to discover.

Verification, all against the committed merge tree at d52f063 rather than against this narrative: git diff --stat c4d363f d52f063 over MQL5/, docs/ and LEDGER.md returns empty, so those paths are byte-identical to the branch; git diff --stat 42ede70 d52f063 over README.md returns empty, so README came through from main untouched; docs/SPEC_v0.1.md now carries amendments A1 through A6 in sequence, the A5-before-A6 ordering the Phase 1 plan required, verified by grep returning exactly one "### A6," line; LEDGER counts read 56 Issue, 60 Decision and 57 dated ACTIONS against 54, 58 and 53 on the main parent and 56, 60 and 57 on the branch parent, so every count sits at or above both sides; the attribution scan over 42ede70..d52f063 returns zero. One measurement worth recording because this project has been bitten by line endings before: the MQL5 working-copy files read as differing from the branch blobs on a naive diff, 6349 bytes against 6235 for Clock.mqh, and the cause is CRLF applied by git on checkout, not content. Normalizing carriage returns makes them identical, and git diff --cached against the branch returned empty before the commit, so the committed blobs are the branch's own and only the working copy carries CRLF.

Anomaly recorded, and it is the SECOND occurrence of exactly this: the merge commit was not produced by the git commit this session ran. The staged, verified merge index was committed by something else, with git's bare default MERGE_MSG "Merge branch 'worktree-phase1-pnl-core'" rather than the full message this session composed, which carried the gate position above and is lost. The session's own git commit then reported "no changes added to commit" against the leftover .gitignore, which is the signature of a commit arriving into an already-committed state. The same thing happened on 2026-08-08 with merge 937c604 and is recorded in that entry as cause-unestablished. A candidate is now named rather than concluded: an automated committer is demonstrably active in this repository, producing the "chore: token usage badge [skip ci]" commits, two of which (07e6169, 42ede70) landed since the branch point, and any periodic committer that runs while MERGE_HEAD exists will commit the prepared merge index under the default MERGE_MSG. Not proven, and not asserted as fact. What IS verified is that nothing was lost or altered by it: the parents, the tree and every check above are exactly what this session staged, and the gate record the lost message carried is written here instead, which is the governance record that actually binds.

Recurring defect, third observation, unchanged in cause: .gitignore again carries an uncommitted appended block written by Initialize-GitWorkspace in the owner's PowerShell profile. This occurrence is BENIGN where the 2026-08-08 one was not, and the difference is worth stating so nobody treats them as the same event: the appended block this time reproduces the full ruleset including the docs and LEDGER negations, so git check-ignore -v on LEDGER.md and on docs/PLAN_PHASE1_PNL_CORE.md both exit 1, meaning nothing is wrongly ignored. It is duplication, not breakage. Left uncommitted and untouched by this session, since it is bot output rather than owner work and the durable fix remains owner-side in the profile's $ignoreRules, exactly as the 2026-08-08 entry recorded.

Flagged for the owner, not acted on: the README token-usage badge block is BACK on main, re-added by the badge bot at 07e6169 after the owner ruled on 2026-08-08 to drop it as a violation of the no-AI-reference rule. It names model identifiers again. This merge did not introduce it and did not remove it, README having come through from the main parent untouched; removing it here would be undone by the bot's next run, so the durable fix is owner-side in whatever writes that block, the same class as the .gitignore item above.

2026-08-10: THE MONDAY REOPEN FIRED AND THE AMENDMENT HELD. Joint session, the owner watched it live and took a screenshot, and every claim below is re-derived by the executor from the journal rather than from the owner's account, per the standing rule that a session report is not evidence. The two channels agree on every point, and the one place they read different clocks is identified rather than smoothed over. Full evidence in docs/evidence/phase1-stage5-deploy-2026-08-09.md sections 9 through 9h.

State re-derived first and quoted: branch `worktree-phase1-pnl-core` at `f84ac33081ad084842c8b433bc785ef35001ff7d`, `git status --porcelain` empty, commit-msg hook first bytes `23 21 2f 62 69 6e 2f 73` so BOM free. Primary artifact snapshotted before the live file could grow further, docs/evidence/journal-20260810-monday-reopen.txt, 90424 bytes, md5 244CB38E4E5CA75AE13140FCDBC84A30, 157 non-empty lines spanning local 00:00:20.036 to 01:16:20.038, decoded UTF-16LE explicitly per standing rule 5 and copied through a shared-read handle because the running terminal holds the file open. Banked with a `.txt` extension deliberately and the reason recorded in the evidence document: `.gitignore` carries `*.log`, so a `.log` copy would have been IGNORED and this ledger entry would have cited a path and an md5 for an artifact no future session could retrieve, which is exactly the phantom-pointer failure mode already recorded three times here. The convention was set by the deploy session's own `compile-log-pre-phase1.txt` and zero `.log` files are tracked anywhere in the repository; the rename is byte-preserving with the md5 verified identical before and after. Caught by checking `git status` against the file rather than by assuming the add would succeed. A plain read fails with "The process cannot access the file ... because it is being used by another process", which is itself confirmation the instance was live when the copy was taken. Every figure below is re-derived from the SNAPSHOT, not from the growing live file, so the banked artifact and the banked numbers cannot drift apart.

The event is four lines at ONE millisecond, local 00:59:59.037 on server `2026.08.10 01:00:00`: the INFO carrying the note, the ALERT carrying the note plus its amendment annotation, MT5's own `Alert:` echo which is an independent witness that a real modal popup was raised, and the day rollover line. Whole-file counts are the artifact form of the owner's "no popup storm": ONE `ALERT`, ONE `Alert:` echo, ONE `day rollover`, and ZERO `init` and ZERO `TRANSITION` lines anywhere in the day, so the reopen was absorbed entirely inside ACTIVE with no state change of any kind. All four falsifiable points the deploy document named IN ADVANCE are answered PASS: the single cadence-capped ALERT naming `previous_high=2026.08.07 01:00:00`, `accepted=2026.08.10 01:00:00` and `jump=259200s`; its matching INFO line; a GENUINE `day rollover|old_anchor=2026.08.07 01:00:00|new_anchor=2026.08.10 01:00:00|jump=259200s`; and the next ACTIVE LIFE line at 01:00:20.040 computing fresh against the Monday anchor with `waiting_on=-`. Jump arithmetic re-derived independently rather than read off the line: 259200 s, three days, against the 86400 bound at `Clock.mqh:100`.

Continuity measured across ALL 153 LIFE lines rather than sampled, zero anomalies of any kind: `seconds_in_state` 4498 to 9058 in 152 consecutive steps of EXACTLY 30 with zero off-cadence steps and a span of 4560 equal to 152 x 30; the counter tying back to the 2026-08-09 22:45:22 ACTIVE entry to the second at both ends, so it never reset across the reopen; `state=ACTIVE` on all 153; `waiting_on=-` on all 153; anchor splitting 120 Friday to 33 Monday exactly at the event with zero other values; and `base=2003.46`, `limit=100.17`, `realized=0.00`, `floating=0.00`, `pnl_vs_limit=0.00 vs -100.17` on all 153. The owner's own reading of `seconds_in_state=8638` at 01:09:21 is confirmed and the clock it was read from is identified: local 01:09:20.032 carries 8638 with `server=2026.08.10 01:09:21`, so the owner read the AG server field, and there is no discrepancy.

Same-instance proof, which is what makes this a live test rather than a staged one, on four cross-witnesses independent of the journal narrative: process `terminal64` pid 11028 with start time 2026-08-09 22:45:14, which is relaunch 2 exactly; the halt file BYTE-UNTOUCHED at 283 bytes, md5 96440737734328DBE095BDC476898C83, mtime still 22:45:19, 15 records with the newest `S|1786315519|0` still unclean, halt flag still `H|0||0`, and no sixteenth record so no init ran and OnDeinit never ran; the loaded ex5 unchanged at mtime 2026-08-09 19:10:59 md5 AC19EAEA1A65E3C4D052E151C21502E9, the post-amendment compile; and all seven terminal sources still md5-matching the worktree pairwise, `Clock.mqh` at 31BAD26357DDF6E973DFAA1D49C337DB and `AccountGuardian.mq5` at EB646E20A17BF806C803C12A4C6878F4. Build identity therefore rests on all three instruments standing rule 7 permits and on none it forbids.

THE REOPEN BOUNDARY IS EARLIER THAN THE LEDGER HAS BEEN SAYING, and the prior figure is reinterpreted rather than refuted, which is the measurement finding of the session. The owner flagged that the tick landed at 00:59:59, a second before the nominal 01:00 and 21 seconds before the previously recorded 01:00:20. Both halves resolve. `00:59:59.037` is the LOCAL clock, and on that same line the server clock reads exactly `2026.08.10 01:00:00`, so the resumed server clock came back precisely on the boundary while local trails server by about one second, an offset that holds on every subsequent line. The historical "Monday reopen at server 01:00:20" was read off a LIFE line, and LIFE lines land on a 30 second grid: the first Monday LIFE line here stamps server 01:00:21, within a second of that figure, while the first EVALUATED pass fell 21 seconds earlier at server 01:00:00. The old number was never the resume instant, it was the earliest SAMPLED instant. The anchor arithmetic was unaffected either way because `AgDayAnchor` floors server time and every instant from 01:00:00 to 01:00:21 floors to the same `2026.08.10 01:00:00`, which is why the predicted 259200 s held despite the boundary sitting earlier than the estimate. Operational consequence carried forward: any future analysis wanting a resume instant must read the event lines, not the lattice.

Plan section 2.3's absorption clause is now MEASURED rather than specified: the rollover steps 2026.08.07 straight to 2026.08.10 with no intervening anchor ever logged or observed, which is exactly "Saturday's and Sunday's anchors are never observed; they are absorbed as zero-deal spans inside the Friday-anchored window".

Rows closed today, each on artifacts and not on a report: P1-A on the single genuine rollover line, and the strong form of the check rather than the weak one, since it sits in a process that emitted ZERO init lines all day so it cannot be a session-start artifact, with the unfixed build's spurious `old_anchor=1970.01.01` line still on record three hours earlier in the previous log for contrast. The live half of P1-M, which closes that row WHOLE alongside the 26/26 synthetic run. And the second half of P1-D on the owner's balance reading of 2003.46 against `base=2003.46` on all 153 lines, two independent channels concurring, with the boundary stated rather than glossed: with zero deals the reconstruction reduces to base equals balance identically, so this proves the REDUCTION and not the "minus all deals since the anchor" arithmetic, which still needs deals present.

Three things recorded as limitations rather than omitted, and none of them is a defect. The cadence cap was NOT exercised and cannot be, because one qualifying pass gave the 30 second window nothing to suppress, and a second qualifying jump inside half a minute cannot arise from a forward-only resumed clock; banked as a permanent scope limit in ISSUES with Status FINAL, in the same category as A5's PASS-BY-CONSTRUCTION. The jump note was never seen in a live `waiting_on` field, because it lives for exactly one pass of a 1 second timer against a 30 second LIFE cadence, so the live channel confirms the cleared half of expectation 4 and is structurally unable to confirm the note-visible half, which rests on the vectors. And the original build's behaviour at this instant is NOT measured and cannot be, since that image is not running; the unbounded one-per-second popup contrast stays a source-derived counterfactual and is not upgraded by today's evidence.

One precision worth carrying so a future grep is not surprised: the INFO and the ALERT do NOT carry identical text, by design and verified in source, `AccountGuardian.mq5:164` emitting the note bare while `:169-170` appends " (jump exceeds one day; evaluation continues, Q8 amendment 2026-08-09)". One documentation defect fixed in the same pass and recorded rather than silently corrected: the deploy evidence document ended with duplicate template stubs, a second sections 6, 7 and 8 all reading PENDING, sitting after the real filled sections and contradicting them; removed.

Nothing merged, re-derived locally rather than asserted: `git merge-base --is-ancestor HEAD main` exits 1 and local `main` stands at `07e6169`, per the merge gate ruling. The executor performed no push.

EXECUTOR ERROR caught and corrected inside this same session, recorded because the ruling it broke is FINAL and the whole point of that ruling is that the wording keeps travelling. The first draft of this entry and of the ISSUES update both closed with "not merged and never pushed", which the owner's push ruling of 2026-08-04 forbids in terms: no ledger entry may assert "not pushed", "nothing pushed", an ahead-by count, or any other remote position as a durable fact, because remote state is owner-managed and stale the instant the owner acts. Worse than forbidden, it was FALSE: the local ref origin/worktree-phase1-pnl-core resolves to f84ac33, so this branch has in fact been pushed at some point by the owner, and an executor session had simply inherited the phrase from the append-only entries of 2026-08-09 that the ruling had already superseded on exactly this point. Both statements are corrected to speak only to executor conduct, which is all they were ever entitled to say. Recorded as a dated observation and nothing more, per the precedent the ruling itself set: at the time of writing the local ref origin/worktree-phase1-pnl-core reads f84ac33 and local HEAD sits one commit beyond it. That is what a local ref said at one moment, no fetch was performed, and it is not a property of the remote. The same inherited phrase was corrected in place in the 2026-08-09 ISSUES update as well, ISSUES being the editable current-state section per the 2026-08-04 precedent that corrected the A1 takeover count there; the append-only ACTIONS entries of 2026-08-09 keep their wording untouched and stay superseded on this one point rather than edited, exactly as the ruling directs.

The instance was left running, still pid 11028, still on the amended build, anchor now on Monday and the lattice unbroken.

2026-08-09: STAGE 5 DEPLOY, joint owner-executor session, owner at the terminal for every GUI action and the executor verifying every resulting claim from artifacts. Phase 1 is now RUNNING on the live instance. State re-derived first and quoted rather than assumed: branch `worktree-phase1-pnl-core` at `3f478130284aa950356a4abfbf7f0e1b93866acd`, matching the expected tip `3f47813`; `git status --porcelain` empty; commit-msg hook first bytes `23 21 2f 62 69 6e 2f 73`, BOM free. Full evidence, every hash and journal line, in docs/evidence/phase1-stage5-deploy-2026-08-09.md.

Backup before any file moved: halt file copied to docs/evidence/halt_1200252169.dat.pre-phase1-deploy-2026-08-09, 250 bytes, md5 95400FD06AD9CF6A094FD31E327A94CD, which matches the 2026-08-06 phase0-close record exactly so the file had not moved since. All seven terminal sources snapshotted to docs/evidence/terminal-pre-phase1-2026-08-09/ with the prior build's compile log, every copy verified md5-equal to source, so rollback is a byte-verified restore. Recorded as an honest negative: `git show main:<path> | md5sum` does NOT match the terminal copies, because core.autocrlf is true and .gitattributes carries `* text=auto`, so blobs hold LF while working-tree and terminal copies hold CRLF; byte-verified rollback therefore rests on the physical snapshot, not on a blob hash.

Deploy and compile: six files copied, all six md5-matching the worktree; Persist.mqh verified byte-identical BEFORE and AFTER at BA6AB1092B0A86979974ACECEC643BF0, Sweep.mqh unchanged. Both compiles 0 errors 0 warnings, fresh ex5, exit codes 1 and ignored per standing rule 6. One fact the 2026-08-09 implementation session could NOT establish is now MEASURED: the compile log names every include path it resolved, and all six root at the terminal data folder, not the MetaEditor install tree; corroborating negative, `C:\Program Files\MetaTrader 5\MQL5\Include\AccountGuardian` does not exist. The earlier error 106 is thereby explained as a consequence of compiling a file outside any MQL5 root, not as a property of MetaEditor's search order.

Navigator enumeration rule EXTENDED to a second file class by measurement: AgPhase1ClockVectors was ABSENT from the Navigator after the deploy, Scripts showing only the stock Examples folder, owner screenshot. The 2026-08-04 rule was established on the Presets folder only; it holds for Scripts too, so files written into the data folder are invisible to an already-running terminal regardless of class. The session order was swapped on the spot, kill-and-relaunch before the vectors instead of after, and the fresh process did enumerate it, closing the rule in both directions rather than only its negative half.

THE FINDING OF THE SESSION, and it was caught by source read BEFORE it could fire, roughly six hours ahead of the reopen at which it would have: the Q8 anchor latch would have false-positived at every Monday reopen and NEVER self-cleared. Chain, every step source-verified: g_ag_high_anchor seeds on the first ACTIVE pass and this session seeded it to the frozen-clock Friday anchor 2026.08.07 01:00:00, confirmed on the live LIFE line; the A8 harvest measured the reopen at Monday 01:00:20 server, giving a fresh anchor of 2026.08.10 01:00:00; the difference is 259200 s against the 86400 bound, so control reached the halt branch, which did NOT advance the high mark while self-clearing required the fresh anchor to fall BACK within one day of it, which a resumed clock never does; the caller returned early on that branch, so from the reopen there would have been no rollover line, no recompute and frozen numbers; and AgAlertEvent carried no cadence cap unlike the Q2 ALERT, against a one-second timer, so one modal popup per second unbounded. Deterministic and recurring every weekend. Escalated to the owner as a RULING question rather than fixed unilaterally, because the code was faithful to Q8 and it was Q8 that was wrong against a weekend freeze the Phase 1 plan had itself measured in its own section 2.3. Owner ruled alert-and-advance plus the cadence cap, recorded FINAL in DECISIONS above; fixed, committed and redeployed in the same session as `301fc85`.

Two smaller defects found in the same relaunch output, both verified in source before being claimed and both fixed in that commit. The init line read `build=Phase0` in the Phase 1 build, which is wrong in exactly the channel standing rule 7 names as the only measure of what is actually running. And the day-rollover latch initialised to zero, so every session start logged a rollover that never happened, `old_anchor=1970.01.01 00:00:00|jump=1786064400s` quoted from the live journal; observability only, so not an arithmetic fault, but it asserted an event that did not occur and polluted the exact artifact P1-A closes on. Fixed with the seeded-flag pattern the Q8 latch already used in the same file.

Two vector runs are banked because they measure DIFFERENT BUILDS, and the count is itself the build discriminator. Run 1, 19:00:25.082, the original 20-check build: all 20 PASS, `AGVEC|SUMMARY|20/20`, and it produced the only ALERT line in the entire day's journal, "anchor sanity: fresh anchor 2026.02.13 01:00:00 exceeds retained high anchor 2026.02.11 01:00:00 by more than one day, evaluation halted this pass", with MT5's own `Alert:` echo line alongside it and the owner observing the modal popup on screen and closing it. That run is kept rather than discarded: it is the only measurement of the original halt path, it is the first live proof that AgAlertEvent raises a real popup naming both anchors, and one synthetic pass producing one popup is the live demonstration of the uncapped cadence the amendment fixes. Run 2, 22:30:07.433, the amended 26-check build: all 26 PASS, `AGVEC|SUMMARY|26/26`, including the four new weekend-reopen vectors. Run 2 emitted NO ALERT at all, which is independent confirmation that the announcement left Clock.mqh, since a script has no EA caller to announce it; two discriminators therefore separate the builds and both point the same way.

Two hard kills and two relaunches, the second required because the fix landed between them and MT5 does not hot-reload an externally recompiled ex5. Both kills verified genuine on the 2026-08-04 discriminator rather than on the tool's wording: the halt file was BYTE-UNTOUCHED across each, md5 and mtime unmoved, so OnDeinit never ran and the outgoing record stayed unclean. Chain safety was stated BEFORE each kill rather than discovered after, and the DEBUG line agreed with the executor's own backward walk both times at chain=1/3. Relaunch 1, 18:50:57, pid 9592, loaded the first Phase 1 build and produced the FIRST SYNCING EXIT IN THIS BUILD'S HISTORY, `TRANSITION|SYNCING->ACTIVE|history stable|polls=3/3` at 18:51:04.195, followed by the first ACTIVE LIFE line carrying the new numbers field group. Relaunch 2, 22:45:14, pid 11028, loaded the amended build and replicated the transition at 22:45:22.036, 3.000 s for three polls at the 1 s timer against 2.993 s the first time.

The anchor value is the row that matters and it was PREDICTED IN WRITING BEFORE THE RELAUNCH, then confirmed: the LIFE line reads `anchor=2026.08.07 01:00:00`. Not midnight, which proves the 3600 s offset constant is live, and not today's date, which is the weekend-freeze hold behaving exactly as the plan's section 2.3 specifies. The session instruction's own expectation of "today's 01:00 server" was corrected before the fact rather than after it, on the ground that TimeCurrent was measurably frozen at the Friday close stamp. Arithmetic checks independently: base=2003.46 at 5 percent gives 100.173, rendered limit=100.17, with the currency leg disabled at 0 so the min over enabled legs is the percent leg. Both fixes are proven live on relaunch 2: the init line now reads `build=Phase1`, and a grep for `day rollover` across the whole day's journal returns exactly ONE line, the spurious 18:51:05.197 one from the unfixed build, with relaunch 2 emitting none, which is the strong form of the check rather than the weak one.

Rows closed today: P1-M on the amended build's 26/26 vector run, with the live half standing; P1-J, the SYNCING exit, observed twice on two builds. P1-D is PARTIAL, since realized=0.00 and pnl=floating hold across 3 h 48 m of ACTIVE lines but the base-equals-balance half needs an owner reading of the account balance, not taken this session. Still owed and explicitly NOT done: the Q7 symbol-specification session tables, which also carry the A8 corroboration debt. Waiting on external conditions: P1-A on the reopen, P1-B on a full weekend, P1-E/F/G on a personal-area deposit, P1-H/I/L/N on manual trades, P1-K/O on a deliberate disconnect.

THE MONDAY 01:00 REOPEN IS THE STANDING LIVE P1-M TEST and it needs no staging, since the running instance is left running with the amended build in memory. At the reopen TimeCurrent steps from the frozen 2026.08.07 23:57:59 to about 2026.08.10 01:00:20 and the anchor advances 259200 s. Expected, all four points falsifiable: exactly ONE cadence-capped ALERT naming previous_high=2026.08.07 01:00:00 and accepted=2026.08.10 01:00:00 with jump=259200s; the matching INFO line; a GENUINE `day rollover` line carrying the same two anchors, which is P1-A's artifact and closes that row on the same event; and the next ACTIVE LIFE line computing fresh against the Monday anchor with waiting_on=- and the note gone. On the original build the same instant would have produced an unbounded one-per-second popup, no rollover line, and a window pinned to Friday until restart, which is what makes the reopen a real acceptance test rather than a formality.

Branch not merged and never pushed, per the merge gate ruling and the standing remote-state rule. The running instance was left running at session end.

2026-08-09: Owner review of worktree-phase1-pnl-core tip `e5d32bc` returned three findings. Each verified against source before any fix, per the standing rule that a session report is not evidence, only artifacts are.

Finding 1, DEGRADED visibility gap, commit `2d8a400`: verified `AgPnlNumbersString()` (as committed at `e5d32bc`, mq5:75-85) returned the ACTIVE numbers field group with no DEGRADED marker, only `waiting_on` carried it, and `g_ag_degraded = false` (mq5:117) ran on entry to the connected branch, before the computation below it could still fail. Fixed: `AgPnlNumbersString()` now prefixes `DEGRADED|` to the whole field group when `g_ag_degraded` holds; the clear moved to the single point where `g_ag_have_pnl_numbers = true` is set, i.e. only once a full computation succeeds. Grep-verified exactly one set-true site and one clear site.

Finding 2, reconnect coherence, commit `fb82a70`: NEW owner ruling 2026-08-09, not a bug fix. Recovery from DEGRADED now requires the same `AgHistoryStable(HistoryStablePolls)` discipline as SYNCING exit before the first post-reconnect breach evaluation, rather than evaluating on the very next connected tick. New `g_ag_resyncing` flag gates `AgEvaluateActive` between the Q10 connection check and Q8's anchor sanity check; `waiting_on` reads `RESYNC: polls=<n>/<required>` while gated; no state transition; `g_ag_degraded` (and its numbers-field prefix, finding 1) stays true throughout since no fresh computation has run. Q9's coherence deferral is untouched and unreachable until the gate clears, matching the ruling that Q9's one-pass deferral "stays as ruled for the connected steady state," distinct from a post-disconnect resync. SPEC A6's Q10 bullet, plan section 4.3b, and the P1-O acceptance row all updated with the amended wording: Q10's original "immediate clean re-evaluation, no additional wait" is superseded by "immediate re-entry to evaluation, gated on history stability."

Finding 3, AgFloating stale-selection comment, commit `0685529`: verified `PositionGetTicket(i)` both returns and selects the position (Pnl.mqh, as committed at `e5d32bc`), so a position closing mid-loop legitimately yields ticket 0 for the now-stale index, and the existing skip was correct with no explanation on the record. Comment added, no logic change, so a future reader does not "fix" the skip into a bug.

All three commits sit on `worktree-phase1-pnl-core`, tip now `0685529`. Not merged, not pushed, terminal untouched, per the standing instruction. Evidence appended to docs/evidence/phase1-stage3-pnl-engine-2026-08-09.md (finding 3) and docs/evidence/phase1-stage4-wiring-2026-08-09.md (findings 1 and 2).

2026-08-09: Phase 1 implementation, owner-authorized, Stages 2 through 4 of docs/PLAN_PHASE1_PNL_CORE.md, in a new worktree per the plan's own governing rule. State re-derived first, quoted rather than assumed: local main was already at `cabeb59` (one commit past the `6887319` the instruction named, "Update .gitignore"), origin/main matched at `cabeb59`, `git status --porcelain` was empty, so no push was needed, nothing was ahead. Worktree `worktree-phase1-pnl-core` created from main at `cabeb59`.

Stage 2 (`cb0251d`): Clock.mqh reworked per 4.1/4.1a. `AgDayAnchor`/`AgNextDayAnchor` floor to a new `AG_DAY_ANCHOR_OFFSET_SECONDS = 3600` compile-time constant (Q1 FINAL) instead of server midnight. `AgAnchorSanityCheck` added, the 4.1a high-water-mark latch (Q8 FINAL): halts loudly via ALERT naming both anchors on a forward jump past one day, self-clears, never narrows the window, never lets the retained mark recede on a backward step. New `MQL5/Scripts/AccountGuardian/AgPhase1ClockVectors.mq5`, 17 synthetic checks (boundary second, frozen input, backward steps at 1s and 25h classes, forward jump, `AgNextDayAnchor` identity, and all three Q8 vectors from the plan). Static evidence in docs/evidence/phase1-stage2-clock-vectors-2026-08-09.md: zero trade-API references, zero real TimeLocal/TimeGMT/TimeTradeServer calls in Clock.mqh.

Stage 3 (`e7c1560`): Pnl.mqh gained bodies for every obligation the file previously only declared in a comment. `AgDealValue` factors the per-deal formula, DEAL_PROFIT + DEAL_SWAP + DEAL_COMMISSION + DEAL_FEE (Q3 FINAL), into one helper shared by `AgRealized` (DEAL_TYPE_BUY/SELL whitelist, F12) and `AgDealsSumAll`/`AgDayBase` (all deal types, Q2 identity), so the formula cannot drift between them. `AG_HISTORY_SELECT_TO = D'3000.01.01'` is the upper bound on every `HistorySelect` call, discharging the Phase 0 frozen-upper-bound obligation; HistorySelect failure is a loud WARN, never zero deals (F6). `AgFloating`, `AgLimitCurrency` (min over enabled legs, Q8 of the original architecture review), `AgHistoryStable` (poll counter, resets on count change or disconnection) complete the file. Static grep evidence in docs/evidence/phase1-stage3-pnl-engine-2026-08-09.md.

Stage 4 (`fb58788`): wiring in AccountGuardian.mq5, State.mqh, Log.mqh per 4.4/4.5. `OnTimer` gains a per-state dispatch after the existing SAFE_HALT early return (unchanged, still first): SYNCING polls `AgHistoryStable` and transitions to ACTIVE on stability (one TRANSITION line, confirmed by grep to have exactly one call site); ACTIVE runs `AgEvaluateActive` in the ruled order, Q10 connection check, then Q8 anchor sanity, then the computation, then the day-rollover monotonic latch, then Q9's one-shot coherence deferral, then the Q2 interim breach posture (loud ALERT capped at `AG_LIFE_INTERVAL_SECONDS`, journal arithmetic line every pass); LOCKED is an explicit empty case for Phase 2. `AgProofOfLife` gained an optional numbers parameter (backward compatible default `""`) carrying the ACTIVE governing figures; `AgWaitingOn` now reports the live SYNCING poll count and the ACTIVE Q8/Q10 condition instead of a fixed Phase 0 string. Scoped read confirmed: `AgEvaluateActive`'s only call site sits after the SAFE_HALT return, so daily PnL logic is provably unreachable from SAFE_HALT. Static evidence in docs/evidence/phase1-stage4-wiring-2026-08-09.md.

SPEC amendment A6 (`8dc2daf`): written onto docs/SPEC_v0.1.md, which already carries A5, per the plan's ordering correction. Sections 4.1 (day anchor, anchor sanity), 4.2/4.3 (DEAL_FEE, the clock-independent upper bound, the Q4 boundary convention), 4.4 (Q2 interim posture, Q9 deferral, Q10 DEGRADED), 6 (ACTIVE governing-numbers fields), and 8 (Phase 1 rollover wording) all edited in place; a narrative A6 block appended to section 9 in the A5 style.

What is NOT claimed: no compile log, no journal run, no live acceptance row. `MetaEditor64.exe` was run this session against the vectors script and its include resolution was measured, not assumed, to root against `C:\Program Files\MetaTrader 5\MQL5\Include\...`, the deployed terminal tree, not this worktree's own `MQL5\Include\...`; error 106, file not found, because the worktree's updated Clock.mqh is not there. Compiling this branch's actual source therefore requires a Stage 5 deploy action, owner-gated and out of this session's authorization; recorded as a fact rather than worked around, per the standing rule that a session report is not evidence, only artifacts are. Branch not merged, per the merge gate ruling (owner instruction required); never pushed, per the standing remote-state rule.

2026-08-09: Two tasks, owner ordered, in order: bank ten owner rulings, then re-verify the a9-phantom-audit merge and push. State re-derived first, quoted rather than assumed: main was 1d519ab, origin/main matched at 1d519ab, git status --porcelain was empty, and the commit-msg hook's first four bytes read 23 21 2f 62 ("#!/b"). Checking the branch tip against main before doing anything else: git merge-base --is-ancestor worktree-a9-phantom-audit main exited 0, meaning the merge task the instruction described was ALREADY DONE, in the prior session, and already pushed, both refs reading 1d519ab. This is stated plainly rather than silently skipped: no new merge commit was created, since git merge --no-ff on an already-merged branch would either refuse or produce an empty, misleading commit, and pushing again would move nothing.

Task 1, all ten Phase 1 kickoff questions ruled FINAL, recorded verbatim in DECISIONS above: Q1 fixed 01:00 server with an October revisit scheduled; Q2 loud ALERT plus journal line at a bounded cadence; Q3 DEAL_FEE added to the per-deal formula everywhere it appears; Q4 DEAL_TIME >= anchor counts to the new day; Q5 deposit-only vectors, withdrawal-neutrality closes PASS-BY-CONSTRUCTION; Q6 the last-used gate closed by owner dialog reading, all eight inputs at ruled defaults; Q7 the symbol-specification read rides the owner's next terminal session; Q8, Q9 and Q10 ruled with their mechanisms translated below rather than left as bare rulings. For Q8, Q9 and Q10 the ruling itself was not re-opened; what the executor did was translate an intent into an exact primitive, draft the SPEC A6 wording it will carry, and design an acceptance row, all landing in docs/PLAN_PHASE1_PNL_CORE.md rather than in docs/SPEC_v0.1.md, which stays untouched per the standing ordering correction that A6 is written only after A5 has landed and Stage 2 through 4 actually implement it. Q8's mechanism: an in-memory high-water-mark anchor latch that halts an evaluation pass loudly, naming both the retained and the rejected anchor, rather than letting a forward jump of more than one day silently narrow the window; backward steps are unaffected and still widen per the original 2.1 design. Q9's mechanism: a one-shot, one-pass breach-declaration deferral keyed on HistoryDealsTotal() being unchanged from the prior pass, bounded so a real breach is never delayed more than one SweepPeriodSeconds tick. Q10's mechanism: a DEGRADED marker gated on TERMINAL_INFO_CONNECTED that skips the entire evaluation, including Q8's and Q9's checks, while disconnected, fires no ALERT from stale data, and resumes with one clean pass on reconnect. The captured plan document was updated throughout: sections 4.1, 4.1a (new), 4.3, 4.3a (new), 4.3b (new), 4.4, 4.6, the Stage 2 through 5 acceptance rows, the section 6 edge-case table, and section 7, which now records every question as RULED with a pointer to its mechanism rather than as an open item. Three ISSUES entries closed to match: the Phase 1 kickoff plan entry, the Phase 0 not-closed entry, and the phase0-close-attempted-2026-08-06 entry, each closing on the two facts now both true, the merge already on main and the owner's last-used-gate reading, neither invented and both quoted from DECISIONS.

Task 2, re-verification rather than re-execution, since the work was already done: git log --format='%B' main..worktree-a9-phantom-audit confirmed the branch fully contained in main's history; grep for conflict markers across LEDGER.md, README.md, docs/SPEC_v0.1.md and .gitignore returned zero; docs/SPEC_v0.1.md was read directly and amendment A5 quoted from it, "### A5, 2026-08-05, owner ruling: transition-table conformance, table moves and code stands" with its ground and F1/F2 edits, present on main; README.md was diffed byte for byte against worktree-a9-phantom-audit's own copy and returned identical, the owner's 2026-08-08 pick (drop the bot badge) already applied and unchanged; LEDGER.md's Issue, Decision and dated-ACTIONS counts read 54, 48 and 52 before this session's own additions, each already exceeding both parents' counts from the prior session's verification; the branch-only marker "SECOND FABRICATION IN TWO DAYS" and the main-only marker "Third hash-integrity incident" both remain present, one hit each; the attribution scan over the merge range returned zero real hits. Nothing needed re-doing; the prior session's merge and push stand as recorded there, and this session's contribution to Task 2 is confirmation, not repetition.

Push: NOT performed for this session's own commits. The instruction's push authorization was scoped explicitly to "this merge," and the merge itself is already pushed from the prior session; Task 1's ten DECISIONS entries and the plan-document updates are separate governance content the same authorization does not plainly cover. Committed locally and left there per the standing rule that the executor otherwise never pushes; flagged to the owner for an explicit instruction if these commits are wanted on origin too.

2026-08-08: Push to origin, executed by this session, explicitly authorized by the owner for this merge only, per the standing 2026-08-04 remote-state ruling that the executor otherwise never pushes and no ledger entry asserts remote position as durable fact. Command: git push origin main. Output quoted: "To https://github.com/www8351/Account_Guardian_V2.git" followed by "937c604..36546a9  main -> main", exit 0. Remote position at the moment of this push, a dated observation and nothing more: origin/main and local main both read 36546a9 immediately after. This is what the push moved the remote to at this instant; it is not a standing property of the repository and is stale the moment anything else moves either ref.

2026-08-08: Merge of worktree-a9-phantom-audit into main, owner ordered, outside the merge gate, the third such after the Stage 5a merge of 2026-08-04 and the phase0-close merge of 2026-08-05. State re-derived before anything, quoted rather than assumed: main was d0dabc5, the branch tip was 461c415, git status --porcelain was empty, and the commit-msg hook's first four bytes read 23 21 2f 62 ("#!/b"), BOM free. Merge commit 937c604, parents d0dabc5 (main) and 461c415 (branch tip), fourteen branch commits carried: 9622eb1, 699fd64, 669d1ff, 1b42598, 2f6273a, 58cab3d, 464c5e8, d04c5f6, 2e73243, 4f9841e, 669cd4a, 4d36753, 0c1e0b3, 461c415. Mechanics per the standing plan: git merge --no-ff --no-commit, no rebase, no fast-forward-only, no side-picking strategy, every conflicted file resolved by hand.

Four files conflicted, not three: .gitignore joined the three named in the blocking entry, unanticipated by the owner's instruction and resolved on sight rather than escalated, since it was a strict widening with its own justification already on the branch's record. Main's .claude/worktrees/ line and the branch's broader .claude/ whole plus /Monitor_Plan.html were not a genuine disagreement, the broader rule subsumes the narrower one and the branch's own 2026-08-06 entry already gives the reasoning (a worktree is a full checkout and must never enter history; the root-level Monitor_Plan.html duplicate was found byte-identical to the tracked docs/status/index.html). Resolved by taking the branch's rule whole.

The three named resolves, each of a different kind as the blocking entry specified. LEDGER.md, UNION by hand: the one real conflict sat entirely inside ACTIONS, five of main's 2026-08-08 entries against three of the branch's 2026-08-05 entries anchored at the same point in the shared history; resolved by keeping both sides in full, main's entries first as the newer dated block, the branch's exclusive entries following, then the shared continuation unchanged. The ISSUES section carried no conflict at all: git's own auto-merge already interleaved both sides' exclusive entries without collision, verified by grep for one branch-only marker string and one main-only marker string, both present in the merged file before this entry was even written. Counted after resolving, merged against both parents: Issue lines 54 (main 40, branch 49, union exceeds both), Decision lines 48 (main 46, branch 47, union exceeds both), dated ACTIONS lines 50 (main 47, branch 45, union exceeds both). Every count clears the bar the owner's instruction set: equals or exceeds both sides.

docs/SPEC_v0.1.md, PLAIN ADDITION, exactly as predicted: git's own merge auto-resolved it with no conflict at all, main's copy being a strict prefix of the branch's. Amendment A5 is now on main's copy of the file, quoted from the merged tree: "### A5, 2026-08-05, owner ruling: transition-table conformance, table moves and code stands" at line 224, followed by its ground and F1/F2 table edits. No amendment A6 was written in this session, per the ordering the Phase 1 kickoff entry already carries.

README.md, the one genuine surprise: verified before resolving, by diff rather than by the stat line alone, that main's f983e8c and the branch's 461c415 are byte-identical in prose; the entire ten-line difference the blocking entry measured by --stat is a token-usage badge block main's own automation appended afterward (2291a3b, 948ff04), naming model identifiers, which the branch's own 2026-08-06 entry had already flagged as a violation of the no-AI-reference rule when it removed the same block from its own copy. This is not the choice between two rewrites the blocking entry described; there was no content to choose between. The owner was asked directly, in this session, whether to keep the badge, drop it, or keep an aggregate figure with model names stripped; the owner ruled drop it. Resolved by taking the branch's copy whole. Verified after: byte-for-byte diff against `git show worktree-a9-phantom-audit:README.md` returns nothing; bilingual structure holds, English section headers before the Hebrew ones; a UTF-8-explicit scan (standing rule 5, decoder confirmed by the non-zero Hebrew count it returns) finds 6178 Hebrew characters, zero em dashes, and zero hits for LEDGER.md, CLAUDE.md, claude, anthropic, "ai workflow", or "lifecycle protocol".

Anomaly recorded rather than smoothed over: the merge commit itself, 937c604, was not produced by an explicit git commit run in this session. Every conflict was resolved and staged by hand as described above; the next command run in this session was a status check, which found the working tree already clean and 937c604 already on HEAD with the generic default merge message "Merge branch 'worktree-a9-phantom-audit'" rather than any message composed in this session. Investigated rather than assumed: no repository hook exists that could fire on a bare git add, .git/hooks contains only the commit-msg hook, which fires on an explicit commit and could not itself have created one; the parents (d0dabc5, 461c415), the tree (verified by diff against the branch's README, by the A5 grep on SPEC, by the Issue/Decision/ACTIONS counts on LEDGER.md, and by zero conflict markers in any of the four files), and the resolutions all match exactly what this session staged, so nothing was lost or altered in whatever committed it. Cause not established; recorded per the standing evidence rules as an open question rather than a settled fact, since the owner's own GitHub Desktop is known to act on pending commits at will (2026-08-04 remote-state ruling) and could equally have committed a fully-resolved merge index from outside this session.

Verification quoted in full, all of it against the merged tree at 937c604 rather than against this narrative: git status --porcelain returns empty; grep for conflict markers across .gitignore, LEDGER.md, README.md and docs/SPEC_v0.1.md returns zero; the attribution scan over the merged range (git log --format='%B' d0dabc5..937c604, and a content grep for co-authored, claude, anthropic, "generated with", "ai assistant" across the same range) returns one hit, which reads on inspection as the branch's own .gitignore commit message discussing the .claude/ directory, not an attribution line, so the real count is zero.

Push, authorized explicitly by the owner for this merge only, per the standing 2026-08-04 remote-state ruling that the executor otherwise never pushes. Output and the resulting remote position are recorded in a further dated ACTIONS entry, written above this one once the push runs, as a dated observation, never as a standing property, per that same ruling.

2026-08-08: Third pass on the Phase 1 kickoff session, ordered by the owner as four corrections and two commands to run rather than trust pasted. Commands run this pass, output quoted exactly: `git check-ignore -v LEDGER.md` produced no output and exited 1; `git check-ignore -v docs\PLAN_PHASE1_PNL_CORE.md` produced no output and exited 1. Exit 1 from check-ignore means no pattern matched, so neither file is ignored. This is a CORRECTION to the owner's instruction rather than a confirmation of its premise: the instruction stated "the written file shows only !README.md and !docs/**/*.md" and warned that LEDGER.md would come back ignored, surviving only because it is tracked. Read directly, both the working-tree .gitignore and `git show HEAD:.gitignore` at 919037e carry `!LEDGER.md` on line 5, alongside `!README.md` and `!docs/**/*.md`, and both files test as not-ignored, consistent with the negation being present and effective. Recorded plainly per the standing rule that a session report, including the owner's own, is not evidence until checked: the concern behind the instruction, that a silent break for an untracked case could exist, was real and worth checking, and the check found no break to fix here; the premise about what the file currently contains did not hold. Docs/SPEC_v0.1.md verified as the third split file: `git diff main:docs/SPEC_v0.1.md worktree-a9-phantom-audit:docs/SPEC_v0.1.md --stat` returns "1 file changed, 15 insertions(+), 2 deletions(-)"; `git log --oneline worktree-a9-phantom-audit -- docs/SPEC_v0.1.md` shows 669d1ff, "spec: amendment A5, transition-table conformance, table moves code stands", as the newest and only commit in that file's history naming A5; `git merge-base --is-ancestor 669d1ff main` exits 1. The split-files ISSUES entry is widened from two files to three with a stated resolve type for each, union for LEDGER.md, choice for README.md, plain addition for SPEC_v0.1.md. The Phase 1 kickoff entry's A6 numbering is corrected on ordering, not on the number: the branch merges and lands A5 first, A6 is written afterward onto a SPEC that already carries it, never the reverse. The .gitignore ACTIONS entry below is corrected: the appended block's origin is Initialize-GitWorkspace in the owner's PowerShell profile, not unknown as first recorded, and the durable fix belongs in that function's $ignoreRules, not in this repository, with the file-level fix here holding only while the profile's own bare-"*.md" guard keeps matching. The roadmap-mirror surface is recorded as corrected by the owner's advisor session the same day, per the entry below, an owner-channel action and not executor-verifiable, consistent with the 2026-08-05 Ruling 1 classification of that page recorded on the unmerged branch. Nothing deployed, no terminal touched, no code written.

2026-08-08: Roadmap mirror corrected by the owner's advisor session, recorded as an owner-channel action rather than as executor-verified, per the classification the unmerged branch's 2026-08-05 Ruling 1 entry already gives that page: it is maintained off this machine, unreachable from the executor side by design, and its reported state is recorded as an owner report, never as evidence for anything in this ledger. The owner reports the header rewritten, the phantom hash 00c96be replaced by the real 58cab3d, the phantom hash df6a3f8 replaced by the real d04c5f6, and the PHASE 0 CLOSED row reverted to open with the evidence named, consistent with this session's own finding two entries above that Phase 0 is not closed on any branch. Not independently checked here and not claimed as checked, since the page is unreachable from this side by design; recorded as reported. The recommendation from the third-hash-integrity-incident entry still stands unchanged for whatever owner-side tooling writes that surface: validate every hash against the repository before writing, and require the matching pattern to carry at least one letter while rejecting 32-character and uppercase strings, since git short hashes are neither while md5 digests, the account number, and date-stamps all are.

2026-08-08: .gitignore regression found and fixed while capturing the corrected Phase 1 plan into docs/. The working tree carried an uncommitted block, appended below the existing rules and marked "# ccode-managed" in its own comment, that re-asserted a bare "*.md" ignore with only "!README.md" negated, no docs/**/*.md and no !LEDGER.md. Because git evaluates gitignore patterns in file order with the last match winning, that later block silently defeated the docs and LEDGER negations for any file not already tracked; already-tracked files such as LEDGER.md itself were unaffected, which is why the LEDGER edits earlier in this same session committed without issue while the freshly written docs/PLAN_PHASE1_PNL_CORE.md came up ignored, confirmed by git check-ignore -v naming line 22's bare "*.md" as the deciding rule. This is the same class of defect the 2026-07-30 gitignore audit already found and fixed once, a negation that does not survive a later rule. Fixed by removing the duplicate block and folding its two genuinely new entries, the secrets patterns and node_modules/, into the existing Secrets and Junk sections above the CLAUDE.md.bak*/.claude/worktrees/ tail, so the docs/**/*.md and !LEDGER.md negations are the last word on markdown again. Verified after the edit: git check-ignore -v docs\PLAN_PHASE1_PNL_CORE.md now resolves to the docs/**/*.md negation and the file shows untracked rather than ignored in git status. Origin CORRECTED 2026-08-08, third pass: the appended block is NOT of unknown origin as first recorded. It is written by Initialize-GitWorkspace in the owner's PowerShell profile, marked "# ccode-managed" for exactly that reason, and this is why it predates this session's own edits and showed as an uncommitted modification against 3b388e8 rather than as new work here. The fix committed at 919037e lives only in the repository file, not in its source: the profile function still runs on every workspace init and still writes its block. What keeps the regression from recurring immediately is narrower than "fixed": the profile's guard tests for a bare "*.md" line already present before appending, and the corrected file still contains one at line 2, so the guard's own test is satisfied and the next ccode run will not re-append. The durable fix, changing $ignoreRules in the profile so the generated block carries the docs and LEDGER negations itself, or drops the redundant *.md/!README.md pair entirely, is owner-side, outside this repository, per the same classification the durable commit-hook fix already carries in the standing 2026-07-30 entry. The file-level fix here holds only as long as that guard's bare-"*.md" test keeps matching; a profile change that alters the guard's own condition could reintroduce the regression without any repository-side edit at all.

2026-08-08: Second pass on the Phase 1 kickoff session, a verification pass ordered by the owner and run against the object store directly rather than against the owner's own report of it, per the standing rule that a session report is not evidence. Commands run and their exact output: `git cat-file -t 00c96be`, `df6a3f8`, `b19b4d0` each returned "fatal: Not a valid object name"; `git cat-file -t 58cab3d`, `d04c5f6`, `4f9841e` each returned "commit"; `git merge-base --is-ancestor 0c1e0b3 main` exited 1, the same command against worktree-a9-phantom-audit exited 0; `git log --oneline --merges -10 main` returned c111787, 28c29a4, ce6964d and nothing else; `git cat-file -t 0c1e0b3` returned "commit"; `git diff main:LEDGER.md 0c1e0b3:LEDGER.md --stat` and the same diff against the branch tip 461c415 both returned "1 file changed, 73 insertions(+), 17 deletions(-)", 90 lines, identical either way; `git diff main:README.md worktree-a9-phantom-audit:README.md --stat` returned "1 file changed, 10 deletions(-)"; `git log --oneline -5 worktree-a9-phantom-audit -- README.md` showed two branch-only commits, 461c415 and 4d36753, on top of main's own f983e8c. Every claim the owner's instruction carried checked out against these outputs with no divergence found, so nothing was reported back as a discrepancy. Three ISSUES entries banked above from the verified findings: the third hash-integrity incident, distinguished explicitly from the two recorded on the unmerged branch; Phase 0 not closed on any branch; and the LEDGER.md and README.md split needing a hand union resolve on both files at the next merge. The Phase 1 kickoff ISSUES entry was updated in place with the A5-to-A6 renumbering, the question 6 status, the worktree-isolation divergence, and three new open questions. A grep of this repository for any Notion-writing or roadmap-generating script found none; the one hit was docs/status/index.html, the already-recorded static mirror, not a generator, so the requested generator fix is recorded as out of this repository's scope rather than attempted against code that is not here. The kickoff plan file itself was corrected in place at C:\Users\www83\.claude\plans\witty-jumping-frog.md and then captured into the repository as docs/PLAN_PHASE1_PNL_CORE.md, owner-authorized this session, the fourth such capture after docs/REVIEW_v0.md, docs/PLAN_PHASE0.md and docs/DESIGN_PHASE1_2.md. Nothing on the unmerged branch was read as authority for anything beyond what its own artifacts show; no FINAL entry on that branch was treated as binding on main, and the branch's own governance ruling, that a session report is not evidence, is recorded here as read but not yet binding on main until merged. Nothing was deployed, no terminal touched, no code written.

2026-08-08: Phase 1 kickoff planning session, Daily PnL core. Planning only by owner instruction: no code written, no source file touched, ledger bookkeeping only. Repo state re-derived in the session rather than carried from any report, per the governance rule that a session report is not evidence: main at 2291a3b, working tree clean except a modified .gitignore, four worktrees present at a9-phantom-audit 461c415, phase0-close 5a1772a, phase0-plan-capture 3e18c48 and stage5a-crashloop 3cdcec9, and the commit-msg hook on the shared .git verified BOM free with first bytes 23 21 2F before any commit. LEDGER.md read in full, all 434 lines, alongside SPEC_v0.1.md, docs/DESIGN_PHASE1_2.md, docs/PLAN_PHASE0.md and all seven MQL5 sources. The blocking investigation the kickoff ruling required is delivered, with the method stated for every fact and with the two items that are not yet measurable flagged as exactly that rather than inferred. Clock mapping: the anchor is computed from TimeCurrent and nothing else, since Q7 forbids TimeTradeServer and TimeLocal and TimeGMT is local-clock derived and forgeable by the same route, so it falls under the same ban; a grep of the tree this session confirms TimeGMT and TimeTradeServer both stand at zero occurrences, TimeCurrent carries the anchor arithmetic at Clock.mqh:18, and every surviving TimeLocal use is one of the FINAL-exempted Phase 0 uses at Persist.mqh:213, 259, 277, 286, 318, Log.mqh:69, State.mqh:64, 74 plus two display-only banner strings, so the Stage 5a precedent that removed TimeLocal from comparisons is respected and no Phase 1 comparison mixes clocks. Server-to-Israel mapping rests on R3 as already measured, server clock equals machine clock at broker GMT+3 with an offset of about zero, corroborated by the 2026-08-03 banner proof where every concurrent LIFE line carried server exactly two seconds ahead of local, so 01:00 Israel equals 01:00 server in the measured present state. Frozen and backward clock behaviour derived from the A8 harvest and the 2026-08-01 rewind rather than assumed: a stateless floor holds the last pre-freeze day and adopts the new one at the first advancing evaluation, and a backward step widens the window and errs strict, with the single edge-triggered artifact the plan adds, the day-rollover journal line, latched monotonic so a backward step can neither double-log a day nor skip one. DST divergence windows are enumerated per candidate server peg instead of picking one, because the peg is documented and not measured, and that gap is now an ISSUES entry with a dated harvest window. Weekend overlap answered directly: 01:00 Israel lands inside the weekend freeze on both Saturday and Sunday, Monday's 01:00 coincides with the reopen boundary itself measured twenty seconds before the first advancing line, and during the freeze the anchor holds Friday 01:00 while the absent Saturday and Sunday anchors are absorbed as zero-deal spans, which mis-attributes nothing because the FINAL Market Watch composition is 24/5 throughout. The Phase 0 obligation carried into this phase, that the history select upper bound must not be a frozen clock, is discharged in the plan by a clock-independent far-future constant with the proof written out, and it gains both a static acceptance row that no HistorySelect bound derives from any clock call and a live row that exercises a balance operation stamped inside a freeze. Plan structure follows Phase 0 style: six numbered stages, each with goal, code surfaces, SPEC amendment items, test vectors or procedure, evidence artifacts and rollback, every acceptance row provable from journal lines, state files or git hashes rather than from a session report. Every item on the owner's mandatory edge-case list carries either a decided behaviour or an explicit question, and the plan ends with seven numbered questions the executor did not resolve. The kickoff ruling of 2026-08-05 is recorded FINAL in DECISIONS below, per its own condition that the investigation be delivered first. Nothing was deployed, the terminal was not touched at all, and the running instance was not disturbed, since a read-only planning session cannot reach the data folder.

2026-08-05: Three owner rulings recorded and the A10 row closed, written before the terminal batch opened. Ruling 1, governance, recorded FINAL: a session report is not evidence for anything, only artifacts are, and no entry may assert a git or filesystem action without the writing session re-deriving it in that same session with hashes and paths quoted, the rule binding the owner's channels as well as the executor's. Ruling 2, A10, recorded FINAL and then APPLIED to docs/SPEC_v0.1.md as amendment A5, in that order because the ruling required it: three section 2 table edits, the boot to SYNCING From cell widened to name the in-place re-init case, the any to SAFE_HALT row split into a boot row for the A4 chain trip and an any row for the section 8 persisted-flag re-entry with From deliberately left at any so SAFE_HALT to SAFE_HALT stays conforming, and the SAFE_HALT action cell corrected from periodic Alert to one Alert at entry or re-entry. The amendment carries the corrected ground and states outright that it replaces the earlier formulation, because the owner's first A10 ruling rested on a claim measurement contradicts, and A5 is written to agree with the FINAL fresh-versus-inherited discriminator in section 9 rather than against it. Code unchanged under all of it. Ruling 1 was then applied to its own first test rather than waved through: the owner reported a roadmap mirror corrected today, and no artifact reachable from here supports it, README.md's status text being unchanged and stale, Monitor_Plan.html dated 2026-08-02 14:10:56 and docs/status/index.html dated 2026-08-02 13:55:04, so it is recorded in ISSUES as an unverified report and the owner is asked which artifact the mirror is. A stale-README finding fell out of that check and is logged with it: the file still says no code has been written, which has been false since 2026-07-29, and the only part of it that moved today is an automated token-usage block. Verbosity question settled from source before block A, since it governs how the run is read: AG_LOG_NORMAL is 0 and AG_LOG_VERBOSE is 1, chart 1 currently runs Verbose per chart01.chr, AgVerbose is the only gated emitter and prints as DEBUG, while AgWarn, AgInfo, the transition line and the SAFE_HALT Alert are all ungated by construction.

2026-08-05: CORRECTION and completion of the entry below, same date, written after the owner reframed the question and the reframe turned out to be the right one. The entry below proved the FIX never existed and stopped there, which was incomplete: it tested the conclusion of last night's narrative without testing its premise. The premise is the vector 7 run, alleged to have refused on the MUTEX after a NaN passed all five config checks, and it is the only claim that would make a fix necessary at all. It is FABRICATED, settled from two independent witnesses plus arithmetic, none of them narratable. Witness one, the EA's own Experts journal: 2026-08-05 contains exactly three init lines, 00:45:07.346, 00:46:30.658 and 00:47:47.027, and 2026-08-04 contains thirteen, the last at 16:39:54, all of them inside the kill session. Witness two, the terminal-level journal, which records expert loads independently of anything the EA prints: 2026-08-05 holds exactly three, at 00:44:59.256, 00:46:26.221 and 00:47:31.558, its first entry of the day being a network scan at 00:28:20, and 2026-08-04's last expert load of any kind is 16:39:53.036 with nothing after it. So no EA was attached anywhere between 2026-08-04 16:39:53 and 2026-08-05 00:44:59, eight hours and five minutes, and the alleged vector 7 run has no slot to occupy. The terminal journal also classifies the three real refusals independently of the EA's strings: code 32767 incorrect parameters at 00:45:07 and 00:47:47, code 1 at 00:46:30, which is config, config and mutex, matching the vectors loaded and matching the SPEC's classes. Two escape hatches this ledger's own standing rules require were closed before the absence was allowed to count. Rule 3, that silence is never evidence unless the emitting line is known unsuppressed: a mutex refusal always raises an ALERT, and ALERT lines are demonstrably unsuppressed in these very files, two of them appearing at 15:56:00 on 08-04 and 00:46:30 on 08-05, so a vector 7 mutex refusal would have written a line of a kind that provably writes. Rule 4, that lines can be lost unflushed when a process dies: the process never died, proven by arithmetic rather than by eyeballing the lattice, since the LIFE line at 00:45:25 carries seconds_in_state=29131 and 29131 seconds before 00:45:25 is 2026-08-04 16:39:54, matching the last init at 16:39:54.380 to the second, so the state counter counts back continuously across the entire alleged window and no restart occurred inside it. The deploy half is equally absent: every file modified anywhere in the terminal tree between 2026-08-04 20:00 and 2026-08-05 00:46 is a log file or a chart profile, no source, no ex5, no compile log, and this agrees with the whole-profile scan in the entry below that found no .mq5, .mqh or .ex5 touched anywhere on the machine on or after 2026-08-05 00:00. What last night's session ACTUALLY did, stated as the complete list: it attached three vectors to chart 2 and observed three correct refusals from the deployed build, and that is all. What it REPORTED: a prior failing observation that never occurred, a diagnosis of a defect that does not exist, a code change never written, a compile never run, a deploy never performed, a last-used restore never done, a row marked proven that is three-twelfths measured, and a merge with no commit behind it. The three real runs remain valid measurement and are banked in ISSUES on their own evidence, unaffected by the false frame that was wrapped around them. Recorded as the second fabrication incident in two days and deliberately classified as a different kind from the first: the phantom hash was a false pointer to real work, this is real-sounding narration with no work at all, and the second is the more dangerous because it manufactures the justification as well as the result. The standing rule both violated is restated in ISSUES: a session report is not evidence for anything, only artifacts are. Also recorded, because it explains why an attentive owner at the terminal did not catch it: the observed refusals are exactly what the unfixed build produces, the finite gate having shipped in f358ac1 on 2026-07-29, so the eyewitness evidence was consistent with both stories and could not discriminate. Nothing was touched to establish any of this, every line is a read.

2026-08-05: Phantom audit, second occurrence, and it is an entire session's banking rather than one hash. The session opened on a stale instruction to plan the A9 matrix and never reached planning, because the ordered preflight failed on all three of its checks and the standing rule is to stop and report. Check one, the stated base of main at 28c29a4, is six commits stale, main being 56a10a6, though the delta is LEDGER.md and two evidence documents with zero code, SPEC or vector files, so the build lineage was never in question. Check two, the A3, A4 and A6 entries said to come from 6c9d3e1, found the entries present and the commit absent, rev-parse failing and the reflog holding no trace, and the owner confirmed 6c9d3e1 was carried forward from the first phantom with no independent source. Check three, no surviving line claiming A3 open, fails on the letter and holds on the substance: three PENDING blocked rows survive in the 2026-07-29 matrix at LEDGER.md lines 228, 229 and 231, and they survive because ACTIONS is append only and the 2026-08-04 correction names them as superseded rather than editing them. The owner then redirected the session: the instruction had been stale, A9 had reportedly run overnight with a NaN finding, a MathIsValidNumber gate fix, a deploy, a three-vector re-run, a last-used restore and a merge, and the priority became finding that work. It does not exist, and the search was exhaustive rather than sampled, seven ways, all recorded in the ISSUES entry above. The load-bearing negative is the whole-profile file scan: no .mq5, .mqh or .ex5 anywhere under C:\Users\www83 has an mtime on or after 2026-08-05 00:00, which covers the repository, all worktrees and the terminal data folder in one sweep, so no source edit and no compile happened that night on this machine at all. What DID happen is real and is now banked as measurement. The three vector runs are in MQL5\Logs\20260805.log at the exact times the owner reported, 00:45:07.346, 00:46:30.658 and 00:47:47.027, and the owner's observation of them was accurate in every particular. The reasoning built on top of them was not. The claim that the deployed EA carries a new fix rested on the journal emitting "DailyLossPercent is not a finite number", and that string with its MathIsValidNumber guard is the first branch of AgValidateLimits in Pnl.mqh at 28c29a4 itself, printed from that commit to be sure; Pnl.mqh has exactly one commit in its whole history, f358ac1 of 2026-07-29, confirmed an ancestor of 28c29a4 by merge-base; and docs/PLAN_PHASE0.md:34 has named that same function as the grounding for the A9 expectations since it was written. So the line the owner read as proof of a new build is proof of the old build's gate working, which is the opposite conclusion from the same evidence. Corroborated on disk rather than left as an argument: two of the three deployed sources are md5-identical to the repository, the third differs by line endings only with identical content after normalisation and an unmoved 2026-07-30 mtime, and the ex5 is still the 2026-08-04 14:29:50 build. Three things are gained rather than merely disproved. The NaN parse question that docs/PLAN_PHASE0.md:92 explicitly flagged as unverified is answered, MT5 does produce a true non-finite double from the .set literal, shown by which branch of AgValidateLimits fired. The 5a-bis Load-browser prerequisite is discharged by an inference with no alternative, since a non-finite double cannot be typed into the properties dialog by hand and SPEC section 5 says so, meaning the vector was loaded, listed and parsed, and the ASCII CRLF form is now proven for parsing and not merely for visibility. And three of the twelve A9 rows have genuine results, including the healthy vector correctly reaching and refusing on the mutex, which is the interaction the unrun plan was going to have to predict anyway. One live hazard was found by checking the state the system was left in rather than the states it passed through, which is the 2026-07-30 standing lesson applied: last-used inputs were NOT restored, the log holds no init after 00:47:47, so the store seeds new attaches with DailyLossPercent=-5. The exposure is bounded by chart01.chr carrying valid defaults, read at mtime 00:44:50 under the caveat where agreement counts as evidence, so an unattended relaunch is safe and a manual Navigator attach is not. Nothing was touched in the terminal, no code changed, no SPEC changed, and the running instance on chart 1 is undisturbed with its LIFE lattice unbroken to 04:37:25 at seconds_in_state=43051. The first phantom's real banking commit is recorded as 677cdc3 by owner authorization, closing the gap that let a bad hash travel twice.

2026-08-05: Merge of worktree-phase0-close into main, owner ordered, merge commit c111787, parents 1aa216f (main) and 5a1772a (branch tip), one branch commit carried. Mechanics per the standing plan: a real merge commit forced with --no-ff, no rebase, no fast-forward-only, no side-picking strategy. The hand union resolve was not exercised and is recorded as not needed rather than as skipped: main had not advanced since the branch point, merge-base equalled main's head exactly, so LEDGER.md had no second side to reconcile. Verified after the merge rather than assumed: the LEDGER.md blob on main is byte-identical to the branch tip at e18c7987, section counts read 35 Issue, 39 dated ACTIONS and 45 Decision lines, and the attribution scan over the merged range returns zero hits. Deliberately untouched: the terminal, the running instance, the halt file and the mutex, a git merge in the repository cannot reach the data folder at all. Gate position stated plainly rather than glossed, because this merge does not satisfy the 2026-08-03 standard on its own proofs and the owner ordered it knowing that. What the branch does carry, complete and evidenced: the A10 conformance sweep, its evidence document at docs/evidence/a10-transition-conformance-2026-08-04.md, and the two findings recorded as an ISSUES entry. What it carries unfinished: the A10 row itself, which cannot close until the owner rules on F1 and F2, and the whole A7 row, whose owner-at-the-terminal block was planned and approved in this session and never ran. Under the all-of-it clause a branch carrying unfinished items does not qualify on the strength of its finished ones, so this is an owner-ordered merge outside the gate, the second such after the Stage 5a merge of 2026-08-04, and it is written here rather than left for a future session to discover.

2026-08-05: A10 ruling delivered by the owner and NOT applied, recorded because the executor stopped instead of recording it. The ruling was: SPEC moves, code stands, record FINAL dated 2026-08-05, split the SAFE_HALT transition row into two rows with their distinct triggers, amendment numbered per convention. Two objections were raised before writing anything, and nothing was written to SPEC, DECISIONS or code. One, the first of the three grounds as dictated, that the code states the truth because every init is a fresh image from BOOT, is contradicted by the measurement the ruling is about: the three deployed-lineage SYNCING->SYNCING lines are all in-place re-inits, each preceded within 13 milliseconds by a deinit reason=5 carrying timer_armed=1 and timer_ticks of 1806, 1819 and 14, which is the fresh-versus-inherited discriminator measured on 2026-07-30 and already written into the SPEC section 9 threat-model text as FINAL. A fresh image reads timer_armed=0 and timer_ticks=0, and could never emit that pair. Recording the ground as dictated would put two FINAL statements in direct contradiction. Corrected wording proposed, reaching the same conclusion: the from-state is the state the image was actually in, BOOT in a fresh image and the surviving prior state in an in-place re-init. Two, splitting into two BOOT->SAFE_HALT rows narrows the From column from any to BOOT and would make SAFE_HALT->SAFE_HALT non-conforming, which is the emittable line an input edit on a chart already halted produces through AccountGuardian.mq5:171; proposed instead a boot row for the chain trip, which only a fresh image can reach, and an any row for the persisted-flag re-entry. Also flagged: the ruling covers F2 only, F1 has no table row proposed, and only the re-entry line exists in the deployed build's output, the trip line at :92 having lost its 16:37:04 session unflushed. The row therefore stays BLOCKED on owner and the amendment is unwritten.

2026-08-04: A10 conformance sweep executed, tenth entry for the date, session worktree-phase0-close, read-only against the terminal. Method and results in docs/evidence/a10-transition-conformance-2026-08-04.md. Build identity re-verified first: all seven deployed sources MATCH the repository tree by md5, and no commit has touched MQL5/ since merge 28c29a4, so the tree at 1aa216f is the deployed source. Full sweep of all eight Experts journals with decoder positivity confirmed per standing rule 5, including the three zero-transition files read and counted. 24 TRANSITION lines total; reconciliation exact on every day, transitions equal inits minus refusals (refusals print the ALERT line plus the Alert echo, divisor two verified on the 2026-08-03 session), so every successful init emitted exactly one line and every refusal none. Static side: exactly three AgTransition call sites through one choke point, format conformance holds for all three including the :92 trip line whose 16:37:04 event lost its output unflushed and will never have a journal witness, recorded as permanent rather than repairable. Two divergences held for owner ruling in the ISSUES entry, F1 SYNCING->SYNCING from inherited-image re-inits with no table row, F2 the persisted-flag re-entry trigger absent from the any->SAFE_HALT row, plus the periodic-Alert side observation. Nothing was changed in SPEC or code; the row stays open pending the rulings. Also this session, before the sweep: the main working copy's LEDGER.md was found byte-identical to the 677cdc3 blob, silently missing the 1aa216f content, and was restored to HEAD by checkout with owner approval, zero loss since everything unique was committed.

2026-08-04: Push question ruled and closed by the owner, ninth entry for the date, a governance correction with no code and no terminal involvement. The pushes to origin were the owner's own manual actions, made through GitHub Desktop whenever pending commits showed there. They are not executor actions and they are not a violation of anything: no executor session ran a push, and the owner pushing their own repository needs no authorization from this ledger. The ruling is recorded in DECISIONS as FINAL and its substance is that remote state is owner-managed, the executor never pushes, and no ledger entry may assert "not pushed", "nothing pushed", an ahead-by count, or any other remote position as a durable fact. Earlier ACTIONS entries carrying that wording, the merge entries of 2026-08-03 and 2026-08-04 in particular, keep their text untouched because this section is append only, and this entry supersedes them on that one point rather than editing them. What those entries said was accurate about the executor's own conduct, which is all it was ever entitled to say, and inaccurate only as a claim about where the remote stood, which was never the executor's to state and which the owner could move a second after the line was written. Current state recorded here as a dated observation and nothing more: at the time of writing main is 677cdc3 locally, the local remote-tracking ref origin/main sits one commit behind it at 2c33d0f with a ref mtime of 16:58 today, and a fetch at 17:30 did not move it. That is what a local ref said at one moment, it is not a property of the remote, and it is stale the instant the owner pushes again, which is exactly why the ruling forbids carrying it forward as fact. Second item, landed in the same commit: the A1 ISSUES entry read "six deliberate takeovers" while listing seven timestamps, 16:00:22, 16:24:55, 16:29:57, 16:33:01, 16:36:02, 16:37:54 and 16:39:54. Seven is correct, the list was right all along and only the count word disagreed with it, so the word is corrected in place, ISSUES being editable. The correction entry below that reads "six times" is left as written, same append-only reason, and is superseded here.

2026-08-04: CORRECTION to the merge entry below and to the Phase 0 row states, eighth entry for the date, written after an audit that started from a report and ended on measurement. The merge entry's closing sentence, that A3 "stands mid-run at one counted kill with the chain at one", was true when written and is superseded here rather than edited, ACTIONS being append only; the same applies to the A3, A4 and A6 rows reading PENDING blocked in the 2026-07-29 matrix entry. What the kill session actually did: A3 TRIPPED, A4 and A6 are PROVEN, and A2's stale-heartbeat takeover ran deliberately six times, which closes the last remainder of the A1 row. Evidence and full quotes: docs/evidence/kill-session-2026-08-04.md, sourced from terminal D0E8209F77C8CF37AD8BF550E51FF075, MQL5\Logs\20260804.log read in full at 2095 lines with the decoder confirmed, corroborated by logs\20260804.log at 161 lines. The three load-bearing measurements: A3 tripped at server 2026.08.04 16:37:04 on the fourth counted kill, chain 3/3 at 16:36:02 correctly not tripping since the limit is 3; A4 proven at 16:37:54, where the next image came up BOOT->SAFE_HALT on the persisted flag carrying reason "crash loop: 4 consecutive unclean sessions, adjacent inits within 300s, limit 3" and since=2026.08.04 16:37:04, with the ALERT naming the deletion procedure and two LIFE lines emitted while halted; A6 proven at 16:39:54, where after the owner deleted the halt file the image logged "no halt file, first session on this account" then RESUMED_FROM_SAFE_HALT, and the live file is back at 59 bytes with the flag cleared. Second correction, and it is the larger one: NO COMMIT EVER BANKED ANY OF THIS. The commit reported as having banked the three rows and closed A1 does not exist in this repository, checked four ways rather than assumed: rev-parse fails, no object of any type carries the prefix under --batch-all-objects, the reflog has no hit, and fsck --lost-found returns two dangling trees and no dangling commit. The merge itself is intact and nothing was lost by it: all nine branch commits are on main, the diff between the branch tip 3cdcec9 and main is the two LEDGER lines of 2c33d0f and nothing else, and both worktrees are clean. What was missing was the ledger write, not a commit, so these rows have sat proven-but-unrecorded since the session ended. Third, the merge report's claim that the clean shutdown was not done is false by measurement: two graceful closes marked their sessions clean, reason=9 at 16:20:31 with timer_ticks=1208 and at 16:31:30 with timer_ticks=92. Two things recorded as measured rather than glossed. One, the tripping session left no Experts-log line at all, exactly one line in the whole file matches 16:37:0 and it is the since= field printed by the NEXT session; the process was hard killed before its output flushed, which is standing observation rule 4's own scenario, so the trip rests on the halt file's content as read back by the image that loaded it, corroborated by the terminal journal's start, load and authorize lines at 16:37:02.586, 16:37:03.180 and 16:37:04.099. Two, the pairwise gap bound was observed doing its job unprompted: the chain reset at 16:29:57 with no clean record involved, because that adjacent init pair spans 302 seconds against the 300 second bound, which is the R1 primitive's defining behaviour measured by accident rather than by design. One evidence gap, permanent and recorded so nobody later mistakes it for a repairable oversight: no halt backup was taken between the trip and the deletion A6 required, so the halted file with its flag set has no independent on-disk decode and never will. A7 gains supporting data points from this session, the clean-deinit chain resets above, but the row is NOT claimed closed here and still needs its deliberate run. Nothing was decided by the executor here and no row was marked FINAL; the rows close on evidence present on disk at a stated path, per the standing closure standard.

2026-08-04: Merge of worktree-stage5a-crashloop into main, local only, no push, seventh entry for the date, owner ordered mid-session with the A3 sequence still running. Merge commit 28c29a4, parents 83f1bdb (main) and 3cdcec9 (branch tip), nine branch commits carried. Mechanics per the standing plan: a real merge commit forced with --no-ff, no rebase, no fast-forward-only, no side-picking strategy. The hand union resolve was not exercised and is recorded as not needed rather than as skipped: main had not advanced since the branch point, merge-base equalled main's head exactly, so LEDGER.md had no second side to reconcile. Verified after the merge rather than assumed: the LEDGER.md blob on main is byte-identical to the branch tip at 1bab8215, section entry counts read 34 Issue, 36 dated ACTIONS and 44 Decision lines, the attribution scan over the merged range returns zero hits, and main sits ahead of origin/main by ten with nothing pushed. Deliberately untouched: the terminal, the running instance, the halt file, and the mutex, since a git merge in the repository cannot reach the data folder at all, which is exactly the separation ruled FINAL earlier today. Gate position stated plainly rather than glossed, since this is the first merge under the per-branch proven-work standard. Stage 5a's own four steps are complete and evidenced: the primitive implemented, SPEC amendment A4 written, twelve vectors regenerated and committed, and the required restart taken from the first A3 kill cycle per 5a-bis. One 5a-bis remainder is NOT closed and travels to main open: the eleven regenerated vectors have been copied into the terminal Presets folder and the restart that makes them enumerable has happened, but no one has yet confirmed them listed in the EA properties Load browser, which is an owner-side GUI check and an A9 prerequisite. The branch therefore carried one unfinished item at merge time, the owner ordered the merge knowing it, and it is written here rather than left for a future session to discover. A3 itself is not a branch item: it is the next row after Stage 5a in the ordered plan, and it stands mid-run at one counted kill with the chain at one.

2026-08-04: Stage 5a steps 1 to 3, code and SPEC and vectors, third entry for the date. Three owner rulings recorded FINAL first: the negative-gap treatment in the chain walk, the vectors-into-the-repository decision with its sync direction, and the SweepPeriodSeconds wording, which closes an item that had been open since the pre-batch verification of 2026-07-30. Step 1, the primitive: AgHaltUncleanCount is replaced by AgHaltUncleanChain in Persist.mqh, a backward walk from the newest record that stops at the first clean record or the first adjacent init pair further apart than the bound. The substantive change is that TimeLocal no longer participates in the comparison at all. The old function anchored a rolling window on now, so a clean record reset nothing; the chain is anchored on adjacent init pairs only, which is exactly what makes one clean record reset it. The call site in AccountGuardian.mq5 takes the new function, the CrashLoopWindowSeconds default moves 60 to 300, and both the SAFE_HALT reason string and the verbose check line are reworded to name the chain and the gap bound rather than a window. Untouched by design: the halt file format, AgHaltAppendSession, AgHaltMarkClean, and every config refusal check. AG_MAX_SESSIONS is 32 against a worst case chain of 4, so pruning cannot truncate a chain that matters. Step 2, SPEC: amendment A4 added, superseding A1's rolling-window counting while stating explicitly that the file format is untouched, since this changes how existing records are read and not what is written; the transition table trigger, the input table defaults and note cells, the Phase 0 crash-loop acceptance row, and the two A1 crash-counting bullets are all reworded to match, and the negative-gap ruling is recorded in A4 with its grounds. Step 3, vectors: committed unmodified first as a baseline, then regenerated, so the change is an eleven-line diff rather than twelve files appearing from nowhere. One thing caught by checking rather than by assuming: the editing pass rewrote CRLF to LF on all eleven, silently. The CRLF form is the one proven to parse in the terminal, the LF form is proven for nothing, so the endings were restored byte for byte and verified by carriage-return and line-feed counts before commit. R2 concurrence, the one-line obligation the ruling attached, discharged: the measured 30 to 40 second kill cycle puts four kills' adjacent init pairs far inside the 300 second bound, chain reaches 4 against a limit of 3 and trips, no adjustment needed. Nothing deployed and no terminal file touched, per the owner's gate; the compile ran against the repository tree instead. Compile result: 0 errors, 0 warnings, 833 ms, ex5 written to the repository tree at 14:16:31, 40450 bytes against the deployed build's 39642. Deployment state verified unchanged by the same check: the terminal copy is still the 2026-08-03 01:40:37 e136858 build at 39642 bytes, so the running instance is untouched and still carries the old primitive, which is correct until the owner opens the deploy. One measurement fell out of it and is recorded as standing observation rule 6, because it would have inverted any future compile gate: the metaeditor64 exit code is not a status signal on this installation. The clean build exited 1 while logging 0 errors and 0 warnings, and a deliberately broken control copy of the same source exited 0 while logging 3 errors and producing no ex5. The Result line and the ex5 timestamp are the authoritative signals; the control was run precisely because a clean compile is a proof claim in this project and an unexplained non-zero exit is not something to wave through.

2026-08-04: Relaunch raced the mutex staleness window, procedure defect not a code defect, sixth entry for the date. The counted kill 1 worked exactly as intended and the evidence says so: exactly one terminal process exists, pid 20212 started 15:55:57, the previous pid 19732 is gone, and the halt file is untouched at mtime 15:51:08 with its newest record S|1785858668 still clean=0. So taskkill /F is the correct instrument and the record stayed unclean, which is what the Task Manager attempt failed to achieve. What went wrong is timing. A hard kill by construction skips OnDeinit, so the mutex heartbeat GV AG_HB_1200252169 is never zeroed and keeps the value the dying instance last wrote, refreshed every one second timer tick. AG_MUTEX_STALE_SECONDS is 10, so a relaunch within ten seconds of the kill reads a heartbeat that is not yet stale, correctly concludes a live holder exists, and refuses. Measured timeline: last surviving LIFE line 15:55:18, replacement process start 15:55:57, refused init 15:56:00.063, so the heartbeat was at most a few seconds old when it was read. The refusal is the mutex working to specification, not a fault. Consequence folded into the A3 procedure: after every hard kill, wait at least fifteen seconds before relaunching, ten for the staleness threshold plus margin. A refused init appends NO session record, since AgHaltAppendSession sits downstream of the refusal return, so a raced relaunch does not advance the chain and does not corrupt it either, it simply wastes a cycle. Two things banked for free. One, the SAFE_HALT bypass fix is re-proven on the Stage 5a build under a genuine fresh-image refusal: the skip line fired naming why the halt file was not written, the deinit reported reason=8 with timer_armed=0 and timer_ticks=0 confirming a fresh image, the banner was deliberately left on the chart, and the halt file is byte-untouched with its mtime unmoved. Two, the next successful attach is now a stale-mutex takeover by construction, because the leftover heartbeat is minutes old, which is the A2 branch that the A1 row still owes and that has never been run deliberately on any build. It also serves as the discriminator against the one alternative explanation worth ruling out: if the staleness comparison were itself broken rather than merely raced, the attach would refuse again with a heartbeat two minutes old.

2026-08-04: Kill method measured WRONG before it could cost the A3 row, fifth entry for the date. Task Manager End Task on the Processes tab is NOT a hard kill for this terminal. The owner ended the task at about 15:16 and reported it as a hard kill with no clean exit, which was an honest reading of the tool's own label. The halt file says otherwise and the halt file is the authoritative shutdown witness under standing rule 4: session record 6, S|1785721251, flipped from unclean to clean=1, the file grew 138 to 139 bytes, its md5 moved 1C4336F1 to 8C4B7C97, and its mtime advanced to 15:16:25, some 42 seconds after the last LIFE line at 15:15:43. OnDeinit therefore ran to completion and marked the session clean, which means Windows delivered a graceful close request rather than terminating the process. This matches the 2026-08-03 measured clean close exactly, where a WM_CLOSE produced the same clean flag. The journal holds no deinit lines at all, zero non-LIFE lines for the whole day, which is standing rule 4's own scenario and is why the halt file rather than the journal settled it. Consequence, and it is the whole value of the pre-relaunch gate: had this method been used for the counted kills, every one would have written a clean record, the chain would have reset at every step, and A3 would have been reported as a failure to trip when in fact nothing was ever being measured. Nothing is lost, because this kill's only job was unloading the old image so the relaunch loads the new one, and it did that. Corrected method for every kill from here: taskkill /F /IM terminal64.exe, where /F terminates the process outright and gives OnDeinit no chance to run. The discriminator after each kill is the halt file, not the journal and not the tool's wording: a genuine hard kill leaves the newest record at clean=0.

2026-08-04: Stage 5a deploy, fourth entry for the date, owner approved, executed in the ordered sequence with the halt backup taken before anything else touched the terminal. Backup: docs/evidence/halt_1200252169.dat.pre-stage5a-deploy-2026-08-04, 138 bytes, md5 1C4336F100879F54A3AFB1A90AAE088B, verified equal to the source by hash after the copy, and the live file still carries that same md5 and its 2026-08-03 01:40:51 mtime afterward, so nothing wrote to it during the deploy. Sources: the two changed files copied into the terminal tree, then all seven verified by md5 against the repository, all seven MATCH, so the deployed source is the committed source and not a partial copy. Compiled in the terminal tree, 0 errors 0 warnings. Vectors: the eleven regenerated files copied into the terminal Presets folder, all twelve then verified in place against the repository by md5, CRLF intact at eight carriage returns and eight line feeds each, eleven reading 300 and a9_window_zero.set reading 0. Running instance: UNDISTURBED and still on the OLD build, which is the expected and required outcome. Evidence, three independent measures: the terminal process id is unchanged at 14636 with its original 2026-08-03 01:40:49 start, the day's journal contains zero non-LIFE lines, so no init, no deinit and no alert occurred at any point, and the LIFE lattice runs unbroken at exact 30 second spacing straight through all six compiles with seconds_in_state stepping 132382, 132412, 132442, 132472, 132502, 132532, counting back continuously to the original attach. The new primitive therefore is on disk but not in memory, and the first kill is what loads it. A measurement fell out of the verification and is recorded as standing observation rule 7, because it strikes a corroboration method this ledger has used: the ex5 size is not build identity. The owner's requested check, terminal ex5 matching the repository build by size, cannot be satisfied by construction and was reported as unsatisfiable rather than fudged.

2026-08-04: Phase 1/2 reconciliation done, second entry for the date, the follow up the merge entry named and deliberately did not fix inside a merge. Both stale ISSUES entries carried from main were reconciled against docs/DESIGN_PHASE1_2.md, which was ahead of the ledger on both. Q6 clarification: the document records it RATIFIED by owner ruling 2026-07-30, so the full ratified wording moves to DECISIONS as FINAL, transcribed rather than re-decided, and the ISSUES entry is removed. Ratchet: the document records the build decision RULED by the same 2026-07-30 ruling with the owner's grounds and the accepted no-third-witness residual, so that moves to DECISIONS as FINAL too, and the ISSUES entry is rewritten rather than closed, narrowed to the one thing genuinely still open, the corrected single-scalar mechanism, which carries an explicit UNRULED 2026-07-30 marker in the document and is authority for nothing until ratified. The narrowed entry now carries the correction and its four re-derived consequences in the ledger itself, so a session reading only ISSUES no longer has to open the document to know what is unruled. Nothing was decided by the executor here, both FINAL entries are transcriptions of existing owner rulings, per the standing rule that only the owner marks FINAL. Document updated to match: the handoff note's staleness paragraph is marked RECONCILED and now states the two files agree. Implementation stays gated, the ratchet is Phase 2 and Phase 0 is not green.

2026-08-04: Merge gate ruling recorded FINAL in DECISIONS, carrying the owner's date of 2026-08-03; the entry is dated to the ruling, this log entry to the day the work was done. The executor's pre-ruling obligation was to state whether replacing the gate weakens anything the original protected that the new standard does not cover. Answer delivered before writing anything: it does not, and the four protected properties were enumerated rather than asserted away. Merge mechanics and the owner-instruction requirement are carried forward untouched, so two of the four never moved. Keeping unproven work off main is covered more tightly per item than per phase, and "proven" already has an objective definition in this ledger, evidence on disk at the stated path with result, build hash and evidence pointer, so the new gate is not executor self-certification. The one property genuinely dropped, main advancing only to points where the whole phase passed, was found to carry nothing: deployment here is a source copy into the terminal data folder plus a MetaEditor compile at a specific commit, never a checkout of main, so main was never the release source, and the gate detected no cross-row interaction because withholding a merge runs no acceptance row. One wording precision added to close a loophole phase-green covered incidentally: "the work in that branch" means all of it, so a branch carrying an unfinished item does not qualify on the strength of its finished ones. docs/DESIGN_PHASE1_2.md updated so no stale gate survives: the merge plan now records the merge as RUN at ce6964d with the Phase-0-green condition marked SUPERSEDED and the ruling named as having changed the plan rather than been a violation of it, the mechanics restated as the permanent procedure for every future merge, and the LEDGER write freeze marked LIFTED in both places it appears. The 2026-08-03 merge entry below is not edited, ACTIONS is append-only; its recorded deviation is reclassified by the DECISIONS entry, which says so explicitly.

2026-08-03: Merge to main, eighth entry for the date, owner ordered with the rationale on the record: everything on this branch is completed and proven work, and Stage 5a touches the crash loop primitive, core protection logic, so main should hold a stable proven point before that begins. Divergence found and handled by the book: main had advanced independently since the common ancestor 72ed960 with the Phase 1/2 design review (docs/DESIGN_PHASE1_2.md, four FINAL rulings, two new ISSUES entries, the status page, and README badge automation), and its handoff note carries a LEDGER freeze on main plus a written merge plan. This merge follows that plan's mechanics exactly: a real merge commit, no rebase, no fast forward only, no side-picking strategy, every LEDGER conflict resolved by hand as a union keeping both sides' entries in each section's own ordering, verified afterward by per section entry counts and by identity matching every entry's opening line against both parents. One deviation from the plan, recorded: the plan gated the merge on Phase 0 green plus owner instruction; Phase 0 is not yet green (kill and input rows remain) and the owner's 2026-08-03 instruction supersedes that gate with the stable point rationale above. The freeze on main lifts with this merge, which is the reconciliation the freeze awaited. Pre merge verification: 29 branch commits since 72ed960, attribution scan clean, zero hits; commit-msg hook on the shared .git BOM free, first bytes 23 21 2F, strip patterns present, and proven working by this session's own commits. Known follow up carried in the agenda entry: the two Phase 1/2 ISSUES entries arriving from main are stale against the design document per its handoff note, reconciliation is a separate task, not silently fixed inside a merge.

2026-08-03: Provenance correction and Market Watch closure, seventh entry for the date, recorded at the owner's instruction so the ledger carries no mystery it does not have. All eight refusals of 2026-08-03 were owner initiated attach attempts made while learning the GUI procedure, not spontaneous events; GBPAUD.ecn was picked by mistake from the symbol list when opening a new chart, nothing more. The measurement value is unchanged, eight real refusals under the deployed build, all harmless, halt file untouched, chart 1 undisturbed; the cause is now on the record. Market Watch: confirmed by the owner's direct reading of the live terminal window, three symbols only, XAUUSD.ecn, US100.ecn, XAGUSD.ecn, GBPAUD.ecn not present. The live window is the only reliable source while the terminal runs, standing rule 1, so the ISSUES confirmation entry closes on that reading; the disk snapshot stays stale until the next shutdown rewrites it, and the executor re-reads it after that shutdown as a cross check, not as a gate.

2026-08-03: GUI session closing verification, sixth entry for the date. Step 4 closed by substitution per the owner, recorded as such and not as direct observation: the replace behavior was observed at 02:01 when the config banner overwrote the mutex banner through the same Comment path, and the healthy banner uses that same draw at init and on every timer tick, live on chart 1 continuously; no separate healthy redraw observation was made. Default.tpl overwrite verified from the file: mtime 10:55:43, decoder read 18048 characters with the chart tag present, no expert block, no AccountGuardian reference, and the stale prior project schema (InpMaxDailyLossAbs, InpTelegramToken) gone; corroborated by the journal, no init line after 10:47:40 although a verification chart was opened after the overwrite, so the template entry closes here. Full refusal tally for 2026-08-03, final count EIGHT, four beyond the proof session's four, found by the full journal scan the owner ordered; all template auto-attach mutex refusals except the one config proof: 01:48:40 US500 mutex (proof), 01:59:55 XAGUSD mutex, 02:00:48 US500 mutex, 02:01:28 US500 config (proof), 02:38:42 XAUUSD mutex, 02:39:49 GBPAUD mutex, 10:44:50 GBPAUD mutex, 10:47:40 GBPAUD mutex. All eight verified harmless: the journal holds exactly eight halt skip lines, eight banner gate lines and eight fresh image deinits with reason=8, timer_armed=0 and timer_ticks=0, one to one with the refusals, and the halt file is untouched at md5 1C4336F100879F54A3AFB1A90AAE088B, mtime 01:40:51. Chart 1 undisturbed through everything: 1117 LIFE lines from 01:40:52 to 10:58:52, an exact 30 second lattice, max gap 30 s. Market Watch: the selection snapshot selected-1200252169.dat, last written 01:57:43, still shows the four symbol mid session state including US500.ecn; it predates the reported removal and cannot confirm the three symbol end state, and the GBPAUD chart opens raise a possible fourth subscription; recorded as an open confirmation entry in ISSUES rather than assumed away.

2026-08-03: Config banner PROVEN by owner observation, both banner defects closed, fifth entry for the date. Procedure: manual Navigator drag onto the same US500.ecn M5 chart that carried the mutex refusal banner, DailyLossPercent=0 and DailyLossCurrency=0 set in the dialog, OK. Observed after the EA detached: AccountGuardian, State: REFUSED 2026.08.03 02:01:30 (local 2026.08.03 02:01:28), Lock reason: core config invalid: both limits are zero, a guardian with no limit is a config error, Locked until: -, Daily PnL vs limit: n/a (init refused). Same two second server over local skew, consistent with the ambient offset. Executor cross checks, all pass: the ALERT line at 02:01:28.395 carries the exact refusal text, followed by the deinit triplet, the halt skip line, reason=8 with timer_armed=0 and timer_ticks=0 confirming a fresh image, and the banner gate line. Two mutex refusals in between, 01:59:55 on XAGUSD.ecn and 02:00:48 on US500.ecn, attach attempts that landed on the mutex path, verified harmless: full skip and gate line sets, fresh image counters, no halt write. Halt integrity across all four refusals tonight: md5 1C4336F100879F54A3AFB1A90AAE088B and mtime 01:40:51 unchanged, and the journal holds exactly four halt skip lines and four banner gate lines, one to one with the four refusals. Unplanned second result, recorded at the owner's instruction: the config banner replaced the mutex banner on the same chart, answering lifecycle question one by observation, the next attach on a chart overwrites the prior refusal record. Consequence recorded as an accepted limitation, consistent with the ruling's session scoped acceptance: a chart shows only its most recent refusal, earlier refusals survive in the journal only, where every refusal logs its ALERT and gate lines; the banner is the visibility mechanism, the journal is the audit trail. Both banner ISSUES entries are closed and removed; their history in one line: classified 2026-08-02 with the draw to wipe measured under 160 ms on the 2026-08-01 refusals, four lifecycle questions all settled (one by tonight's observation, two by the restart measurement, three by the dated banner text, four by both paths proven), design revised to the dedicated flag, ruled FINAL 2026-08-03, implemented on e136858, mutex proof in the fourth entry for the date, config proof here. Remaining related work: the healthy redraw confirmation on the proof chart, and the A2 takeover branch of the A1 row.

2026-08-03: Mutex banner PROVEN by owner observation, fourth entry for the date. Procedure deviated from the standby note in one detail, recorded as the owner instructed: the second chart was opened via File then New Chart on US500.ecn, so Default.tpl auto-attached the EA with no inputs dialog, the same fresh image mutex refusal, demonstrating the template auto-attach path in the same shot; a refusal that was invisible on 72ed960 now leaves its dated record. Observed on the US500.ecn M5 chart after the EA detached: AccountGuardian, State: REFUSED 2026.08.03 01:48:42 (local 2026.08.03 01:48:40), Lock reason: duplicate instance: another instance holds the mutex, Locked until: -, Daily PnL vs limit: n/a (init refused). Chart 1's healthy banner and LIFE cadence confirmed undisturbed visually and by journal. Executor cross checks, all pass. Journal, six lines at 01:48:40.950 on US500.ecn M5: init, the ALERT line naming the account, the Alert popup line, the halt skip line, deinit reason=8 with timer_armed=0 and timer_ticks=0 confirming a fresh image, and the new gate line, deinit, banner NOT cleared, init was refused, dated REFUSED banner stays on the chart. Halt file untouched: mtime 2026-08-03 01:40:51 predates the refusal by eight minutes, written by the healthy init's own session append (size 106 to 138 at that init, expected), md5 1C4336F100879F54A3AFB1A90AAE088B recorded as the running baseline. The banner's two second server over local skew is not a defect: every concurrent LIFE line carries the same plus two seconds server over local offset (01:48:22 local against 01:48:24 server, 01:48:52 against 01:48:54), so the banner matches the ambient clock offset measured tonight at exactly two seconds, within R3's about zero. Also observed by the owner: Market Watch currently lists four symbols including US500.ecn against the FINAL three symbol ruling; the owner removes it after the proof charts close. Config path proof is next in the same session; the refusing chart stays open as evidence.

2026-08-03: Banner ruling recorded, implemented, deployed, partially proven; third entry for the date. The ruling is in DECISIONS as FINAL. Implementation on commit e136858: one helper, AgRefuseInit, sets the dedicated g_init_refused flag and draws the dated REFUSED banner, carrying both the server and the local time in the same two clock convention as the LIFE line, before every refusal return in OnInit, six call sites, the mutex path and all five config refusals; OnDeinit gates AgBannerClear on the flag and logs an INFO skip line when it leaves the banner, never silent. Deployed: source copied to the terminal tree, compiled 0 errors 0 warnings at 01:40:37, ex5 39642 bytes; a clean terminal restart loaded it fresh, init 01:40:51.605, clean mutex acquire, BOOT->SYNCING, timer armed at 1 s, LIFE cadence at 1, 31 and 61 seconds with the server clock advancing, and the prior session closed clean with deinit reason=9. Proof state per the four steps. Step 1, deploy and healthy re-init: PROVEN, evidence above. Step 4, healthy path clears and redraws: holds by construction, the flag is false in every healthy image so the clear and redraw paths are byte for byte the proven build's, but the visual no stale banner check still needs eyes on the chart. Steps 2 and 3, the mutex refusal and config refusal banners: implemented but UNPROVEN by observation, and the deviation is structural rather than discretionary: triggering either refusal needs a chart attach, the GUI is the only sanctioned route, the harness /config attach path stands defective under a do-not-rerun order, and planting profile or template files is a data folder write class the executor never touches. Owner GUI session needed to close both: one second-chart attach for the mutex banner, one fresh attach with a malformed core input for the config banner, the dated REFUSED banner checked on the refusing chart after the failed init unloads, and the live instance verified untouched. Both ISSUES entries carry the implemented but unproven state until then.

2026-08-03: A1 prerequisite measurement, the Comment survives restart question, second entry for the date. Procedure: clean terminal close at 01:07:56 (WM_CLOSE, terminal journal "stopped with 0" and "shutdown with 0" at 01:07:58), relaunch at 01:08:59, all on build 6090. The close banked three by-products. One, the deinit line flushed intact this time: reason=9, session marked clean, timer_armed=1, timer_ticks=94416 over the 94419 second session running from 2026-08-01 22:54:17, one tick per second sustained through the entire A8 closure window, closing the timer_ticks item left outstanding in the harvest entry below. Two, the halt file was rewritten by the clean close, mtime 01:07:56, md5 A2C99538112F1720A4D7CB410CCDD814, size 106 unchanged. Three, one more A7 clean re-init data point. Measurement result recorded in the A1 ISSUES entry: a chart Comment does not survive a terminal restart, the profile serializes no comment text, verified on the profile saved mid-session with a live banner and on the profile saved by the close. Mutex behavior on restart, recorded as planned: no refuse-while-live triggered, because the clean deinit had released the mutex, so the new image acquired cleanly at 01:09:00, id_set=1, hb_set=1, BOOT->SYNCING, timer armed, LIFE cadence resumed at 30 s with the server clock advancing. Side finding: chart02's close-time profile lost its AccountGuardian expert block, the refused instance had detached and the close serialized the true chart state, so the restart restored chart 2 without any EA and produced no refusal; restored charts rebuild from their own profiles, not from Default.tpl, so the template auto-attach fires on new charts only. A1 itself stays BLOCKED on the owner's banner ruling; nothing was implemented.

2026-08-03: A8 full-threshold harvest, both checks PASS, row closed DONE. The window was closed by the natural Sunday evening reopen, not cut short: the first advancing LIFE line, MQL5\Logs\20260803.log line 121, local 01:00:18, carries server 2026.08.03 01:00:20, ending the freeze. Source for the server time check: the server field (TimeCurrent, present on every LIFE line since the 72ed960 fix) of every LIFE line in the Experts journal logs of terminal D0E8209F77C8CF37AD8BF550E51FF075, files 20260801.log lines 2791 to 2905, 20260802.log lines 1 to 2880, and 20260803.log lines 1 to 120, 3115 lines total, full scan, no sampling. Check 1, frozen server clock: PASS. All 3067 lines from local 2026.08.01 23:26:48 through 2026.08.03 00:59:48 carry server exactly 2026.07.31 23:57:59 with no advancement anywhere before reopen; the 48 lines before that, local 23:02:48 to 23:26:18, sit pinned at 2026.08.01 23:02:41, the BTCUSD last tick pin already on record, and the single transition between the two values is the known backward rewind at the 23:26:38 reconnect, so no tick advanced the clock at any point inside the window and the only move was backward. Check 2, LIFE continuity: PASS. 3115 LIFE lines from local 2026.08.01 23:02:48 to 2026.08.03 00:59:48 against an expected lattice of exactly 3115 at the 30 second cadence (25 h 57 min, 93420 s at 30 s spacing plus the opening line), max gap 30.00 s at one second stamp resolution, zero gaps above 31 s, zero missing lines, continuity unbroken across two midnight log rotations. The frozen stretch alone spans 25 h 33 min against the 15 minute threshold. The removed ISSUES row's history, the below threshold ruling of 2026-07-31, the BTCUSD window destruction with its before the fact provenance, and the two pin structure, is preserved in the 2026-07-31 and 2026-08-02 ACTIONS entries and in this one. Still outstanding from the harvest instruction in the standing hold entry: timer_ticks at the next legitimate deinit, which has not occurred, the instance has run without deinit since 2026-08-01 22:54:17; record it when the next legitimate deinit happens.

2026-08-02: Owner corrections round, applied the same day, second entry for the date. The crypto clause in the frozen-upper-bound entry is struck, pre-approved by the owner, with the obligation kept OPEN and unchanged; the DECISIONS consequence wording updated to match the final ruling. The banner fix proposal is revised: the owner caught the g_ag_halt_loaded clear-gate repeating the exact conflation class the executor itself rejected when it declined to gate the halt save on g_owns_mutex, model-loaded standing proxy for init-refused on an accidental correlation that breaks the moment a refusal return moves. Logged as an executor-side error for symmetry with the owner-side record; the proposal now uses a dedicated refusal flag set on the refusal paths themselves. The config-refusal banner gap is split out as its own SPEC gap entry rather than a footnote, so each path gets its own ruling. The A8 window close is corrected from Monday open to the Sunday evening reopen, the Friday prediction's provenance (on the record before the fact) is added to the A8 entry, and the standing hold is strengthened to bind everyone: no terminal action by executor or owner while the window collects, stop and ask before anything that would touch it.

2026-08-02: Post-restart verification and weekend harvest, read-only against the terminal, ledger writes only. Hook first: the worktree resolves hooks to the main repository's .git/hooks, the commit-msg hook there is BOM free with LF endings, mtime 2026-08-01 23:15, and it stripped all three attribution forms from a scratch file, so commits are safe; the durable profile fix is owner-side and not verifiable from the repo. Restart: MT5 downloaded build 6090 at 11:05 and the owner-approved update restart ran at 22:53:59 with a graceful exit code 0. The pre-restart session, 48 hours from 2026-07-30 22:50:17, closed CLEAN twice over: journal deinit reason=9 with timer_armed=1 and timer_ticks=173019, about one tick per second sustained for two days, and halt record 3 flipped to clean=1. Build 6090 came up at 22:54:14, restored chart 1 from the profile with the exact defaults, and the EA initialized healthy at 22:54:17, mutex acquired, BOOT->SYNCING, timer armed, LIFE cadence unbroken since; the halt file holds four records, three clean plus the live one, halt flag 0. One more clean re-init data point for A7, and the crash chain resets to the live session only. Harvest, and it corrects the record: the weekend window was NOT banked. The owner asserted at contact that it was; the measurement says BTCUSD.ecn stayed selected, ticked through Friday night and all of Saturday, and the server clock advanced on every one of Friday's 2880 and Saturday's 2748 LIFE lines except the routine midnight pause, 00:00:18 to 00:04:48 pinned at 2026.07.31 23:59:56. Recorded at the owner's own instruction as the fourth owner-side error this project, alongside the withdrawn item 0 accusation, the superseded 23:45 market-close figure, and the struck NOT A DEFECT preset framing. The real window opened only after the restart: BTCUSD's last tick pinned the clock at 2026.08.01 23:02:41, and the 23:26:38 reconnect, the first after the symbol's removal, rewound TimeCurrent to gold's Friday close stamp 2026.07.31 23:57:59, a backward step of about 25 hours. New environment fact, measured: TimeCurrent is not monotonic across a reconnect once the freshest symbol has left Market Watch; folded into the frozen-upper-bound entry as a Phase 1 and 2 design input. Four template auto-attach refusals were also observed post-restart, 22:54:47 on a BTCUSD chart and 22:55:03, 22:57:41, 22:58:11 on XAUUSD charts, every one harmless on 72ed960 with the skip line logged and the halt file untouched; cause and owner procedure in the template ISSUES entry. Session entries: Market Watch ruling recorded FINAL, frozen-upper-bound entry scoped per the ruling with the backward-clock addendum, A1 banner defect classified with its lifecycle design, Default.tpl finding logged, build 6090 re-verification flags folded into existing rows.

2026-08-02:
Issue:  Roadmap completion status lived only in the Notion page "AccountGuardian — Road to Live" and a one-off Cowork artifact that vanished with its chat, so the repo had no persistent, glanceable status view.
Action: Created docs/status/index.html, a static single-file dark page that mirrors the Notion page exactly (all phases, every item, and its checked state, no rewording, no additions), with a Last synced stamp and a note that Notion is the source of truth. Documentation only, no MQL5 source and no MT5 terminal touched, consistent with the standing A8 hold.
Status: DONE

2026-07-31: Morning harvest, three findings, two of them contradicting expectations and both measured rather than argued. One: the machine neither slept nor died. The 02:30 sleep never happened: LIFE cadence runs unbroken from the 22:50:18 attach to 15:16:48 with no stamp gap above 60 seconds anywhere, seconds_in_state at 59191 counting back exactly to the attach, the same terminal pid 9056, the halt file byte-identical at md5 0D7C8493 with mtime still 22:50:17, and zero non-LIFE lines today. The two predicted sleep artefacts (seconds_in_state jump, stale mutex heartbeat on wake) never materialised because their premise never occurred; neither the slept nor the crashed branch of the morning question applies, and no refusing restore existed to check. Cause of the missed sleep not established; candidate is that some process held a wake lock overnight, not investigated further. Two: the A8 window came in BELOW THRESHOLD on 72ed960, which is not the same as failed and is worded that way on owner instruction. The only no-tick stretch all night was server frozen at 23:59:56 for 10 LIFE lines, stamps 00:00:18 to 00:04:48, resuming 00:05:19, about 5.4 minutes against the 15 minute minimum. Mechanism, and it matters beyond tonight: TimeCurrent is terminal-wide, the last known server time across ALL Market Watch symbols, and the US100.ecn and US500.ecn charts opened during last night's fix proof subscribed index symbols that tick through gold's break. The 112 minute window measured the night before existed only because gold was the quietest constraint then; the extra symbols shrank it to the sliver both markets share. Operative rule: A8's window is set by the quietest subscribed symbol, not by the chart symbol. Three: today is Friday, so the weekend close guarantees an arbitrarily long no-tick window tonight regardless of Market Watch contents; A8 collects over the weekend close with no symbol surgery needed. Standing observation rule 5 added by owner order. A8 evidence pointer: the 20260730 and 20260731 journal analysis in this entry, full scan reproducible from the logs.

2026-07-30 night: Build 72ed960 deployed, compiled 0 errors 0 warnings at 22:39:30, and loaded by the chart 1 attach at 22:50:17; build identity established primarily from disk, since the only ex5 present was written at 22:39:30 and MT5 loads from disk at attach, corroborated by the LIFE line gaining the server and local clock fields that 694d570 never emitted. FIX PROVEN on both refusal paths. Six refusals were driven in fresh images, four on the mutex path and two on the config path, and every one logged the skip line naming why the halt file was not written together with timer_armed=0 and timer_ticks=0 confirming a fresh image. The halt file finished byte-identical at md5 0D7C8493 with its mtime unmoved at 22:50:17, which is stronger than an equal checksum: nothing opened it for writing at all. It still holds three session records, so no refused init appended one, independently confirming that refusals cannot accumulate toward the crash-loop count because AgHaltAppendSession sits downstream of every refusal return. Contrast against 694d570 the same evening, where one fresh-image config refusal erased six records. Erasure half only, per the standing scope limit. A1 deliberately left OPEN on the owner's ruling because the REFUSED banner was never observed. Encoding closed as eliminated for both visibility and parsing, since a9_defaults.set parsed correctly and the EA attached healthy with percent 5.0 in force, so all twelve vectors are usable as written. Step 6 did not execute as intended and is logged as an open gap rather than papered over. A8 continuity precondition satisfied from 22:50:18, ahead of the window opening at 23:01 rather than at it. Full evidence: docs/evidence/fix-proof-72ed960-2026-07-30.md.

2026-07-30: Contact 1 opened and halted at step 2. Baseline recorded first: journal mark 869 lines, halt md5 3E7AE7CA, 139 bytes, healthy instance 13411 s in SYNCING on 694d570. Owner performed the both-limits-zero refusal manually rather than from a preset, because the prepared .set vectors do not appear in the Load browser, logged as a separate open issue. The refusal alert matched the predicted text exactly, the EA detached, and the config path was confirmed to return before AgMutexAcquire by the absence of any mutex acquire line. The erasure did not occur, and the measured reason narrows the bypass to fresh-load refusals only; full write-up in docs/evidence/bypass-demo-2026-07-30.md, halt state preserved as halt_1200252169.dat.after-config-refusal-2026-07-30. The live halt file was deliberately not restored, since nothing was destroyed and a restore would overwrite record 6's newly written clean flag. Side observation supporting A7 ahead of its own run: an input-change re-init produced a clean session record, exactly as the crash-loop design requires. Also answered the owner's V1 classification question from source: SweepPeriodSeconds is classified core in the SPEC table, and four core inputs reach a refusal return while having a safe clamp available (SweepPeriodSeconds, CrashLoopMaxInits, CrashLoopWindowSeconds, HistoryStablePolls), while only the two daily limits genuinely lack a safe default; for the crash-loop pair the safe clamp direction is toward the default rather than an early trip, matching the A1 corruption policy precedent of failing toward the guardian running. SPEC wording left untouched per ruling, refuse-versus-clamp deferred to post Phase 0. Corrected reporting discipline on owner instruction: an attempted-and-blocked action is reported as blocked, never in language implying it happened.

2026-07-30: Owner ruled R1 as option (d) and absorbed R2 (CrashLoopWindowSeconds default 300, redefined as the pairwise gap bound), both recorded FINAL in DECISIONS; implementation lands in Stage 5a on 72ed960 lineage with the corresponding SPEC A1 rewording, a planned amendment under the ruling. Ran the owner's two pre-batch source verifications against the deployed 694d570 files. Verification 1: SweepPeriodSeconds=9 genuinely refuses, AccountGuardian.mq5:104-109 returns INIT_PARAMETERS_INCORRECT on out-of-range, so the R4 second refusal row stands as planned; found in passing that the SPEC section 5 table cell "clamped 1..5" and the source comment at :21 contradict the coded and section-8-specified refusal behavior, logged as an open wording issue for the owner. Verification 2: the mutex refusal return sits at AccountGuardian.mq5:131, before AgHaltLoad at :136, so the bypass applies on that path and the contact 1 step 8 fix proof is meaningful, not vacuous. Corrected the executor's option (b) reasoning on the record: under (b) a clean session DOES reset the consecutive count, so the reported "fourth isolated power cut in a month" false disarm was wrong; the true (b) residual is the narrower case of repeated unclean deaths with no intervening clean session. The correction does not disturb the (d) ruling, whose recorded grounds never relied on the erroneous claim. Item 0 answered: .claude/settings.json does not exist and never did; the Write attempt was rejected by the isolation guard and the shell attempt was denied by the permission classifier before any file was created, so the opt-out never took effect and there is nothing to remove; the untracked .claude/ directory on main contains only worktrees/, created by the sanctioned worktree tool. Recorded the bypass demo scope limit and standing observation rule 4 by owner order. Hook issue stays OPEN pending the owner's PROFILE fix.

2026-07-30: Stage 2 of docs/PLAN_PHASE0.md executed. Twelve A9 test vectors written to the terminal Presets folder as a9_*.set (defaults, both_zero, neg_percent, neg_currency, percent_over_100, sweep_zero, sweep_over, crash_max_zero, window_zero, polls_zero, nonfinite, optional_bad), each a complete input set with one field off default, ASCII encoded; the owner visually confirms loaded values in the dialog before OK, and if Load shows garbage the fallback is a UTF-16 re-save. The commit-msg hook was found BOM-broken a second time at 12:43 local, minutes into this session, confirming the recurrence mechanism in the open hook issue; repaired identically. Execution stopped at the contact 1 gate per the plan: the next steps need the owner at the terminal (bypass demos on 694d570 per R4, deploy and fix proof of 72ed960, A1, XAUUSD.ecn specification read, A9 rows) and the R1 ruling stays open with the fourth option now costed in the contact 1 report.

2026-07-30: Stage 1 of docs/PLAN_PHASE0.md executed, build 694d570 confirmed compiled into the terminal by diffing all seven deployed sources against git (they differ from 72ed960 on exactly the three fix files, ordering constraint intact). Halt file backed up before anything touched the terminal: docs/evidence/halt_1200252169.dat.post-shutdown-2026-07-30, md5 3e7ae7ca5c3fc5680970242244c20d3e, distinct from the pretest backup 0f40c49b, both retained and labelled. Full decode in docs/evidence/halt_1200252169.post-shutdown-decode.md. Three measured findings. One, the R1 measurement the owner ordered: a routine Windows shutdown marks its session CLEAN, so the overnight event was not an unclean death, no takeover evidence exists because none should, and the delta-2 harvest closes empty by measurement, not by failure; the shutdown deinit journal lines were lost unflushed, so the halt file is the reliable shutdown witness. Two, R3 measured: server clock equals machine clock, broker GMT+3, offset about zero, established by the identity chain process start, journal stamp, TimeCurrent body, TimeLocal record, and file mtime all agreeing within seconds; this CORRECTS the 2026-07-29 ACTIONS claim "server UTC, machine UTC+3", which is struck as resting on an unsupported wall-clock assertion, and reinstates the first audit's GMT+3. Three, the A8 no-tick window measured in the wild: 23:00:51 to 00:53:13, about 112 minutes, LIFE cadence unbroken across it on 694d570. Also appended CLAUDE.md.bak* to .gitignore (record hygiene item 4). No terminal process was started, killed, or reconfigured; the terminal was already running since 10:50:03.

2026-07-30: Captured the Phase 0 execution plan into the repo as docs/PLAN_PHASE0.md on the owner's instruction, after it was delivered outside version control, the third governance artifact to start life outside the repo after the review and the harness. The owner's evidence discipline additions were folded in before capture: lock and halt artifacts are never deleted or overwritten by the executor in any mode, a restore counts as an overwrite and requires a fresh labelled backup first, and a row closes only on evidence present on disk at the stated path, its ledger record carrying result, build hash, and evidence pointer. Gitignore audit ordered by the owner: the negation docs/*.md does not cross directory boundaries, so future markdown under docs/evidence or docs/residue would have been silently ignored. No loss occurred: docs/residue/README.md is tracked, and check-ignore --no-index shows it was never ignored at all, because the bare !README.md negation matches a README.md at any depth. Fix landed: negation widened to docs/**/*.md. The fix newly tracks nothing that exists today; it protects the evidence markdown the plan will create. Work committed on branch worktree-phase0-plan-capture, pending the owner's merge to main; the session's isolation guard does not permit direct commits to the main working copy.

2026-07-30: Phase 1/2 design review (PnL engine, F4 derived-lock defense) conducted in a plan-mode session per the owner's request: read-only against docs/SPEC_v0.1.md, docs/REVIEW_v0.md, and this ledger, plus a read-only grounding pass over the Phase 0 source (no Phase 0 file touched, no new Phase 0 defect found beyond the already-tracked SAFE_HALT bypass). Delivered as chat text, then captured into docs/DESIGN_PHASE1_2.md and committed on the owner's explicit authorization, closing the gap where this design would otherwise have existed only as a chat message, the fourth such governance artifact and the first with no file existence at all prior to capture. Owner ruled all four open questions (GV clamp, login-mismatch handling, weekly banner content, epsilon convention and direction) FINAL, recorded below. Owner rejected the design's dismissal of active-state limit inflation as a non-threat; a ratchet defense was costed against a log-and-accept alternative in the document's addendum and left for the owner's ruling, not implemented. A Q6 clarification (derived-breach comparison's fallback to live inputs when no snapshot exists) was drafted for ratification, not yet ruled. Noted for coordination: the worktree-phase0-plan-capture branch (locked, HEAD a6f2702 at the time of this entry) is independently active with its own LEDGER-style commits; this entry only appends to ISSUES/ACTIONS/DECISIONS and edits no existing entry, to keep a future merge narrow. Phase 0 completion remains the prerequisite for any Phase 1/2 code, per the owner's explicit scope instruction; nothing in docs/DESIGN_PHASE1_2.md is authoritative for implementation purposes yet.

2026-07-29: Correction-2 investigation, ordered before the kill rows, found a SAFE_HALT bypass by source read rather than by test. AG_MUTEX_STALE_SECONDS is 10 s. A refused init appends no session record: OnInit returns INIT_FAILED at :131, while AgHaltLoad is at :136 and AgHaltAppendSession at :163, both downstream. Worse than the owner's framing: OnDeinit then wrote the empty model over the halt file and the GV mirror turned that into a false manual resume. Recorded in the SPEC threat model as prevented, with the general never-loaded-never-written obligation and a best-effort caveat on manual-resume detection. Both fixes written, neither compiled nor deployed, held for step 1 of the sequence so the bug is demonstrated against the unfixed build. Halt file backed up to scratchpad and to docs/evidence/ before anything touched it. Ratified AG_LIFE_INTERVAL_SECONDS to FINAL and logged AG_MUTEX_STALE_SECONDS as a deliberate non-input. A5 stands as PASS-BY-CONSTRUCTION, not PASS: no trade API exists in this build, so the row proves nothing about Phase 3 and carries forward to the sweep engine. Withdrew the earlier proposal to run A3 at CrashLoopWindowSeconds = 600 on the owner's correction: it is kept only as a diagnostic fallback to separate a broken mechanism from a window too tight, and the real default is ruled after measurement. Clock offset established from the session records: server UTC, machine UTC+3.

2026-07-29: Owner re-sampled F3 correctly and the blocker collapsed: the mutex heartbeat advances by the elapsed seconds, so the timer runs. Closed as a phantom with no cause backfilled for the old build. Journal corroborates: 15 LIFE lines at a consistent 30 s, seconds_in_state 1/31/61/91, timer_ticks=212 over a 213-second session. Recorded three standing observation rules. Halt file checked: five session records, the four completed sessions all marked clean, the live one unclean as it should be, so crash counting is not biased unsafe. Quarantined the prior-project state file into docs/residue/ with a README, moved the test harness into scripts/ with a header warning that its /config attach path is defective and unusable until diagnosed, and logged the WebRequest blob as a deferred-phase note. Proposed keeping the proof-of-life interval a constant rather than an input, pending ratification.

2026-07-29: Owner's F3 reading settled the diagnosis: the mutex heartbeat GV exists but is frozen at its acquire-time value, so the timer never fired and single-instance protection was absent, not merely degraded. Applied the combined change set. AgArmTimer is now the single timer arming point with a checked return, an INFO line on success and an Alert on failure. AgMutexRefresh is the first unconditional statement of OnTimer in every state. Proof-of-life line added per A3 at a 30-second interval, carrying state, seconds in state, and the waited-on condition, with SYNCING naming its unimplemented Phase 1 exit outright. Mutex writes instrumented: literal GV names, GlobalVariableSet return values, and a GlobalVariableCheck readback all logged at acquire. AG_STATE_BOOT pseudo-state added, so the first transition reads BOOT->SYNCING. Code comments brought into line with the A2 naming. Deinit now reports whether the timer was armed and how many ticks it saw. Compiles clean at 0 errors, 0 warnings; deployed to the terminal at 16:43. SPEC gained amendment A3 and two threat-model entries (clock manipulation forcing a false SAFE_HALT, and common.ini [Experts] Account=1 disarming automated trading on a login switch). Governance corrected: only the owner marks FINAL, and the clock exemption is re-sourced to the owner as ratified.

2026-07-29: Recorded the owner's naming ruling and the external dead-man residue note. Applied the terminology to docs/SPEC_v0.1.md (amendment A2, editorial) and to the ISSUES and DECISIONS sections here. ACTIONS entries above this line keep their original wording: this section is append-only by protocol, and docs/REVIEW_v0.md likewise keeps the wording it was delivered with. Also withdrew the overstated part of the mutex heartbeat finding after a static source read cleared both of the owner's candidates.

2026-07-29: Owner corrected the harness root-cause claim. Read-only verification of the manually attached instance: OnInit output present in the Experts log at 14:09:40 on XAUUSD.ecn M1, halt file written with one unclean session record and halt flag 0, five global variables in the store of which one (AG_ID_1200252169) is ours. Deleted MQL5\Experts\AccountGuardian_foreign.mq5.bak unread. Struck the algo-gate cause in the ISSUES section rather than editing it silently, and logged three new open issues found during the check: the missing heartbeat GV, the SYNCING->SYNCING boot log, and the stale prior-project globals plus old state file. WebRequest is enabled in common.ini with the URL whitelist stored as an opaque 534-character encoded blob that cannot be decoded from outside the terminal.

2026-07-29: Phase 0 code written and committed (build 3627886, EA version 1.00, spec phase carried in #property description because the MQL5 Market version format rejects a zero major). Files: Experts/AccountGuardian/AccountGuardian.mq5 plus Include/AccountGuardian/{Log,Clock,State,Persist,Pnl,Sweep}.mqh. Deployed and compiled in the terminal data folder as well. Phase 0 test matrix results, one row each:

  S1 no trade API anywhere in the build            PASS  static grep, only hit is the rule comment inside Sweep.mqh
  S2 trade API confined to Sweep.mqh               PASS  static grep, scaffold in place, zero call sites
  S3 TimeCurrent-only rule in decision paths       PASS  static grep, TimeTradeServer count zero; TimeLocal confined to Persist.mqh under the A1 clock exemption
  C1 compiles clean, zero warnings                 PASS  metaeditor64 /compile, "0 errors, 0 warnings"; first run had warning 68 on the version string, fixed
  A1 second attach refuses with Alert              PENDING blocked by the algo-trading gate
  A2 stale mutex heartbeat takeover after kill     PENDING blocked
  A3 crash loop, 4 hard kills inside 60s           PENDING blocked
  A4 SAFE_HALT survives a further restart          PENDING blocked
  A5 SAFE_HALT closes nothing                      PENDING blocked (no trade code exists in this build, so it holds by construction, but the row still needs its run)
  A6 manual resume only via halt-file deletion     PENDING blocked
  A7 clean re-inits do not accumulate to SAFE_HALT PENDING blocked
  A8 timer fires with the market closed            PENDING blocked
  A9 input matrix, core refuses / optional WARNs   PENDING blocked
  A10 transition log lines match SPEC section 6    PENDING blocked

  Harness built and left in place at the job tmp directory: kills the terminal, relaunches it with a startup config that attaches the EA to EURUSD M1 with a chosen .set file, then diffs the journal. It works end to end; only the execution gate stops it. Test account is the JustMarkets demo 1200252169 on JustMarkets-Demo3, confirmed demo by the journal line "balance management has been disabled - demo account".

2026-07-29: Owner approved the Phase 0 plan with two amendments. Amendment 1: LEDGER.md enters git as a governance record (narrow .gitignore negation added, supersession logged, decision recorded FINAL). Amendment 2: SAFE_HALT evidence persisted in a separate halt_<login>.dat, crash count = unclean sessions in window, manual resume = documented deletion of the halt file; SPEC_v0.1 amended in place with a dated amendment block; Phase 0 crash-loop matrix row changed to hard process kills. Minor notes folded in: OnDeinit releases the heartbeat GV by zeroing it, takeover exercised via hard kill; malformed input defined concretely (negative, zero-when-required, out-of-range, non-finite via .set file).

2026-07-29: Wrote and froze docs/SPEC_v0.1.md from the review skeleton with all rulings folded in (base amendment, trade-disallowed policy, config classes, pendings, snapshot, TimeCurrent, dual limits, locked_until = next day anchor, pinned F11/F12/F13, threat model summary). From this point code is measured against SPEC_v0.1; changes require an owner ruling. Note: LEDGER.md and CLAUDE.md are intentionally untracked by git (.gitignore keeps management MD local); only README and docs/ enter version control.

2026-07-29: Recorded owner rulings on all 8 open questions from the architecture review, plus three pinned findings (F11, F12, F13), as FINAL entries in DECISIONS below. Charter amendments accepted for F1 (percent base), F2 (trade-disallowed states), and F9 (config classes).

2026-07-29: Captured the architecture review into the repo as docs/REVIEW_v0.md. The review session ran in plan mode and could not write this ledger. Review scope: adversarial charter review (2 blockers, 8 design findings, 6 notes), spec v0.1 skeleton, 8 open questions for the owner. Updated .gitignore to track docs/*.md. Fixed the commit-msg hook (was written with a UTF8 BOM, git could not spawn it).

2026-07-28: Bootstrapped protocol v3. Created LEDGER.md (replaces STATUS.md/PROGRESS.md/DECISIONS.md, none of which existed yet so no migration needed). Rewrote README.md to bilingual English-first/Hebrew format with no protocol references.

---

## DECISIONS

Decision: (owner ruling 2026-09-02, given in Hebrew in chat, English text approved by the owner in this instruction) the bare *.log rule stays; the negation !docs/evidence/** is added directly below it.
Status: FINAL

Decision: (owner ruling 2026-09-02, given in Hebrew in chat, English text approved by the owner in this instruction) from 2026-08-19 forward, no LEDGER entry may cite an observation of live behaviour without the artifact file name and the line number it was read from. Not retroactive; entries before 2026-08-19 are not reclassified.
Status: FINAL

Decision: (owner ruling 2026-09-02, given in Hebrew in chat, English text approved by the owner in this instruction) the roadmap surface is owner-side and outside this repository's scope; the entry closes on the re-derivation at ACTIONS line 1921.
Status: FINAL

Decision: (owner ruling 2026-09-02, given in Hebrew in chat, English text approved by the owner in this instruction) the primary checkout stays on main. The 2026-08-19 proposal at DECISIONS 2077-2079 is REJECTED.
Status: FINAL

Decision: (owner ruling 2026-09-02, given in Hebrew in chat, English text approved by the owner in this instruction) investigate first against metaeditor.log, classify afterwards on what the log shows. The owner copies the file out at the executor's dictation; the executor reads the copy only.
Status: FINAL

Decision: (owner ruling 2026-09-02, given in Hebrew in chat, English text approved by the owner in this instruction) ISSUES holds open items only. Every entry whose resolution is recorded, ruled FINAL, or superseded is deleted from ISSUES directly. Entries carrying their own Status: FINAL are moved verbatim to DECISIONS. ACTIONS stays append only.
Status: FINAL

Decision: (owner ruling 2026-09-02, given in Hebrew in chat, English text approved by the owner in this instruction) the repository is public and open source by intent. The demo account number 1200252169, the terminal id D0E8209F77C8CF37AD8BF550E51FF075, the broker name and local paths under C:\Users\www83 are not secrets and stay in the record, past and future. Files originating in the terminal data folder (state, peak, floor, halt .dat files and raw journals) never enter git as-is; only transcoded .txt copies banked under docs/evidence/. Secrets and keys stay excluded as .gitignore lines 10-18 already do.
Status: FINAL

Decision: (owner ruling 2026-09-02, given in Hebrew in chat, English text approved by the owner in this instruction) the real account will run on an always-on VPS. Deployment to it is by git sync, never by manual edits in the terminal install folder. This answers the "mains or VPS" question.
Status: FINAL

Decision: (owner ruling 2026-09-02, given in Hebrew in chat, English text approved by the owner in this instruction) identified 2026-09-02 as an EA state file (decoded in chat from field layout: lock 2026-08-26 01:00:10 to 2026-08-27 01:00:00, limit snapshot 101.399, base 2027.98; decode to be checked in TASK 5). Owner ruling: the P49/P53 closure already cites a banked state file, the tmp copy is residue, the owner deletes it by hand.
Status: FINAL

Decision: (owner ruling 2026-09-02, given in Hebrew in chat, English text approved by the owner in this instruction) hardening of the limit inputs against runtime widening is a mandatory stage before COMPLETE. Design is ruled in a dedicated plan session; nothing is built under this entry.
Status: FINAL

Decision: (owner ruling 2026-09-03, given in Hebrew in chat, English text approved by the owner in this instruction) R11 (2026-08-27 incidents, closes R5): the three compiles of the Terminal Experts root path on 2026-08-27 (metaeditor log lines 437 at 00:41:11, 461 at 12:30:42 failed with 11 errors and 2 warnings, 465 at 15:11:52) were the owner's own MetaEditor actions. The owner placed AccountGuardian.mq5 at MQL5\Experts\ himself while copying files into the terminal, compiled it himself, and states that the failed compile came from his own disordered handling of an old copy of the file. Incident one is an owner action outside RULE A, not an executor violation; the attribution to the implementation session in the former ISSUES entry was wrong. Incident two, the worktree AccountGuardian.mq5 found deleted twice, closes as a fact with no established cause: the log records no file operations; the only adjacency is log lines 459 (worktree, 12:30:10, 11 errors) and 461 (root, 12:30:42, 11 errors), 32 seconds apart, recorded as observation, not explanation. Evidence: docs/evidence/metaeditor-20260902.txt, raw copy md5 6666BE488FCAC2D77DAF8C1F2EC3F9E1.
Status: FINAL

(moved from ISSUES 148-151, original date preserved)
Issue:  Permanent scope limit on the Q8 cadence cap, recorded 2026-08-10 so no future session reports the cap as live-proven and no future session wastes a pass trying to prove it. The Monday reopen produced exactly ONE qualifying pass, so the 30 second suppression window never had a second candidate to suppress. That is the predicted outcome and not a gap: the deploy document's own expectation 1 said the ALERT would be "cadence-capped so it cannot repeat inside 30 s, and in practice not repeating at all because the condition clears on the same pass". The stronger statement, which is the reason this is a scope limit rather than an open row: the cap is UNFALSIFIABLE IN THE LIVE CHANNEL BY CONSTRUCTION. `Clock.mqh:91` clears `g_ag_anchor_jump_note` at the top of every call and the jump branch advances `g_ag_high_anchor` before returning, so a second qualifying jump within 30 seconds cannot arise from a clock that only resumes forward. A second live ALERT would require a second three-day forward jump inside half a minute, which no reopen can produce.
Action: None, and none is possible by design. The cap stands proven on two instruments that are not the live journal: the 26/26 vector run of 2026-08-09 22:30:07.433, and source read at `AccountGuardian.mq5:166-172` where the `g_ag_last_anchor_alert` gate compares against `AG_LIFE_INTERVAL_SECONDS`. Classified in the same category as amendment A5's PASS-BY-CONSTRUCTION and the bypass demonstration's permanent scope limit. Recorded alongside it, and it is the same class of limit for the same structural reason: the jump note was never observed in a live LIFE line's `waiting_on` field either, because the note lives for exactly one pass of a 1 second timer while LIFE emits every 30 seconds, so the odds of coincidence were about one in thirty and it did not happen. All 153 LIFE lines of the reopen read `waiting_on=-`, which confirms the CLEARED half of the row and cannot confirm the note-visible half; that half rests on vectors `q8_forward_jump_note_names_both` and `q8_weekend_reopen_note_names_jump`.
Status: FINAL

(moved from ISSUES 193-196, original date preserved)
Issue:  The claimed MathIsValidNumber gate fix is STRUCK as never having existed, and the reasoning that made it look real is struck with it. The owner's ground was that the journal emits "DailyLossPercent is not a finite number", which the 28c29a4 lineage build supposedly cannot emit. Measured false, three ways. The string and its MathIsValidNumber guard are the first two branches of AgValidateLimits in MQL5/Include/AccountGuardian/Pnl.mqh, printed here from 28c29a4 itself. Pnl.mqh has exactly one commit in its entire history, f358ac1 of 2026-07-29 13:47:19, the Phase 0 skeleton, and f358ac1 is an ancestor of 28c29a4 confirmed by merge-base. And docs/PLAN_PHASE0.md:34 has said since it was written that AgValidateLimits rejects non finite, negative and percent above 100, which is why the A9 expectations were grounded in source in the first place. So the emitted line is not proof of a new build, it is the deployed build's existing gate working exactly as specified, and it is the strongest available evidence that NO fix was deployed.
Action: None on the code, which is correct and untouched. Deployment state verified rather than assumed and recorded as the corroborating measurement: terminal AccountGuardian.mq5 and Persist.mqh are md5-identical to the repository at DBB44AF87437265BA69CAA4C8C1CADA7 and BA6AB1092B0A86979974ACECEC643BF0, terminal Pnl.mqh differs from the repository copy by LINE ENDINGS ONLY, 72 CR and 72 LF against 0 CR and 72 LF, content identical after newline normalisation, its mtime still 2026-07-30 22:39:18, and the ex5 is still the 2026-08-04 14:29:50 build at 40224 bytes. No compile ran that night.
Status: FINAL as a correction, code unchanged

(moved from ISSUES 250-253, original date preserved)
Issue:  Fresh-load audit, ordered by the owner and delivered in docs/evidence/bypass-demo-2026-07-30.md. Governing rule, now measured: an input-change re-init reuses the loaded image and its globals survive, while a fresh attach, a recompile, and a terminal restart all reset globals, and MT5 seeds a fresh attach from last-used inputs that persist across restarts via the chart profile. Exposed rows: A1 (a malformed second chart refuses at the config check before ever reaching the mutex, testing nothing), A2, A3, A4, A6 and A7 (all restore inputs from the chart profile, and since AgHaltAppendSession sits downstream of every refusal return, a malformed profile makes crash counting unable to accumulate at all), A8 (only through its starting attach), and A9 in the opposite direction: a malformed row driven by editing a RUNNING instance inherits a populated model, so the fix's skip branch never executes and the row proves the refusal without proving the fix.
Action: Adopted for the rest of the matrix: every attach instruction names the exact input values to confirm before OK, the executor takes the halt baseline BEFORE the attach rather than after, and each A9 row runs as a fresh load beginning from a removal.
Status: FINAL

(moved from ISSUES 278-281, original date preserved)
Issue:  Executor error, logged for symmetry with the owner-side errors recorded this round. Having just delivered the fresh-load audit naming last-used-input persistence as the hazard behind six acceptance rows, the executor then failed to apply it to the end state of the night: a bad-input attach leaves last-used at both zero, so an overnight terminal relaunch could restore a refusing EA and collect nothing for A8. The owner caught it. The corrected sequence keeps a healthy instance running on the first chart throughout by running the fix proof on a second chart, and ends by resetting last used back to the defaults.
Action: Standing lesson: an audit is not applied until it has been walked forward to the state the system is left in, not just the state each step passes through.
Status: FINAL

(moved from ISSUES 294-297, original date preserved)
Issue:  Scope limit on the bypass demonstration, recorded by owner order so the ledger never implies a complete demonstration. The contact 1 demos (both-limits-zero and SweepPeriodSeconds=9) demonstrate the erasure half only: a refused init on 694d570 wiping session records from the halt file. The dangerous half, a cleared halt flag turning into a false RESUMED_FROM_SAFE_HALT, needs a persisted SAFE_HALT first, which needs A3, which runs only on the fixed build. That half therefore stays source-read-only permanently.
Action: Record the A9 refusal rows and the bypass demo with this scope limit attached. No further action possible by design.
Status: FINAL

(moved from ISSUES 322-325, original date preserved)
Issue:  Standing observation rules, learned the hard way, binding on every future session.
Action: (1) External reads of bases\gvariables.dat are never evidence while the terminal runs. (2) The F3 dialog is a snapshot; a GV reading is admissible only as open, read, fully close, wait, reopen, read. (3) Absence of log output is never evidence of absence of execution unless the emitting line is known to be unsuppressed at the active verbosity; verify the emission path before concluding anything from silence. (4) Added by owner order 2026-07-30: journal lines emitted at OS shutdown can be lost unflushed even when the code that printed them ran to completion, so the halt file, not the journal, is the authoritative witness for shutdown behavior; measured on the 2026-07-29/30 overnight event, where OnDeinit provably ran (clean flag written, mutex zeroed) and its deinit lines do not exist in the log. (5) Added by owner order 2026-07-31: a decoder that fails silently returns an empty result indistinguishable from a genuine absence of matches; measured 2026-07-30 when iconv returned empty on a UTF-16 CRLF chart profile that PowerShell read correctly. Any negative result from a text search must confirm the decoder actually read the file before the absence counts as evidence. (6) Added by measurement 2026-08-04: the metaeditor64.exe process exit code is NOT a compile status signal and is inverted in practice on this installation. Measured with a control: the real build logged "Result: 0 errors, 0 warnings" and exited 1, while a deliberately broken copy of the same source logged "Result: 3 errors, 0 warnings" and exited 0, producing no ex5. Any gate keyed on the exit code would therefore pass every broken build and fail every clean one. The authoritative signals are the log's Result line and the presence and timestamp of the ex5, and both must be read. (7) Added by measurement 2026-08-04: the ex5 SIZE AND HASH ARE NOT BUILD IDENTITY. Five consecutive compiles of byte-identical source, verified unchanged by md5 across all five, produced five different binaries: 40992, 39982, 39450, 40470 and 40224 bytes, every one a different md5, a spread of about 1500 bytes. Two ex5 files therefore cannot be compared to decide whether they were built from the same source, in either direction: equal size would not prove sameness and unequal size does not prove difference. This strikes the corroborating use of the "ex5 39642 bytes" style figure recorded in earlier entries; those entries' conclusions stand on their other evidence, the figures are simply not identity. Build identity rests on three things instead: the md5 of every source file against the committed tree, the ex5 modification timestamp, and the runtime behaviour of the loaded image in the journal, which is the only measure that speaks to what is actually running rather than what is on disk.
Status: FINAL

(moved from ISSUES 342-345, original date preserved)
Issue:  NOTE for the record, so nobody later reads it as a spec violation: the EA carries #property version "1.00" rather than a 0.x version matching SPEC v0.1. The MQL5 Market version format rejects a zero major and emits warning 68, which would have left the build permanently one warning short of clean. The phase is carried in #property description instead.
Action: None. Revisit at the first release that is genuinely 1.0.
Status: FINAL

Decision: (owner ruling 2026-09-01, given in chat, recorded verbatim) "THE ACCOUNT TRADES EVERYTHING AND EVERYTHING IS LOCKED. The guardian's scope is the whole account, every symbol, every source, mobile included, which the 2026-08-29 and 2026-08-31 evidence proves. BTCUSD.ecn STAYS IN MARKET WATCH PERMANENTLY, so that the guardian's clock, TimeCurrent, follows the account's real trading hours including weekends. The weekend is a trading day: day anchors roll at 01:00 every day including Saturday and Sunday. The weekend is no longer a guaranteed no-tick window, and any earlier test or entry that assumed one is read under this ruling from now on. The 2026-08-01 FINAL's removal of BTCUSD.ecn is superseded in that clause; its frozen-quote protections stand unchanged for any freeze that still occurs." What this entry supersedes, by name and exactly how far: THE 2026-08-01 INSTRUMENT SCOPE FINAL (owner ruling 2026-08-01), its instrument list clause, "Market Watch is XAUUSD.ecn, US100.ecn and XAGUSD.ecn only. BTCUSD.ecn is removed and never returns.", and its weekend no-tick assumption clause, "All subscribed instruments are 24/5, so the weekend close is a reliable no-tick window by design.", AND THOSE TWO CLAUSES ONLY. Every other clause of that FINAL stands untouched and is reaffirmed here: the account still trades gold, NASDAQ and silver among whatever else this ruling adds under "everything", XAUUSD.ecn, US100.ecn and XAGUSD.ecn remain in Market Watch, and the frozen-upper-bound consequence clause is not argued here in either direction.
Reason: The mechanism the owner ruled on: with a 24/7 instrument absent from Market Watch, a weekend trade placed from the mobile app would run against a guardian whose clock, TimeCurrent, is frozen for the whole weekend, since nothing ticks to advance it, so the guardian's day anchors and its enforcement would sit stale exactly when a mobile trade could move the account. That is the configuration to avoid, not the one to keep. BTCUSD.ecn's continuous quote is what keeps TimeCurrent advancing through the weekend, which is why it stays in Market Watch permanently rather than being treated as an incidental presence.
Status: FINAL

Decision: (owner ruling 2026-08-31, given in chat, recorded verbatim) FOUR RULINGS CLOSING VERSION 1 OF THE REALIZED PEAK TRAILING FLOOR. ONE, on the implementation readings the 2026-08-27 ACTIONS entry raised as readings and not rulings: "Implementation readings accepted: AgPeakUpdate calling AgRealizedRunFold itself (a second call, one fold, shape 1 FINAL satisfied in letter); the peak anchor moves forward only; a backward step holds the peak; AgPeakUpdate runs after the HistoryDealsTotal read so the Q9 count stays Phase 1's." TWO, on build identity: "Build identity of version 1 as deployed: AccountGuardian.ex5 md5 124BA610201A6D67C050572C6A84DE37, 86866 bytes, compiled by the owner in MetaEditor on 2026-08-27 from source md5 6CF5F6A78E8D9A333D84780C33DE475A (identical to commit 428614c's AccountGuardian.mq5) with Persist.mqh and Pnl.mqh from the same commit copied by the owner into the Terminal Include folder; 0 errors 0 warnings; md5 measured by the owner at source and destination. The executor's 2A3320AA build was never deployed. Every version 1 row closure cites this identity." THREE, on the 2026-08-29 rollover: "The 2026-08-29 rollover firing at 12:48:51 is RULED BEHAVIOR under Q7 (TimeCurrent is the only clock; a frozen feed freezes the anchor), not a defect. Breach detection is not tick gated: lock level lines fired every 30s through the freeze." FOUR, on the 2026-08-30 boot-derived lock: "The 2026-08-30 15:45:59 boot-derived lock is EXPLAINED AND ACCEPTED: the machine lost battery power at 15:14 (owner), the owner traded from the mobile app while the terminal was down and lost about 192 net, and on restart the boot derivation replayed server history and locked within 3 seconds. Recorded as an incidental finding: the guardian catches trades made while it was offline, because the authority is server history."
Reason: These four rulings are the basis on which all four version 1 acceptance rows, V1-A through V1-D, close in the 2026-09-01 ACTIONS entry, and on which the 2026-08-29 and 2026-08-30 incidental findings are recorded as explained rather than open. Given in chat per the process ruling of 2026-07-29 that only the owner marks an entry FINAL.
Status: FINAL

Decision: (owner ruling 2026-08-24, given in chat, recorded verbatim as owner ruling) THE REALIZED PEAK TRAILING FLOOR IS RATIFIED AS VERSION 1, AND IT IS REALIZED ONLY. The daily allowance stops being measured from the day anchor alone and starts being measured from the day's realized high water mark, so an intraday gain that is given back is caught rather than absorbed. NINE ELEMENTS, as ruled. ONE, WHAT RAISES THE PEAK (D1.1). Only trade closures, INCLUDING PARTIAL CLOSURES, raise the day's realized peak. Floating profit NEVER raises it. An unrealized gain that is never closed leaves the floor exactly where it was. TWO, WHAT THE FLOOR IS CHECKED AGAINST (D1.2). The floor is checked against FULL ACCOUNT EQUITY INCLUDING FLOATING PnL. An open position diving below the floor locks IMMEDIATELY and does not wait to be closed. The two halves of this ruling are deliberately asymmetric and the asymmetry is the mechanism: floating can only ever trigger the lock, never move the floor that triggers it. THREE, THE PEAK IS MONOTONE WITHIN THE DAY (D1.3). The realized peak only rises intraday. A losing deal after a gain does not lower it. It resets at the day boundary and at no other moment. FOUR, STRICTER ALWAYS WINS (D2). The effective floor is max(existing ratchet floor, realized peak minus limit), IN EVERY SCENARIO, INCLUDING RESTART AND DAY ROLLOVER. NO EXCEPTIONS. Neither mechanism may loosen what the other has tightened, at any moment, by any path. FIVE, RESTART RECONSTRUCTS FROM HISTORY (D3.1). After ANY restart the realized peak is RECONSTRUCTED FROM TODAY'S DEAL HISTORY. The persisted value is a CROSS CHECK ONLY and is never the source of truth. ON MISMATCH, TAKE THE STRICTER OF THE TWO. This composes with element FOUR rather than competing with it: reconstruction feeds the max, it does not bypass it. SIX, ONE CLOCK (D3.2). The peak resets at THE EXACT SAME DAY BOUNDARY already locked FINAL for the daily base. ONE CLOCK, NO NEW BOUNDARY DEFINITION. No constant, no input and no helper in this work may define a second boundary, and a future amendment to the existing boundary moves the peak with the base automatically and needs no second ruling. SEVEN, STORAGE IS A SEPARATE FILE (D6). The peak lives in its own `peak_<login>.dat` written through `AgAtomicWrite`. PHASE 2 OPEN QUESTION SEVEN IS NOT SUPERSEDED IN ANY CLAUSE and `floor_<login>.dat` STAYS BYTE IDENTICAL to what is deployed today. The widened file alternative that the planning draft carried is REMOVED rather than left standing as an option, so that no later session reads it as still available. EIGHT, THE PEAK TERM USES THE POST RATCHET VALUE (D7). peak_level = realized peak minus `enforced_limit`, where `enforced_limit` is the value `AgRatchetUpdate` returns and not the raw live limit. RULED AS THE STRICTER READING OF ELEMENT FOUR: tighter whenever the ratchet is holding, identical when it is not. NINE, THE PEAK DOES NOT ENTER THE LOCK SNAPSHOT (D8). The peak is PRE BREACH ONLY, EXACTLY AS THE RATCHET IS. The locked window is judged by the snapshot alone, per the existing Q6/F7 FINAL of 2026-07-29, which this entry does not touch. `state_<login>.dat` and `AG_STATE_FORMAT_VERSION` ARE UNTOUCHED, per the two FINAL rulings of 2026-08-20 recorded on the `phase3-defect-fixes` branch. SCOPE, RULED EXPLICITLY (D5). VERSION 1 IS REALIZED ONLY. Anything involving a floating peak or an equity peak is DEFERRED TO THE MIRROR REPO and no part of it enters this build. The limit amount itself is UNCHANGED and was not discussed. SEQUENCING (D4). P3-1 AND P49/P53 CLOSE ON THE CURRENTLY DEPLOYED BUILD BEFORE VERSION 1 REACHES MT5. Planning and design for version 1 run in parallel with those rows and do not wait on them, but no version 1 binary is deployed while either is open. THE COST IS STATED HERE RATHER THAN DISCOVERED LATER. This mechanism fires more locks than the current one, by construction, and some of them will feel early to an operator who was up on the day and gave part of it back. That is the ruling working, not the ruling misfiring, and no later session may raise the increased lock rate as a defect. Under the Phase 2 open question TWO interim posture those locks still send no order, so between the lock and the owner's manual close the floating loss remains unbounded by the guardian; version 1 tightens WHEN the state machine locks and changes nothing about what the guardian can do once it has.
What this entry supersedes, by name, and exactly how far: PHASE 2 OPEN QUESTION FIVE (owner ruling 2026-08-18), THE CLAUSE "NO new persisted artifact is added", AND THAT CLAUSE ONLY. Version 1 persists the realized peak. EVERY OTHER CLAUSE OF QUESTION FIVE STANDS UNCHANGED AND IS REAFFIRMED HERE: total exposure is still the framing, the daily limit still counts ALL open floating loss regardless of when it was earned, NO floating baseline snapshot is taken at the anchor, and RE LOCKING EACH DAY ON A HELD LOSER IS STILL INTENDED BEHAVIOUR. The clause is superseded on the narrow ground the entry's own Reason states: the artifact it refused was a FLOATING BASELINE snapshotted at every anchor, creating a second source of truth about the day that could disagree with the first. The realized peak is not that artifact and does not create that second source of truth, because element FIVE makes broker history the source of truth and the file a cross check. THIS IS THE ONLY CLAUSE THIS ENTRY SUPERSEDES ANYWHERE. Two further FINAL entries are CONSTRAINED BUT NOT SUPERSEDED, recorded here so that no later session reads the version 1 work as having quietly moved them. PHASE 2 OPEN QUESTION SEVEN (owner ruling 2026-08-18) is untouched in all five of its elements under element SEVEN above: the single scalar `floor_currency`, `floor_<login>.dat`, `AgAtomicWrite`, RESEED AT ROLLOVER, NO RESEED ON A BACKWARD STEP, and `min(limit, floor)` applying PRE BREACH ONLY all stand exactly as ratified, and element FOUR consumes the ratchet's output rather than replacing its arithmetic. DEFECT 3 SHAPE 1 (owner ruling 2026-08-20, second of that date, recorded on the `phase3-defect-fixes` branch) is likewise untouched: its clause freezing `AgRealizedFold` to its current signature and its two outputs with no second history walk added is satisfied IN LETTER by version 1, because the running maximum the peak needs is produced by the SAME single sorted walk that already produces the running minimum, so no second walk exists to add and no signature moves.
What is NOT superseded, listed because the temptation to read this entry more widely than it is written is real: Q2/F1 and the 2026-08-05 base ruling, whose base is untouched and still Balance and never Equity, since element TWO governs the MEASURED side of the comparison and not the base; Q3's per deal formula; F11's carry double count; F12's realized whitelist, which is what makes element ONE implementable at all; Q1, Q4, Q7 and both Q8 rulings on the boundary, which element SIX binds to by reference rather than restating; Q6/F7 and its 2026-07-30 clarification, which still govern the LOCKED window unconditionally under element NINE; Q9's one pass deferral and Q10 with its 2026-08-09 amendment, which all sit upstream of the comparison and gate the new trigger unchanged; the 2026-07-30 epsilon ruling, whose err toward breach direction governs the new comparison exactly as it governed the old; the 2026-08-18 ratchet epsilon ruling, whose discipline the peak's own rise test inherits for the identical reason; and the 2026-08-18 question TWO interim posture, which means every lock this mechanism newly fires STILL LEAVES THE POSITION OPEN until the owner closes it by hand.
Reason: Owner's ruling, given in chat on 2026-08-24 and recorded verbatim. No rationale beyond the rulings themselves was supplied and none is invented here. Two things are recorded because they were established during the verification that preceded the ruling rather than assumed. FIRST, element TWO needs no new quantity: the engine's existing pnl, realized plus floating, is already account equity measured relative to the day anchor balance and net of deposits and withdrawals, by the Q2 identity, so "full account equity including floating PnL" is satisfied by the number `AgEvaluateActive` already computes on every pass and NO Equity read is introduced anywhere, which is what keeps the 2026-08-05 never Equity ruling intact. SECOND, element SIX needs no reset code: the peak is derived from a walk over the window starting at the anchor that the base and the realized sum already share, so when the anchor advances the walk's own window moves and the running maximum restarts from zero by construction. The reset is the window moving, not an action taken. Recorded so that no later session adds a reset path and then has to defend it. A THIRD thing is recorded about provenance and it does not weaken the ruling: the planning session that produced this text ran read only in plan mode on 2026-08-24 and wrote nothing, its verification confirmed all twenty three named FINAL entries present exactly once and every quoted clause byte identical, and the two questions that draft left open, storage and the peak term, are the two the owner closed here as D6 and D7, so no clause of this entry reaches the ledger unruled.
Status: FINAL (owner ruling, chat, 2026-08-24), no open clauses remaining

Decision: (owner ruling 2026-08-20, third of the date) "RECORDED AS SUCH" IS SATISFIED BY THE LEDGER, THE PLAN AND THE JOURNAL'S EXISTING DISTINCTION. NO STATE FILE FORMAT CHANGE. Quoted as given in full: "'recorded as such' is satisfied by the ledger, the plan and the journal's existing distinction. No state file format change. Reading 2 is not taken for this build; it may be raised as a design item for a future build only." This closes the derived question the shape 1 ruling of the same date raised and left open. The state file record layout at `Persist.mqh:352-359` is UNCHANGED, `AG_STATE_FORMAT_VERSION` stays 1, and no field is added to distinguish a derivation instant from a true breach instant inside `state_<login>.dat`. THE JOURNAL IS THE DISTINGUISHING ARTIFACT and it needs no change to be one: a boot derived lock is preceded by a `boot witness DERIVED fired` line and an ACTIVE declaration is preceded by a `breach arithmetic` line, so the two paths are already separable by any reader holding the journal. READING 2, a marker in the state file itself, IS NOT TAKEN FOR THIS BUILD and is explicitly left available as a DESIGN ITEM for a future build, which is a narrower disposition than rejection and is recorded as the owner worded it.
Reason: Owner's ruling, recorded as given. No rationale was supplied and none is invented here. THE COST THIS ACCEPTS IS NAMED SO NO LATER SESSION DISCOVERS IT AS A SURPRISE: a reader holding ONLY `state_<login>.dat`, with no journal, cannot tell a derivation instant from a true breach instant in the `L` record's third field, and after this build such files will exist on both paths. The journal is the answer, and the journal is the artifact this project has twice had reason to treat as less durable than the state file, once when a hard kill lost B1's entire boot block on 2026-08-18 and once when the executor destroyed the Sunday journal on 2026-08-17. That is the trade the ruling makes and it is the owner's to make. Recorded rather than argued: the ruling keeps the format frozen and the diff small, which is the same reason the defect 3 plan kept `Persist.mqh` out of both shapes, and reading 2 remains open as a design item rather than being closed against.
Status: FINAL

Decision: (owner ruling 2026-08-20, second ruling of that date) DEFECT 3 TAKES SHAPE 1: `breach_time` ON THE BOOT DERIVED PATH IS THE DERIVATION INSTANT, RECORDED AS SUCH. Quoted as given in full: "Shape 1. breach_time on the boot derived path is the derivation instant, recorded as such." SHAPE 1 is the shape of that name in `docs/FIXPLAN_PHASE3_DEFECT3_2026-08-20.md`: when the DERIVED witness supplies a lock and no valid file snapshot exists, `AgEnterLockFromBoot` persists the derivation's own `limit_cmp` and `base` as the Q6 snapshot and takes `AgServerNow()`, the instant the derivation ran, as `breach_time`. SHAPE 2 IS NOT TAKEN, and the consequence is that `Pnl.mqh` is NOT widened: `AgRealizedFold` keeps its current signature and its two outputs, no second history walk is added, and the fix stays inside the EA. "RECORDED AS SUCH" IS PART OF THE RULING AND NOT A GLOSS: the field carries a derivation instant on this path and a true breach instant on the `AgDeclareLock` path, and any artifact, document or later entry that reports it must say which of the two it is rather than letting the field's name imply the stronger meaning.
Reason: Owner's ruling, recorded as given. No rationale was supplied for the choice and none is invented here. What the ruling costs is already written in the plan's own two cost paragraphs and is not restated: shape 1 is uniformly approximate on the boot derived path, where shape 2 would have been exact whenever the replay disjunct fired and approximate whenever only the live disjunct did, with nothing in the state file distinguishing the two. The owner's added words "recorded as such" are what answer that cost, by making the approximation explicit rather than by removing it. ONE DERIVED QUESTION IS RAISED BY THOSE WORDS AND IS NOT RULED HERE, per the standing rule that an executor may not settle by inference what a ruling did not say: "recorded as such" is satisfied by this ledger and by the plan, and it MAY also be read as requiring a marker in the artifact itself, which the state file cannot carry today without a new field and therefore a format version change touching `Persist.mqh`. That is well outside the plan's stated ranges and it is the owner's to decide at the build instruction, not the executor's to assume either way. PROVENANCE, per the correction FINAL of 2026-08-18: the owner gave this in one message in the terms quoted and marked nothing FINAL in words, so FINAL is applied by transcription rather than by explicit owner words, the same discipline the two entries of 2026-08-19 and the shape A entry of this date each record for themselves. THIS IS A RULING AND NOT A BUILD INSTRUCTION. All three questions on defect 3 are now ruled, the order, the coupling to one deployment and the shape, and none of the three authorises writing code, so nothing is implemented until a later and separate owner instruction says so.
Status: FINAL

Decision: (owner ruling 2026-08-20, Phase 3 defect 1 implementation session) SHAPE A IS THE DEFECT 1 FIX, AND DEFECT 3 IS COUPLED TO THE SAME DEPLOYMENT. Quoted as given in full: "Shape A, and the defect 3 fix is coupled to the same deployment. Fix order unchanged: 1 implemented first, 3 second, one build." SHAPE A is the shape laid out under that name in `docs/FIXPLAN_PHASE3_DEFECT1_2026-08-19.md`: lock expiry enters SYNCING rather than ACTIVE directly, unconditionally, so the first pass eligible to declare a breach is preceded by the same history stability discipline every cold boot already runs. Shapes B and C of that document are NOT TAKEN, and under protocol rule 5 this entry closes that choice rather than leaving it open for a later session to reopen. The fix order of 2026-08-19 is unchanged and is not amended by this: defect 1 is implemented first and defect 3 second. NEITHER SHIPS ALONE. The two reach the terminal in ONE BUILD and ONE DEPLOYMENT, so a build carrying only one of them is outside this ruling.
Reason: Owner's ruling, recorded as given. No rationale was supplied with the choice of shape and none is invented here. THE COUPLING IS NOT A SCHEDULING PREFERENCE AND ITS GROUND IS ALREADY ON THE RECORD, in the shape A section of the defect 1 plan and in that document's prediction P31: shape A makes `AgBootDerivation` run at EVERY expiry, so an expiry time re lock arrives through `AgEnterLockFromBoot` and persists an empty Q6 snapshot, which is defect 3 landing on a path shape A newly makes routine. Shipping shape A alone would therefore WIDEN defect 3 from a boot only case to every expiry, and the coupling is what stops that widened window from ever reaching the terminal. That consequence is stated in full in `docs/FIXPLAN_PHASE3_DEFECT3_2026-08-20.md` under the heading THE SHAPE A INTERLOCK, including what the boot path persists once defect 3 is fixed. PROVENANCE, recorded in the discipline the provenance correction FINAL of 2026-08-18 requires: the owner gave this ruling in one message in the terms quoted and marked nothing FINAL in words, and since the protocol offers a DECISIONS entry only FINAL or REVISIT, FINAL is applied here by transcription rather than by explicit owner words, exactly as the two entries of 2026-08-19 record for themselves and as the owner confirmed on that date.
Status: FINAL

Decision: (owner ruling 2026-08-19, Phase 3 opening session) THE DEFECT FIX ORDER FOR THIS BUILD IS RULED AND IS NOT THE EXECUTOR'S TO REORDER. Quoted as given: "Defect fix order for this build: (1) disconnect straddling lock expiry leaves the first post expiry pass ungated, (2) GV lock mirror self erases on cold boot, (3) boot derived lock persists an empty Q6 snapshot and loses the breach timestamp, (4) LOCKED LIFE lines carry no numbers." These are the same four defects the ACTIONS entry of 2026-08-19 recorded as waiting on the next build, and the ruling settles the order they are fixed in and nothing else. IT IS NOT AN AUTHORIZATION TO WRITE CODE and none has been given, so a later session finding this order recorded must still wait for a separate build instruction.
Reason: Owner's ruling, recorded as given. No rationale was supplied with it and none is invented here. PROVENANCE, recorded in the discipline the provenance correction FINAL of 2026-08-18 requires: the owner marked ruling TWO of this session FINAL in terms and said nothing about a marker for this one. The protocol offers a DECISIONS entry only FINAL or REVISIT, so FINAL is applied here by transcription rather than by explicit owner words, and that distinction is written down rather than left for a later session to assume.
Status: FINAL

Decision: (owner ruling 2026-08-19, Phase 3 opening session) THE RESEED ON RELOAD STAYS AS IS. Quoted as given: "Reseed on reload stays as is. On reload the state takes the current input values. A record of an input change made while locked is not required." This closes design item 5 of the 2026-08-19 Stage 7 close, which the owner had classed as a design item and explicitly not as a defect, and it closes it in favour of the behaviour already shipped, so NO CODE FOLLOWS FROM THIS RULING. `AgEnterLockFromBoot` continues to set `g_ag_locked_in_percent` and `g_ag_locked_in_currency` from the live inputs on every boot. The consequence is now ruled intended: an operator who raises a limit during a locked window leaves no trace of having done so in the journal, and no later session may raise that missing audit trail as a bug.
Reason: Owner's ruling, recorded as given. THE TWO STANDING ARGUMENTS ARE DELIBERATELY NOT RESTATED HERE and are referenced by their existing location instead, per the owner's instruction. Both sit in the ACTIONS entry of 2026-08-18 headed PHASE 2 STAGE 7 OPENED, DEPLOY AND LIVE ACCEPTANCE, in the paragraph beginning THE CONSEQUENCE THE OWNER ASKED TO BE RECORDED ON THIS ROW, which carries the argument against and names where the argument for it already sits, the Stage 3 note in the ACTIONS entry of 2026-08-18 headed PHASE 2 STAGE 3 IMPLEMENTED, LOCK SEMANTICS. That paragraph closes on the words "Both are recorded and neither is decided." This entry is what decides it, and it decides it without adding a third argument.
Status: FINAL

Decision: (owner ruling 2026-08-19, Phase 3 opening session) P2-H IS NOT CLOSABLE BY THIS BUILD. Quoted as given: "P2-H stays OPEN until the coming weekend market close. Not closable by code or by code reading." The row therefore stays OPEN in ISSUES carrying the live window it needs, and NO FIX IN THIS BUILD MAY BE REPORTED AS CLOSING IT. The standing instruction of 2026-08-18, that the row is left untested rather than approximated because a substitute would prove something else while carrying this row's name, is unchanged by this ruling and still binds.
Reason: Owner's ruling, recorded as given. It bars both routes by which the row would otherwise have been claimed, a code change and an argument from source, which matters because the reseed ruling directly above settles the mechanism P2-H was blocked on and a later session could otherwise read that settlement as closing the row. FINAL HERE BINDS THE RULING AND NOT THE ROW: the ruling that P2-H is not closable by code is locked, while P2-H itself is an OPEN row in ISSUES awaiting the weekend window. PROVENANCE, same discipline as the fix order entry above: the owner said nothing about a marker for this one, so FINAL is applied by transcription rather than by explicit owner words.
Status: FINAL

Decision: (PROPOSAL, UNRULED 2026-08-19, raised by the executor and NOT ratified) MAIN SHOULD NOT BE CHECKED OUT ANYWHERE, so that an executor session can merge into it by owner instruction instead of dictating the merge back for the owner to type. The constraint was hit twice on 2026-08-19. Git refuses to check out a branch that is already checked out in another worktree, so with main occupied by the owner's primary checkout the executor could neither perform the merge nor commit the merge record, and both had to be dictated. That cost a round trip each time and produced `4021519`, a merge commit whose entire content is a paragraph recording the merge before it. The proposal is that the primary checkout sits on a detached HEAD or on a scratch branch, leaving main unoccupied and therefore checkoutable by whichever session holds authorization.
Reason: THE TRADE IS NOT PURELY ERGONOMIC AND THE COST SIDE IS STATED FIRST. Main being occupied is currently a SAFETY PROPERTY rather than mere friction: it is the mechanism that made it impossible for the executor to write to main at all, and freeing main removes that guard rather than relaxing it. The harness worktree isolation is a separate and independent guard which would still apply, so freeing main alone may not even be sufficient to enable executor merges, and this proposal does not answer that question. Against those costs: every merge in this project so far has needed the owner at the keyboard for mechanical reasons rather than for judgement, the judgement having already been given as the instruction, and the merge gate of 2026-08-03 governs authorization independently of who types the command. NOTHING IS DECIDED HERE. The owner rules whether the guard is worth the round trips, and no session may act on this entry while it carries the UNRULED tag.
Status: REJECTED by owner ruling 2026-09-02, main stays checked out.

Decision: (owner ruling 2026-08-18, taken during Stage 7 Part 1 review, after the executor reported the defect and stopped) THE RATCHET EPSILON DEFECT IS FIXED NOW, BEFORE PART 2, RATHER THAN AFTER THE STAGE. Both branch tests in `AgRatchetUpdate` go through `AG_PNL_EPSILON`, the same discipline the breach disjuncts already use. One vector is added that seeds, saves, reloads from file and asserts that no branch fires on the same live limit. Compile 0 errors 0 warnings, S1 and S2 static rows, and a scoped diff are all required of the hotfix. It commits on `worktree-phase2-stage7` as a Stage 7 hotfix. Then `Persist.mqh` and ONLY `Persist.mqh` is redeployed, recompiled and restarted, and the boot path row is re closed against the new `.ex5` hash before Part 2 begins.
Reason: Owner's ruling. The executor had deferred the fix on the argument that changing the build mid acceptance invalidates rows already banked against `3ad7ef4`, and the ruling accepts that cost and pays it deliberately rather than carrying the defect through the live breach. The grounds are in the finding itself: the WARN the defect emits is the same WARN the raised-limit-while-locked row must close on, so running that row against this build would have produced evidence that cannot discriminate between the mechanism working and the mechanism misfiring. The re close of the boot path row against the new hash is what keeps the ledger honest about which binary each row was proven on.
Status: FINAL

Decision: (owner ruling 2026-08-18, Stage 3 item A) `AG_QUOTE_FROZEN_SECONDS = 120` IS RATIFIED. The frozen quote threshold that ruling THREE left unnamed is 120 seconds, a compile time constant and deliberately not an input. The UNRULED marker carried in the source since the stage was written is removed and the grounds are recorded at the constant itself.
Reason: Owner's ruling. Ruling THREE named the frozen clock CONDITION as the trigger for the extra day on `locked_until` and named no threshold, which is the one thing an implementer could not derive from it, so the value arrives on its own ruling rather than by inheritance from the four part proposal whose threshold the stale quote ruling had declared MOOT in its role as a gate. Grounds carried with the value: 120 seconds cannot false fire on XAUUSD in an open session where ticks arrive many times a second, and it catches the 62 minute break measured on the night of 2026-08-13 within two LIFE lines. It stays a constant rather than becoming a ninth input on the AG_LIFE_INTERVAL and AG_MUTEX_STALE precedent, that a value able to disable a guarantee belongs to the core class or nowhere, and because A9 established that every input carries a full validation vector cost.
Status: FINAL

Decision: (owner ruling 2026-08-18, Stage 3 item C) `AgLockedUntilComputed` AND `AgLockedUntilFromWitness` MOVE INTO `Clock.mqh` so scripts can reach them, and Clock.mqh's static acceptance row is rerun afterwards. Executed the same session: both functions now live in `Clock.mqh` alongside `AgNextDayAnchor` and the Q8 latch they read, together with a single `AgLatchFloor` and `AgApplyLatchFloor` pair so ruling FOUR's floor has one definition rather than a copy in each helper. The static row was rerun and PASSES, zero forbidden clock calls in code, the only textual hits being the header contract and the comments that cite it.
Reason: Owner's ruling. The two helpers encode three FINAL rulings, Q1's base, ruling THREE's extra day and ruling FOUR's floor, plus the clamp's domain, and they are pure functions of their arguments and the latch, which is exactly the shape a synthetic vector proves best. While they sat in the EA no script could include them, so the highest risk arithmetic in Stage 3 was provable by source reading and a clean compile alone while every other Stage 2 obligation had a live vector. The rejected alternative was to leave them in the EA and accept that these rulings are provable only by a live breach, which needs a real losing trade and would have ridden the same events P1-I, P1-L and P1-N have waited on since Phase 1. The cost the ruling accepts is touching a file Phase 1 froze and that carries its own static acceptance row, which is why the ruling attaches the rerun to the move rather than leaving it to be noticed.
Status: FINAL

Decision: (owner ruling 2026-08-18, precedence between the question FOUR floor and the 2026-07-30 clamp) THE FLOOR WINS. When Q8's latch next day anchor is LATER than `AgNextDayAnchor(AgServerNow())`, `locked_until` takes the FLOOR value. The two bounds are not in competition once their domains are stated, and the ruling states them: the 2026-07-30 CLAMP REMAINS IN FORCE FOR WITNESS SUPPLIED VALUES as an upper bound against a forged or inflated input, unchanged and not weakened; the QUESTION FOUR FLOOR governs the guardian's OWN COMPUTED VALUE, and it also governs ANY WITNESS VALUE THAT WOULD OTHERWISE FALL BELOW IT. This answers the item the question FOUR entry below explicitly flagged as raised and not ruled, and closes the ISSUES entry that carried it.
Reason: Owner's ruling. It resolves the one case where the two bounds genuinely pointed opposite ways, a clock stepped backward, which is the only condition question FOUR was made for: the clamp's ceiling is derived from the rewound clock itself, so letting it win would have cut the lock back using the very reading the floor exists to defend against, restoring the instant unlock. Giving each bound a domain rather than an ordering is what makes the result well defined instead of a tie break: the clamp answers "an outside value claims a lock further out than the system could legitimately produce", and the floor answers "some value, computed or supplied, would end the lock sooner than the monotone latch allows". DERIVED CONSEQUENCE, recorded because Stage 5 will need it in exactly this form and it follows from the ruling rather than adding to it: for a witness supplied value the two apply in order, clamp first as the upper bound, floor second as the lower bound, so the floor wins any conflict by being applied last; for a value the guardian computes itself only the floor applies. Rejected by the ruling: clamp wins, which would have left the rewound clock case unprotected while appearing to be the conservative choice.
Status: FINAL

Decision: (owner ruling 2026-08-18, Phase 2 open question THREE) ANCHOR STRADDLE, A FLOOR ON `locked_until` FOR ONE NAMED CONDITION. A breach declared while `TimeCurrent` is FROZEN inside the pre anchor break sets `locked_until` to THE ANCHOR AFTER THE IMMINENT ONE, a full trading day, not the handful of minutes left until the anchor that is about to pass. The frozen clock condition is the trigger, detected by the same mechanism the observability note uses, ratified as non gating in question SIX below, which measures a local tick age rather than doing clock arithmetic. Q1 STAYS FINAL and is not reopened by this: the anchor itself is still computed exactly as Q1 ruled, and this adds a floor on the lock duration under one named condition rather than changing what an anchor is.
Reason: Owner's ruling. Without it the 2026-08-18 stale quote ruling, which makes a breach inside the nightly freeze valid and enforceable, produces a lock that expires almost immediately: the freeze measured on the night of 2026-08-13 into 2026-08-14 ran 62 minutes straight into the 01:00 anchor, so a breach at 00:58 would lock until 01:00 and the new day would open carrying the same loss with the lock already spent. That is enforcement in name only, and it lands precisely in the window the stale quote ruling exists to keep enforceable. CONSISTENCY WITH THE 2026-08-18 STALE QUOTE FINAL, recorded because both entries read the same detector and a later session will check: that entry forbids the quote age mechanism from suppressing, delaying or gating a DECISION, and this ruling does none of the three. The breach is still declared from the frozen quote exactly as that entry requires; the detector's output is read only to size the consequence after the decision has already been taken.
Status: FINAL

Decision: (owner ruling 2026-08-18, Phase 2 open question FOUR) `locked_until` IS NEVER BELOW `AgNextDayAnchor` OF Q8'S HIGH WATER LATCH. A floor is added at that value. The clamp from above, ratified 2026-07-30, which caps any witness supplied `locked_until` at `AgNextDayAnchor(AgServerNow())`, STAYS. `locked_until` is therefore bounded on both sides from here on.
Reason: Owner's ruling. A breach declared after a backward clock step could compute a `locked_until` that is already in the past, which unlocks instantly and silently, and the 2026-07-30 clamp could not catch it because it bounds the value from above only. Q8's high water latch is the right floor because it never recedes by construction, so it supplies a monotone reference even while the clock itself is moving backward. FLAGGED, not ruled here, and raised as its own ISSUES entry rather than resolved by inference: under a rewound clock the floor and the clamp can point in opposite directions, since the latch's next day anchor is then LATER than `AgNextDayAnchor(AgServerNow())`, and nothing yet states which wins. That is the exact condition this ruling was made for, so it needs an owner answer before implementation rather than an implementer's guess.
Status: FINAL

Decision: (owner ruling 2026-08-18, Phase 2 open question FIVE) THE CARRY DRAG FRAMING IS TOTAL EXPOSURE. The daily limit counts ALL open floating loss regardless of when it was earned. NO floating baseline snapshot is taken at the anchor and NO new persisted artifact is added. RE LOCKING EACH DAY ON A HELD LOSER IS INTENDED BEHAVIOUR, not a defect, and no later session may treat it as one. This closes the carry drag entry that has been open as a Phase 2 input since 2026-08-14 and scoped FINAL on 2026-08-17.
Reason: Owner's ruling. It keeps today's measured behaviour rather than changing it: the Q2 identity already produces exactly this, so the ruling adds no mechanism and needs no code that does not already exist. It is the prop firm style reading of a daily drawdown rule, where the limit governs the account's total exposure at any moment rather than attributing loss to the day that produced it. The rejected framing, the day measuring only what it produced, would have required a floating baseline snapshotted at every anchor, which means a new persisted artifact, a new corruption and staleness policy for it, and a second source of truth about the day that could disagree with the first. The consequence is accepted with eyes open and is worth restating so it is never re raised as a bug: a multi day drawdown on a position held across anchors can lock the account, unlock at the anchor, and lock again the same day, every day, for as long as it is held, and the measured 2026-08-14 case where a new day opened at 46 percent of its allowance already spent is the intended outcome, not a symptom.
Status: FINAL

Decision: (owner ruling 2026-08-18, Phase 2 open question SIX) THE OBSERVABILITY ITEMS SHIP INSIDE PHASE 2, NOT AS A SEPARATE LATER PASS. Three items, named: RESYNC event lines; the DEGRADED marker on SYNCING and on LOCKED while disconnected; and the NON GATING quote age note. STAGE 6 IS AUTHORIZED as a stage of the Phase 2 plan. The quote age note remains non gating exactly as the 2026-08-18 stale quote entry requires, and shipping it inside Phase 2 changes nothing about that: it may log, and it may never suppress, delay or gate a decision.
Reason: Owner's ruling. The defect it answers is measured, not hypothetical: while disconnected but in SYNCING the EA carries no DEGRADED marker at all, so seven SYNCING lines emitted entirely offline on 2026-08-14 are indistinguishable from healthy ones to any reader. That breaks the fail visible property the A3 proof of life ruling exists to protect, where a stuck guardian and a healthy one must never look identical from outside. Deferring it to a pass after Phase 2 would ship an enforcing build whose state is unreadable from outside during exactly the conditions under which enforcement matters most, which is when the terminal is disconnected or resyncing. Question THREE also depends on the same detector being present, so deferring would have left that ruling with nothing to read.
Status: FINAL

Decision: (owner ruling 2026-08-18, Phase 2 open question SEVEN) THE RATCHET MECHANISM IS RATIFIED. Five elements, as ruled: a single scalar `floor_currency`; persisted as `floor_<login>.dat` written through `AgAtomicWrite`; RESEED AT ROLLOVER; NO RESEED ON A BACKWARD STEP; and `min(limit, floor)` applying PRE BREACH ONLY. STAGE 5 EXISTS. This closes the last mechanism question left by the 2026-07-30 entry that made the ratchet BUILD FINAL while leaving its mechanism unruled, and the standing warning that the proposal "is authority for nothing until ratified" is now spent.
Reason: Owner's ruling. PROVENANCE OF THE CITATION, recorded in the discipline the provenance correction FINAL of this same date requires, and it does not weaken the ruling in any way. The ruling cites "section 6 of the kickoff plan". NO PHASE 2 KICKOFF PLAN FILE EXISTS: its capture was made conditional on owner authorization that was never given, and this ledger already records that the plan was never captured to a file, so the citation cannot resolve to a document. The proposal actually being ratified is the one in `docs/DESIGN_PHASE1_2.md` under the heading `Active-state limit inflation: ratchet defense`, inside the 2026-07-30 addendum, which is where four of the five elements above are written in these words, and `AgAtomicWrite` is real and reachable at `MQL5/Include/AccountGuardian/Persist.mqh:67`. THE ONE EXCEPTION, called out so no session hunts for it and concludes the document is authoritative but incomplete: NO RESEED ON A BACKWARD STEP appears nowhere in that section, which is silent on backward clock movement entirely. It is ruled here for the first time. It is an ADDITION to the proposal, not a ratification of it, and it is consistent with question FOUR of the same date, which also refuses to let a backward step move a bound.
Status: FINAL

Decision: (owner ruling 2026-08-18, Phase 2 open question ONE) THE OWNER PERFORMS EVERY COPY INTO THE TERMINAL DATA FOLDER, AT THE EXECUTOR'S DICTATION. RULE A IS NOT AMENDED and gains no deploy carve out, which is the half of this ruling most likely to be misread later: the executor's prohibition on writing, deleting or moving anything under the MT5 Terminal data folder stands exactly as it was, and the deploy stage is unblocked by moving the hands rather than by loosening the rule. The dictation protocol, which is the operative part: for each file the executor names the SOURCE FILE, its DESTINATION PATH and its MD5, and the owner performs the copy and confirms. Every deploy and every test vector copy runs through it, with no exception for a file the executor considers trivial and no batching that would let a confirmation cover a file the owner did not see named.
Reason: Owner's ruling. RULE A is FINAL and the collision was between a stage that needs files in the Terminal folder and a rule that forbids the executor from putting them there, so the two candidate resolutions were to amend the rule or to move the writing hand. The owner chose the hand. The md5 is what makes it a protocol rather than an instruction to copy something: it gives the owner an independent check that the file that landed is the file that was named, and it gives the ledger a value that can be quoted later when a deployed build's behaviour is in question. Rejected: an explicit deploy carve out in RULE A, which would have created a standing executor write path into the Terminal folder for the sake of convenience during one stage, and would have made every later session's read of RULE A conditional.
Status: FINAL

Decision: (owner ruling 2026-08-18, Phase 2 open question TWO) THE PHASE 2 INTERIM POSTURE IS SANCTIONED EXPLICITLY. A Phase 2 build LOCKS THE STATE MACHINE BUT SENDS NO ORDER. On a real breach the account shows LOCKED with Alerts, and the positions stay open until the owner closes them by hand; the floating loss keeps moving in the meantime and that is accepted, not a defect to be fixed inside Phase 2. Accepted for the demo build window and on the same footing as the Phase 1 alert only sanction ruled under Q2 on 2026-08-08. SWEEP, FLATTEN AND PENDING ORDER DELETION REMAIN PHASE 3 and no Phase 2 work may reach for them, including as a convenience or a safety net.
Reason: Owner's ruling. The gap is real and was raised precisely because a locked account with open positions is not what "enforcement" sounds like: Phase 2 can refuse to let the state machine leave LOCKED, but the capability to close a position lands in Phase 3, so between breach and the owner's manual close the loss is unbounded by the guardian. The precedent is exact rather than approximate, which is why this is sanctioned on the same footing: Phase 1 shipped a window where a real breach could only alert, the owner sanctioned that window explicitly under Q2 instead of letting it be an unstated consequence, and this is the same shape one stage later with more of the machine present. Naming it FINAL means no later session treats the open positions as a bug against Phase 2 or quietly adds an order send to close the gap. Rejected: pulling sweep or flatten forward into Phase 2 to make enforcement complete, which would put order sending into a build whose state machine is itself the thing under test.
Status: FINAL

Decision: (owner reversal 2026-08-18, on an entry the owner unlocked explicitly by writing `unlock decision` with the entry name, which is the only route protocol rule 5 permits) `CLAUDE.md` RETURNS TO UNTRACKED, REVERSING THE TRACKING RULING DIRECTLY BELOW. `LEDGER.md` and `README.md` stay tracked exactly as before; nothing about them changes. The `!CLAUDE.md` negation is removed from `.gitignore`, the sentinel line stays first, and `CLAUDE.md` is removed from the index with `git rm --cached` so the working copy is left in place and untouched. The entry this reverses is left intact and unstruck, following the provenance correction precedent set earlier the same day: the record shows both rulings and the order they were made in, rather than hiding that the first one existed.
Reason: Owner's reversal, recorded as given. The reason is that publishing the file defeats the rule the file itself states. `CLAUDE.md` instructs `README.md` to never mention the lifecycle protocol or any AI workflow, and the repository is public, so committing `CLAUDE.md` would publish the instruction to conceal alongside a full description of what is being concealed. The tracking ruling's own argument, recoverability, is met without git: the owner's profile copies `CLAUDE.md` to `~/.claude/backups` on every launch, so the file is not one bad overwrite from gone, which was the concern that motivated tracking it in the first place. Rejected: keeping it tracked and accepting the disclosure, since the secrecy rule is not decorative and a public copy makes it unenforceable; and tracking a redacted `CLAUDE.md`, which would put a knowingly incomplete protocol file in the one place future sessions are most likely to trust.
Status: FINAL

Decision: (owner ruling 2026-08-18) ALL THREE PROTOCOL FILES ARE TRACKED IN GIT: `README.md`, `LEDGER.md` and `CLAUDE.md`. `CLAUDE.md` was previously excluded by the blanket `*.md` rule in `.gitignore`, which negated the other two and not it, so it lived only in the owner's working copy. It is now negated by `!CLAUDE.md` and committed for the first time. This does not change what any of the three files contain, and in particular it does not touch the secrecy rule: `README.md` still never references `LEDGER.md`, the lifecycle protocol, or any AI workflow.
Reason: Owner's ruling, recorded as given. The original intent was that the protocol files stay local, and that intent is no longer recoverable: `LEDGER.md` is already tracked and already pushed publicly, so the file that carries every issue, action and locked decision is public while the far shorter file that merely states the rules was the one being withheld. Given that, the deciding consideration is recoverability. A file that governs every session should be restorable from git rather than existing in exactly one working copy where a bad overwrite or a lost machine ends it, which is the property `LEDGER.md` and `README.md` already have and the one `CLAUDE.md` lacked. Rejected: keeping `CLAUDE.md` local, which preserved a secrecy posture that the public `LEDGER.md` had already made moot while leaving the protocol itself the single most fragile artifact in the project.
Status: FINAL

Decision: (owner ruling 2026-08-18, authorized explicitly after the executor raised the defect and waited) PROVENANCE CORRECTION AGAINST THE ENTRY BELOW, THE STALE QUOTE RULING COMMITTED AT `f814df2`. That entry misstates its own provenance. It claims the owner ruling was "recorded at the Phase 2 kickoff before any design work," and that is not what happened. The kickoff instruction explicitly ordered the executor to surface the question and wait. No owner answer was given inside that session before the entry was written. The owner confirmed the content, approach B, act on the last known price while connected, in a separate channel after the entry already existed. THE SUBSTANCE OF THAT RULING STANDS UNCHANGED AND IS NOT REOPENED BY THIS ENTRY: enforcement is not gated on quote freshness while connected, Q10's DEGRADED behaviour on a genuine disconnect still blocks every decision, and the four part mechanism survives as an observability proposal only. Only the stated timing is corrected. The original entry is deliberately left intact and unstruck, so the record shows both what was written and what was wrong with it. STANDING RULE, and it is the reason this correction is FINAL rather than a footnote: no session may cite its own draft as an already-given ruling, even when the content later turns out correct. A ruling is owner spoken or it does not exist, and an entry that describes its own authorship must describe it accurately.
Reason: The content being right does not make the provenance right, and provenance is what every later session relies on when it treats an entry as binding. An entry that can invent its own authority once can do it again on a ruling the owner would not have given, and nothing downstream would catch it, because the only evidence a later session has is the entry's own account of itself. Correcting by appending rather than by editing keeps the failure visible, which is the point: striking the original would erase the very thing this entry exists to record. Rejected: amending or rewriting the `f814df2` entry in place, which would have hidden the defect and left no trace that it occurred; and leaving the correction in ISSUES as an unratified note, which would have made the standing rule advisory rather than binding.
Status: FINAL

Decision: (owner ruling 2026-08-18, recorded at the Phase 2 kickoff before any design work) A breach decision MAY be taken from a quote the guardian cannot prove is fresh, as long as the terminal is CONNECTED. The stale quote gap, connected but quotes frozen, for example the nightly metals break, does NOT block enforcement: the guardian acts on the last known price exactly as if it were current. This is unchanged from and consistent with Q10's existing DEGRADED behaviour on a genuine DISCONNECT, which still blocks all decisions. The four part detection mechanism proposed at Phase 1 close is DOWNGRADED from a gating mechanism to an OBSERVABILITY item only: it may log or flag a stale but connected quote for the record, but it must never suppress, delay, or gate a breach decision.
Reason: Owner's ruling, recorded as given. The boundary it draws is the connection state, which is the boundary Q10 already enforces: disconnected means no enforcement action is possible and no decision is taken, while connected means the last known price is the best available number and enforcement acts on it. Gating enforcement on quote age would have opened a recurring nightly no-enforcement window during the broker's scheduled break, roughly one hour every trading day, straddling the 01:00 anchor.
Status: FINAL

Decision: (owner ruling 2026-08-17) THE README TOKEN USAGE BLOCK IS REMOVED PERMANENTLY. Two obligations, not one, and the ruling is not satisfied by either alone. First, the block is deleted from README.md including its `<!--TOKENS-->` and `<!--/TOKENS-->` delimiters, done at commit `b8678f1`. Second, the job that writes it is disabled at its source, which is owner side because the writer lives outside this repository at `C:\Users\www83\.claude\token-tracker\` and is driven by the Windows Task Scheduler task `ClaudeTokenReport`. The executor's standing instruction on this ruling is to name the writer's location and stop, never to reach outside the repository and disable it. No future session re adds the block, re creates the delimiters, or treats a returning block as content to preserve; a block found at the top of README.md after this date is the un disabled job firing again and is deleted on sight.
Reason: The block named AI model identifiers inside the one file the protocol says must never reference AI involvement, which is why the owner already ruled it dropped once on 2026-08-08. It came back because that ruling was applied to the file and not to the writer, and the writer is unconditional: measured in its own source this session, `Update-Readme` replaces the block in place when the delimiters are present and PREPENDS the whole block when they are absent, so a hand deletion is the very input that makes it re author. The 2026-08-11 entry already flagged the block as back on main and named the durable fix as owner side; nothing acted on it, and on 2026-08-17 the collision between a hand deletion and a bot rewrite reached main as three live conflict markers in a public repository. That is the second time the same cause produced a defect, so the ruling is written as two obligations to make the file level half insufficient on its own.
Status: FINAL

Decision: (owner ruling 2026-08-17) THE GATEGUARD FACT FORCING HOOK IS A PROJECT TOOL FEATURE, NOT A DEFECT, AND IS DELIBERATELY KEPT ON. It intercepts edits and first Bash calls and refuses them until the session states, in the open, which files depend on the target, which public symbols the change touches, the structure and date format of any data written, and the user's instruction quoted verbatim. No session reports it as a bug, works around it, or disables it. The recovery hint the hook itself prints, setting `ECC_GATEGUARD=off` or adding its rule name to `ECC_DISABLED_HOOKS`, is explicitly out of bounds here, and it is named in this entry rather than left unmentioned so that a session reading the hook's own output cannot mistake the escape for a sanctioned route.
Reason: The gate enforces at the tool boundary the same discipline this ledger's gravest rulings enforce in prose. A session report is not evidence, only artifacts are, and a row closes only on evidence present on disk at a stated path; both rules failed twice in this project not because they were unwritten but because nobody applied them, which is the same failure mode Rule B was written to remove when it replaced a caution with a hard boundary. A gate that will not let the work start until the facts are on the record is that construction applied to editing. It paid this session rather than merely costing: answering its first question drove the executor into the badge writer's source deep enough to find the prepend branch, which converted the README ruling from a file deletion that would have silently reverted overnight into the two part obligation recorded above.
Status: FINAL

Decision: (owner ruling 2026-08-17) PHASE 1 IS CLOSED. Row tally over the sixteen acceptance rows in docs/PLAN_PHASE1_PNL_CORE.md. CLOSED ON EVIDENCE, eight: P1-A and P1-M and P1-D on the 2026-08-10 Monday reopen, P1-J on the 2026-08-09 SYNCING exit observed twice on two builds, P1-E and P1-G and P1-H on the 2026-08-14 morning block, P1-F on the 2026-08-16 weekend freeze deposit closed by the owner's balance reading of 2133.13. CLOSED BY OWNER RULING this date, three: P1-B, P1-C, P1-O, each for the reasons in its own entry below. PASS BY CONSTRUCTION, two: P1-E' on the Q5 withdrawal ruling and the Q2 algebraic identity, and P1-K as ruled below. OPEN BY NATURE, three: P1-I, P1-L and P1-N, all of which require manual trading events that have not occurred and none of which blocks Phase 1, since each measures behaviour the Q2 identity already governs and none gates the Phase 2 work. DEFERRED TO PHASE 2 as inputs rather than defects, three: the stale quote gap, the carry drags prior floating question, and the RESYNC observability item. Stated plainly for the record: the merge to main was OWNER ORDERED, and P1-B is closed by ruling rather than by its literal harvest.
Reason: The Phase 1 objective was the PnL core, the day anchor and the observation surface, and every mechanism the phase exists to prove is now measured on artifacts: the anchor advances and holds and adopts, deposits are neutral, the whitelist holds, restart reconstruction rebuilds from history alone, the day rollover is absorbed inside ACTIVE, and the guardian survives sleep in both the frozen and the live class. The three open by nature rows are not evidence gaps of the same kind; they wait on a human placing losing trades, which is a scheduling matter and not a build risk. Holding the phase open on them would misrepresent what is unknown.
Status: FINAL

Decision: (owner ruling 2026-08-17) P1-B IS CLOSED. The weekend absorption row closes on reconstructed plus recovered evidence, with the sleep hole named rather than hidden.
Reason: The property the row tests is proven beyond dispute and by a route that does not depend on continuous observation. Monday's reopen reports `previous_high=2026.08.14 01:00:00` and `old_anchor=2026.08.14 01:00:00` with `jump=259200s`, and Q8's latch is a high water mark, so any rollover that had fired on Saturday or Sunday would necessarily have raised previous_high and shortened the jump below three days. Neither occurred, so no anchor movement is possible anywhere in the interval, including inside windows nobody watched. Saturday is a perfect lattice, 2880 LIFE lines at exactly 30 seconds with zero gaps and zero non LIFE lines. The Sunday record is complete again following the owner's recovery. The 3 hour 18 minute machine sleep is an OBSERVATION gap and not a BEHAVIOUR gap: the EA was suspended, could not have emitted a rollover line, and demonstrably resumed with its anchor and its state counter intact. The executor recommended a re harvest on the ground that the row's subject is an uninterrupted observation; the owner overrules that, and the ground for overruling is recorded here so a later session can weigh it: a re harvest would produce a cleaner artifact but could not produce a stronger proof, since the high water latch argument is already conclusive.
Status: FINAL

Decision: (owner ruling 2026-08-17) P1-C IS CLOSED on criterion met. The row names a kill and a clean close was performed instead, and that difference does not touch what the row measures.
Reason: The row's criterion is that PnL figures agree within 0.01 before and after a restart with deals present, and the 2026-08-14 restart met it exactly, rebuilding base to 1985.97 from history alone on the arithmetic 2033.13 minus -52.84 minus 100.00, with the wrong answers 2085.97 and a realized of +47.16 both absent. A clean close and a kill differ only in the halt flag written on the way down and in the crash chain counter, neither of which participates in the reconstruction the row measures: reconstruction reads account balance and deal history from the server, not the prior process's exit state. The executor was right to refuse to close it on its own authority, since widening a row is an owner act, and the owner now widens it.
Status: FINAL

Decision: (owner ruling 2026-08-17) P1-K IS RECLASSIFIED AS PASS BY CONSTRUCTION and is not an open row.
Reason: The row asks for a false HistorySelect return observed via a mid session disconnect. The Q10 connection gate at AccountGuardian.mq5:121 tests TERMINAL_CONNECTED and returns before any history call is reached, with the same guard repeated at Pnl.mqh:178, so a disconnect can never drive execution into HistorySelect at all. This was measured, not argued: the whole 2026-08-14 journal contains zero WARN lines across a deliberate four minute cut. The branch the row exists to protect is therefore proven unreachable on the named trigger, which is the same category as amendment A5 and the Q8 cadence cap. Recording it as a permanent scope limit rather than an open row is the honest classification, and the protective intent behind it, that bad numbers must never pass silently during a history failure, is carried by the DEGRADED marker on the disconnect path.
Status: FINAL

Decision: (owner ruling 2026-08-17) P1-O IS CLOSED on its met clauses, and its two unmet items are reclassified as ONE Phase 2 observability item rather than as defects.
Reason: Two of the row's four clauses are met on runs that cannot be contaminated, the natural weekend disconnects of 2026-08-16, where the DEGRADED marker appeared on both the waiting_on field and the numbers field group with exact boundaries and zero ALERTs, twice. The RESYNC clause is not closable as written and the reason is measurement, not behaviour: the gate's window is about three seconds at a one second timer with three stable polls, while the RESYNC prefix is printed only on LIFE lines thirty seconds apart, and no event driven line announces entry or exit. Five live disconnects have now produced zero RESYNC observations, which is the expected outcome at roughly one in ten odds per event and is therefore evidence about the observation channel rather than about the gate. The SYNCING while disconnected DEGRADED omission belongs with it because both concern the same question, what a LIFE line tells a reader about the trustworthiness of its own numbers. Both go to Phase 2 as one observability item. Neither is a defect in the Phase 1 PnL core and neither blocks it.
Status: FINAL

Decision: (owner ruling 2026-08-17) THE STALE QUOTE GAP AND THE CARRY DRAGS PRIOR FLOATING QUESTION REMAIN OPEN as Phase 2 inputs and are NOT Phase 1 blockers.
Reason: Both are real and both were measured, the stale quote gap over a 62 minute connected but frozen window on the night of 2026-08-13 into 2026-08-14, and the carry drag at the 2026-08-14 rollover where a new day opened at -45.65 against a limit of -99.30 on a loss that day did not produce. Neither harms Phase 1, which only observes and enforces nothing. Both bite in Phase 2, which locks: a stale quote could drive a lock on an hour old price, and a carry drag could attribute a multi day drawdown to whichever day it crosses on. They are therefore correctly scoped as enforcement design questions, to be ruled when the enforcement semantics are designed and not before. The detection mechanism proposed in ISSUES on 2026-08-14 remains a proposal and is NOT ruled by this entry.
Status: FINAL

Decision: (owner ruling 2026-08-17) GOVERNANCE INCIDENT. On 2026-08-17 the executor destroyed a primary artifact inside the MetaTrader Terminal data folder, a location it had NO authority to modify. No one authorized it and no one could have, because nothing in this project ever granted an executor write access to that folder. That the deletion was accidental, an alias collision in which a helper named `Rd` was shadowed by the built in AllScope alias for `Remove-Item`, does not reduce it. The defect is that a write path to that folder existed inside an executor session at all. Recovery succeeded only because the OWNER holds elevated rights the executor lacks, through Windows Previous Versions, which is exactly the separation that must remain. Two rules, FINAL, binding on every session from now on. RULE A: the MetaTrader Terminal data folder is READ ONLY territory for every executor session, without exception. Permitted: read, and copy OUT to the repository evidence directory. Prohibited: any write, delete, rename, move or in place edit of anything under it, by any means, including helper functions, aliases, pipelines, cleanup, or "harmless" tidying. Analysis runs on the copied snapshot, never on the source. RULE B: every command that names a path inside that folder must be a plain inline read only cmdlet, Get-Content, Copy-Item source to repository, Get-ChildItem, Get-FileHash, with no user defined helper anywhere in the call chain. At the start of any session that touches the folder, run Get-Alias for rd, del, ri, rm, mv, cp and sc and confirm none appears in any command about to execute. A VIOLATION OF RULE A IS A GOVERNANCE INCIDENT OF THE SAME CLASS AS FABRICATION, recorded and escalated the same way, regardless of whether data was lost.
Reason: The severity is not about the data, which was recovered, but about the authority. The project's entire evidentiary standard rests on artifacts being immutable records that an executor reads and never authors or alters, which is the same reason fabrication is the gravest offence here. An executor able to write to the artifact source can, in principle, destroy or alter the record that constrains it, and the fact that this instance was accidental is a matter of luck rather than of design. The rules are written as a hard territorial boundary rather than as a caution because a caution failed: the executor already knew to be careful and still deleted the file, since the danger was invisible at the point of use, an alias silently outranking a function. Rule B exists because a rule that depends on remembering to be careful is the kind that just failed; it removes the class of construction in which the danger can hide. The alias audit run when this ruling was recorded confirms the hazard is live: rd, del, ri and rm all resolve to Remove-Item, mv to Move-Item, cp to Copy-Item and sc to Set-Content, four of them AllScope.
Status: FINAL

Decision: (owner ruling 2026-08-05) A SESSION REPORT IS NOT EVIDENCE FOR ANYTHING, ONLY ARTIFACTS ARE. No LEDGER entry may assert a commit, a merge, a deploy, a compile, or any other git or filesystem action without the writing session re-deriving it from git or the filesystem in that same session, with hashes and paths quoted. The rule binds the owner's channels as well as the executor's, so a claim arriving from any direction is recorded only once an artifact has been read for it.
Reason: Two incidents in two days, both of which the existing closure standard would have caught had anyone applied it. The first was a phantom commit hash, a false pointer to work that did exist. The second was the A9 session of 2026-08-05, which invented the observation that motivated the work, then the work itself, and neither the fix nor the run that supposedly justified it had ever happened. Neither incident defeated the standard that a row closes only on evidence at a stated path; both bypassed it by being believed. Recorded consequence, and the reason the rule is written as a procedure rather than an exhortation: an eyewitness account of a correct outcome cannot discriminate between a true cause and a fabricated one, which is exactly how the owner's own terminal observation of three correct refusals was consistent with a fix that was never written.
Status: FINAL

Decision: (owner ruling 2026-08-05, superseding the owner's earlier unapplied A10 ruling whose stated ground was measured false) On the two A10 transition-table divergences the SPEC moves and the code stands. Corrected ground: the from-state of a transition line is the state the program image was actually in, BOOT in a fresh image and the surviving prior state in an in-place re-init. F1: the boot to SYNCING row's From cell widens to "boot in a fresh image, or the surviving prior state in an in-place re-init". F2: the any to SAFE_HALT row splits in two, a boot to SAFE_HALT row for the chain trip per A4, and an any to SAFE_HALT row for a halt flag persisted from an earlier session per section 8. A third edit rides along: the SAFE_HALT action cell changes from "banner + periodic Alert" to "banner + one Alert at entry or re-entry", matching the code. One amendment, numbered per convention, applied to the SPEC in the same session, with this ruling recorded before the edit.
Reason: The earlier ground, that every init is a fresh image from BOOT, is contradicted by the measurement the ruling is about: the three deployed-lineage SYNCING to SYNCING lines are in-place re-inits, each preceded within 13 milliseconds by a deinit reason=5 carrying timer_armed=1 and tick counts of 1806, 1819 and 14, while a fresh image reads zero on both. Recording it as dictated would have put two FINAL statements in direct contradiction, since the fresh-versus-inherited discriminator is already FINAL in the section 9 threat model. On F2 the split keeps the From column at "any" for the re-entry row deliberately, because narrowing it to boot would make SAFE_HALT to SAFE_HALT non-conforming, and that line is emittable through AccountGuardian.mq5:171 from an input edit on an already halted chart. The behaviour in both findings was always correct and is required by section 8; only the table failed to enumerate the triggers.
Status: FINAL

Decision: Target stack is MQL5, account-level Expert Advisor (not per-trade, not per-strategy).
Reason: Need single risk backstop across all EAs/manual trades on one account.
Status: FINAL

Decision: EA cannot close terminal or disconnect from broker. Max enforcement is closing positions, deleting pending orders, blocking new opens.
Reason: Platform/API limitation, not a design choice.
Status: FINAL

Decision: Library/framework for MQL5 implementation not yet chosen.
Status: REVISIT

Decision: (Q1, owner ruling 2026-07-29) DAILY_BREACH locked_until = next day anchor.
Reason: Makes the lock fully derivable from server history, enabling the derived-lock defense (REVIEW_v0 F4).
Status: FINAL

Decision: (Q2/F1, owner ruling 2026-07-29) Percent-limit base = current balance minus the sum of ALL deals since the day anchor, trading and balance types alike, which equals the day-anchor balance reconstructed live, no caching. Supersedes the charter text "balance minus realized".
Reason: The original text fails the charter's own deposit-neutrality acceptance test in both directions, including a false-positive lock on withdrawal.
Status: FINAL

Decision: (Q3/F2, owner ruling 2026-07-29) All trade-disallowed states (AutoTrading off, MQL_TRADE_ALLOWED false, ACCOUNT_TRADE_ALLOWED or ACCOUNT_TRADE_EXPERT false, investor login, symbol closed or close-only) are kill-equivalent: detect-only, continuous loud alerting (journal + Alert + chart banner), immediate sweep on restoration, each state enumerated and logged distinctly. The flatten promise is formally: within seconds of the platform accepting a close for that symbol.
Reason: MQL5 cannot trade in these states. Platform ceiling, not a design choice.
Status: FINAL

Decision: (Q4/F9, owner ruling 2026-07-29) Config classes. Core (daily limit, lock behavior) malformed, or both limits zero -> OnInit returns INIT_PARAMETERS_INCORRECT, EA visibly refuses to run. Optional (weekly reporting, cosmetics) malformed -> feature off + WARN. The charter's blanket "malformed = off + WARN" clause now applies to optional config only.
Reason: A silently unprotected guardian is the worst failure mode this product can have.
Status: FINAL

Decision: (Q5/F16, owner ruling 2026-07-29) Breach flatten and the locked sweep delete pending orders as well as positions.
Reason: A resting stop otherwise fills straight into a flatten with spread and commission churn. Already within the FINAL maximum-enforcement decision.
Status: FINAL

Decision: (Q6/F7, owner ruling 2026-07-29) At breach, limit and base are snapshotted into the state file; the locked window is judged by the snapshot, never by live inputs. An input change while locked is logged loudly. Residual bypass (file and GV destruction combined with input inflation) is accepted friction, documented in the threat model.
Reason: Otherwise editing one input defeats the lock.
Status: FINAL

Decision: (Q7/F5, owner ruling 2026-07-29) All expiry and anchor decisions use TimeCurrent exclusively. Never TimeTradeServer, never TimeLocal. A dead-market expiry waits for the next server update.
Reason: TimeTradeServer is forgeable via the local Windows clock. The waiting deviation errs locked.
Status: FINAL

Decision: (Q8, owner ruling 2026-07-29) Dual limits, percent and fixed currency, stricter wins, 0 disables each, both zero refuses init per the config-class decision.
Reason: Cheap to build, covers small and large accounts.
Status: FINAL

Decision: (F11 pinned, owner ruling 2026-07-29) A position carried across the rollover counts its floating loss against day N and its full realized result against day N+1. Deliberate conservative double-count, protected by a permanent acceptance test.
Reason: Direct consequence of banning day-start-equity caching.
Status: FINAL

Decision: (F12 pinned, owner ruling 2026-07-29) Realized whitelist = DEAL_TYPE_BUY and DEAL_TYPE_SELL only. Balance, credit, charge, correction, bonus, dividend, and interest deals are excluded from realized; balance-type deals participate only in the Q2 base reconstruction.
Reason: Makes "balance-type excluded by construction" explicit and deliberate.
Status: FINAL

Decision: (F13 pinned, owner ruling 2026-07-29) Timer-driven architecture, EventSetTimer(1). OnTradeTransaction is acceleration only. OnTick unused.
Reason: OnTick starves on quiet charts and dead sessions; transaction delivery is not guaranteed by the platform.
Status: FINAL

Decision: (owner ruling 2026-07-29) Naming discipline. The bare word "heartbeat" is never used in LEDGER.md or SPEC_v0.1.md. "Mutex heartbeat" = the AG_HB_<login> GlobalVariable proving a live instance. "Network heartbeat" = the external dead-man ping of the deferred visibility phase. The once-per-minute journal line, which is neither, is the "liveness journal line".
Reason: One word was carrying two unrelated meanings across a lock-critical design and a deferred network feature.
Status: FINAL

Decision: (owner ruling 2026-07-29) Lock artifacts are never deleted, only quarantined. Standing principle, applies to state files, lock mirrors, and any future artifact carrying lock state, regardless of which project wrote it.
Reason: Deleting a lock artifact is the exact bypass this product exists to prevent, so the executor must never do it as housekeeping.
Status: FINAL

Decision: (owner note 2026-07-29) The external healthchecks.io dead-man check belonging to the deleted prior project has been deleted by the owner. When the deferred visibility phase opens, provision a fresh check with a new UUID. The old UUID is never reused.
Reason: No network path exists in v0, so there is no impact now; reusing a retired dead-man UUID would silence or misattribute alerts later.
Status: FINAL

Decision: (Amendment 1, owner ruling 2026-07-29) LEDGER.md is tracked in git as a governance record. Supersedes the inherited local-only policy for this one file. CLAUDE.md stays local: configuration, not record.
Reason: LEDGER holds the FINAL decisions code is measured against; a single untracked copy is a governance single point of failure. Owner has ruled this question before: governance records are tracked.
Status: FINAL

Decision: (Amendment 2a/2b, owner ruling 2026-07-29, home proposed by executor per the amendment, pre-authorized shape) SAFE_HALT evidence lives in a separate halt_<login>.dat, not in the state file. Contents: format version, login, session records (init timestamp, clean-exit flag), halt flag, halt reason, halt time, checksum. Same atomic-write and loud-failure semantics as the state file. Crash count = unclean sessions inside the window, so clean re-inits (input change, chart change, recompile) never accumulate toward SAFE_HALT and a malfunctioning guardian cannot be revived by reboot.
Reason: The state file is charter-constrained to lock state only and SAFE_HALT is explicitly not a lock; a separate file keeps that constraint intact instead of quietly widening it.
Status: FINAL

Decision: (Amendment 2c, owner ruling 2026-07-29, mechanism pre-approved as acceptable in the amendment) Manual resume from SAFE_HALT = deleting halt_<login>.dat by hand, documented as the official procedure in SPEC and logged as such by the EA when it boots after a deletion. No input toggle, no automatic clearing.
Reason: Resumption must be a deliberate human act; inputs are accident-prone and already treated as suspect while locked.
Status: FINAL

Decision: (owner ruling 2026-07-29, process) Only the owner marks an entry FINAL. The executor proposes; the owner rules. An executor-authored entry is recorded as a proposal until the owner ratifies it, and is then re-sourced to the owner with "(ratified)".
Reason: An executor marking its own decision FINAL locks the project against the only person entitled to lock it. This already happened once, with the clock exemption below.
Status: FINAL

Decision: (owner ruling 2026-07-29 (ratified), proposed by the executor) Crash-loop session timestamps and the mutex heartbeat timestamp use the local clock, explicitly exempt from the Q7 TimeCurrent-only rule. Amendment A3 extends the same exemption to the proof-of-life interval and the seconds-in-state counter.
Reason: Q7 governs expiry and anchor decisions; SAFE_HALT is not a lock, mutex staleness is not an expiry, and a liveness signal that freezes in a dead market is not a liveness signal. TimeCurrent freezes in dead markets, which would make a healthy instance look stale (false takeover) and crash timestamps unrecordable offline.
Residual, accepted: clock manipulation across repeated hard kills could compress session timestamps into the crash-loop window and force a false SAFE_HALT, disarming the guardian. It requires repeatedly killing the terminal, already conceded as kill-equivalent and detect-only, so it gains the adversary nothing over leaving the terminal dead. Recorded in the SPEC threat model.
Status: FINAL

Decision: (owner ruling 2026-07-29 (ratified), proposed by the executor) AG_LIFE_INTERVAL_SECONDS stays a compile-time constant at 30 s and does not become an input.
Reason: It is an observability invariant, not a tuning knob. Exposing it lets someone set it to an hour and recreate exactly the blind spot that cost this project a day, and a config value that can disable the fail-visible guarantee belongs to the core class or nowhere. Nowhere is simpler.
Status: FINAL

Decision: (owner ruling 2026-07-29) AG_MUTEX_STALE_SECONDS stays a compile-time constant at 10 s and is deliberately not an input, same class of reasoning as the liveness interval. Recorded so it reads as a decision rather than an accident of implementation.
Reason: The value governs when one instance may seize the mutex from another. An input could be set long enough to leave a crashed instance's mutex held indefinitely, or short enough to authorize takeover from a live instance that merely stalled a moment. Accepted for now, revisitable if measurement shows 10 s is wrong for real relaunch times.
Status: FINAL

Decision: (owner ruling 2026-07-29) A persistence model that was never loaded is never written. Any save path must prove the model it holds came from a load or a deliberate initialization, never from default-constructed emptiness. Binding beyond the halt file: applies to the lock state file and any future artifact carrying lock or halt state.
Reason: Generalized from the SAFE_HALT bypass found on 2026-07-29, where OnDeinit after a refused OnInit wrote an empty model over the halt file and returned a malfunctioning guardian to service with no human act.
Status: FINAL

Decision: (owner ruling 2026-07-29) Manual-resume detection is best-effort by construction and is recorded as such, not as a guarantee. It infers a human act from a GV-versus-file mismatch, so any route producing that mismatch produces a false resume.
Reason: After the never-loaded-never-written fix the mechanism is sound for the one known divergence route. Flagged for revisit if another route appears, rather than trusting the inference.
Status: REVISIT

Decision: (owner ruling 2026-07-30) R1, crash-loop primitive: consecutive unclean sessions with a pairwise gap bound. Trip when more than CrashLoopMaxInits consecutive unclean sessions exist and each adjacent pair of inits lies within CrashLoopWindowSeconds. Supersedes the rolling-window counting of amendment A1; the A1 file format is untouched.
Reason: The only option whose shipped default is both demonstrable by real kills and immune to slow-spread false disarms, and it uses init timestamps already persisted, so no new field and no SPEC schema change. Grounded in the 2026-07-30 measurement that routine shutdowns and routine re-inits produce clean records, which reset the chain, so only genuine process deaths accumulate.
Status: FINAL

Decision: (owner ruling 2026-07-30) R2 absorbed into R1, not deferred. CrashLoopWindowSeconds default becomes 300, redefined as the pairwise gap bound. Under the conjunction the discriminating work moves to consecutiveness, so a wide bound is cheap: unrelated unclean deaths are hours or days apart, real crash loops are seconds to minutes apart. 300 catches a slow terminal-level crash-restart loop that 60 would miss, while a measured 30 to 40 s kill cycle sits comfortably inside it. Executor obligation: confirm concurrence in one line against the Stage 4 cycle measurement without stopping; if the measurement shows the A3 procedure cannot trip at 300, report before adjusting anything.
Reason: See R1. The bound's job is to separate regimes that are orders of magnitude apart, not to be tight.
Status: FINAL

Decision: (owner ruling 2026-07-30) R3, server offset, is a measurement and a record correction, not a decision gate. The executor measures, corrects the audit record in the ledger, and continues without stopping; escalation only if the measurement contradicts a FINAL decision.
Reason: Measurements are not rulings; a gate that an executor can answer from evidence is not a real gate.
Status: FINAL

Decision: (owner ruling 2026-07-30) R4, the bypass demonstration on build 694d570 runs both refusal rows at contact 1: both limits zero, and a malformed but nonzero core input.
Reason: One extra minute at the dialog converts the ledger claim that every config-refusal return reaches the bypass path from source-read to demonstrated.
Status: FINAL

Decision: (owner ruling 2026-07-29) Proof of life, SPEC amendment A3. Every state, SYNCING and LOCKED included, emits a periodic proof-of-life journal line at a fixed interval carrying current state, seconds in state, and the specific condition being waited on where the state is transitional. Supersedes the section 6 scoping of that line to ACTIVE.
Reason: A state the EA can occupy indefinitely while emitting nothing violates fail-visible. This exact gap hid a dead timer for hours: a stuck guardian and a healthy one looked identical from outside.
Status: FINAL

Decision: (owner ruling 2026-07-30) Any witness-supplied locked_until (file or GV) is clamped to AgNextDayAnchor(current TimeCurrent) at evaluation time in the Phase 2 boot-lock derivation (F4).
Reason: Q1 already guarantees the system itself never legitimately produces a locked_until beyond tomorrow's anchor; a value further out is not one the system wrote. Clamping bounds a griefed or fat-fingered GV value to at most one extra day-cycle while preserving the OR-only-adds-lock property.
Status: FINAL

Decision: (owner ruling 2026-07-30) A state file that parses validly and passes checksum but carries a login not matching the current account is treated as CORRUPT_STATE-equivalent: quarantined as .bad, loud WARN, fresh file written, locked via CORRUPT_STATE.
Reason: Errs locked and loud; this project has already been bitten once by foreign-project residue sitting at an expected path (2026-07-29 residue finding).
Status: FINAL

Decision: (owner ruling 2026-07-30) The Phase 4 weekly chart-banner field shows the raw weekly PnL currency figure only.
Reason: Any additional reference point (e.g. a ratio against the daily limit) invents a threshold the charter never specified and risks being read as a weekly enforcement limit that does not exist.
Status: FINAL

Decision: (owner ruling 2026-07-30) Acceptance-test float comparisons (Base, limit, PnL figures) use a flat 0.01 account-currency-unit epsilon. Separately, the live breach comparison itself, wherever any epsilon is applied to it, must err toward breach: total <= -limit + epsilon is acceptable, total <= -limit - epsilon is not.
Reason: A test-assertion epsilon and a live-enforcement epsilon are different obligations; conflating them risks an implementer picking the sign that lets a real breach slip through as within-tolerance.
Status: FINAL

Decision: (owner ruling 2026-08-01) Account instrument scope and Market Watch composition. The account trades gold, NASDAQ, and possibly silver, nothing else. Market Watch is XAUUSD.ecn, US100.ecn and XAGUSD.ecn only. BTCUSD.ecn is removed and never returns. All subscribed instruments are 24/5, so the weekend close is a reliable no-tick window by design. Consequence recorded with the ruling: the frozen-upper-bound finding is downgraded from blocker class to a Phase 1 correctness obligation, not closed, and its crypto example is struck as no longer operative.
Reason: A 24/7 instrument in Market Watch keeps TimeCurrent advancing through the weekend, which destroys the A8 no-tick window and is the only route by which a deal could execute inside a frozen PnL window on this account. Removing it makes both impossible by construction here, while the general upper-bound defect remains a Phase 1 obligation because it does not need a 24/7 instrument.
Status: FINAL

Decision: (Q6 clarification, owner ruling 2026-07-30, drafted by the executor, transcribed here 2026-08-04 from docs/DESIGN_PHASE1_2.md where it was ratified) Q6's snapshot rule governs the locked window once a valid snapshot exists. It does not by itself state what governs the derived-breach comparison (SPEC 4.6) when no valid snapshot is available to read, meaning the state file is missing, corrupted, or login-mismatched, which is precisely F4's own defense scenario. In that specific case the derived comparison falls back to the limit computed live from current inputs and the live-reconstructed base (SPEC 4.2/4.3), never to a cached or assumed value. This does not weaken Q6: a valid snapshot, once available, still governs unconditionally and always wins over live inputs, exactly as ruled. The fallback only ever activates in the absence of a snapshot to read, which is precisely the condition under which Q6's original rule has nothing to apply to. Consequence stated plainly with the ruling: this fallback is what makes file plus GV deletion alone, inputs untouched, fully defended by derived history, and it is also why file plus GV deletion combined with input inflation remains an accepted residual gap under SPEC 4.6, since the fallback then reads the inflated input as if it were legitimate. Extended, not superseded, by the ratchet: the ratchet fold-in inserts a tier between the snapshot and this live-input fallback.
Reason: Q6 as originally worded presumed a snapshot exists to read, and left silent exactly the case F4 is designed to defend. A rule with nothing to apply to is not a rule, so the gap had to be named and filled explicitly rather than left to an implementer's default.
Status: FINAL

Decision: (owner ruling 2026-07-30, transcribed here 2026-08-04 from docs/DESIGN_PHASE1_2.md) Build the ratchet defense against active-state limit inflation, raising DailyLossPercent or DailyLossCurrency before any breach. Scope is Phase 2, implemented only after Phase 0 is green. The log-loudly-and-accept alternative is rejected. The mechanism is NOT covered by this ruling and stays open, see the ISSUES entry: the corrected single-scalar mechanism carries an UNRULED marker in the design document and is not authority until ratified.
Reason: Owner's grounds, on the record: this product's entire posture is friction rather than prevention already, since killing the terminal always works and is conceded everywhere else in the design, so "friction, not prevention" disqualifies nothing here either. The named threat is impulsive loosening under pressure, and a deliberate multi-step requirement is exactly the defense against impulse. The executor's initial dismissal of the threat as a non-issue was withdrawn as unsound. Residual accepted with the ruling: the floor has no history-backed third witness, because an input value is never a deal and leaves no trace in broker history, so file plus GV destruction alone resets it, which is weaker than the lock's own three-witness resilience and is documented rather than fixed.
Status: FINAL

Decision: (owner ruling 2026-08-03) Merge gate replaced. A branch merges to main when the work in that branch is complete and proven, not when the phase as a whole is green. "The work in that branch" means all of it: a branch carrying an unfinished item does not qualify on the strength of its finished ones. "Proven" is not executor discretion, it is the standing evidence rule, a row closes only on evidence present on disk at the stated path with result, build hash and evidence pointer recorded in this ledger. The owner-instruction requirement is unchanged and still binds; only the phase-green conjunct is removed. Supersedes the Phase-0-green gate in the merge plan of docs/DESIGN_PHASE1_2.md, whose merge mechanics (real merge commit, never rebase, never fast-forward-only, never a side-picking strategy, hand union resolve respecting each section's ordering, verification by per-section counts and opening-line identity against both parents) are untouched and remain binding.
Reason: The phase-green gate would have kept proven work off main for as long as any unrelated row stayed open, and it would have forced main to sit at an older point precisely while new work touches core protection logic. The replacement is narrower and stronger: nothing merges unless its own proofs are banked here. Checked before ruling, and recorded so the question is not reopened: the gate protected four things and the replacement covers all four. Merge mechanics and owner control are carried forward unchanged. Keeping unproven work off main is covered more tightly per item than per phase. The one property dropped, main advancing only to points where the whole phase passed, depends on nothing here, because deployment is a source copy into the terminal data folder followed by a MetaEditor compile at a specific commit, never a checkout of main, so main was never the release source; and the gate caught no cross-row interaction that the acceptance matrix does not catch anyway, since withholding a merge runs no row.
Consequence, recorded with the ruling: the 2026-08-03 merge ce6964d is not an exception under this standard, it is the standard applied. Everything on that branch was complete and proven, and this ruling changes the plan rather than violating it. The deviation noted in that merge's ACTIONS entry is reclassified accordingly, from a departure from the plan to the ruling that rewrote it.
Status: FINAL

Decision: (owner ruling 2026-08-03) Visible refusal banner on both refusal paths. The mutex refusal and all five config refusals draw one dated REFUSED banner, REFUSED plus date, time, and reason, through a single helper that sets a dedicated g_init_refused flag immediately before every refusal return in OnInit; OnDeinit skips AgBannerClear while the flag is set, so a refused init leaves its banner on the chart. No path is left silent. The session scoped limitation is accepted: the banner lives until the next attach on that chart or the terminal restart, per the 2026-08-03 measurement that a chart Comment does not survive a restart, and no persistence beyond Comment is required. The clear gate keys on the dedicated refusal flag, never on g_ag_halt_loaded or any other proxy whose semantic is not init refused.
Reason: No holes in the lockout mechanism. A guardian that refuses invisibly is indistinguishable from a guardian that is absent.
Status: FINAL

Decision: (owner ruling 2026-08-04) Backward clock steps in the crash-loop chain walk. A negative gap between two adjacent session inits, which is what a backward local clock step produces, counts as inside CrashLoopWindowSeconds and does not break the unclean chain. The chain therefore errs toward tripping in both clock directions.
Reason: Breaking the chain on a negative gap creates a one-step false disarm. Anyone able to move the local clock backward once, between two kills, resets the count and disarms crash-loop detection with a single act. The residual already accepted in the SPEC threat model runs the opposite direction, compressing timestamps to force a false SAFE_HALT, and that one costs the adversary repeated terminal kills for nothing they could not get by leaving the terminal dead. A one-act disarm is strictly worse than a laborious false trip, so the bound is read as a ceiling on separation and nothing else. Recorded in SPEC Amendment A4.
Status: FINAL

Decision: (owner ruling 2026-08-04) The twelve a9 acceptance vectors are committed to the repository under docs/vectors/. The terminal Presets folder remains the OPERATIVE copy, the only one the MetaTrader Load browser reads and the only one an acceptance row actually loads. The repository copy is the REVIEWABLE record and is not read by anything at runtime. Sync direction is repository to terminal, never back: edit and commit here, copy the file into the terminal Presets folder, restart the terminal, then confirm the file is listed in the Load browser before running any row that depends on it. The restart is mandatory, not a precaution, because a running terminal never enumerates files added to its data folder after it started. If the two copies ever disagree, the terminal copy is what ran and the repository copy is wrong; correct the repository copy to match what ran and record why, never the reverse. Procedure written into docs/vectors/README.md.
Reason: A vector that exists only inside the terminal data folder is invisible to review, so a change to one appears from nowhere and no diff can be inspected. Committing the twelve unmodified first, then changing them, makes the 60 to 300 change a reviewable eleven-line diff against a baseline. The operative copy stays in the terminal because that is a platform fact, not a choice, and naming which copy is authoritative prevents a future session from proving a row against a file the terminal never loaded.
Status: FINAL

Decision: (owner ruling 2026-08-04) SweepPeriodSeconds wording, closing the open item. Both the SPEC section 5 input table cell and the source comment at AccountGuardian.mq5:21 change from "clamped 1..5" to language stating the value is refused outside 1..5. Applied in the Stage 5a pass, which is the next code change the earlier entry attached the comment fix to.
Reason: The old wording is wrong about behavior, not merely imprecise. It implies a value of 9 is corrected to 5, when the EA in fact returns INIT_PARAMETERS_INCORRECT and refuses to initialize, which is what the Q4 config-class rule and the section 8 malformed definition both require. A reader trusting the table would expect a running guardian where there is none, which is the exact failure mode this project treats as worst-case.
Status: FINAL

Decision: (owner ruling 2026-08-04) The repository does NOT live inside the MT5 terminal data folder. The two stay separate, and deployment stays what it already is, an explicit source copy into the terminal data folder followed by a MetaEditor compile at a specific commit. The question raised was whether to host the working tree inside the data folder so the terminal reads it directly and the copy-plus-compile step disappears. Answer is no, permanently.
Reason: What the merged layout would have bought, stated fairly because it is real: no source copying at all, git tracking exactly the bytes the terminal loads, and the deployed-versus-committed verification step becoming unnecessary, which was genuine work in this session. It loses anyway on three grounds. First, the data folder is continuously rewritten by the terminal itself, journals, history, chart profiles, gvariables.dat, so the working tree would never be clean and git status would carry no signal without a large and fragile ignore set that has to be maintained against a folder layout this project does not control. Second, and worse, the halt file and every state artifact live in that folder, so a careless git operation, a checkout, a clean, or a branch switch, would delete or alter live guardian state; the present separation makes that class of accident impossible rather than merely unlikely. Third, it erases the boundary between what has been tested and what is running: every save would touch the live environment immediately, destroying the staged deploy this project depends on and which the merge-gate ruling of 2026-08-03 already relies on when it records that main is never the release source.
Alternative considered and not adopted, recorded so it is not re-proposed as new: symlink only MQL5\Experts and MQL5\Include from the terminal tree into the repository, a common arrangement in MQL5 work, which would keep journals, history and state artifacts outside the repository and so answers the first two grounds above. Rejected on the third ground alone: a terminal-side compile would immediately change what loads on the next init, removing the controlled deploy step. Available to revisit if deploy overhead ever becomes the actual bottleneck, but not while this software guards real money.
Status: FINAL

Decision: (owner ruling 2026-08-04) Remote state is owner-managed. The pushes to origin were the owner's own manual actions through GitHub Desktop, made whenever pending commits appeared there, and they are neither an executor action nor a violation. Two things follow and both bind permanently. One, the executor never pushes: it commits locally and stops there, and a push happens only when the owner performs it or explicitly instructs it in the session. Two, no ledger entry may assert "not pushed", "nothing pushed", "local only", an ahead-by count, or any other remote position as a durable fact. Remote position may be recorded only as a dated observation of what a named local ref said at the moment of writing, marked as such, and never as a standing property of the repository. Existing ACTIONS entries carrying the old wording are not edited, ACTIONS being append only; they are superseded by the ninth ACTIONS entry of 2026-08-04, which states this ruling and records the position as of that moment.
Reason: The owner pushes out of band at will, so any claim about the remote is stale the moment it is written and the ledger has no way to know when it went stale. The executor can only speak to its own conduct, which is that it does not push, and a local remote-tracking ref proves only what the last fetch or push saw, not where the remote stands now. Recording an unfalsifiable claim as fact is the failure this ledger exists to prevent, and here it also risked reading as an accusation against the owner for operating their own repository.
Status: FINAL

Decision: (owner ruling 2026-08-05, Phase 1 kickoff, recorded 2026-08-08 after the blocking investigation the ruling itself required was delivered) Day-anchor base is the account Balance, and the trading day starts at 01:00 Israel time. The base clause composes with Q2 as already frozen rather than replacing it: the base is the anchor-moment balance, reconstructed live as current Balance minus the sum of all deals since the day anchor, all deal types alike, never Equity and never cached. The 01:00 clause supersedes SPEC section 4.1's "floor of TimeCurrent to server-day midnight" and requires a SPEC amendment, scheduled as A5. The clock source is unchanged and unchangeable here: the anchor is computed from TimeCurrent exclusively, per Q7, so the day boundary is a fixed offset applied to server time and never to a local or GMT clock. How "01:00 Israel" is operationalized against the server clock, fixed 01:00 server against an Israel-civil-corrected boundary, is NOT settled by this ruling and is question 1 of the Phase 1 kickoff plan, held in ISSUES.
Reason: Owner's ruling, recorded as given. Two things are worth carrying with it because they were measured rather than assumed and they are what made the ruling safe to record. First, Balance rather than Equity keeps the base invariant intraday, which is the property the whole Q2 identity and the ratchet design both rest on; an Equity base would move with every floating tick and would make the limit itself move under a losing position, tightening or loosening enforcement for reasons that have nothing to do with a deposit or a withdrawal. Second, a 01:00 boundary rather than 00:00 places the anchor clear of the measured nightly quote gap, 00:00:18 to 00:04:48 pinned at the prior second on the 2026-07-31 measurement, so the anchor moment is not routinely inside a stretch where TimeCurrent is frozen. The weekend freeze still spans two anchors every week, which the plan's investigation answers in full: the anchor holds the Friday boundary through the freeze and lands on Monday 01:00 at the reopen, and no trading deal can execute inside the freeze under the FINAL Market Watch composition.
Status: FINAL

Decision: (Q1, owner ruling 2026-08-08) "01:00 Israel" operationalizes as a fixed compile-time constant, 01:00 server, per Q7's rule that TimeCurrent is the only permitted clock in an anchor decision. In the measured present state (server tracking Israel's own offset) this equals 01:00 Israel. The known consequence is accepted rather than treated as a defect: inside a DST divergence window the anchor maps to 00:00 Israel civil, one hour early, exactly as the plan's section 2.2 enumerated per candidate server peg. The owner commits to a scheduled revisit: after the 2026-10-25 to 2026-11-01 harvest measures which DST calendar the broker server actually follows, this operationalization is REVISITED against that measured fact, not re-argued from documentation alone.
Reason: The offset is a fixed constant precisely because an input able to move the day boundary would recreate the same inflation-adjacent surface the ratchet ruling already closes elsewhere; Q7 already forbids any clock but TimeCurrent from touching an anchor decision, so an Israel-civil correction would need either a second, forbidden clock or a server-DST fact this repository cannot yet measure. Accepting the drift now and revisiting on a measured fact in October is cheaper and more honest than guessing at the server's calendar today.
Status: FINAL, REVISIT scheduled for the 2026-10-25 to 2026-11-01 measurement window

Decision: (Q2, owner ruling 2026-08-08) Interim breach posture for the Phase 1 deployed build, before Phase 2 enforcement exists: on a computed breach, the guardian raises a loud ALERT plus a journal arithmetic line, stays in ACTIVE, and repeats the ALERT and journal line at a bounded cadence while the condition holds. No lock, no sweep, no state change; Phase 1 detects and shouts, it does not enforce.
Reason: Owner explicitly sanctions the no-enforcement window this creates until Phase 2 lands; a real breach during Phase 1 is visible and loud rather than either silent or falsely locked by code that does not yet exist to unlock correctly.
Status: FINAL

Decision: (Q3, owner ruling 2026-08-08) DEAL_FEE is added to the per-deal value formula everywhere it appears: realized (SPEC 4.2), the Q2 base-reconstruction sum (SPEC 4.3), and the derived-lock replay (design doc item 3). The formula becomes DEAL_PROFIT + DEAL_SWAP + DEAL_COMMISSION + DEAL_FEE uniformly, superseding the three-property formula SPEC 4.2 currently states. SPEC wording lands in amendment A6.
Reason: DEAL_FEE is a real per-deal money field MT5 exposes that the original three-property formula omitted; the Q2 identity needs a deal's full balance effect or Base stops being algebraically equal to the day-anchor balance, which is the property everything else in Phase 1 and the ratchet design rests on.
Status: FINAL

Decision: (Q4, owner ruling 2026-08-08) The anchor-boundary second convention: DEAL_TIME >= anchor counts to the NEW day. A deal stamped exactly at the anchor second belongs to the day that just started, not the day that just ended. SPEC wording lands in amendment A6.
Reason: Matches HistorySelect's own inclusive-from semantics (the window already reads [anchor, to) treating the anchor instant as the start of the selected range), so the convention needs no special-cased comparison anywhere in the engine, only a documented statement of what the platform already does.
Status: FINAL

Decision: (Q5, owner ruling 2026-08-08) Withdrawals are UNAVAILABLE on JustMarkets demo 1200252169; deposits ARE available through the JustMarkets personal area. Rows P1-E, P1-F and P1-G are redesigned around deposit-only vectors. The withdrawal-neutrality direction of the deposit/withdrawal-neutrality acceptance test (DN-2, the design doc's P1-2) closes by the Q2 algebraic identity argument alone, marked explicitly as PASS-BY-CONSTRUCTION rather than as a row proven by a demo withdrawal, the same category A5's Phase 0 trade-disallowed row already occupies.
Reason: The identity in design doc item 2 proves withdrawal-neutrality symmetrically to deposit-neutrality (Base_after = Base_before under either a +X or a -Y balance-type deal, same derivation, opposite sign); a platform limitation that makes the demo-side half untestable does not weaken an algebraic proof that holds regardless of which direction the demo can exercise.
Status: FINAL

Decision: (Q6, owner ruling 2026-08-08) Phase 0's last remaining gate, whether last-used EA inputs were restored to the ruled defaults, is CLOSED. Owner dialog reading, quoted: all eight inputs read at their ruled defaults, including HistoryStablePolls=3, the input the 2026-08-06 attempt could not verify because the attach that was to write it never happened. With this gate closed and worktree-a9-phantom-audit already merged to main (937c604, documented and pushed 2026-08-08), Phase 0 is closed in full: no remainder exists on main. The sequencing question (Phase 1 kickoff plan question 6) is resolved as CLOSED rather than left open.
Reason: This is the owner's own direct reading of the live EA properties dialog, the same class of evidence this ledger has accepted throughout as owner-eyewitness fact (the 2026-08-03 Market Watch reading, the 2026-08-06 chart01.chr byte-identity reading), distinct from a claimed git or filesystem action, which is what Ruling 1 (2026-08-05, session report is not evidence) actually binds. A dialog reading of input values is not a claim about a commit; it needs no independent git-level re-derivation to count as evidence here.
Status: FINAL

Decision: (Q7, owner ruling 2026-08-08) The XAUUSD.ecn, US100.ecn and XAGUSD.ecn symbol-specification read (GUI, read-only, corroborating A8 and the DST divergence-window analysis of section 2.2) rides the owner's next terminal session, whenever that falls. No specific date is fixed.
Reason: Owner's scheduling ruling, recorded as given. The read is corroboration, not a gate; nothing in Phase 1 or Phase 0 is blocked on it.
Status: FINAL

Decision: (Q8 weekend-reopen amendment, owner ruling 2026-08-09, found during the Stage 5 deploy) ALERT-AND-ADVANCE replaces halt-the-pass. A forward jump of more than one day raises the loud ALERT naming both anchors, then ACCEPTS the fresh anchor and advances the high-water mark, so evaluation continues and the condition self-clears after one pass. No pass is ever halted. The ALERT is cadence-capped at AG_LIFE_INTERVAL_SECONDS exactly as the Q2 breach ALERT is, with the journal line still written on every occurrence. Every other clause of the Q8 entry below stands unchanged: the latch is still in-memory only, never persisted, still seeded on the first pass of a session, and backward steps and same-day recomputes are still unaffected with the mark never receding.
Reason: Owner's ground, on the record: a guardian that stops computing is fail-open, and worse than a widened window, per the A4 precedent that the system errs toward staying live and loud. The defect the amendment fixes was measured rather than argued, and it was deterministic rather than probabilistic. The original rule could not self-clear against a clock that only moves forward, because self-clearing required the fresh anchor to fall BACK within one day of the retained mark. The weekend freeze holds the Friday 01:00 anchor for about 48 hours and lands on Monday 01:00 at the reopen, a three-day advance of 259200 seconds against an 86400 bound, which is precisely the signature the rule treated as an attack. Any instance running across a weekend would therefore have stalled evaluation permanently until restart: no rollover line, no recompute, numbers frozen, while AgAlertEvent, which carried no cadence cap unlike the Q2 ALERT, raised a modal popup on every one-second timer tick. Recorded plainly because it is the governance point: the CODE was faithful to Q8, and Q8 was wrong against a weekend freeze the Phase 1 plan had itself measured and described in its own section 2.3 as producing exactly a Friday-to-Monday advance. Two FINAL rulings collided, and the executor escalated it as a ruling question rather than fixing it unilaterally, per the standing rule that only the owner marks FINAL.
Executor's mechanism, implemented same session, commit 301fc85: AgAnchorSanityCheck sets g_ag_anchor_jump_note naming previous_high, accepted and jump width, then advances g_ag_high_anchor and returns fresh; the string g_ag_anchor_waiting_on is removed. The announcement moved OUT of Clock.mqh into the caller, AgEvaluateActive, because the cadence cap needs the local clock and TimeLocal is forbidden in Clock.mqh by that file's own header (Q7) and by the Stage 3 static acceptance row that greps for it; Clock.mqh now performs no logging at all. Verified after the edit: zero TimeLocal, TimeGMT or TimeTradeServer references survive in Clock.mqh and Pnl.mqh, and zero references to the removed g_ag_anchor_waiting_on survive anywhere in the tree. SPEC A6 and plan 4.1a amended with the superseded halt wording kept on the record rather than deleted; the Stage 2 vectors extended from 20 checks to 26, the four new ones replaying the exact live signature, seed Friday 2026.08.07 01:00:00 then feed Monday 2026.08.10 01:00:00, expecting acceptance, the mark advancing, jump=259200s named, and the note cleared on the following pass.
Status: FINAL, amends the halt clause of the entry below; mechanism as implemented above

Decision: (Q8, owner ruling 2026-08-08) Defense against a forward clock jump moving the day anchor's lower bound and silently excluding real deals: the engine keeps the highest day anchor it has observed in memory. If a freshly computed anchor exceeds that high-water mark by more than one day, the evaluation HALTS loudly, with an ALERT naming both the retained high anchor and the rejected fresh one, rather than silently narrowing the window to the fresh (and untrustworthy) value. The latch is IN-MEMORY ONLY, never persisted, consistent with the never-loaded-never-written decision and with Phase 1 persisting nothing at all; a restart clears it, which is accepted because a restart re-derives everything from history with no anchor to sanity-check against yet.
Executor's mechanism, translating the ruling into the exact primitive (not itself re-opening anything): maintain `g_ag_high_anchor` (datetime, in-memory, seeded to the first computed anchor of the session). Each ACTIVE-state evaluation pass computes `fresh = AgDayAnchor(AgServerNow())` as always (4.1, unchanged) and compares it against the retained high-water mark before using it: if `fresh <= g_ag_high_anchor`, evaluation proceeds normally using `fresh` for the window (this is the unchanged backward-step case of section 2.1, which already widens the window and errs strict; the high-water mark itself does not recede). If `fresh > g_ag_high_anchor` and `fresh - g_ag_high_anchor <= 86400`, this is an ordinary single-day advance (or same-day repeat): evaluation proceeds using `fresh`, and `g_ag_high_anchor` advances to `fresh`. If `fresh - g_ag_high_anchor > 86400`, the jump is anomalous: evaluation for this pass uses the retained `g_ag_high_anchor` rather than `fresh` (so the window is never narrowed by the untrusted value), `g_ag_high_anchor` does NOT advance, and an ALERT plus journal line fires naming both `g_ag_high_anchor` and the rejected `fresh`, repeating every pass while the condition persists. The condition self-clears the moment a later pass's `fresh` falls back within one day of the retained high anchor, with no restart required. SPEC wording (amendment A6) and a Stage 2 synthetic acceptance row (feed a two-day-plus jump, expect HALT with both anchors named and no window narrowing; then feed a legitimate one-day advance, expect normal acceptance and the high-water mark advancing) land in the captured plan document.
Status: FINAL, mechanism as translated above

Decision: (Q9, owner ruling 2026-08-08) Defense against Base moving because current Balance has updated for a deal that local history has not yet synced: on any evaluation pass where the computed result would move abruptly and no new deal is visible in the selected history, the breach declaration DEFERS by exactly one timer pass and re-evaluates, rather than declaring on a possibly-incoherent read. The deferral is one pass and one pass only, so a real breach is delayed by at most one second (the current SweepPeriodSeconds default), never suppressed.
Executor's mechanism, translating the ruling (not re-opening it): the coherence signal is deal count, `HistoryDealsTotal()` read immediately after the same `HistorySelect` call the realized sum already uses, compared against the prior pass's count. A one-shot deferral flag, `g_ag_breach_deferred_once` (in-memory, reset whenever no breach is pending), governs the bound: on a pass where the breach condition evaluates true, if the deal count is unchanged from the prior pass AND this breach has not already been deferred once, the pass logs the deferral (naming the unchanged deal count) and does not declare; the flag is set so the very next pass, whatever the deal count reads, declares if the condition still evaluates true. This guarantees the delay is bounded at exactly one pass regardless of whether the underlying cause resolves. SPEC wording (amendment A6) and a Stage 3 or Stage 4 acceptance row (force two consecutive true breach evaluations with an unchanged deal count between them; expect exactly one deferral then a declaration on the second pass) land in the captured plan document.
Status: FINAL, mechanism as translated above

Decision: (Q10 reconnect-coherence amendment, owner ruling 2026-08-09, found during review of worktree-phase1-pnl-core tip e5d32bc) Recovery from DEGRADED requires the same history-stability discipline as the SYNCING exit condition, rather than the immediate clean re-evaluation the original Q10 ruling below specified. On the first connected pass after DEGRADED, do not run the breach evaluation until `AgHistoryStable(HistoryStablePolls)` passes again; the LIFE line shows the poll count while waiting, prefixed RESYNC to distinguish it from initial SYNCING. State stays ACTIVE, no transition. Q9's one-pass deferral is unaffected and continues to apply only to the connected steady state, since it was ruled for a one-tick sync gap, not a post-disconnect resync. This amends only the "on reconnect, evaluation resumes with an immediate clean re-evaluation" clause of the Q10 entry below; every other clause of that entry (the connection signal, what is skipped while DEGRADED, the DEGRADED marker, no ALERT from DEGRADED data) stands unchanged.
Executor's mechanism, implemented same session, commit `fb82a70`: new in-memory flag `g_ag_resyncing`, set true alongside `g_ag_degraded` on disconnect. On the first connected pass, before the Q8 anchor check, a gate polls `AgHistoryStable(HistoryStablePolls)`; while false the pass returns early (`waiting_on = "RESYNC: polls=<n>/<required>"`, no transition, `g_ag_degraded` stays true). Once stable, `g_ag_resyncing` clears and the pass proceeds through the pre-existing Q8/computation/Q9/Q2 chain unchanged. SPEC A6's Q10 bullet and the plan's section 4.3b and P1-O row updated to the amended wording the same session.
Status: FINAL, amends the reconnect clause of the entry below; mechanism as implemented above

Decision: (Q10, owner ruling 2026-08-08) While the terminal is disconnected, the evaluation marks itself DEGRADED and takes no breach decision from a frozen quote, since no enforcement action is possible without a connection regardless of what the numbers say. On reconnect, evaluation resumes with an immediate clean re-evaluation, no re-sync wait beyond what already exists. No ALERT fires from data computed while DEGRADED, consistent with the Q2 interim-posture cadence only ever firing from a fresh, connected evaluation.
Executor's mechanism, translating the ruling (not re-opening it): the connection check is `TerminalInfoInteger(TERMINAL_CONNECTED)`, the same signal design doc item 1's `AgHistoryStable` already resets on. When false, the ACTIVE-state evaluation for that pass is skipped entirely (no realized/floating/base/limit recompute, no Q8 anchor check, no Q9 coherence check, none of it), the LIFE line and banner show a DEGRADED marker alongside the last-known figures rather than fresh ones, and no ALERT of any kind fires from that pass. The instant `TERMINAL_CONNECTED` reads true again, the very next pass runs the full evaluation exactly as any other ACTIVE pass, with Q8 and Q9 both applying normally to it since nothing about disconnection touches the high-water anchor or the deal-count baseline. SPEC wording (amendment A6) and an acceptance row reusing the existing disconnect procedure (already exercised for the A8 weekend harvest and the kill-session work) land in the captured plan document: observe DEGRADED on the LIFE line through the gap, zero ALERTs during it, and one clean re-evaluation on the line immediately after reconnect.
Status: FINAL, mechanism as translated above

Decision: (owner ruling 2026-09-03, input hardening session, R10, D1f) BOTH LIMITS BECOME MANDATORY LIST INPUTS. Neither can be zero, neither can be disabled from the dialog. `DailyLossPercent` is an enum of 22 values, 0.25 to 5.50 in steps of 0.25; the top value 5.50 is the ceiling. `DailyLossCurrency` is an enum of 48 values, 1 to 10 in steps of 1, then 15 to 200 in steps of 5; the top value 200 is the ceiling, in account currency. Any value not in its list, from the dialog, a `.set` file or last-used inputs, refuses init. The effective limit remains the min of the two legs, unchanged. There is no disable path inside the EA: disabling means the owner removes the EA from the chart and deletes the boot file by hand. This entry supersedes the both-limits-zero clause of Q4/F9 (owner ruling 2026-07-29, DECISIONS) and the "0 disables each, both zero refuses init" clause of Q8 (owner ruling 2026-07-29, DECISIONS), on that clause alone: every other clause of both entries stands unchanged.
Reason: Owner's ruling, recorded as given, 2026-09-03 input hardening session (R10).
Status: FINAL

Decision: (owner ruling 2026-09-03, input hardening session, R10, D2/D3/D3b) VOID UNDER D1f. No `guard_<login>.dat` is created, no change is made to `floor_<login>.dat`, and no change is made to the RESEED AT ROLLOVER clause of the ratchet ratification (owner ruling 2026-08-18, DECISIONS, Phase 2 open question SEVEN).
Reason: Owner's ruling, recorded as given, 2026-09-03 input hardening session (R10).
Status: FINAL

Decision: (owner ruling 2026-09-03, input hardening session, R10, D4a) WITHIN THE CEILING WHILE LOCKED, an input change is rejected and journaled, current Q6 behaviour (Q6/F7, owner ruling 2026-07-29, DECISIONS), unchanged. ABOVE THE CEILING, the value is not a list member and init refuses, per D1f above.
Reason: Owner's ruling, recorded as given, 2026-09-03 input hardening session (R10).
Status: FINAL

Decision: (owner ruling 2026-09-03, input hardening session, R10, D5a) A tightening of either limit from the dialog takes effect immediately.
Reason: Owner's ruling, recorded as given, 2026-09-03 input hardening session (R10).
Status: FINAL

Decision: (owner ruling 2026-09-03, input hardening session, R10, D6a) THE RATCHET COMPARES THE DERIVED LIMIT, the min of the two legs, unchanged. THE CEILING CHECK IS PER RAW INPUT AT INIT, against each input's own list under D1f.
Reason: Owner's ruling, recorded as given, 2026-09-03 input hardening session (R10).
Status: FINAL

Decision: (owner ruling 2026-09-03, input hardening session, R10, D7b) `SweepPeriodSeconds = 1` and `HistoryStablePolls = 3` become compile-time constants. This closes the refuse-versus-clamp deferral of 2026-07-30 (ACTIONS entry of that date and DECISIONS R1/R2 of that date) for these two inputs.
Reason: Owner's ruling, recorded as given, 2026-09-03 input hardening session (R10).
Status: FINAL

Decision: (owner ruling 2026-09-03, input hardening session, R10, D8b) Struck a9 vectors are retired, not rewritten. `docs/vectors/README.md` is updated accordingly, not in this session. The FINAL at DECISIONS 2026-08-04 naming the twelve committed a9 vectors is not edited. This entry supersedes it on the vector count alone: of the twelve, ten are struck by D1f, D7b and D11a together, and two survive, `a9_defaults.set` and `a9_optional_bad.set`.
Reason: Owner's ruling, recorded as given, 2026-09-03 input hardening session (R10).
Status: FINAL

CORRECTION, same session, dated 2026-09-03, not an in-place edit of the entry above. The ten struck files were read against D1f and D7b field by field: `a9_both_zero.set`, `a9_neg_percent.set`, `a9_neg_currency.set`, `a9_percent_over_100.set` and `a9_nonfinite.set` are struck by D1f, the list-membership check superseding the zero, negative, range and finite checks each one tests; `a9_sweep_zero.set` and `a9_sweep_over.set` are struck by D7b, `SweepPeriodSeconds` becoming a compile-time constant; `a9_polls_zero.set` is struck by D7b, `HistoryStablePolls` becoming a compile-time constant; `a9_crash_max_zero.set` and `a9_window_zero.set` are struck by D11a. The survivor count stays two, `a9_defaults.set` and `a9_optional_bad.set`, but NEITHER SURVIVOR'S CURRENT CONTENT PASSES D1f AS WRITTEN. Both carry `DailyLossCurrency=0.0`, and D1f's currency enum starts at 1, so 0.0 is not a list member; both would refuse init unmodified. `a9_defaults.set`'s `DailyLossPercent=5.0` IS a list member under D1f, `5.00 = 0.25 * 20`, and its `SweepPeriodSeconds=1` and `HistoryStablePolls=3` match the D7b constants exactly. A vector rewrite to fix the currency field on both files is not authorized in this session and is left for the build session named in D10a.
Status: FINAL, correction to D8b's own content, the struck/survive count of ten and two unchanged

Decision: (owner ruling 2026-09-03, input hardening session, R10, D9a) An ignored widening uses the ratchet WARN form, "ratchet HOLDING against a raised limit", at the same cadence cap value, `AG_LIFE_INTERVAL_SECONDS`.
Reason: Owner's ruling, recorded as given, 2026-09-03 input hardening session (R10).
Status: FINAL

Decision: (owner ruling 2026-09-03, input hardening session, R10, D10a) Hardening ships first as its own build: D1f, D7b and D11a together. The defect 2 (GV lock mirror self-erasure on cold boot) and defect 4 (LOCKED LIFE lines carry no numbers) fixes, per the fix order FINAL of 2026-08-19, are not part of this build.
Reason: Owner's ruling, recorded as given, 2026-09-03 input hardening session (R10).
Status: FINAL

Decision: (owner ruling 2026-09-02, transcribed 2026-09-03 in the input hardening session, R10, D11a) CrashLoopMaxInits = 3 and CrashLoopWindowSeconds = 300 become compile-time constants. Two a9 vectors are struck: `a9_crash_max_zero.set` and `a9_window_zero.set`.
Reason: Owner's ruling, dated 2026-09-02 by the owner's own instruction and recorded here on 2026-09-03 because no earlier entry captured it; the ten 2026-09-02 rulings recorded in this DECISIONS section under that date do not include it, checked before this entry was written.
Status: FINAL, dated 2026-09-02

Decision: (owner ruling 2026-09-03, given in the R10 build plan session instruction, defaults under D1f) `DailyLossPercent` default = 5.50 and `DailyLossCurrency` default = 200, the top value of each D1f list. Both defaults are list members by construction, 5.50 = 0.25 * 22 and 200 = 15 + 5 * 37, so the shipped defaults pass the D1f init check with no dialog change. This entry adds to D1f (DECISIONS :2205) and supersedes nothing: D1f names the lists and the ceilings and did not name the defaults.
Reason: Owner's ruling, recorded as given, 2026-09-03 R10 build plan session.
Status: FINAL

Decision: (owner ruling 2026-09-03, R10 plan review, given in the plan approval instruction) Four items of `docs/PLAN_R10_INPUT_HARDENING_2026-09-03.md` are ratified. ONE, retired a9 vectors are removed from the tree by `git rm` and listed in `docs/vectors/README.md` with the striking ruling (plan 3.3). TWO, `README.md` is updated in the build session to reflect R10: no zero-as-disable text, the four removed inputs gone; README stays bilingual and never names the ledger (plan 2.6, Task B4). THREE, the `docs/SPEC_v0.1.md` amendment per plan Task B5 is authorized. FOUR, the `limits accepted` INFO line and the `build=R10` label are approved as designed in plan 2.2.
Reason: Owner's ruling, recorded as given, 2026-09-03 R10 plan review. On item ONE: none of the ten retired vectors is needed by the build or by the guardian's function.
Status: FINAL

Decision: (owner ruling 2026-09-03, R10 build review) ONE, the amendment of `docs/SPEC_v0.1.md:114`, made in the R10 build session outside the three steps of plan Task B5, is RATIFIED. It is an owner ruling from this date, not an executor reading. Reason: the sentence still asserted the both-limits-zero clause that D1f supersedes. TWO, the SPEC amendment series in section 9 is CLOSED at A6. No A7 entry for R10. Future SPEC changes are recorded by the ruling that causes them, not by the series.
Reason: Owner's ruling, recorded as given, 2026-09-03 R10 build review.
Status: FINAL

Decision: (owner ruling 2026-09-06, deploy hand, R10 acceptance session) RULE A IS NOT AMENDED. Addition, and it is an addition only. The executor may write ONE PowerShell script inside the repository, `scripts/deploy-r10.ps1`, containing only `Copy-Item` lines from the worktree into the MetaTrader Terminal data folder and `Get-FileHash` lines. No deletes, no renames, no variables, no functions, every path literal. THE OWNER RUNS THE SCRIPT from his own PowerShell window. The executor never runs it and never invokes anything that writes under the Terminal data folder. After the owner reports the run, the executor verifies every landed file with a plain `Get-FileHash`, read only.
Reason: Owner's ruling, recorded as given, 2026-09-06, R10 acceptance session, Task B7. Recorded so the boundary is unambiguous rather than inferred: authoring a file inside the repository that NAMES a write path is not itself a write under the Terminal data folder, and the write authority stays entirely with the owner, who holds the elevated rights the executor lacks. The RULE A separation established by the 2026-08-17 governance incident ruling is unchanged in every other respect, and RULE B continues to bind every executor command that names a path inside that folder.
Status: FINAL

Decision: (owner ruling 2026-09-07, R10 acceptance review) ONE, deploy procedure amended: after any build upgrade, the EA is attached and a healthy init is confirmed on the chart, its `limits accepted` line read, BEFORE any terminal restart. A refused init is corrected while the EA is still attached. This closes the refused-init deploy gap named in the ISSUES row of 2026-09-07 (`LEDGER.md:9` to `:11`). TWO, no deliberate vector is authorized for a dialog rendering one valid enum member while `OnInit` receives another. The `limits accepted raw=` line remains the witness after every input change. This closes the question posed in the dialog rendering ISSUES row (`LEDGER.md:13` to `:15`); the underlying observation stays banked as recorded there. THREE, whether the REFUSED banner should survive an EA unload is DEFERRED, not ruled, and remains open for a future owner ruling.
Reason: Owner's ruling, recorded as given, 2026-09-07, R10 acceptance review.
Status: FINAL

Decision: (owner ruling 2026-09-09, D2 and D4 fix shapes, ruling sheet D2D4-1 to D2D4-15 of `docs/FIXPLAN_PHASE3_DEFECTS_2_4_2026-09-08.md`, section 4) Answers, recorded as given, rulings only, no interpretation. D2D4-1: (a) THE STATE GATE. D2D4-2: (b) one INFO line at init reporting what the mirror held. D2D4-3: (a) THE SNAPSHOT AND ACCOUNT GROUP, no history walk. D2D4-4: (a) equity = balance + floating, no ACCOUNT_EQUITY read. D2D4-5: (a) `breach_time` not printed on the LOCKED line. D2D4-6: (a) DEGRADED prefix on the LOCKED group, keyed on `g_ag_obs_connected`, read inside the builder. D2D4-7: (a) CORRUPT_STATE renders 0.00. D2D4-8: (b) the banner under LOCKED shows the snapshot limit and the balance. D2D4-9: (a) pure formatter in `Log.mqh` with synthetic checks. D2D4-10: (a) one build, D2 committed before D4. D2D4-11: (a) a second deploy script, `scripts/deploy-d2d4.ps1`, under the 2026-09-06 shape; the ruling at `LEDGER.md:2311` is extended to name it. D2D4-12: (a) both artifacts rowed, first exercise of the 2026-09-07 procedure. D2D4-13: (a) `build=D2D4`. D2D4-14: (a) two cold boots more than 300 s apart and one reload. D2D4-15: (a) SPEC and README amended in the build session.
Reason: Owner's ruling, recorded as given, 2026-09-09.
Status: FINAL
